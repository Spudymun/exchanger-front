# Урок 1.1: Что такое монорепозиторий и зачем он нужен

> **🎯 Цель урока**: Понять концепцию монорепозитория и его преимущества на примере ExchangeGO

## 📖 Введение

Представьте, что вы строите дом. У вас есть два варианта:

1. **Multi-repo подход** - построить отдельные здания для кухни, спальни, гостиной
2. **Monorepo подход** - построить один дом с разными комнатами

Монорепозиторий - это **один Git репозиторий**, который содержит **несколько связанных проектов**.

## 🤔 Проблема Multi-repo подхода

### Традиционный подход (до монорепо)

В проекте обмена валют у нас было бы:

```
exchanger-frontend/     # Отдельный репозиторий
├── package.json
├── src/
└── components/

exchanger-admin/        # Отдельный репозиторий
├── package.json
├── src/
└── admin-components/

exchanger-docs/         # Отдельный репозиторий
├── package.json
└── docs/

exchanger-ui-library/   # Отдельный репозиторий
├── package.json
└── ui-components/
```

### ❌ Проблемы этого подхода:

1. **Дублирование кода**:

   ```typescript
   // В exchanger-frontend/
   const CURRENCY_CODES = ['BTC', 'ETH', 'USDT'];

   // В exchanger-admin/ (тот же код!)
   const CURRENCY_CODES = ['BTC', 'ETH', 'USDT'];
   ```

2. **Версионирование кошмар**:

   ```bash
   # Обновили UI библиотеку с версии 1.2.0 до 1.3.0
   # Теперь нужно обновить в 3 репозиториях вручную!
   npm install ui-library@1.3.0  # В каждом проекте отдельно
   ```

3. **Сложность разработки**:
   - Нужно переключаться между репозиториями
   - Сложно тестировать изменения комплексно
   - CI/CD для каждого проекта отдельно

## ✅ Решение: Монорепозиторий

### Наша архитектура ExchangeGO:

```
exchanger-front/                 # ОДИН репозиторий
├── apps/                        # Готовые приложения
│   ├── web/                     # Основное приложение
│   ├── admin-panel/             # Админ панель
│   └── docs/                    # Документация
├── packages/                    # Общие библиотеки
│   ├── constants/               # Общие константы
│   ├── ui/                      # UI компоненты
│   ├── exchange-core/           # Бизнес логика
│   └── utils/                   # Утилиты
└── package.json                 # Общие зависимости
```

## 🎯 Преимущества монорепо на примере обменника

### 1. **Единый источник истины для констант**

```typescript
// packages/constants/src/currencies.ts
export const CRYPTOCURRENCIES = ['BTC', 'ETH', 'USDT', 'TRX'] as const;
export const EXCHANGE_LIMITS = {
  BTC: { min: 0.001, max: 10 },
  ETH: { min: 0.01, max: 100 },
} as const;
```

Теперь ВСЕ приложения используют ОДНИ константы:

```typescript
// apps/web/src/components/ExchangeForm.tsx
import { CRYPTOCURRENCIES, EXCHANGE_LIMITS } from '@repo/constants';

// apps/admin-panel/src/components/CurrencySettings.tsx
import { CRYPTOCURRENCIES, EXCHANGE_LIMITS } from '@repo/constants';
```

### 2. **Переиспользование UI компонентов**

```typescript
// packages/ui/src/components/Button.tsx - создали один раз
export function Button({ children, variant = 'primary' }) {
  return <button className={`btn-${variant}`}>{children}</button>;
}
```

Используется везде:

```typescript
// В web приложении
import { Button } from '@repo/ui';

// В админ панели
import { Button } from '@repo/ui';

// В документации
import { Button } from '@repo/ui';
```

### 3. **Синхронное обновление**

```bash
# Добавили новую валюту? Одна команда обновит ВСЁ:
git commit -m "Add DOGE currency support"
# Автоматически обновится в web, admin, docs
```

### 4. **Типобезопасность между проектами**

```typescript
// packages/exchange-core/src/types/Order.ts
export interface ExchangeOrder {
  id: string;
  fromCurrency: CryptoCurrency;
  toCurrency: 'UAH';
  amount: number;
}
```

```typescript
// apps/web/ использует те же типы что и apps/admin-panel/
import type { ExchangeOrder } from '@repo/exchange-core';

function processOrder(order: ExchangeOrder) {
  // TypeScript проверяет типы автоматически!
}
```

## 🏗️ Архитектура нашего монорепо

### Apps (Приложения)

- **`apps/web/`** - Основное web приложение (Next.js)
- **`apps/admin-panel/`** - Панель администратора (Next.js)
- **`apps/docs/`** - Документационный сайт (Next.js)

### Packages (Библиотеки)

- **`packages/constants/`** - Бизнес константы и конфигурация
- **`packages/ui/`** - UI компоненты (shadcn/ui + кастомные)
- **`packages/exchange-core/`** - Бизнес логика обменника
- **`packages/utils/`** - Утилитарные функции
- **`packages/hooks/`** - React хуки

## 🔍 Анализ реального кода

Давайте посмотрим как это работает в нашем проекте:

### Пример 1: Константы валют

```typescript
// 📁 packages/constants/src/currencies.ts
export const CRYPTOCURRENCIES = ['BTC', 'ETH', 'USDT-TRC20', 'USDT-ERC20', 'TRX'] as const;

export type CryptoCurrency = (typeof CRYPTOCURRENCIES)[number];
```

```typescript
// 📁 apps/web/src/server/trpc/routers/exchange.ts
import { CRYPTOCURRENCIES } from '@repo/constants';

// Автоматическая валидация на сервере
if (!CRYPTOCURRENCIES.includes(currency)) {
  throw new Error('Unsupported currency');
}
```

### Пример 2: UI компоненты

```typescript
// 📁 packages/ui/src/components/forms/Select.tsx
export function Select({ options, value, onChange }) {
  return (
    <select value={value} onChange={onChange}>
      {options.map(option => (
        <option key={option.value} value={option.value}>
          {option.label}
        </option>
      ))}
    </select>
  );
}
```

```typescript
// 📁 apps/web/src/components/exchange/CurrencySelect.tsx
import { Select } from '@repo/ui';
import { CRYPTOCURRENCIES } from '@repo/constants';

export function CurrencySelect() {
  const options = CRYPTOCURRENCIES.map(currency => ({
    value: currency,
    label: currency
  }));

  return <Select options={options} />;
}
```

## 💡 Почему именно монорепо для обменника?

### 1. **Консистентность данных**

В обмене валют критично чтобы курсы, лимиты, комиссии были одинаковые везде.

### 2. **Быстрая разработка**

Добавили новую валюту → сразу доступна в web и admin панели.

### 3. **Единая безопасность**

Валидация, типы, ограничения применяются ко всем приложениям.

### 4. **Простое тестирование**

Можно тестировать весь flow: web → admin → API одновременно.

## ✅ Проверка знаний

1. **Вопрос**: Чем монорепозиторий отличается от multi-repo подхода?

   <details>
   <summary>Ответ</summary>

   Монорепо - один Git репозиторий с несколькими проектами. Multi-repo - отдельный репозиторий для каждого проекта. Монорепо обеспечивает единый источник истины, упрощает зависимости и синхронизацию.
   </details>

2. **Задача**: Найдите в коде где используются константы из `@repo/constants`:

   ```bash
   # Поиск в проекте
   grep -r "@repo/constants" apps/
   ```

3. **Вопрос**: Какие проблемы решает централизация UI компонентов?

   <details>
   <summary>Ответ</summary>
   - Устраняет дублирование кода
   - Обеспечивает единый дизайн
   - Упрощает обновления (изменили в одном месте - обновилось везде)
   - Ускоряет разработку
   </details>

## 🚀 Практическое задание

**Задание**: Изучите структуру нашего монорепо:

1. Откройте корневую папку проекта
2. Изучите содержимое `apps/` и `packages/`
3. Найдите где в коде импортируется `@repo/constants`
4. Посмотрите на `package.json` файлы в разных пакетах

**Команда для поиска**:

```bash
find . -name "package.json" -not -path "./node_modules/*" | head -10
```

## 📚 Дополнительные материалы

- [Монорепо vs мультирепо: сравнение подходов](https://docs.google.com/document/d/1KZjQeqQzNRt4Y_vD-QcZZ4vK7b_PQ6YK7K9Z2h3Q8XE/edit)
- [Зачем Google использует монорепо](https://research.google/pubs/pub45424/)
- [Архитектура нашего проекта](../../docs/core/ARCHITECTURE.md)

---

[← Назад к главе](./README.md) | [Урок 1.2: Turborepo →](./lesson-1.2-turborepo-system.md)
