# Урок 1.4: Практика - настройка рабочего окружения

> **🎯 Цель урока**: Настроить локальное окружение и убедиться что всё работает правильно

## 📖 Введение

Этот урок полностью практический. Мы настроим проект с нуля и протестируем все системы. К концу урока у вас будет полностью рабочее окружение для разработки.

## 📋 Checklist подготовки

### ✅ Системные требования

Убедитесь что у вас установлено:

- **Node.js 18+**

  ```bash
  node --version  # Должно быть v18.0.0 или выше
  ```

- **npm 8+**

  ```bash
  npm --version   # Должно быть 8.0.0 или выше
  ```

- **Git**

  ```bash
  git --version   # Любая современная версия
  ```

- **VS Code** (рекомендуется)

## 🚀 Шаг 1: Клонирование и установка

### 1.1 Клонирование репозитория

```bash
# Клонируйте проект (замените URL на актуальный)
git clone https://github.com/your-org/exchanger-front.git
cd exchanger-front
```

### 1.2 Установка зависимостей

```bash
# Установка всех зависимостей монорепо
npm install

# Это может занять 2-5 минут в зависимости от скорости интернета
```

**Что происходит?**

- npm устанавливает зависимости в корне
- Turborepo управляет зависимостями пакетов
- Создаются symlinks между пакетами для локальной разработки

### 1.3 Проверка установки

```bash
# Проверяем что Turborepo работает
npx turbo --version

# Проверяем структуру
ls -la apps/ packages/
```

**Ожидаемый результат**:

```
apps/
├── admin-panel/
├── docs/
└── web/

packages/
├── constants/
├── exchange-core/
├── hooks/
├── ui/
└── utils/
```

## 🔧 Шаг 2: Первая сборка

### 2.1 Сборка всех пакетов

```bash
# Полная сборка проекта
npm run build

# При первой сборке увидите прогресс каждого пакета
```

**Ожидаемый вывод**:

```
• Packages in scope: @repo/constants, @repo/exchange-core, @repo/ui, @repo/utils, @repo/hooks, web, admin-panel, docs
• Running build in 9 packages
• Remote caching disabled

@repo/constants:build: cache miss, executing 2f8a9c3d6e1b4a5c
@repo/exchange-core:build: cache miss, executing 7d2b1f9e8c4a6b3d
...
✓ Completed in 45s
```

### 2.2 Повторная сборка (тест кэширования)

```bash
# Повторная сборка для проверки кэша
npm run build
```

**Ожидаемый результат**:

```
• Packages in scope: @repo/constants, @repo/exchange-core, @repo/ui, @repo/utils, @repo/hooks, web, admin-panel, docs
• Running build in 9 packages
• Remote caching disabled

@repo/constants:build: cache hit, replaying logs 2f8a9c3d6e1b4a5c
@repo/exchange-core:build: cache hit, replaying logs 7d2b1f9e8c4a6b3d
...
✓ Completed in 3s
```

**✅ Успех**: Время сборки сократилось с 45 секунд до 3 секунд!

## 💻 Шаг 3: Запуск dev сервера

### 3.1 Запуск всех приложений

```bash
# Запуск в dev режиме
npm run dev
```

**Что происходит?**

- Запускаются все три приложения параллельно:
  - Web: http://localhost:3000
  - Admin Panel: http://localhost:3001
  - Docs: http://localhost:3002

### 3.2 Проверка приложений

Откройте в браузере:

1. **Web приложение**: http://localhost:3000
   - Должна открыться главная страница обменника
   - Проверьте что форма обмена работает

2. **Admin Panel**: http://localhost:3001
   - Должна открыться админ панель
   - Интерфейс может отличаться от web

3. **Docs**: http://localhost:3002
   - Должен открыться сайт документации

### 3.3 Тест hot reload

```bash
# В новом терминале измените файл
echo "/* Test comment */" >> packages/ui/src/components/ui/Button.tsx

# Сохраните и посмотрите в браузер - должно обновиться автоматически
```

## 🧪 Шаг 4: Тестирование зависимостей

### 4.1 Тест импортов между пакетами

Создайте тестовый файл:

```bash
# Создаем тестовый файл
cat > test-imports.mjs << 'EOF'
// Тест импортов между пакетами
import { CRYPTOCURRENCIES } from './packages/constants/dist/index.js';
import { calculateUahAmount } from './packages/exchange-core/dist/index.js';

console.log('✅ Constants loaded:', CRYPTOCURRENCIES.slice(0, 2));
console.log('✅ Exchange core loaded:', typeof calculateUahAmount);
console.log('🎉 All imports working!');
EOF

# Запускаем тест
node test-imports.mjs

# Удаляем тестовый файл
rm test-imports.mjs
```

### 4.2 Тест TypeScript

```bash
# Проверка типов во всем проекте
npm run check-types

# Должно пройти без ошибок
```

### 4.3 Тест линтинга

```bash
# Проверка качества кода
npm run lint

# Может показать предупреждения, но не должно быть критических ошибок
```

## 🎯 Шаг 5: Тест конкретного пакета

### 5.1 Сборка отдельного пакета

```bash
# Собираем только UI пакет
npx turbo build --filter=@repo/ui

# Проверяем что собралось
ls -la packages/ui/dist/
```

### 5.2 Запуск только web приложения

```bash
# Останавливаем все (Ctrl+C если запущены)
# Запускаем только web
npx turbo dev --filter=web

# Откройте http://localhost:3000
```

### 5.3 Тест изменения константы

```bash
# Измените константу
echo "export const TEST_CONSTANT = 'Hello from constants';" >> packages/constants/src/index.ts

# Пересоберите
npm run build

# Проверьте что изменение применилось
grep -r "TEST_CONSTANT" packages/constants/dist/
```

## 🔍 Шаг 6: Изучение структуры

### 6.1 Изучение package.json файлов

```bash
# Посмотрите зависимости в корневом package.json
cat package.json | grep -A 20 '"dependencies"'

# Посмотрите зависимости UI пакета
cat packages/ui/package.json | grep -A 10 '"dependencies"'
```

### 6.2 Изучение turbo.json

```bash
# Посмотрите конфигурацию Turborepo
cat turbo.json

# Обратите внимание на pipeline и зависимости
```

### 6.3 Изучение TypeScript конфигурации

```bash
# Базовая TS конфигурация
cat tsconfig.json

# Конфигурация web приложения
cat apps/web/tsconfig.json
```

## ✅ Шаг 7: Проверочные тесты

### Тест 1: Проверка монорепо

```bash
# Все пакеты должны собираться
npm run build

# Проверяем что создались dist папки
find packages/ -name "dist" -type d
```

**Ожидаемый результат**:

```
packages/constants/dist
packages/exchange-core/dist
packages/hooks/dist
packages/ui/dist
packages/utils/dist
```

### Тест 2: Проверка приложений

```bash
# Все приложения должны запускаться
npm run dev &
sleep 10

# Проверяем что порты отвечают
curl -s http://localhost:3000 | grep -q "DOCTYPE" && echo "✅ Web OK"
curl -s http://localhost:3001 | grep -q "DOCTYPE" && echo "✅ Admin OK"
curl -s http://localhost:3002 | grep -q "DOCTYPE" && echo "✅ Docs OK"

# Останавливаем
pkill -f "next dev"
```

### Тест 3: Проверка кэширования

```bash
# Очищаем кэш
npx turbo clean

# Засекаем время первой сборки
time npm run build

# Засекаем время повторной сборки
time npm run build

# Вторая должна быть значительно быстрее
```

## 🛠️ Настройка VS Code (рекомендуется)

### Установите расширения:

```json
// .vscode/extensions.json (уже создан в проекте)
{
  "recommendations": [
    "bradlc.vscode-tailwindcss",
    "esbenp.prettier-vscode",
    "ms-vscode.vscode-typescript-next",
    "formulahendry.auto-rename-tag"
  ]
}
```

### Настройки рабочего пространства:

```json
// .vscode/settings.json (уже создан в проекте)
{
  "typescript.preferences.includePackageJsonAutoImports": "on",
  "typescript.suggest.autoImports": true,
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode"
}
```

## 🔧 Troubleshooting

### Проблема: npm install падает с ошибкой

**Решение**:

```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Переустановите
npm install
```

### Проблема: Турборепо не найден

**Решение**:

```bash
# Установите турборепо глобально
npm install -g turbo

# Или используйте npx
npx turbo --version
```

### Проблема: Порт занят

**Решение**:

```bash
# Найдите процесс на порту 3000
lsof -ti:3000

# Убейте процесс
kill -9 $(lsof -ti:3000)

# Или измените порт
PORT=3001 npm run dev
```

### Проблема: TypeScript ошибки

**Решение**:

```bash
# Пересоберите все пакеты
npm run build

# Перезапустите TypeScript сервер в VS Code
# Ctrl+Shift+P -> "TypeScript: Restart TS Server"
```

## 🎉 Финальная проверка

Если всё настроено правильно, вы должны:

- ✅ Успешно собрать проект командой `npm run build`
- ✅ Запустить dev сервер командой `npm run dev`
- ✅ Открыть три приложения в браузере
- ✅ Видеть hot reload при изменении файлов
- ✅ Получать автокомплит и проверку типов в VS Code

## 📚 Что дальше?

После настройки окружения переходите к [Главе 2: Next.js App Router](../chapter-02-nextjs-app-router/), где мы изучим современную архитектуру маршрутизации Next.js 15.

## 📋 Шпаргалка команд

```bash
# Основные команды разработки
npm install           # Установка зависимостей
npm run build        # Сборка всех пакетов
npm run dev          # Запуск dev серверов
npm run lint         # Проверка качества кода
npm run check-types  # Проверка TypeScript

# Turborepo команды
npx turbo build                    # Сборка с кэшем
npx turbo build --filter=web      # Сборка конкретного пакета
npx turbo clean                    # Очистка кэша
npx turbo --graph                  # Граф зависимостей

# Полезные команды
npm run build:analyze              # Анализ bundle size
npm run storybook                  # UI документация
```

---

[← Урок 1.3](./lesson-1.3-package-architecture.md) | [Глава 2: Next.js App Router →](../chapter-02-nextjs-app-router/README.md)
