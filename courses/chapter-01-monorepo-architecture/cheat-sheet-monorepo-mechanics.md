# 🎯 Шпаргалка: Механики монорепо архитектуры

> **Создано на основе детального разбора с AI-ассистентом**  
> Все ключевые механики, которые работают "под капотом" нашего монорепо

---

## 🏗️ Архитектура пакетов - Система уровней

### Иерархия зависимостей (снизу вверх):

```
┌─────────────────┐  Уровень 4: Приложения
│  apps/web/      │
│  apps/admin/    │  ← Используют всё нижнее
│  apps/docs/     │
└─────────────────┘

┌─────────────────┐  Уровень 3: UI и состояние
│  packages/ui/   │
│  packages/hooks/│  ← Используют уровни 1-2
└─────────────────┘

┌─────────────────┐  Уровень 2: Бизнес-логика
│ exchange-core/  │
│ packages/utils/ │  ← Используют уровень 1
└─────────────────┘

┌─────────────────┐  Уровень 1: Фундамент
│packages/constants│  ← Независимые
└─────────────────┘
```

### 🎯 Ключевое правило:

**Уровень может зависеть только от нижних уровней. Никаких циклических зависимостей!**

---

## 🔗 Symlinks - "Живые ярлыки"

### Что это такое простыми словами:

**Symlink** = ярлык который **автоматически показывает актуальный файл**

### Как работает в нашем проекте:

```
📁 packages/ui/Button.tsx           ← ОРИГИНАЛ (1 файл)
                ↑
apps/web/node_modules/@repo/ui      ← ССЫЛКА 1 (ярлык)
                ↑
apps/admin/node_modules/@repo/ui    ← ССЫЛКА 2 (ярлык)
```

### Механика изменений:

1. Изменили `packages/ui/Button.tsx` (поменяли цвет)
2. Symlinks **мгновенно** показывают новую версию
3. Все приложения видят изменения **без копирования файлов**

### 🎯 Ключевая фишка:

**Один файл - много имен. Изменил в одном месте → работает везде!**

---

## 🎣 React Hooks - "Нервная система" приложения

### Аналогия с нервной системой:

- **Мозг** = React Engine
- **Нервы** = Хуки (useState, useEffect, кастомные)
- **Органы** = Компоненты

### Типы хуков в нашем проекте:

#### 1. **Собственная логика** (внутри хука):

```typescript
function useDebounce(value, delay) {
  const [debouncedValue, setDebouncedValue] = useState(value);
  // ВСЯ логика debounce написана здесь
  return debouncedValue;
}
```

#### 2. **Внешняя логика** (из exchange-core):

```typescript
function useOrderValidation() {
  const validation = useMemo(() => {
    return OrderValidator.validate(order); // Используем готовую функцию
  }, [order]);
}
```

#### 3. **Смешанная логика** (самое частое):

```typescript
function useTrading() {
  const [amount, setAmount] = useState(0); // Собственная React логика
  const commission = calculateCommission(amount); // Внешняя бизнес-логика
  const submitOrder = async () => {
    /* ... */
  }; // Смешанная логика
}
```

### 🎯 Правило распределения:

- **packages/hooks/** - переиспользуемые хуки для всех приложений
- **apps/\*/hooks/** - хуки специфичные для конкретного приложения
- **Внутри компонентов** - локальные хуки (useState, useEffect)

---

## 📦 Файлы зависимостей - "Карта снабжения"

### Почему разбросаны по файлам:

#### ❌ Один файл (плохо):

```json
// mega-package.json - ВСЕ зависимости в одном месте
{
  "dependencies": {
    "react": "^19.0.0", // Для UI
    "next": "15.1.3", // Для веб-приложения
    "express": "^4.18.0", // Для админки
    "markdown-it": "^13.0.0" // Для документации
  }
}
// Проблема: UI получает Express (зачем?), документация получает Next.js (не нужно!)
```

#### ✅ Разделенные файлы (хорошо):

```json
// packages/ui/package.json - ТОЛЬКО для UI
{
  "dependencies": {
    "react": "^19.0.0",        // Нужно для компонентов
    "clsx": "^2.1.1"           // Нужно для CSS классов
  }
}

// apps/web/package.json - ТОЛЬКО для веб-приложения
{
  "dependencies": {
    "@repo/ui": "*",           // Наши компоненты
    "next": "15.1.3"           // Фреймворк
  }
}
```

### 🎯 Преимущества разделения:

- **Точность** - каждый получает только нужное
- **Скорость** - быстрее установка и сборка
- **Переиспользование** - пакеты можно использовать отдельно

---

## ⚡ Turborepo - "Умный оркестратор"

### Как работает кэширование:

#### 1. **Отслеживание изменений через хеши**:

```bash
# Turborepo создает "отпечатки" файлов:
packages/constants/: hash_abc123
packages/ui/: hash_def456
apps/web/: hash_ghi789

# При изменении packages/ui/Button.tsx:
packages/ui/: hash_NEW_xyz ← ИЗМЕНИЛСЯ!
```

#### 2. **Граф зависимостей**:

```
constants → exchange-core → ui → apps/web
                        ↘ apps/admin
```

#### 3. **Умная пересборка**:

```bash
# Изменили packages/ui/Button.tsx
npm run build

⚡ CACHED packages/constants    (не зависит от UI)
⚡ CACHED packages/utils        (не зависит от UI)
⚡ CACHED packages/exchange-core (не зависит от UI)
✓ BUILDING packages/ui          (изменился!)
✓ BUILDING apps/web             (зависит от UI)
✓ BUILDING apps/admin           (зависит от UI)
```

### 🎯 Ключевая механика:

**Turborepo пересобирает только измененные пакеты + тех кто от них зависит**

---

## 🔧 TypeScript - "Умная паутина типов"

### Механика автоматического обновления типов:

#### 1. **Исходное состояние**:

```typescript
// packages/constants/currencies.ts
export const CRYPTOCURRENCIES = ['BTC', 'ETH', 'USDT'] as const;
export type CryptoCurrency = (typeof CRYPTOCURRENCIES)[number];
// Тип: 'BTC' | 'ETH' | 'USDT'
```

#### 2. **Добавляем DOGE**:

```typescript
export const CRYPTOCURRENCIES = ['BTC', 'ETH', 'USDT', 'DOGE'] as const;
// Тип АВТОМАТИЧЕСКИ стал: 'BTC' | 'ETH' | 'USDT' | 'DOGE'
```

#### 3. **TypeScript "видит" изменение везде**:

```typescript
// packages/ui/CurrencySelect.tsx - АВТОМАТИЧЕСКИ поддерживает DOGE
function handleSelect(currency: CryptoCurrency) {
  // currency теперь может быть 'DOGE' тоже!
}

// packages/exchange-core/calculations.ts - ТРЕБУЕТ обработать новый case
function calculateRate(currency: CryptoCurrency) {
  switch (currency) {
    case 'BTC':
      return 2500000;
    case 'ETH':
      return 90000;
    case 'USDT':
      return 37;
    case 'DOGE':
      return 15; // TypeScript требует добавить!
  }
}
```

### 🎯 Что происходит на фоне:

1. **TypeScript Language Server** отслеживает изменения файлов
2. **Строит граф типов** в памяти
3. **Находит зависимые файлы** и обновляет типы
4. **Уведомляет IDE** об ошибках и автокомплите

---

## 🔄 Полный цикл изменений - Пример с добавлением DOGE

### Что происходит при добавлении новой криптовалюты:

```typescript
// 1. Изменяете константу
export const CRYPTOCURRENCIES = ['BTC', 'ETH', 'USDT', 'DOGE'] as const

// 2. TypeScript МГНОВЕННО обновляет типы везде (еще до сборки!)

// 3. Turborepo видит изменение файла и запускает пересборку
npm run build

// 4. Turborepo пересобирает по цепочке зависимостей:
//    constants → exchange-core → ui → apps

// 5. Symlinks обеспечивают что приложения видят новые файлы
//    apps/web/node_modules/@repo/constants → packages/constants (новая версия)

// 6. Результат: DOGE работает везде автоматически!
```

### Что нужно дополнительно (если есть):

- Добавить иконку для DOGE
- Добавить курс обмена для DOGE
- Добавить моки для тестов с DOGE
- Все остальное работает **автоматически** благодаря архитектуре!

---

## 🌐 Hot Reload - "Живые обновления"

### Механика работы в режиме разработки:

#### 1. **File System Watcher следит за файлами**:

```javascript
// Упрощенно - что делает Next.js
fs.watch('packages/ui', (eventType, filename) => {
  if (filename === 'Button.tsx') {
    console.log('🔥 Button изменился! Обновляем...');
    hotReload();
  }
});
```

#### 2. **Webpack Hot Module Replacement**:

- Создает "патч" только для измененного модуля
- Отправляет через WebSocket в браузер
- Браузер заменяет модуль без перезагрузки страницы

#### 3. **Symlinks обеспечивают мгновенный доступ**:

```
Разработчик A: изменил packages/ui/Button.tsx
                        ↓ symlink
Разработчик B: apps/web видит изменения мгновенно
                        ↓ hot reload
Браузер: обновляется без перезагрузки
```

---

## 🎯 Ключевые принципы для запоминания

### 1. **Архитектура слоев**:

> Нижние уровни не знают о верхних. Зависимости только "вниз по течению".

### 2. **Symlinks**:

> Один файл - много ярлыков. Изменил в одном месте → работает везде.

### 3. **TypeScript**:

> Умная паутина типов. Изменение типа автоматически распространяется везде.

### 4. **Turborepo**:

> Пересобирает только нужное. Хеширование + граф зависимостей = умная сборка.

### 5. **Хуки**:

> Нервная система приложения. Могут содержать собственную логику, использовать внешнюю, или комбинировать.

### 6. **Файлы зависимостей**:

> Каждый пакет получает только то что нужно. Разделение = точность + скорость.

---

## 🚀 Практические команды для отладки

```bash
# Посмотреть граф зависимостей Turborepo
npx turbo build --graph

# Найти все symlinks в проекте
find . -type l -ls

# Поиск использования пакета
grep -r "@repo/constants" packages/ apps/

# Очистить кэш Turborepo
npx turbo clean

# Посмотреть логи TypeScript Language Server
# VS Code: Ctrl+Shift+P → "TypeScript: Open TS Server Log"

# Проверить хеши файлов (что отслеживает Turborepo)
npx turbo build --dry=json
```

---

**💡 Главная идея**: Монорепо - это **экосистема**, где каждая часть знает свою роль и все работает как единый организм благодаря symlinks, TypeScript, Turborepo и правильной архитектуре!
