# Урок 1.2: Turborepo - система сборки и кэширования

> **🎯 Цель урока**: Понять как Turborepo ускоряет разработку через умное кэширование и параллельную сборку

## 📖 Введение

Представьте кафе с несколькими поварами. Без системы каждый повар готовит все блюда с нуля каждый раз. С Turborepo - повара **переиспользуют** готовые ингредиенты и готовят **параллельно**.

Turborepo - это **оркестратор монорепозитория**, который:

- Кэширует результаты сборки
- Выполняет задачи параллельно
- Понимает зависимости между пакетами
- Ускоряет CI/CD в разы

## 🤔 Проблема без Turborepo

### Традиционная сборка (медленно):

```bash
# Сборка каждого пакета по очереди
cd packages/constants && npm run build    # 30 сек
cd packages/utils && npm run build        # 45 сек
cd packages/ui && npm run build           # 60 сек
cd packages/exchange-core && npm run build # 40 сек
cd apps/web && npm run build              # 120 сек

# Итого: 295 секунд (почти 5 минут!)
```

### ❌ Проблемы:

1. **Последовательная сборка** - ждем каждый пакет
2. **Повторная работа** - пересобираем неизмененные пакеты
3. **Нет кэширования** - каждый раз с нуля
4. **Ручная оркестровка** - нужно помнить порядок

## ✅ Решение: Turborepo

### Наша конфигурация `turbo.json`:

```json
{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "build/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    }
  }
}
```

## 🚀 Как работает Turborepo

### 1. **Граф зависимостей**

Turborepo понимает какой пакет от какого зависит:

```
packages/constants     (независимый)
       ↓
packages/exchange-core (зависит от constants)
       ↓
packages/utils         (зависит от exchange-core)
       ↓
packages/ui            (зависит от utils)
       ↓
apps/web               (зависит от всех packages)
```

### 2. **Параллельная сборка**

```bash
npm run build

# Turborepo выполняет параллельно:
# Уровень 1: packages/constants (независимо)
# Уровень 2: packages/exchange-core (после constants)
# Уровень 3: packages/utils + packages/ui (параллельно)
# Уровень 4: apps/web (после всех packages)
```

### 3. **Умное кэширование**

```bash
# Первая сборка
npm run build
# ✓ Built packages/constants
# ✓ Built packages/exchange-core
# ✓ Built packages/utils
# ✓ Built packages/ui
# ✓ Built apps/web

# Вторая сборка (ничего не изменилось)
npm run build
# ⚡ CACHED packages/constants
# ⚡ CACHED packages/exchange-core
# ⚡ CACHED packages/utils
# ⚡ CACHED packages/ui
# ⚡ CACHED apps/web
# Время: 2 секунды вместо 295!
```

## 🔍 Анализ реального кода

### Наша pipeline конфигурация:

```json
{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    }
  }
}
```

**Разбор**:

- `"dependsOn": ["^build"]` - сначала собери все зависимости
- `"outputs": [".next/**", "dist/**"]` - кэшируй эти папки

### Пример сборки packages/ui:

```json
// packages/ui/package.json
{
  "scripts": {
    "build": "tsc && tsc-alias",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@repo/constants": "*", // Зависимость
    "@repo/utils": "*" // Зависимость
  }
}
```

Turborepo знает: **сначала собрать constants и utils, потом ui**.

## 💻 Практические команды

### Основные команды Turborepo:

```bash
# Сборка всех пакетов с кэшированием
npm run build

# Разработка с hot reload
npm run dev

# Линтинг всех пакетов
npm run lint

# Тесты с кэшированием
npm run test

# Очистка кэша
npx turbo clean

# Информация о кэше
npx turbo info
```

### Сборка конкретного пакета:

```bash
# Собрать только UI пакет (+ его зависимости)
npx turbo build --filter=@repo/ui

# Собрать только web приложение
npx turbo build --filter=web

# Разработка только web приложения
npx turbo dev --filter=web
```

## 🎯 Преимущества в нашем проекте

### 1. **Скорость разработки**

```bash
# Изменили только константы
echo "export const NEW_CURRENCY = 'DOGE';" >> packages/constants/src/index.ts

npm run build
# ⚡ CACHED packages/utils
# ⚡ CACHED packages/ui
# ✓ Built packages/constants (только этот пакет)
# ✓ Built packages/exchange-core (зависит от constants)
# ✓ Built apps/web (зависит от exchange-core)
```

### 2. **CI/CD оптимизация**

В GitHub Actions Turborepo использует **remote cache**:

```yaml
# .github/workflows/ci.yml
- name: Build with cache
  run: |
    npx turbo build --cache-dir=.turbo
  env:
    TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
```

Результат: CI сборка **с 10 минут до 2 минут**.

### 3. **Команда разработчиков**

```bash
# Разработчик A изменил UI компонент
npm run build  # Кэш создается локально

# Разработчик B получает изменения
git pull
npm run build  # Использует кэш от разработчика A (если настроен remote cache)
```

## 🔧 Конфигурация в деталях

### Объяснение нашего `turbo.json`:

```json
{
  "ui": "tui", // Красивый интерфейс в терминале
  "pipeline": {
    "build": {
      "dependsOn": ["^build"], // ^ означает "зависимости этого пакета"
      "outputs": [".next/**", "dist/**"] // Что кэшировать
    },
    "dev": {
      "cache": false, // Dev режим не кэшируем
      "persistent": true // Процесс не завершается
    },
    "lint": {
      "dependsOn": ["^build"] // Линтинг после сборки зависимостей
    }
  }
}
```

### Зависимости пакетов:

```typescript
// packages/ui/package.json - зависит от utils
{
  "dependencies": {
    "@repo/utils": "*"
  }
}

// packages/utils/package.json - зависит от constants
{
  "dependencies": {
    "@repo/constants": "*"
  }
}
```

Turborepo автоматически создает граф: `constants → utils → ui`.

## 🎭 Сценарии использования

### Сценарий 1: Изменение константы

```bash
# Изменили EXCHANGE_LIMITS в constants
npm run build

# Turborepo пересобирает:
# ✓ constants (изменился)
# ✓ exchange-core (использует constants)
# ✓ web (использует exchange-core)
# ⚡ CACHED ui (не использует constants напрямую)
```

### Сценарий 2: Изменение UI компонента

```bash
# Изменили Button в packages/ui
npm run build

# Turborepo пересобирает:
# ⚡ CACHED constants
# ⚡ CACHED exchange-core
# ⚡ CACHED utils
# ✓ ui (изменился)
# ✓ web (использует ui)
```

### Сценарий 3: Ничего не изменилось

```bash
npm run build

# Turborepo:
# ⚡ CACHED ВСЁ
# Время: ~2 секунды
```

## ✅ Проверка знаний

1. **Вопрос**: Что означает `"dependsOn": ["^build"]` в turbo.json?

   <details>
   <summary>Ответ</summary>

   Это означает "сначала выполни команду build во всех зависимостях этого пакета, потом в самом пакете". Символ ^ указывает на зависимости.
   </details>

2. **Задача**: Запустите сборку два раза подряд и посмотрите на разницу во времени:

   ```bash
   npm run build  # Первый раз
   npm run build  # Второй раз - должен быть кэш
   ```

3. **Вопрос**: Почему в dev режиме отключено кэширование?

   <details>
   <summary>Ответ</summary>

   В dev режиме файлы постоянно изменяются и кэш быстро устаревает. Плюс нужен hot reload для мгновенного обновления при изменениях.
   </details>

## 🚀 Практическое задание

**Задание**: Исследуйте работу Turborepo:

1. **Запустите сборку с отчетом**:

   ```bash
   npx turbo build --dry=json
   ```

2. **Посмотрите граф зависимостей**:

   ```bash
   npx turbo build --graph
   ```

3. **Очистите кэш и сравните время**:

   ```bash
   npx turbo clean
   time npm run build  # Засеките время

   time npm run build  # Повторите - должно быть быстрее
   ```

4. **Измените файл и посмотрите что пересобирается**:

   ```bash
   # Добавьте комментарий в packages/constants/src/index.ts
   echo "// Test comment" >> packages/constants/src/index.ts

   npm run build  # Посмотрите что пересобралось
   ```

## 📊 Сравнение производительности

| Операция           | Без Turborepo | С Turborepo | Ускорение |
| ------------------ | ------------- | ----------- | --------- |
| Полная сборка      | 295 сек       | 180 сек     | 1.6x      |
| Повторная сборка   | 295 сек       | 2 сек       | 147x      |
| Изменение 1 пакета | 295 сек       | 45 сек      | 6.5x      |
| CI/CD сборка       | 600 сек       | 120 сек     | 5x        |

## 📚 Дополнительные материалы

- [Документация Turborepo](https://turbo.build/repo/docs)
- [Как работает кэширование в Turborepo](https://turbo.build/repo/docs/core-concepts/caching)
- [Remote Caching для команд](https://turbo.build/repo/docs/core-concepts/remote-caching)
- [Наша конфигурация turbo.json](../../turbo.json)

---

[← Урок 1.1](./lesson-1.1-what-is-monorepo.md) | [Урок 1.3: Архитектура пакетов →](./lesson-1.3-package-architecture.md)
