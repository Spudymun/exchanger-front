# Шпаргалка: Next.js Full-Stack Architecture

> **Основано на реальных вопросах и обсуждениях разработчика**

## 🤔 Главные вопросы и ответы

### 1. "Что такое Server и Client Components?"

#### 🖥️ **Server Components** (по умолчанию)

```typescript
// app/exchange/page.tsx - выполняется на СЕРВЕРЕ
export default async function ExchangePage() {
  // ✅ Можно обращаться к БД напрямую
  const rates = await database.getRates();

  // ✅ Можно использовать секретные ключи
  const apiData = await fetch('https://api.com', {
    headers: { 'Authorization': process.env.SECRET_KEY }
  });

  return <div>Курс Bitcoin: {rates.BTC}</div>;
}
```

#### 💻 **Client Components** ('use client')

```typescript
'use client'; // ← Эта строчка = выполняется в БРАУЗЕРЕ

export function ExchangeForm() {
  // ✅ Можно использовать состояние
  const [amount, setAmount] = useState(0);

  // ✅ Можно обрабатывать события
  const handleClick = () => setAmount(amount + 1);

  return <button onClick={handleClick}>Сумма: {amount}</button>;
}
```

#### 🎯 **Простое правило выбора:**

- **Нужна интерактивность?** (клики, формы, состояние) → Client Component
- **Нужны данные из БД/API?** → Server Component
- **Важно для SEO?** → Server Component

### 2. "Почему фронтенд обращается к БД?"

#### ❌ **Неправильное понимание:**

```typescript
'use client';
// ❌ Фронтенд НЕ обращается к БД напрямую!
const data = await database.query('SELECT * FROM users'); // Невозможно!
```

#### ✅ **Правильная архитектура:**

```
Браузер → tRPC/API → Server Component → База данных
   ↑         ↑            ↑              ↑
Client    API Route   Серверный код   PostgreSQL
Component
```

**Фронтенд НИКОГДА не обращается к БД напрямую!** Все через API.

### 3. "Мне не нужен бэкенд?"

#### 🤯 **Сюрприз: У вас УЖЕ есть бэкенд!**

```
Next.js проект = Фронтенд + Бэкенд в одном
├── app/                    ← Фронтенд (страницы)
├── src/components/         ← Фронтенд (компоненты)
├── src/server/             ← Бэкенд (API, tRPC)
└── pages/api/              ← Бэкенд (API routes)
```

**Вы пишете Full-Stack приложение!** Не только фронтенд.

### 4. "Это не опасно?"

#### ⚠️ **Есть дополнительные риски объединенной архитектуры:**

**Потенциальные уязвимости:**

1. **Случайная утечка серверного кода в клиент**
2. **Смешивание переменных окружения**
3. **Сложность аудита границ выполнения**
4. **Dependency confusion атаки**
5. **Ослабленные CSP политики**

#### 🛡️ **Как минимизировать риски - Полное руководство:**

##### 1. **Предотвращение утечки серверного кода**

**Проблема:** Можно случайно импортировать серверные утилиты в клиентские компоненты

```typescript
'use client';
import { hashPassword } from '../server/utils/auth'; // ❌ Утечка!
```

**Решение 1: ESLint правила**

```json
// .eslintrc.js
{
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["**/server/**", "**/lib/db/**", "**/lib/auth/**"],
            "message": "❌ Серверный код нельзя импортировать в клиентские компоненты!"
          }
        ]
      }
    ]
  }
}
```

**Минимизирует:** Автоматически блокирует импорт серверного кода в клиенте

**Решение 2: Строгое разделение barrel exports**

```typescript
// server/index.ts - ТОЛЬКО серверные экспорты
export { authRouter } from './trpc/routers/auth';
export { createContext } from './trpc/context';
// НЕ экспортируем утилиты с секретами!

// client/index.ts - ТОЛЬКО клиентские экспорты
export { Button } from './components/Button';
export { useAuth } from './hooks/useAuth';

// shared/index.ts - Только общие типы и константы
export type { User, Order } from './types';
export { ORDER_STATUSES } from './constants';
```

**Минимизирует:** Физически разделяет доступные импорты

##### 2. **Защита переменных окружения**

**Проблема:** Можно случайно сделать серверную переменную публичной

```bash
# ❌ ОПАСНО!
NEXT_PUBLIC_DATABASE_URL=postgresql://user:secret@localhost/db
```

**Решение: Строгая типизация и валидация**

```typescript
// lib/env.ts
import { z } from 'zod';

// Схема ТОЛЬКО для сервера
const serverEnvSchema = z.object({
  DATABASE_URL: z.string().url(),
  JWT_SECRET: z.string().min(32),
  SMTP_PASSWORD: z.string(),
  PAYMENT_API_SECRET: z.string(),
});

// Схема ТОЛЬКО для клиента
const clientEnvSchema = z.object({
  NEXT_PUBLIC_API_URL: z.string().url(),
  NEXT_PUBLIC_APP_NAME: z.string(),
  NEXT_PUBLIC_SENTRY_DSN: z.string().optional(),
});

// Валидация на старте приложения
export const serverEnv = serverEnvSchema.parse(process.env);
export const clientEnv = clientEnvSchema.parse({
  NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
  NEXT_PUBLIC_APP_NAME: process.env.NEXT_PUBLIC_APP_NAME,
  NEXT_PUBLIC_SENTRY_DSN: process.env.NEXT_PUBLIC_SENTRY_DSN,
});
```

**Минимизирует:** Предотвращает утечку секретов через типизацию

##### 3. **Bundle анализ и мониторинг**

**Проблема:** Серверные зависимости могут попасть в клиентский bundle

**Решение: Автоматический анализ**

```json
// package.json
{
  "scripts": {
    "build:analyze": "ANALYZE=true npm run build",
    "security:check-bundle": "node scripts/check-bundle-security.js"
  }
}
```

```javascript
// scripts/check-bundle-security.js
const serverOnlyPackages = [
  'bcrypt',
  'bcryptjs',
  'jsonwebtoken',
  'nodemailer',
  'pg',
  'mysql2',
  'mongodb',
  'redis',
  'aws-sdk',
];

// Анализ и проверка bundle на утечки серверных пакетов
```

**Минимизирует:** Автоматически выявляет утечки серверных зависимостей

##### 4. **Content Security Policy (CSP)**

**Решение: Поэтапное ужесточение CSP**

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const csp = `
    default-src 'self';
    script-src 'self' ${process.env.NODE_ENV === 'development' ? "'unsafe-eval'" : ''};
    style-src 'self' 'unsafe-inline';
    connect-src 'self' https://api.exchangego.com;
    object-src 'none';
    base-uri 'self';
  `
    .replace(/\s+/g, ' ')
    .trim();

  response.headers.set('Content-Security-Policy', csp);
  response.headers.set('X-Frame-Options', 'DENY');
  response.headers.set('X-Content-Type-Options', 'nosniff');

  return response;
}
```

**Минимизирует:** XSS атаки и инъекции контента

##### 5. **Специфичные меры для криптобиржи**

**Решение: Дополнительная защита финансовых операций**

```typescript
// Никогда не храните приватные ключи в коде
const WALLET_CONFIG = {
  walletServiceUrl: process.env.WALLET_SERVICE_URL,
  apiKey: process.env.WALLET_API_KEY,
};

// Двойная проверка всех финансовых операций
export async function createOrder(input: OrderInput) {
  const serverValidation = await validateOrderServer(input);
  const balanceCheck = await checkUserBalance(input.userId, input.amount);

  // Логирование ВСЕХ финансовых операций
  await auditLogger.log({
    action: 'ORDER_CREATE_ATTEMPT',
    userId: input.userId,
    amount: input.amount,
    ip: getClientIP(),
  });

  return processOrder(input);
}
```

**Минимизирует:** Финансовые потери от атак

#### 🔒 **Что защищено в вашем проекте:**

- ✅ Авторизация и роли (`middleware/auth.ts`)
- ✅ Rate limiting (`middleware/rateLimit.ts`)
- ✅ Валидация данных (Zod схемы)
- ✅ Разделение server/client кода

#### ⚠️ **Что нужно добавить для максимальной безопасности:**

- 🔧 ESLint правила против утечек серверного кода
- 🔧 Строгую типизацию переменных окружения
- 🔧 Bundle анализ в CI/CD pipeline
- 🔧 Автоматический security audit
- 🔧 Ужесточенный CSP в production
- 🔧 Аудит логирование всех критичных операций

### 5. "Почему не Next.js + NestJS?"

#### 📊 **Сравнение для вашего проекта:**

| Критерий                | Next.js Full-Stack     | Next.js + NestJS       |
| ----------------------- | ---------------------- | ---------------------- |
| **Скорость разработки** | 🚀 2 часа на фичу      | 🐌 3.5 часа на фичу    |
| **Type Safety**         | 🚀 Автоматическая      | ⚠️ Ручное дублирование |
| **Производительность**  | 🚀 0ms (прямые вызовы) | 🐌 +50ms HTTP          |
| **Команда**             | 🚀 1-5 разработчиков   | ⚠️ 5+ разработчиков    |
| **Деплой**              | 🚀 1 сервис            | ⚠️ 2+ сервиса          |

#### 🎯 **Для вашего проекта (30+ endpoints, малая команда):**

**Next.js Full-Stack оптимален!**

### 6. "А можно гибридную архитектуру?"

#### ✅ **Да! Концепция "Core + Services":**

```
┌─────────────────────────────┐    ┌─────────────────┐
│      Next.js Core           │    │  Микросервисы   │
│                             │    │                 │
│ - Основная биржа            │◄──►│ - Telegram бот  │
│ - Web UI                    │    │ - Email сервис  │
│ - Авторизация               │    │ - Аналитика     │
│ - Торговые операции         │    │ - ML сервисы    │
└─────────────────────────────┘    └─────────────────┘
```

#### 🎯 **Что выносить в микросервисы:**

- **Telegram бот** (отдельная технология)
- **Email уведомления** (независимые обновления)
- **Аналитика** (высокая нагрузка)
- **ML сервисы** (Python/специфичные библиотеки)

#### 🏠 **Что оставить в Next.js Core:**

- Основная бизнес-логика
- Web интерфейс
- Авторизация
- Критичные по производительности операции

## 🔧 Практические примеры из проекта

### Архитектура вашего проекта:

```typescript
// ✅ Server Component - загрузка данных
// app/[locale]/exchange/page.tsx
export default async function ExchangePage() {
  return <ExchangeContainer />;
}

// ✅ Client Component - интерактивность
// src/components/HeroExchangeForm.tsx
'use client';
export function HeroExchangeForm() {
  const [amount, setAmount] = useState('');
  return <form>...</form>;
}

// ✅ tRPC API - бэкенд логика
// src/server/trpc/routers/exchange.ts
export const exchangeRouter = createTRPCRouter({
  getRates: publicProcedure.query(async () => {
    return CRYPTOCURRENCIES.map(currency => getExchangeRate(currency));
  }),
});
```

### Безопасность реализована:

```typescript
// ✅ Авторизация
export const protectedProcedure = authMiddleware;

// ✅ Rate limiting
export const rateLimitMiddleware = {
  createOrder: publicProcedure.use(async ({ ctx, next }) => {
    await createRateLimiter('CREATE_ORDER')(ctx.ip);
    return next();
  }),
};

// ✅ Валидация
.input(securityEnhancedCreateExchangeOrderSchema)
```

## 🎯 Ключевые выводы

### 1. **Next.js = Full-Stack фреймворк**

- Не только фронтенд, но и бэкенд в одном проекте
- Server Components выполняются на сервере
- Client Components выполняются в браузере

### 2. **Безопасность не хуже традиционной архитектуры**

- Секреты остаются на сервере
- Границы безопасности те же
- Меньше поверхности атаки (один сервер вместо двух)

### 3. **Производительность лучше**

- Прямые вызовы функций вместо HTTP запросов
- Автоматическая типизация от сервера до клиента
- Меньше сетевых переходов

### 4. **Оптимально для вашего проекта**

- Размер: средний (30+ endpoints)
- Команда: малая (1-5 разработчиков)
- Требования: быстрая разработка MVP

### 5. **Гибридная архитектура возможна**

- Core на Next.js Full-Stack
- Специфичные сервисы как микросервисы
- Постепенное масштабирование

## 🚀 Рекомендации

### Сейчас:

- ✅ Продолжайте развивать Next.js Full-Stack
- ✅ Используйте Server Components для данных
- ✅ Используйте Client Components для интерактивности
- ✅ Применяйте существующие меры безопасности

### В будущем (при росте):

- 🔄 Вынесите Telegram бота в микросервис
- 🔄 Отделите email уведомления
- 🔄 Создайте отдельный сервис аналитики
- 🔄 Рассмотрите NestJS при команде >10 человек

## 🔐 Полный чек-лист безопасности Next.js Full-Stack

### ✅ **Обязательные меры (критичные):**

#### 1. **Защита от утечки серверного кода**

```bash
# Добавить в .eslintrc.js
npm install --save-dev eslint-plugin-import

# Настроить правила no-restricted-imports
# Запретить импорт server/** в client компонентах
```

**Защищает от:** Утечки секретов, API ключей, бизнес-логики

#### 2. **Типизация переменных окружения**

```typescript
// Создать lib/env.ts с валидацией Zod
// Разделить serverEnv и clientEnv
// Валидировать на старте приложения
```

**Защищает от:** Случайной публикации секретов

#### 3. **Security Headers в middleware**

```typescript
// Добавить в middleware.ts:
// - Content-Security-Policy
// - X-Frame-Options: DENY
// - X-Content-Type-Options: nosniff
// - Referrer-Policy: origin-when-cross-origin
```

**Защищает от:** XSS, clickjacking, MIME-type confusion

#### 4. **Bundle анализ**

```bash
# Добавить скрипт проверки bundle
# Автоматически искать серверные пакеты в клиентском коде
# Интегрировать в CI/CD
```

**Защищает от:** Утечки серверных зависимостей

### ⚠️ **Рекомендуемые меры (важные):**

#### 5. **Аудит логирование**

```typescript
// Логировать ВСЕ критичные операции:
// - Создание заказов
// - Изменение балансов
// - Авторизация
// - Смена паролей
// - Административные действия
```

**Защищает от:** Незаметных атак, помогает в расследовании

#### 6. **Rate limiting для критичных операций**

```typescript
// Дополнительные лимиты для:
// - Создания заказов (5/15мин)
// - Вывода средств (3/час)
// - Смены паролей (3/день)
// - API вызовов (100/мин)
```

**Защищает от:** Брутфорса, DDoS, злоупотреблений

#### 7. **Валидация на нескольких уровнях**

```typescript
// 1. Клиентская валидация (UX)
// 2. Серверная валидация (безопасность)
// 3. Валидация в БД (целостность)
// 4. Бизнес-правила (логика)
```

**Защищает от:** Инъекций, некорректных данных

#### 8. **Мониторинг и алерты**

```typescript
// Настроить уведомления на:
// - Множественные неудачные логины
// - Подозрительные финансовые операции
// - Ошибки сервера (500+)
// - Превышение rate limits
```

**Защищает от:** Атак в реальном времени

### 🔧 **Дополнительные меры (для продакшена):**

#### 9. **Secrets management**

```bash
# Использовать внешние сервисы:
# - AWS Secrets Manager
# - HashiCorp Vault
# - Azure Key Vault
# НЕ хранить секреты в .env файлах на продакшене
```

#### 10. **Database security**

```typescript
// - Использовать connection pooling
// - Настроить SSL для БД
// - Ограничить права пользователя БД
// - Регулярные бэкапы с шифрованием
```

#### 11. **Infrastructure security**

```bash
# - HTTPS везде (включая внутренние сервисы)
# - Firewall правила
# - VPN для административного доступа
# - Регулярные обновления системы
```

#### 12. **Compliance для криптобиржи**

```typescript
// - KYC/AML процедуры
// - Аудит трейл всех операций
// - Соответствие финансовому регулированию
// - Регулярные security аудиты
```

### 🚨 **Критичные НЕ ДЕЛАТЬ для криптобиржи:**

#### ❌ **Никогда не делайте:**

1. **Не храните приватные ключи кошельков в коде или БД**
2. **Не используйте слабые алгоритмы хеширования (MD5, SHA1)**
3. **Не доверяйте только клиентской валидации**
4. **Не логируйте пароли и приватные ключи**
5. **Не используйте HTTP для финансовых операций**
6. **Не храните пароли в открытом виде**
7. **Не игнорируйте security обновления зависимостей**

### 📊 **Приоритизация мер безопасности:**

#### 🔴 **Критичный приоритет (внедрить немедленно):**

1. ESLint правила против утечек
2. Типизация env переменных
3. Security headers
4. Аудит логирование финансовых операций

#### 🟡 **Высокий приоритет (в течение недели):**

1. Bundle анализ
2. Усиленный rate limiting
3. Мониторинг и алерты
4. Валидация на всех уровнях

#### 🟢 **Средний приоритет (в течение месяца):**

1. Secrets management
2. Database security
3. Infrastructure hardening
4. Compliance процедуры

### 🔍 **Регулярные проверки (еженедельно):**

```bash
# Security checklist
npm audit --audit-level moderate
npm outdated
node scripts/check-bundle-security.js
node scripts/security-audit.js

# Проверка логов на подозрительную активность
grep "FAILED_LOGIN" logs/app.log | tail -100
grep "RATE_LIMIT_EXCEEDED" logs/app.log | tail -50
```

## 📚 Полезные ссылки

- [Server Components документация](https://nextjs.org/docs/app/building-your-application/rendering/server-components)
- [tRPC документация](https://trpc.io/docs)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Next.js Security](https://nextjs.org/docs/app/building-your-application/configuring/content-security-policy)
- [Ваша структура проекта](../../apps/web/src/server/trpc/)

---

> **Помните:** Безопасность - это процесс, а не состояние. Регулярно обновляйте меры защиты и следите за новыми угрозами. Для криптобиржи безопасность критична - одна уязвимость может стоить миллионы.
