# @repo/tailwind-preset

Централизованный Tailwind CSS preset для ExchangeGO монорепо.

## 🎯 Назначение

Устраняет дублирование 20+ конфигурационных файлов Tailwind CSS в монорепо и обеспечивает:

- ✅ **Единую точку истины** для всех настроек стилей
- ✅ **Автоматическую поддержку темной темы** через CSS переменные
- ✅ **Интеграцию с design-tokens** из `@repo/design-tokens`
- ✅ **Семантические утилиты** для форм и компонентов
- ✅ **Консистентность** стилей между всеми приложениями

## 📦 Установка

Preset автоматически подключается через workspace dependencies:

```json
{
  "devDependencies": {
    "@repo/tailwind-preset": "*"
  }
}
```

## 🚀 Использование

### В Tailwind конфигурации приложения:

```javascript
// apps/web/tailwind.config.js
module.exports = {
  presets: [require('@repo/tailwind-preset/preset')],
  content: ['./app/**/*.{js,ts,jsx,tsx}', '../../packages/ui/src/**/*.{js,ts,jsx,tsx}'],
  // Дополнительные app-specific настройки при необходимости
};
```

### В глобальных стилях:

```css
/* apps/web/app/globals.css */
@import '@repo/tailwind-preset/globals.css';

/* Дополнительные app-specific стили */
```

## 🎨 Включенные возможности

### Семантические цвета

- Автоматическая поддержка светлой/темной темы
- HSL CSS переменные для всех цветов
- Интеграция с design-tokens

### Утилиты компонентов

- `.form-container` - контейнеры форм
- `.exchange-card` - карточки обмена
- `.exchange-connector` - визуальные соединители
- `.focus-ring` - единообразные focus состояния

### Кастомные компоненты

- Form patterns из design system
- Exchange-specific стили
- Interactive states

## 🔧 Архитектура

```
packages/tailwind-preset/
├── preset.js           # Основная конфигурация
├── globals.css         # CSS переменные и утилиты
├── package.json        # Метаданные пакета
└── README.md          # Документация
```

## 🎯 Миграция с дублированных конфигов

### До (проблема):

```javascript
// Дублирование в каждом app/package
module.exports = {
  content: [...],
  theme: {
    extend: {
      colors: {
        // 76+ строк идентичного кода
        border: "hsl(var(--border))",
        // ... дублирование
      }
    }
  }
};
```

### После (решение):

```javascript
// Единственная строка в каждом конфиге
module.exports = {
  presets: [require('@repo/tailwind-preset/preset')],
  content: [...], // Только app-specific content
};
```

## 📝 Преимущества

1. **Устранение дублирования**: Rule 20 compliance
2. **Централизованное управление**: Изменения в одном месте
3. **Типизация**: TypeScript поддержка через design-tokens
4. **Производительность**: Меньше кода для обработки
5. **Консистентность**: Одинаковые стили везде

## 🔄 Обновления

При изменении design tokens или добавлении новых утилит:

1. Обновить `preset.js` или `globals.css`
2. Изменения автоматически применятся во всех приложениях
3. Никаких изменений в app-specific конфигах

## 🛠️ Разработка

Для тестирования изменений:

```bash
# В корне монорепо
npm run dev
# Изменения в preset автоматически подхватываются
```

## 📖 Связанная документация

- [SEMANTIC_DESIGN_SYSTEM.md](../../docs/core/SEMANTIC_DESIGN_SYSTEM.md)
- [DEVELOPER_GUIDE.md](../../docs/core/DEVELOPER_GUIDE.md)
