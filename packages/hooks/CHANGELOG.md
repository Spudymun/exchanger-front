# Changelog

Все важные изменения в пакете `@repo/hooks` документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
и этот проект следует [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [2.0.0] - 2024-12-08

### 🚨 BREAKING CHANGES

#### Удалены deprecated файлы

- **УДАЛЕН**: `src/business/useAuth.ts` - deprecated хук аутентификации
- **УДАЛЕН**: `src/business/useForm.ts` - deprecated хук форм
- **УДАЛЕН**: `src/business/useForm.md` - документация к deprecated хуку

#### Миграция на современные API

- **ОБЯЗАТЕЛЬНО**: Используйте `useFormWithNextIntl` вместо `useForm`
- **ОБЯЗАТЕЛЬНО**: Используйте `useTranslations` из `next-intl` для локализации
- **ОБЯЗАТЕЛЬНО**: Импортируйте hooks из `@repo/hooks/src/client-hooks`

### ✅ Added

#### Новые экспорты в client-hooks.ts

- **ДОБАВЛЕН**: Экспорт `useFormWithNextIntl` из `./business/useFormWithNextIntl`
- **ДОБАВЛЕНЫ**: Типы `UseFormWithNextIntlParams`, `UseFormWithNextIntlReturn`

#### Обратная совместимость

- **СОХРАНЕНЫ**: Типы `UseFormReturn`, `UseFormOptions`, `FormField` через `useFormTypes.ts`
- **СОХРАНЕНЫ**: Типы `AuthMessages` для совместимости

### 🔧 Changed

#### Архитектурные изменения

- **ОБНОВЛЕНА**: Структура пакета - удалены deprecated файлы
- **ОБНОВЛЕНА**: Документация README.md с актуальной архитектурой
- **ОБНОВЛЕНА**: Документация проекта (PROJECT_CODEBASE_CATALOG.md, ARCHITECTURE.md, CODE_REVIEW_PROTOCOLS.md)

#### Улучшения

- **УЛУЧШЕНА**: SSR-safe архитектура - только современные API
- **УЛУЧШЕНА**: Поддержка i18n через `useFormWithNextIntl`
- **УЛУЧШЕНА**: TypeScript типизация для всех экспортов

### 🐛 Fixed

#### Исправления сборки

- **ИСПРАВЛЕНО**: Ошибки TypeScript после удаления deprecated файлов
- **ИСПРАВЛЕНО**: Отсутствующие экспорты `useFormWithNextIntl` в client-hooks.ts
- **ИСПРАВЛЕНО**: Неправильные типы в компонентах web приложения
- **ИСПРАВЛЕНО**: Отсутствующий параметр `t` в формах

#### Миграция компонентов

- **ИСПРАВЛЕНО**: `apps/web/src/components/forms/ExchangeForm.tsx` - добавлен `useTranslations`
- **ИСПРАВЛЕНО**: `apps/web/src/components/exchange-form/SendingCard.tsx` - обновлены типы
- **ИСПРАВЛЕНО**: `apps/web/src/components/exchange-form/ReceivingCard.tsx` - обновлены типы

### 📋 Migration Guide

#### Для форм:

```typescript
// ❌ Старый подход (больше не работает)
import { useForm } from '@repo/hooks';

// ✅ Новый подход
import { useFormWithNextIntl } from '@repo/hooks/src/client-hooks';
import { useTranslations } from 'next-intl';

const t = useTranslations('forms');
const form = useFormWithNextIntl({
  initialValues,
  validationSchema,
  t, // ОБЯЗАТЕЛЬНЫЙ параметр
  onSubmit,
});
```

#### Для аутентификации:

```typescript
// ❌ Старый подход (больше не работает)
import { useEnhancedAuth } from '@repo/hooks';

// ✅ Новый подход
import { useTranslations } from 'next-intl';
import { useNotifications } from '@repo/hooks/src/client-hooks';

const t = useTranslations('auth');
const notifications = useNotifications();
```

### 📊 Результаты

#### Успешные проверки

- ✅ **TypeScript**: Все проверки проходят (11/11 successful tasks)
- ✅ **Тесты**: Все тесты проходят (4/4 successful tasks)
- ✅ **Сборка**: Все приложения собираются успешно (web, admin-panel, docs)
- ✅ **Функциональность**: Полная поддержка i18n сохранена

#### Коммиты

- `1e683de` - Удаление deprecated файлов
- `8b8b8b8` - Миграция компонентов на современные API
- `5660cfa` - Исправление ошибок сборки

---

## [1.x.x] - Previous versions

Предыдущие версии содержали deprecated код, который был удален в v2.0.0.
Для истории изменений см. git log до коммита `1e683de`.
