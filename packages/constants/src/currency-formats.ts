/**
 * Константы для форматов валют и кодировок
 * Согласно ai-agent-rules.yml (правило 19) - все константы должны быть централизованы
 */

export const CURRENCY_FORMATS = {
  // Пока файл пуст, но зарезервирован для будущих констант валют
} as const;

export type CurrencyFormats = typeof CURRENCY_FORMATS;
