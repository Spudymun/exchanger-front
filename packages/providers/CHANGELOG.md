# Changelog

Все важные изменения в пакете `@repo/providers` документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
и этот проект следует [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [1.0.0] - 2024-12-08

### 📋 Added

#### Документация

- **ДОБАВЛЕН**: Подробный `README.md` с полной документацией пакета
- **ДОБАВЛЕН**: `CHANGELOG.md` для отслеживания изменений
- **ДОБАВЛЕНЫ**: Примеры интеграции для Next.js App Router
- **ДОБАВЛЕНЫ**: Best practices и SSR guidelines
- **ДОБАВЛЕНЫ**: Примеры тестирования с test utilities

#### API Documentation

- **ДОКУМЕНТИРОВАНЫ**: Все компоненты (`Providers`, `ThemeProvider`, `ThemeScript`)
- **ДОКУМЕНТИРОВАН**: `useTheme` hook с полным API
- **ДОКУМЕНТИРОВАНЫ**: TypeScript типы и интерфейсы
- **ДОКУМЕНТИРОВАНЫ**: Props и параметры конфигурации

#### Примеры использования

- **ДОБАВЛЕНЫ**: Примеры интеграции в web и admin-panel приложения
- **ДОБАВЛЕНЫ**: Примеры создания Theme Toggle компонента
- **ДОБАВЛЕНЫ**: Примеры кастомизации React Query настроек
- **ДОБАВЛЕНЫ**: Примеры тестирования компонентов

### 🔧 Technical Details

#### Существующая функциональность (документирована)

- **React Query Provider** - централизованное управление server state
  - Настройки: 5 минут staleTime, 1 retry, отключен refetchOnWindowFocus
- **Theme System** - полная поддержка light/dark/system тем
  - Интеграция с `@repo/hooks/useUIStore`
  - SSR-safe архитектура с предотвращением FOUC
- **ThemeScript** - синхронный скрипт инициализации темы
  - Предотвращение Flash of Unstyled Content
  - Fallback логика для безопасности

#### Зависимости

- `@repo/constants` - THEME_MODES константы
- `@repo/hooks` - useUIStore для состояния темы
- `@trpc/react-query` ^11.0.0 - tRPC интеграция
- `@tanstack/react-query` ^5.45.0 - Server state management
- `react` ^19.1.0, `react-dom` ^19.1.0

### 📊 Архитектурные решения

#### Разделение ответственности

- **`Providers`** - React Query + server state management
- **`ThemeProvider`** - Client-side темизация + DOM manipulation
- **`ThemeScript`** - SSR-safe инициализация для предотвращения FOUC

#### SSR-безопасность

- Синхронный скрипт выполняется до React hydration
- `suppressHydrationWarning` для theme-зависимых компонентов
- Правильная инициализация состояния на сервере и клиенте

#### Производительность

- Оптимизированные настройки React Query (5 мин кэш)
- Минимальные рефетчи (отключен refetchOnWindowFocus)
- Эффективное управление DOM классами для тем

### 🎯 Интеграция в экосистеме

#### Используется в приложениях

- `apps/web` - ThemeScript в layout.tsx, ThemeProvider в app-layout.tsx
- `apps/admin-panel` - Providers + ThemeProvider в layout.tsx
- `packages/ui` - useTheme в theme-toggle.tsx компоненте

#### Интеграция с пакетами

- `@repo/constants` - использует THEME_MODES
- `@repo/hooks` - интегрируется с useUIStore
- `@repo/ui` - предоставляет useTheme для компонентов

### 📋 Статистика использования

#### Файлы-потребители

- `packages/ui/src/components/theme-toggle.tsx` - useTheme hook
- `apps/web/app/layout.tsx` - ThemeScript компонент
- `apps/web/src/components/app-layout.tsx` - ThemeProvider
- `apps/admin-panel/app/layout.tsx` - Providers + ThemeProvider

#### Экспорты

- `Providers` - главный провайдер с React Query
- `ThemeProvider` - система темизации
- `useTheme` - хук управления темой
- `ThemeScript` - SSR-safe скрипт инициализации

### 🔄 Migration Guide

Пакет стабилен и не требует миграции. Документация описывает текущее состояние API.

#### Для новых интеграций:

```typescript
// 1. Добавьте ThemeScript в root layout
import { ThemeScript } from '@repo/providers';

export default function RootLayout({ children }) {
  return (
    <html suppressHydrationWarning>
      <head><ThemeScript /></head>
      <body>{children}</body>
    </html>
  );
}

// 2. Используйте провайдеры в app layout
import { ThemeProvider } from '@repo/providers';

export function AppLayout({ children }) {
  return (
    <ThemeProvider>
      {children}
    </ThemeProvider>
  );
}
```

### 📚 Документация

#### Новые разделы в README.md

- **Обзор и назначение** - роль пакета в архитектуре
- **Архитектура компонентов** - детальное описание структуры
- **API документация** - все компоненты и хуки
- **Интеграция в приложения** - примеры для Next.js
- **Работа с темизацией** - полный гайд по темам
- **Тестирование** - test utilities и примеры
- **Производительность** - оптимизации и настройки
- **SSR Guidelines** - best practices для SSR
- **Кастомизация** - примеры расширения функциональности

#### Связанная документация

- Ссылки на документацию других пакетов
- Интеграция с общей архитектурой проекта
- Best practices и рекомендации

---

## [0.0.0] - Previous versions

Предыдущие версии не имели формального changelog.
Для истории изменений см. git log до этого коммита.
