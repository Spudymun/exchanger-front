# Code Review: Мертвый код (Dead Code)

**Дата:** 16 октября 2025  
**Reviewer:** AI Senior Code Reviewer  
**Scope:** Все кодовые файлы из git status

**Критерии поиска:**

1. Неиспользуемые функции и переменные
2. Закомментированный код
3. Недостижимый код (unreachable code)
4. Импорты без использования
5. Устаревшие feature flags

---

## 📋 Статус анализа

- [x] Начало анализа
- [x] Файлы проанализированы: 23 файла
- [x] Отчет завершен

---

## 🔍 Обнаруженный мертвый код

### Категория: Закомментированный код

#### 1. `apps/web/src/server/trpc/routers/exchange.ts` - Закомментированные legacy импорты

**Проблема:** Строки 54-64 содержат большой блок закомментированного кода с "legacy" функциями.

**Детали:**

```typescript
/*
// ⚠️ LEGACY IMPORTS - ЗАКОММЕНТИРОВАНЫ ДЛЯ BACKWARD COMPATIBILITY
// 
// ВАЖНО: В данном файле legacy error creators не использовались напрямую
// Однако для единообразия с другими router файлами добавляем комментарий
// 
// ПОТЕНЦИАЛЬНЫЕ LEGACY FUNCTIONS (если бы использовались):
// - createOrderError('not_found') → createNotFoundError('Order not found')
// - createOrderError('cannot_cancel') → createBadRequestError('Order cannot be cancelled')
// - createOrderError('access_denied') → createForbiddenError('Access to order denied')
//
// createOrderError,
*/
```

**Проблемы:**

- Закомментированный код может вводить в заблуждение
- Неясно почему код закомментирован
- Комментарий упоминает backward compatibility, но код закомментирован "для единообразия"
- Потенциально может быть удален без последствий

**Рекомендация:**

- Удалить весь блок закомментированного кода
- Если нужна историческая справка, добавить ссылку на документацию или commit

**Приоритет:** 🟡 СРЕДНИЙ

---

### Категория: Неоптимальные константы

#### 2. `apps/web/src/server/trpc/routers/exchange.ts` - Использование UNKNOWN_ERROR_MESSAGE

**Проблема:** Константа `UNKNOWN_ERROR_MESSAGE = 'Unknown error'` используется 3 раза.

**Детали:**

- Объявление на строке 18
- Использование на строках 178, 279, 407

**Анализ:**
Хотя формально это не dead code, использование такой константы для всего 3-х мест сомнительно. Лучше было бы:

1. Использовать централизованные error constants из `@repo/constants`
2. Или просто inline строку 'Unknown error' (не требует константы)

**Рекомендация:**

- Переместить в `@repo/constants/src/error-messages.ts`
- Или удалить константу и использовать inline строку

**Приоритет:** 🟢 НИЗКИЙ (косметическая проблема)

---

#### 3. `apps/web/src/server/trpc/routers/exchange.ts` - Неиспользуемая константа DATABASE_URL_REQUIRED_ERROR

**Проблема:** Константа объявлена на строке 19, но используется только в одном месте.

**Детали:**

```typescript
const DATABASE_URL_REQUIRED_ERROR = 'DATABASE_URL environment variable is required' as const;
```

Используется всего 1-2 раза в файле - не требует отдельной константы.

**Рекомендация:**

- Удалить константу
- Использовать inline строку или централизованную error константу

**Приоритет:** 🟢 НИЗКИЙ

---

### Категория: Потенциально неиспользуемые экспорты

#### 4. `packages/constants/src/telegram.ts` - Множественные type exports

**Проблема:** Файл экспортирует 10+ типов (строки 287-300), но не все из них используются в проекте.

**Детали:**

```typescript
export type TelegramApiEndpoint = keyof typeof TELEGRAM_API;
export type TelegramIcon = keyof typeof TELEGRAM_OPERATOR_MESSAGES.ICONS;
export type TelegramHeader = keyof typeof TELEGRAM_OPERATOR_MESSAGES.HEADERS;
export type TelegramStatus = keyof typeof TELEGRAM_OPERATOR_MESSAGES.STATUS_MESSAGES;
export type TelegramAction = keyof typeof TELEGRAM_OPERATOR_MESSAGES.ACTIONS;
export type TelegramTemplate = keyof typeof TELEGRAM_OPERATOR_MESSAGES.TEMPLATES;
export type TelegramClientIcon = keyof typeof TELEGRAM_CLIENT_MESSAGES.ICONS;
export type TelegramClientGreeting = keyof typeof TELEGRAM_CLIENT_MESSAGES.GREETINGS;
export type TelegramClientResponse = keyof typeof TELEGRAM_CLIENT_MESSAGES.RESPONSES;
export type OperatorCancelReasonId = keyof typeof OPERATOR_CANCEL_REASONS;
```

**Требует проверки:**

- Нужно проверить какие из этих типов реально используются
- `TelegramApiEndpoint`, `TelegramIcon`, `TelegramHeader`, `TelegramStatus` могут быть неиспользуемыми
- Эти типы возможно были созданы "на будущее"

**Рекомендация:**

- Провести полный анализ использования через поиск по codebase
- Удалить неиспользуемые типы
- Альтернатива: оставить как "type-safe constants" для будущего использования

**Приоритет:** 🟢 НИЗКИЙ (требует детального анализа)

---

### Категория: Неиспользуемые части TELEGRAM_OPERATOR_MESSAGES

#### 5. `packages/constants/src/telegram.ts` - RATE_STATUS константы

**Проблема:** Блок `RATE_STATUS` (строки 57-65) может быть неиспользуемым.

**Детали:**

```typescript
RATE_STATUS: {
  BINANCE_SUCCESS: '🟡 Курс получен от Binance API',
  COINGECKO_SUCCESS: '🦎 Курс получен от CoinGecko API',
  FALLBACK_MODE: '⚠️ Используется резервный курс (+5%)',
  MOCK_MODE: '🔧 Используется статический курс',
  API_DEGRADED: (currency: string, reason: string) =>
    `⚠️ Проблема с API курсов ${currency}: ${reason}`,
},
```

**Требует проверки:**

- Эти сообщения не используются в `notify-operators.ts`
- Возможно добавлены для будущего использования
- CoinGecko API упоминается, но проект использует Binance

**Рекомендация:**

- Проверить используется ли это в Telegram bot
- Если нет - удалить или переместить в отдельный модуль "future features"

**Приоритет:** 🟡 СРЕДНИЙ

---

### Категория: Избыточные комментарии

#### 6. Множественные файлы - Чрезмерное количество комментариев `{/* */}`

**Затронутые файлы:**

- `apps/web/src/components/exchange/ExchangeContainer.tsx`
- `apps/web/src/components/exchange/ExchangeLayout.tsx`

**Проблема:** Файлы содержат десятки JSX комментариев `{/* Section Name */}` перед каждым блоком.

**Детали:**

```tsx
{
  /* Main Exchange Layout */
}
{
  /* Token Standard */
}
{
  /* Amount Input */
}
{
  /* Sending Info */
}
{
  /* Email Field - используем FormEmailField как универсальный компонент */
}
```

**Анализ:**

- Хотя это не dead code в прямом смысле, излишние комментарии засоряют код
- Структура компонента должна быть понятна без комментариев
- Если нужны комментарии - значит нужен рефакторинг

**Рекомендация:**

- Разбить большие компоненты на подкомпоненты с говорящими именами
- Удалить очевидные комментарии типа `{/* Token Standard */}` перед `<TokenStandardSelector />`
- Оставить только неочевидные комментарии с объяснением "почему"

**Приоритет:** 🟢 НИЗКИЙ (code style issue)

---

### Категория: Потенциально мертвый код (требует проверки)

#### 7. `packages/constants/src/telegram.ts` - TELEGRAM_CLIENT_MESSAGES

**Проблема:** Весь блок `TELEGRAM_CLIENT_MESSAGES` (строки 196-242) может быть неиспользуемым.

**Детали:**

```typescript
export const TELEGRAM_CLIENT_MESSAGES = {
  ICONS: { ... },
  GREETINGS: { ... },
  RESPONSES: { ... },
}
```

**Требует проверки:**

- Используется ли в telegram bot?
- Судя по комментарию "🆕 CLIENT SUPPORT" это новая функциональность
- Возможно функционал не реализован или в разработке

**Рекомендация:**

- Проверить используется ли в apps/telegram-bot
- Если нет - переместить в feature branch или удалить
- Если планируется использовать - добавить TODO с описанием

**Приоритет:** 🟡 СРЕДНИЙ

---

#### 8. `packages/constants/src/telegram.ts` - OPERATOR_CANCEL_REASONS

**Проблема:** Константы причин отмены `OPERATOR_CANCEL_REASONS` (строки 245-284) возможно не используются.

**Детали:**

- 6 различных причин отмены
- Детальные описания для каждой причины
- Не найдено использования в просмотренных файлах

**Требует проверки:**

- Используются ли в telegram bot для выбора причины отмены?
- Если нет - удалить или оставить как будущий функционал

**Рекомендация:**

- Grep search по всему проекту на использование `OPERATOR_CANCEL_REASONS`
- Если не используется - удалить с созданием issue для будущей реализации

**Приоритет:** 🟡 СРЕДНИЙ

---

### Категория: Неоптимальные структуры данных

#### 9. `packages/constants/src/exchange-currencies.ts` - MOCK_CRYPTO_ADDRESSES

**Проблема:** Константа `MOCK_CRYPTO_ADDRESSES` (строки 139-169) содержит моковые адреса, которые возможно не используются.

**Детали:**

```typescript
export const MOCK_CRYPTO_ADDRESSES = {
  BTC: [
    /* 4 адреса */
  ],
  ETH: [
    /* 4 адреса */
  ],
  USDT: [
    /* 4 адреса */
  ],
  LTC: [
    /* 4 адреса */
  ],
};
```

**Требует проверки:**

- Используются ли в тестах?
- Используются ли в development режиме?
- Genesis block address для BTC - скорее всего не используется

**Рекомендация:**

- Если используется только в тестах - переместить в test fixtures
- Если не используется вообще - удалить
- Добавить комментарий с объяснением purpose

**Приоритет:** 🟢 НИЗКИЙ

---

## 📊 Статистика

### По категориям:

- **Закомментированный код**: 1 случай
- **Неоптимальные константы**: 2 случая
- **Потенциально неиспользуемые экспорты**: 6 случаев
- **Избыточные комментарии**: 1 категория (множество файлов)

### По приоритетам:

- 🔴 **ВЫСОКИЙ**: 0 случаев
- 🟡 **СРЕДНИЙ**: 5 случаев
- 🟢 **НИЗКИЙ**: 4 случая

**Итого:** 9 обнаруженных проблем

---

## 🎯 Рекомендации по действиям

### Немедленно:

1. **#1** - Удалить закомментированный legacy код
2. **#5** - Проверить использование RATE_STATUS и удалить если не используется

### В ближайшее время:

3. **#7, #8** - Проверить CLIENT_MESSAGES и OPERATOR_CANCEL_REASONS через grep
4. **#4** - Аудит использования Telegram types
5. **#2, #3** - Рефакторинг error константmanagement

### Backlog:

6. **#6** - Cleanup JSX комментариев при рефакторинге компонентов
7. **#9** - Решить судьбу MOCK_CRYPTO_ADDRESSES

---

## ⚠️ Важные замечания

1. **Некритичность**: Все обнаруженные проблемы не влияют на функциональность
2. **Требуется верификация**: Пункты #4, #7, #8, #9 требуют проверки через полный codebase search
3. **Code style vs Dead code**: Многие проблемы относятся скорее к code style чем к dead code

---
