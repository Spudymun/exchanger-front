# 🔨 Build Commands Guide

Руководство по командам сборки проекта exchanger-front.

## 📋 Основные команды

### `npm run build`

Стандартная сборка проекта с использованием Turbo кеша.

```bash
npm run build
```

- ✅ Использует инкрементальную компиляцию TypeScript
- ✅ Использует Turbo cache (`.turbo/cache`)
- ✅ Быстрая пересборка при небольших изменениях
- 🔧 Собирает **все** packages и apps (10 проектов)

### `npm run rebuild`

**🆕 Полная чистая пересборка всего проекта.**

```bash
npm run rebuild
```

**Что делает:**

1. Удаляет все артефакты сборки:
   - `packages/*/dist` - скомпилированные пакеты
   - `apps/*/.next` - Next.js build cache
   - `apps/*/dist` - dist папки приложений
   - `.turbo` - Turbo cache
   - `**/*.tsbuildinfo` - TypeScript incremental cache
2. Запускает `npm run build` (собирает все 10 проектов)

**Когда использовать:**

- ❌ Ошибки компиляции из-за кеша (например, отсутствует `index.d.ts`)
- ❌ Проблемы с типами после изменения констант
- ❌ Странное поведение после git merge/rebase
- ✅ Перед деплоем в production (для уверенности)
- ✅ После обновления зависимостей

**Время выполнения:** ~2-3 минуты (все 10 проектов)

### `npm run rebuild:web`

**🎯 Rebuild только web приложения и его зависимостей.**

```bash
npm run rebuild:web
```

**Что делает:**

1. Удаляет **только** артефакты web:
   - `apps/web/.next` - Next.js build cache web приложения
   - `packages/*/dist` - все packages (пересоберутся)
   - `.turbo` - Turbo cache
   - `packages/**/*.tsbuildinfo` - TypeScript cache packages
2. Собирает web + его зависимости

**Что НЕ трогает:**

- ✅ `apps/admin-panel/.next` - остаётся нетронутым
- ✅ `apps/docs/.next` - остаётся нетронутым
- ✅ `apps/telegram-bot/.next` - остаётся нетронутым

**Время выполнения:** ~1 минута (быстрее чем полный rebuild)

### `npm run rebuild:admin`

**🎯 Rebuild только admin-panel и его зависимостей.**

```bash
npm run rebuild:admin
```

Удаляет только `apps/admin-panel/.next` + packages, остальные apps остаются рабочими.

### `npm run rebuild:docs`

**🎯 Rebuild только docs и его зависимостей.**

```bash
npm run rebuild:docs
```

Удаляет только `apps/docs/.next` + packages, остальные apps остаются рабочими.

### `npm run rebuild:bot`

**🎯 Rebuild только telegram-bot и его зависимостей.**

```bash
npm run rebuild:bot
```

Удаляет только `apps/telegram-bot/.next` + packages, остальные apps остаются рабочими.

### `npm run rebuild:packages`

**📦 Rebuild только всех packages (без apps).**

```bash
npm run rebuild:packages
```

**Что делает:**

- Удаляет только `packages/*/dist` и `.turbo`
- НЕ трогает `.next` папки apps
- Пересобирает все packages

**Когда использовать:**

- Изменили код в `packages/@repo/*`
- Нужно пересобрать только библиотеки
- Apps уже собраны и их трогать не нужно

**Время выполнения:** ~30-40 секунд

### `npm run clean`

**Глобальная очистка всех артефактов сборки.**

```bash
npm run clean
```

**⚠️ ВНИМАНИЕ:** Удаляет `.next` папки **ВСЕХ** приложений!

Используйте `clean:web`, `clean:admin`, `clean:bot`, `clean:docs` или `clean:packages` для точечной очистки.

## 🔥 Turbo-специфичные команды

### `npm run build:force`

Игнорирует Turbo cache и пересобирает все пакеты.

```bash
npm run build:force
```

**Использует:** `turbo run build --force`

**Отличие от `rebuild`:**

- ❌ НЕ удаляет TypeScript `.tsbuildinfo` кеш
- ❌ НЕ удаляет dist папки
- ✅ Игнорирует только Turbo cache

**Когда использовать:**

- Нужно пересобрать с учётом изменённых environment variables
- Подозрение на проблемы с Turbo cache

## 📊 Таблица сравнения команд

| Команда                    | Что собирает        | Время    | Удаляет cache | Полная сборка |
| -------------------------- | ------------------- | -------- | ------------- | ------------- |
| `npm run build`            | Все (10 проектов)   | ~1-2 мин | ❌            | ❌            |
| `npm run rebuild`          | Все (10 проектов)   | ~2-3 мин | ✅            | ✅            |
| `npm run rebuild:web`      | web + dependencies  | ~1 мин   | ✅            | ✅            |
| `npm run rebuild:admin`    | admin + deps        | ~1 мин   | ✅            | ✅            |
| `npm run rebuild:docs`     | docs + deps         | ~1 мин   | ✅            | ✅            |
| `npm run rebuild:bot`      | telegram-bot + deps | ~1 мин   | ✅            | ✅            |
| `npm run rebuild:packages` | Только packages     | ~30-40 с | ✅            | ✅            |
| `npm run build:force`      | Все (10 проектов)   | ~1-2 мин | Только Turbo  | ✅            |
| `npm run clean`            | Ничего              | ~5 с     | ✅            | -             |

## 🐛 Решение проблем

### Проблема: `Cannot find module '@repo/constants'`

```bash
npm run rebuild
```

### Проблема: `index.d.ts` не генерируется

```bash
npm run rebuild
```

### Проблема: Типы не обновляются после изменения

```bash
npm run rebuild
```

### Проблема: Старые email в production логах

```bash
npm run rebuild
cd apps/web
npm run start
```

## 🎯 Best Practices

### Локальная разработка

```bash
# Обычная работа - используйте dev режим
npm run dev

# Работаете только над web
npm run dev:web

# При проблемах с типами/кешем - точечный rebuild
npm run rebuild:web
cd apps/web && npm run start

# Полная пересборка (редко нужна)
npm run rebuild
```

### Быстрая разработка конкретного app

```bash
# Сценарий 1: Работаете над web
npm run rebuild:web
cd apps/web && npm run start

# Сценарий 2: Работаете над admin панелью
npm run rebuild:admin
cd apps/admin-panel && npm run start

# Сценарий 3: Изменили только packages
npm run rebuild:packages
# Затем запускайте нужный app
```

### После изменений в packages

```bash
# Быстрый вариант - только packages
npm run rebuild:packages

# Надёжный вариант - полный rebuild
npm run rebuild
```

### CI/CD Pipeline

```bash
# Рекомендуется использовать rebuild для чистых сборок
npm run rebuild

# Или для скорости с кешем Turbo
npm run build:force
```

### Production Deployment

```bash
# Всегда используйте полный rebuild перед деплоем
npm run rebuild

# Затем запускайте нужное приложение
cd apps/web
npm run start  # Запуск production сервера
```

## 📖 Дополнительные ресурсы

- [Turbo --force флаг](https://turbo.build/repo/docs/reference/run#--force)
- [TypeScript Incremental Compilation](https://www.typescriptlang.org/docs/handbook/project-references.html)
- [Rimraf (кросс-платформенное удаление)](https://github.com/isaacs/rimraf)

## 🔧 Техническая информация

### Что удаляет `npm run clean`

```bash
# ГЛОБАЛЬНАЯ очистка - удаляет ВСЁ
del-cli "packages/*/dist"      # Все dist в packages
del-cli "apps/*/.next"          # Next.js cache ВСЕХ apps ⚠️
del-cli "apps/*/dist"           # Dist в apps
del-cli ".turbo"                # Turbo cache
del-cli "**/*.tsbuildinfo"      # TypeScript incremental cache
```

### Точечные clean команды

```bash
# Удаляют только конкретный app + packages
npm run clean:web      # только apps/web/.next + packages
npm run clean:admin    # только apps/admin-panel/.next + packages
npm run clean:bot      # только apps/telegram-bot/.next + packages
npm run clean:docs     # только apps/docs/.next + packages
npm run clean:packages # только packages (без apps)
```

**Преимущество:** Другие apps остаются собранными и готовыми к запуску!

### Порядок выполнения `npm run rebuild`

1. **Очистка** (`npm run clean`)
   - Удаление всех артефактов
   - Удаление кешей

2. **Сборка** (`npm run build`)
   - Turbo определяет зависимости
   - Компиляция TypeScript
   - Генерация `.d.ts` файлов
   - Сборка Next.js приложений

### Как работает фильтрация в rebuild:web

```bash
# rebuild:web использует:
turbo run build --filter=web...
```

**Три точки `...`** = включить app И все его dependencies!

```
web зависит от:
├── @repo/constants
├── @repo/ui
├── @repo/hooks
├── @repo/exchange-core
└── @repo/session-management

rebuild:web пересоберёт:
✅ @repo/constants
✅ @repo/ui
✅ @repo/hooks
✅ @repo/exchange-core
✅ @repo/session-management
✅ web (само приложение)
```

Это гарантирует, что web получит актуальные версии всех зависимостей!

## ⚠️ Важные замечания

1. **Не коммитьте** `.tsbuildinfo` файлы в git (они в `.gitignore`)
2. **Не коммитьте** `dist` папки
3. **Используйте `rebuild:web`** вместо `rebuild` для точечной пересборки
4. **`rebuild:web` НЕ удаляет** `.next` других apps - они остаются рабочими!
5. **В CI/CD** предпочтительно полный `rebuild` для воспроизводимых сборок

## 🔥 Частые ошибки

### ❌ Проблема: После `rebuild:bot` упал `web`

```
Error: Could not find a production build in the '.next' directory
```

**Причина:** Раньше `rebuild:bot` вызывал глобальный `clean`, который удалял `.next` всех apps.

**Решение:** ✅ Исправлено! Теперь `rebuild:bot` удаляет только `apps/telegram-bot/.next`

### ✅ Правильное использование rebuild команд

```bash
# Работаете над web - пересобираете только web
npm run rebuild:web
cd apps/web && npm run start  # работает ✅

# Работаете над bot - пересобираете только bot
npm run rebuild:bot
cd apps/telegram-bot && npm run start  # работает ✅

# web всё ещё работает!
cd apps/web && npm run start  # работает ✅
```
