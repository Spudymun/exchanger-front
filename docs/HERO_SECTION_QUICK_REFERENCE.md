# 🚀 Hero Section - Quick Reference

> **Быстрый справочник** для изменения стилей Hero Section обменника

---

## 🎯 Основные настройки

### **📏 Ширина контейнера**

```tsx
// packages/ui/src/components/exchange-form.tsx:15
hero: '... max-w-2xl lg:max-w-4xl mx-auto w-full';
//         ^^^^^^^^^^^^^^^^^^^^^ ЗДЕСЬ МЕНЯТЬ
```

| Вариант                  | Мобильная | Десктопная | Применение        |
| ------------------------ | --------- | ---------- | ----------------- |
| `max-w-xl lg:max-w-2xl`  | 576px     | 672px      | Узкий контейнер   |
| `max-w-2xl lg:max-w-4xl` | 672px     | 896px      | **Текущий**       |
| `max-w-3xl lg:max-w-5xl` | 768px     | 1024px     | Широкий контейнер |

### **📱 Переключение на горизонтальный layout**

```tsx
// packages/ui/src/components/exchange-form.tsx:165
'flex flex-col md:grid md:grid-cols-[1fr_auto_1fr]';
//             ^^ ЗДЕСЬ МЕНЯТЬ BREAKPOINT
```

| Breakpoint | Размер  | Когда использовать          |
| ---------- | ------- | --------------------------- |
| `sm:grid`  | 640px+  | Очень ранний переход        |
| `md:grid`  | 768px+  | **Текущий** (рекомендуется) |
| `lg:grid`  | 1024px+ | Поздний переход             |

### **🎨 Отступы и gaps**

```tsx
// Контейнер
p-4 sm:p-6 md:p-8  // Внутренние отступы

// Между карточками
gap-6 sm:gap-8 md:gap-10  // Расстояния

// Внутри карточек
p-6 sm:p-8  // Отступы в карточках
```

---

## ⚡ Быстрые команды

### **Сделать контейнер шире**

```tsx
// Было
max-w-2xl lg:max-w-4xl

// Стало
max-w-3xl lg:max-w-5xl
```

### **Сделать контейнер уже**

```tsx
// Было
max-w-2xl lg:max-w-4xl

// Стало
max-w-xl lg:max-w-3xl
```

### **Переключиться на горизонтальный layout раньше**

```tsx
// Было
flex flex-col md:grid

// Стало
flex flex-col sm:grid  // На больших мобильных
```

### **Увеличить gaps между карточками**

```tsx
// Было
gap-6 sm:gap-8 md:gap-10

// Стало
gap-8 sm:gap-10 md:gap-12
```

---

## 🔧 Распространенные задачи

### **Задача: Обменник слишком широкий на десктопе**

```tsx
// Уменьшить max-width для lg:
max-w-2xl lg:max-w-3xl  // Вместо lg:max-w-4xl
```

### **Задача: Обменник слишком узкий на десктопе**

```tsx
// Увеличить max-width для lg:
max-w-2xl lg:max-w-5xl  // Вместо lg:max-w-4xl
```

### **Задача: На планшете карточки не переключаются в ряд**

```tsx
// Изменить breakpoint на более ранний:
flex flex-col sm:grid  // Вместо md:grid
```

### **Задача: Текст сжатый, нужно больше места**

```tsx
// Увеличить gaps:
gap-8 sm:gap-10 md:gap-12  // Вместо gap-6 sm:gap-8 md:gap-10

// И padding карточек:
p-8 sm:p-10  // Вместо p-6 sm:p-8
```

---

## ⚠️ Что НЕ трогать

- ❌ **НЕ убирать** `mx-auto` - сломается центрирование
- ❌ **НЕ использовать** `max-w-none` - растянется на весь экран
- ❌ **НЕ ставить** breakpoint `lg:` для CardPair - поздно
- ❌ **НЕ уменьшать** gaps меньше `gap-4` - будет сжато

---

## 🧪 Проверка результата

```bash
# 1. Собрать проект
npm run build:web

# 2. Проверить в браузере
# F12 → Device Toolbar → протестировать:
# - 360px (мобильный)
# - 768px (планшет)
# - 1024px (десктоп)
```

---

📖 **Полная документация:** [`HERO_SECTION_STYLING_GUIDE.md`](./HERO_SECTION_STYLING_GUIDE.md)
