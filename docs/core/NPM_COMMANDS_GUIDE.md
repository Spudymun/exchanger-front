# 🎯 NPM Commands Guide - Полный справочник команд

> **Навигация по проекту без каши в голове**

Этот документ решает проблему понимания npm-скриптов в монорепозитории exchanger-front. Здесь вы найдете ответы на все вопросы: что, где и как запускать.

## 📁 Архитектура: Где выполнять команды

### 🏗️ Структура проекта

```
e:\project\exchanger-front\          ← КОРЕНЬ (99% команд здесь!)
├── apps/
│   ├── web/                        ← Next.js приложение (порт 3000)
│   ├── admin-panel/                ← Админ панель (порт 3002)
│   └── docs/                       ← Документация (порт 3001)
├── packages/
│   ├── constants/                  ← ЕДИНСТВЕННЫЙ с build:clean/force
│   ├── ui/                         ← Только lint + check-types
│   ├── utils/                      ← Только lint + check-types
│   ├── exchange-core/              ← build + check-types
│   └── [другие пакеты]
└── exchanger-gateway/              ← ОТДЕЛЬНЫЙ ПРОЕКТ (NestJS)
```

### ⚡ Принцип работы

**ВАЖНО**: Благодаря **Turborepo**, почти все команды выполняются из **КОРНЯ проекта**!

- **КОРЕНЬ** (`e:\project\exchanger-front\`) - основное место работы
- **ПРИЛОЖЕНИЯ** (`apps/*`) - только для специфичных задач
- **ПАКЕТЫ** (`packages/*`) - редко, в основном для отладки
- **GATEWAY** (`exchanger-gateway/`) - совершенно отдельная экосистема

## 🚀 Команды по категориям

### 1️⃣ РАЗРАБОТКА (Development)

#### В КОРНЕ проекта:

```powershell
# 🎯 Запуск всех приложений сразу
npm run dev                         # web + admin + docs одновременно

# 🎯 Запуск конкретного приложения
npm run dev --workspace=web         # localhost:3000 (основное)
npm run dev --workspace=admin-panel # localhost:3002 (админка)
npm run dev --workspace=docs        # localhost:3001 (документация)

# 🎯 Storybook (UI компоненты)
npm run storybook                   # localhost:6006
```

#### В конкретном приложении (редко нужно):

```powershell
# Находясь в apps/web/
npm run dev    # Только web приложение
npm run build  # Сборка только web
npm run start  # Production режим web
```

### 2️⃣ СБОРКА (Build)

#### В КОРНЕ (рекомендуется):

```powershell
# 🎯 Сборка всего проекта
npm run build                       # Все приложения и пакеты

# 🎯 Специальные скрипты для constants (ЕДИНСТВЕННЫЙ!)
npm run build:clean                 # Очистка TypeScript кеша
npm run build:force                 # Принудительная пересборка
npm run build:rebuild               # clean + force (полный цикл)

# 🎯 Сборка конкретных частей
npm run build --workspace=web       # Только web приложение
npm run build --workspace=admin-panel # Только админка
```

#### Почему build:clean/force работают только для constants?

**АРХИТЕКТУРНОЕ ОБОСНОВАНИЕ:**

- `constants` использует `tsc --build` с инкрементальной компиляцией
- Создает `.tsbuildinfo` файлы для кеширования
- При изменении структуры может понадобиться очистка кеша
- `utils`, `ui` используются напрямую через TypeScript - нет кеша для очистки
- `web`, `admin-panel` - Next.js сам управляет кешем

**ЭТО ПРАВИЛЬНАЯ АРХИТЕКТУРА!** Разные типы пакетов = разные потребности в сборке.

> 📖 **Подробное объяснение**: Детальный анализ архитектурных принципов и стратегий сборки пакетов см. в **[ARCHITECTURE.md - Build Strategies](ARCHITECTURE.md#-build-strategies)**

### 3️⃣ ПРОВЕРКА КАЧЕСТВА (Quality Control)

#### В КОРНЕ (ОБЯЗАТЕЛЬНО здесь!):

```powershell
# 🎯 Базовые проверки
npm run lint                        # ESLint для всех файлов
npm run check-types                 # TypeScript проверка
npm run test                        # Все unit тесты

# 🎯 Дополнительные проверки
npm run test:coverage               # Тесты с покрытием
npm run test:watch                  # Тесты в watch режиме
npm run lint:styles                 # Stylelint для CSS/SCSS

# 🎯 E2E тестирование
npm run test:e2e                    # Playwright тесты
npm run test:e2e:ui                 # Playwright с интерфейсом
npm run test:e2e:headed             # Playwright в браузере
```

#### ⚠️ ДВУХУРОВНЕВАЯ СИСТЕМА ЛИНТЕРОВ

**КРИТИЧЕСКИ ВАЖНО понимать:**

1. **Уровень 1**: `npm run lint` - базовые проверки
2. **Уровень 2**: `git commit` - СТРОГИЕ проверки через pre-commit хуки

```powershell
# ✅ Может пройти без ошибок
npm run lint

# ❌ Но может заблокироваться здесь!
git add .
git commit -m "my changes"          # ФИНАЛЬНАЯ проверка!
```

**Почему разные?**

- Разные конфигурации ESLint
- Pre-commit проверяет только staged файлы
- Добавляет type-check и тесты всего проекта

### 4️⃣ ФОРМАТИРОВАНИЕ

#### В КОРНЕ:

```powershell
# 🎯 Форматирование кода
npm run format                      # Prettier для всех файлов
npm run format:styles               # Stylelint с автофиксом
```

### 5️⃣ ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ

#### В КОРНЕ:

```powershell
# 🎯 Производительность и отладка
npm run lint:benchmark              # Скорость ESLint конфига
npm run clean                       # Очистка build артефактов

# 🎯 Установка и настройка
npm install                         # Все зависимости
npm run prepare                     # Настройка pre-commit хуков

# 🎯 Анализ
npm run build:analyze               # Анализ размера бандла
```

## 🎯 РАБОЧИЕ СЦЕНАРИИ (Workflows)

### 🚀 Начало разработки

```powershell
# 1. Переходим в корень проекта
cd e:\project\exchanger-front\

# 2. Проверяем что все работает
npm run lint
npm run check-types
npm run test

# 3. Создаем ветку
git checkout -b feature/my-task

# 4. Запускаем разработку
npm run dev                         # Все приложения
# ИЛИ
npm run dev --workspace=web         # Только нужное
```

### 🔍 Проверка перед коммитом

```powershell
# 1. Базовые проверки
npm run lint                        # Исправить ошибки
npm run check-types                 # Проверить типы
npm run test                        # Запустить тесты

# 2. ОБЯЗАТЕЛЬНАЯ финальная проверка
git add .
git commit -m "feat: my changes"    # Pre-commit хуки сработают!

# Если коммит не прошел - исправить ошибки и повторить
```

### 🏗️ Работа с constants

```powershell
# Если редактируете константы
cd packages/constants/

# Разработка с автопересборкой
npm run dev                         # tsc --watch

# При проблемах со сборкой
npm run build:clean                 # Очистить кеш
npm run build:force                 # Принудительная сборка
```

### 🧪 Тестирование

```powershell
# Unit тесты
npm run test                        # Все тесты
npm run test:watch                  # Watch режим
npm run test:coverage               # С покрытием

# E2E тесты
npm run test:e2e                    # Автоматические
npm run test:e2e:ui                 # С интерфейсом
```

## ⚠️ ЧАСТЫЕ ОШИБКИ И РЕШЕНИЯ

### 🚨 "build:clean работает только для constants - это ошибка?"

**✅ НЕТ! Это ПРАВИЛЬНАЯ архитектура!**

**Объяснение:**

- `build:clean/force` нужны только для пакетов с инкрементальной компиляцией
- `constants` единственный использует `tsc --build` с кешированием
- `utils`, `ui` используются напрямую - нет кеша для очистки
- `web`, `admin-panel` - Next.js сам управляет своим кешем

### 🚨 "npm run lint прошел, но git commit не работает"

**Причина**: Двухуровневая система линтеров

**Решение**:

```powershell
# 1. Посмотреть РЕАЛЬНЫЕ ошибки
git add .
git commit -m "test"                # Увидите все ошибки

# 2. Исправить и повторить
npm run lint                        # Исправить базовые
git add .
git commit -m "fix: lint errors"    # Попробовать снова
```

### 🚨 "TypeScript ошибки"

```powershell
# Проверить типы
npm run check-types

# Очистить кеш и переустановить
Remove-Item -Recurse -Force node_modules, .next, .turbo, dist
npm install
```

### 🚨 "Приложение не запускается"

```powershell
# Проверить что все зависимости установлены
npm install

# Проверить порты (может быть заняты)
npm run dev --workspace=web         # Если нужен только web

# Очистить кеш
npm run clean
```

## 🌟 GATEWAY проект (отдельно!)

```powershell
# Переходим в отдельный проект
cd e:\project\exchanger-gateway\

# NestJS команды
npm run start                       # Обычный запуск
npm run start:dev                   # Watch режим
npm run start:prod                  # Production

# Тестирование
npm run test                        # Unit тесты
npm run test:e2e                    # E2E тесты
npm run test:cov                    # Покрытие
```

## 📊 БЫСТРАЯ СПРАВКА

| Задача                 | Команда               | Где выполнять |
| ---------------------- | --------------------- | ------------- |
| Запуск разработки      | `npm run dev`         | **КОРЕНЬ**    |
| Сборка проекта         | `npm run build`       | **КОРЕНЬ**    |
| Проверка качества      | `npm run lint`        | **КОРЕНЬ**    |
| Проверка типов         | `npm run check-types` | **КОРЕНЬ**    |
| Тесты                  | `npm run test`        | **КОРЕНЬ**    |
| E2E тесты              | `npm run test:e2e`    | **КОРЕНЬ**    |
| Очистка constants      | `npm run build:clean` | **КОРЕНЬ**    |
| **ФИНАЛЬНАЯ проверка** | `git commit`          | **КОРЕНЬ**    |

## 🎯 КЛЮЧЕВЫЕ ПРИНЦИПЫ

1. **99% команд выполняются в КОРНЕ** благодаря Turborepo
2. **build:clean/force только для constants** - это архитектурно правильно
3. **git commit - финальная проверка** строже чем npm run lint
4. **Разные типы пакетов = разные команды** - это нормально
5. **exchanger-gateway отдельный проект** - переключаться в его папку

## 🔗 Связанная документация

- **[DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md)** - общее руководство разработчика
- **[TASK_IMPLEMENTATION_GUIDE.md](TASK_IMPLEMENTATION_GUIDE.md)** - реализация задач
- **[ARCHITECTURE.md](ARCHITECTURE.md)** - архитектура проекта
- **[PRE_COMMIT_GUIDE.md](../process/PRE_COMMIT_GUIDE.md)** - работа с pre-commit хуками

---

**🎉 Теперь у вас есть полное понимание npm-команд в проекте!**

_Создано: 7 июля 2025_

## 🛡️ Защищенные файлы от автоформатирования

### docs/ai-agent-rules.yml

Этот файл защищен от автоматического форматирования для сохранения точной структуры YAML:

**Механизмы защиты:**

1. **.prettierignore** - исключает файл из prettier форматирования
2. **.lintstagedrc.json** - содержит специальное правило, которое заменяет форматирование на echo-сообщение

**Причины защиты:**

- Файл содержит критически важную структуру с системой триггеров
- Автоформатирование может нарушить иерархию YAML и комментарии
- Версионность файла должна контролироваться вручную

**Как проверить защиту:**

```bash
# Prettier должен пропустить файл
npx prettier --check docs/ai-agent-rules.yml

# Lint-staged должен показать skip-сообщение при коммите
git add docs/ai-agent-rules.yml
npx lint-staged  # Должен показать "Skipping formatting"
```

---
