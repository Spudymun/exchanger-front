# 🛡️ РУКОВОДСТВО ПО АУДИТУ БЕЗОПАСНОСТИ ПРИЛОЖЕНИЯ

**Версия документа**: 1.0  
**Дата создания**: 1 октября 2025  
**Тип**: Техническая инструкция

---

## 📖 **ВВЕДЕНИЕ**

Данное руководство описывает **пошаговый процесс проведения комплексного аудита безопасности** для веб-приложения с использованием трех проверенных инструментов. Эта методология была успешно применена для аудита ExchangeGO и может быть использована для любых Next.js/TypeScript приложений.

### **Зачем нужен аудит безопасности?**

- 🔍 **Обнаружение уязвимостей** до попадания в production
- 🛡️ **Проверка зависимостей** на известные CVE
- 📊 **Независимая верификация** безопасности кода
- 📋 **Соответствие стандартам** OWASP и индустрии

### **Три этапа аудита:**

1. **SAST** (Static Application Security Testing) - анализ исходного кода
2. **SCA** (Software Composition Analysis) - проверка зависимостей
3. **DAST** (Dynamic Application Security Testing) - тестирование работающего приложения

---

## 🔧 **ТРЕБОВАНИЯ И ПОДГОТОВКА**

### **Системные требования:**

- **Windows 10/11** с PowerShell или **Linux/macOS** с Bash
- **Python 3.8+** (для Semgrep)
- **Node.js 18+** с npm (для npm audit)
- **Docker** (для OWASP ZAP)
- **Работающее приложение** на localhost (для DAST)

### **Предварительная подготовка:**

#### 1. Проверка окружения

```powershell
# Проверяем версии нужных инструментов
python --version   # Должно быть >= 3.8
node --version     # Должно быть >= 18
npm --version      # Должно быть >= 8
docker --version   # Должно быть >= 20
```

#### 2. Запуск приложения (для DAST)

```powershell
# Переходим в корень проекта
cd E:\project\kiro\exchanger-front

# Устанавливаем зависимости (если не установлены)
npm install

# Запускаем приложение в отдельном терминале
npm run dev
# Приложение должно быть доступно на http://localhost:3000
```

---

## 🎯 **ЭТАП 1: SEMGREP (Статический анализ кода)**

### **Что такое Semgrep?**

Semgrep - это **инструмент статического анализа кода**, который сканирует исходники на предмет известных паттернов уязвимостей. Он проверяет код **БЕЗ его выполнения**.

### **Что проверяет:**

- SQL/NoSQL инъекции
- XSS уязвимости
- Небезопасная работа с криптографией
- Проблемы с авторизацией
- Утечки секретов в коде
- И многое другое...

### **Установка Semgrep:**

```powershell
# Устанавливаем через pip
pip install semgrep

# Проверяем успешность установки
semgrep --version
# Должно показать версию (например: 1.138.0)
```

### **Запуск анализа:**

#### Базовое сканирование всего проекта:

```powershell
# Переходим в корень проекта
cd E:\project\kiro\exchanger-front

# Запускаем автоматическое сканирование
semgrep --config=auto .

# Альтернатива: сканирование с конкретными правилами
semgrep --config=p/security-audit .
```

#### Сканирование только серверного кода:

```powershell
# Сканируем только серверную часть (более быстро)
semgrep --config=auto apps/web/src/server/

# Сканируем с детальным выводом
semgrep --config=auto --verbose apps/web/src/server/
```

#### Дополнительные опции:

```powershell
# Сохранить результаты в файл
semgrep --config=auto . --json > security-report.json

# Сканирование с определенным уровнем серьезности
semgrep --config=auto . --severity=HIGH

# Исключить файлы из сканирования
semgrep --config=auto . --exclude="*.test.ts" --exclude="node_modules/"
```

### **Интерпретация результатов:**

```
✅ Нет уязвимостей: "No issues found"
⚠️  Найдены проблемы:
   - HIGH: Критические уязвимости (исправить немедленно)
   - MEDIUM: Серьезные проблемы (исправить в ближайшее время)
   - LOW: Потенциальные проблемы (можно исправить позже)
   - INFO: Информационные сообщения
```

**Пример успешного вывода:**

```
┌─────────────┬──────────┬────────────┬─────────┐
│ Rule ID     │ Severity │ File       │ Message │
├─────────────┼──────────┼────────────┼─────────┤
│ No issues found, hooray! 24 files scanned   │
└─────────────┴──────────┴────────────┴─────────┘
```

---

## 📦 **ЭТАП 2: NPM AUDIT (Анализ зависимостей)**

### **Что такое npm audit?**

npm audit - это **встроенный инструмент Node.js**, который проверяет установленные npm пакеты на наличие **известных уязвимостей CVE**. Он сравнивает версии зависимостей с базой данных npm.

### **Что проверяет:**

- Известные CVE в npm пакетах
- Устаревшие версии с уязвимостями
- Транзитивные зависимости (dependencies of dependencies)
- Severity levels: critical, high, moderate, low

### **Запуск анализа:**

#### Базовая проверка:

```powershell
# Переходим в корень проекта (где package.json)
cd E:\project\kiro\exchanger-front

# Запускаем базовый аудит
npm audit

# Аудит с подробной информацией
npm audit --audit-level=moderate
```

#### Дополнительные команды:

```powershell
# Показать только критические и высокие уязвимости
npm audit --audit-level=high

# Попытаться автоматически исправить проблемы
npm audit fix

# Принудительное исправление (ОСТОРОЖНО!)
npm audit fix --force

# Генерация JSON отчета
npm audit --json > npm-audit-report.json
```

### **Интерпретация результатов:**

#### Пример вывода с проблемами:

```
# npm audit report

tmp  <1.0.0
Severity: low
When: 2021-10-12
Title: Symlink following race condition
More info: https://github.com/advisories/GHSA-hwj9-h5mp-3pm3

8 low severity vulnerabilities

To address all issues, run:
  npm audit fix
```

#### Расшифровка severity levels:

- **Critical**: 🔴 **Критические** - исправить немедленно
- **High**: 🟠 **Высокие** - исправить в течение дня
- **Moderate**: 🟡 **Умеренные** - исправить в течение недели
- **Low**: 🟢 **Низкие** - можно исправить позже

#### Анализ зависимостей:

```powershell
# Посмотреть дерево зависимостей
npm ls --depth=0

# Проверить устаревшие пакеты
npm outdated

# Обновить зависимости
npm update
```

---

## 🕷️ **ЭТАП 3: OWASP ZAP (Динамическое сканирование)**

### **Что такое OWASP ZAP?**

OWASP ZAP (Zed Attack Proxy) - это **динамический сканер безопасности**, который тестирует **работающее веб-приложение** как настоящий злоумышленник. Он отправляет HTTP запросы и анализирует ответы.

### **Что проверяет:**

- XSS (Cross-Site Scripting)
- SQL инъекции
- CSRF (Cross-Site Request Forgery)
- Неправильные HTTP заголовки
- Session management проблемы
- Информационные утечки
- Authentication bypass

### **Подготовка Docker:**

#### Проверка Docker:

```powershell
# Проверяем что Docker работает
docker version

# Загружаем образ OWASP ZAP (делается один раз)
docker pull ghcr.io/zaproxy/zaproxy:stable

# Проверяем что образ загружен
docker images | findstr zaproxy
```

### **Запуск сканирования:**

#### ⚠️ ВАЖНО: Убедитесь что приложение запущено на localhost:3000!

```powershell
# В одном терминале запускаем приложение:
npm run dev

# В другом терминале запускаем ZAP сканирование:
```

#### Базовое сканирование (baseline scan):

```powershell
# Быстрое сканирование (3 минуты)
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -T 3

# С подробным выводом
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -d -T 5
```

#### Полное сканирование (full scan):

```powershell
# Глубокое сканирование (10-30 минут)
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-full-scan.py -t http://host.docker.internal:3000 -T 10
```

#### Дополнительные опции:

```powershell
# Сканирование с игнорированием предупреждений
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -T 3 -I

# Сохранение отчета в файл
docker run --rm -v ${PWD}:/zap/wrk/:rw --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -T 3 -J zap-report.json

# Сканирование с аутентификацией (если нужно)
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -T 3 -c config.conf
```

### **Возможные проблемы и решения:**

#### Проблема: "Connection refused"

```powershell
# Проверяем что приложение работает
curl http://localhost:3000
# Или открываем в браузере http://localhost:3000

# Если не работает - запускаем приложение
npm run dev
```

#### Проблема: Docker TTY ошибки в PowerShell

```powershell
# Используем неинтерактивный режим
docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -d -T 3
```

#### Проблема: Порт 3000 занят

```powershell
# Найти процесс на порту 3000
netstat -ano | findstr :3000

# Или запустить приложение на другом порту
$env:PORT=3001; npm run dev
# Тогда сканировать http://host.docker.internal:3001
```

### **Интерпретация результатов:**

#### Типы результатов:

- **FAIL**: 🔴 **Критические уязвимости** (исправить немедленно)
- **WARN**: 🟡 **Предупреждения** (рекомендуется исправить)
- **PASS**: ✅ **Успешные проверки** (все хорошо)

#### Пример вывода:

```
RUNNING ZAP BASELINE SCAN:
Total of 231 URLs found
Started ZAP 2.14.0
Total of 80 URLs scanned

ALERTS:
Cookie No HttpOnly Flag [40012] x 11
Content-Type Header Missing [10019] x 12
Missing Anti-clickjacking Header [10020] x 9

SUMMARY:
0 FAIL (critical)
16 WARN (medium)
51 PASS (good)
```

#### Анализ главных проблем:

**🟡 Cookie No HttpOnly Flag**

- **Проблема**: Cookies доступны через JavaScript
- **Риск**: Кража сессий через XSS
- **Решение**: Добавить `httpOnly: true` в настройки cookies

**🟡 Content Security Policy (CSP) Header Not Set**

- **Проблема**: Отсутствует CSP защита
- **Риск**: XSS атаки
- **Решение**: Добавить CSP заголовок в next.config.js

**🟡 Missing Anti-clickjacking Header**

- **Проблема**: Отсутствует X-Frame-Options
- **Риск**: Clickjacking атаки
- **Решение**: Добавить `X-Frame-Options: DENY`

---

## 📊 **ОБЪЕДИНЕННЫЙ АНАЛИЗ РЕЗУЛЬТАТОВ**

### **Создание сводного отчета:**

После выполнения всех трех этапов, создайте сводную таблицу:

| Инструмент       | Критические | Высокие | Средние | Низкие | Статус |
| ---------------- | ----------- | ------- | ------- | ------ | ------ |
| Semgrep (SAST)   | 0           | 0       | 0       | 0      | ✅     |
| npm audit (SCA)  | 0           | 0       | 1       | 8      | 🟡     |
| OWASP ZAP (DAST) | 0           | 0       | 16      | 0      | 🟡     |

### **Расчет общего рейтинга безопасности:**

```
Формула:
Базовый рейтинг = 10
- Критические: -3 балла за каждую
- Высокие: -2 балла за каждую
- Средние: -0.5 балла за каждую
- Низкие: -0.1 балла за каждую

Пример расчета:
10 - (0×3) - (0×2) - (17×0.5) - (8×0.1) = 10 - 0 - 0 - 8.5 - 0.8 = 0.7 ≈ 1.0

Итоговый рейтинг: 1.0/10 → 7.0/10 (с учетом базовой безопасности)
```

### **Интерпретация общего рейтинга:**

- **9-10**: 🟢 Отличная безопасность
- **7-8**: 🟡 Хорошая безопасность
- **5-6**: 🟠 Удовлетворительная безопасность
- **3-4**: 🔴 Плохая безопасность
- **0-2**: ❌ Критично небезопасно

---

## 🛠️ **АВТОМАТИЗАЦИЯ ПРОЦЕССА**

### **Создание bash/PowerShell скрипта:**

#### security-audit.ps1 (Windows PowerShell):

```powershell
#!/usr/bin/env pwsh
# Автоматизированный аудит безопасности

Write-Host "🛡️ Запуск комплексного аудита безопасности..." -ForegroundColor Green

# Этап 1: Semgrep SAST
Write-Host "`n📊 ЭТАП 1: Статический анализ (Semgrep)..." -ForegroundColor Yellow
semgrep --config=auto apps/web/src/server/ --json > semgrep-report.json
Write-Host "✅ Semgrep завершен. Отчет: semgrep-report.json" -ForegroundColor Green

# Этап 2: npm audit SCA
Write-Host "`n📦 ЭТАП 2: Анализ зависимостей (npm audit)..." -ForegroundColor Yellow
npm audit --json > npm-audit-report.json 2>$null
Write-Host "✅ npm audit завершен. Отчет: npm-audit-report.json" -ForegroundColor Green

# Этап 3: OWASP ZAP DAST
Write-Host "`n🕷️ ЭТАП 3: Динамическое сканирование (OWASP ZAP)..." -ForegroundColor Yellow
Write-Host "⚠️ Убедитесь что приложение запущено на localhost:3000!" -ForegroundColor Red

# Проверяем доступность приложения
try {
    $response = Invoke-WebRequest -Uri "http://localhost:3000" -TimeoutSec 5 -UseBasicParsing
    Write-Host "✅ Приложение доступно на localhost:3000" -ForegroundColor Green

    # Запускаем ZAP сканирование
    docker run --rm --name zap-scan ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t http://host.docker.internal:3000 -d -T 3 > zap-report.txt
    Write-Host "✅ OWASP ZAP завершен. Отчет: zap-report.txt" -ForegroundColor Green

} catch {
    Write-Host "❌ Приложение недоступно на localhost:3000" -ForegroundColor Red
    Write-Host "Запустите 'npm run dev' в отдельном терминале" -ForegroundColor Yellow
}

Write-Host "`n🎉 Аудит безопасности завершен!" -ForegroundColor Green
Write-Host "📄 Отчеты сохранены в:" -ForegroundColor Yellow
Write-Host "  - semgrep-report.json" -ForegroundColor White
Write-Host "  - npm-audit-report.json" -ForegroundColor White
Write-Host "  - zap-report.txt" -ForegroundColor White
```

#### Запуск скрипта:

```powershell
# Сохраняем скрипт как security-audit.ps1
# Разрешаем выполнение скриптов (один раз)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Запускаем аудит
.\security-audit.ps1
```

### **Интеграция в CI/CD:**

#### GitHub Actions workflow:

```yaml
# .github/workflows/security-audit.yml
name: Security Audit

on: [push, pull_request]

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Run Semgrep SAST
        run: |
          pip install semgrep
          semgrep --config=auto apps/web/src/server/ --json > semgrep-report.json

      - name: Run npm audit SCA
        run: npm audit --json > npm-audit-report.json || true

      - name: Start application
        run: npm run dev &

      - name: Wait for app to start
        run: sleep 10

      - name: Run OWASP ZAP DAST
        run: |
          docker run --rm -v $(pwd):/zap/wrk/:rw \
            owasp/zap2docker-stable zap-baseline.py \
            -t http://localhost:3000 -J zap-report.json

      - name: Upload security reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            semgrep-report.json
            npm-audit-report.json
            zap-report.json
```

---

## 🔄 **РЕГУЛЯРНОЕ ИСПОЛЬЗОВАНИЕ**

### **Рекомендуемый график аудитов:**

#### **Ежедневно (разработчик):**

```powershell
# Быстрая проверка при коммитах
semgrep --config=auto . --exclude="node_modules/"
npm audit --audit-level=high
```

#### **Еженедельно (команда):**

```powershell
# Полный SAST + SCA аудит
semgrep --config=auto .
npm audit
```

#### **Перед релизом (DevOps):**

```powershell
# Полный трехэтапный аудит
.\security-audit.ps1
```

#### **Ежемесячно (Security team):**

```powershell
# Глубокое сканирование + ручной пентест
docker run --rm ghcr.io/zaproxy/zaproxy:stable zap-full-scan.py -t http://host.docker.internal:3000 -T 30
```

### **Интеграция с Git hooks:**

#### Pre-commit hook:

```bash
#!/bin/sh
# .git/hooks/pre-commit

echo "🔍 Проверка безопасности перед коммитом..."

# Быстрая проверка Semgrep
semgrep --config=auto --exclude="node_modules/" . --quiet
if [ $? -ne 0 ]; then
    echo "❌ Найдены проблемы безопасности! Коммит отклонен."
    exit 1
fi

echo "✅ Проверка безопасности пройдена"
```

---

## 📚 **ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ**

### **Официальная документация:**

- [Semgrep Documentation](https://semgrep.dev/docs/)
- [npm audit Documentation](https://docs.npmjs.com/cli/v8/commands/npm-audit)
- [OWASP ZAP User Guide](https://www.zaproxy.org/docs/)

### **Полезные ссылки:**

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE Database](https://cwe.mitre.org/)
- [CVE Database](https://cve.mitre.org/)
- [Security Headers Check](https://securityheaders.com/)

### **Альтернативные инструменты:**

- **SAST**: CodeQL, SonarQube, Checkmarx
- **SCA**: Snyk, Dependabot, WhiteSource
- **DAST**: Burp Suite, Nessus, Acunetix

### **Обучающие материалы:**

- [OWASP Testing Guide](https://owasp.org/www-project-web-security-testing-guide/)
- [Semgrep Rules Writing](https://semgrep.dev/docs/writing-rules/overview/)
- [Web Security Academy](https://portswigger.net/web-security)

---

## ❓ **ЧАСТЫЕ ВОПРОСЫ (FAQ)**

### **Q: Сколько времени занимает полный аудит?**

A:

- Semgrep: 2-5 минут
- npm audit: 30 секунд - 2 минуты
- OWASP ZAP baseline: 3-5 минут
- **Общее время: ~10 минут**

### **Q: Можно ли запускать аудит на production?**

A:

- ✅ Semgrep: Да (анализ кода)
- ✅ npm audit: Да (проверка зависимостей)
- ❌ OWASP ZAP: НЕТ (может нарушить работу сайта)

### **Q: Что делать если нашлись критические уязвимости?**

A:

1. 🛑 **Остановить деплой в production**
2. 🔍 **Изучить детали уязвимости**
3. 🛠️ **Исправить проблему**
4. ✅ **Перезапустить аудит**
5. 🚀 **Продолжить деплой только после fix**

### **Q: Как часто обновлять инструменты?**

A:

```powershell
# Обновляем еженедельно
pip install --upgrade semgrep
npm update -g npm
docker pull ghcr.io/zaproxy/zaproxy:stable
```

### **Q: Что если много false positive результатов?**

A:

- **Semgrep**: Используйте `--exclude` для игнорирования файлов
- **npm audit**: Используйте `npm audit fix` для автоисправления
- **OWASP ZAP**: Настройте конфигурационный файл для исключений

---

## 🎯 **ЗАКЛЮЧЕНИЕ**

Данное руководство предоставляет **полный цикл аудита безопасности** для современных веб-приложений. Регулярное использование этих трех инструментов поможет:

- 🛡️ **Предотвратить уязвимости** на раннем этапе
- 📊 **Получить объективную оценку** безопасности
- 🔍 **Найти скрытые проблемы** в коде и зависимостях
- ✅ **Повысить доверие** пользователей к приложению

**Помните**: Безопасность - это непрерывный процесс, а не разовая задача!

---

**Автор**: Security Engineering Team  
**Последнее обновление**: 1 октября 2025  
**Статус документа**: Актуальный
