# 🎨 Hero Section Styling Guide - Полное руководство по стилизации обменника

**Создано:** 15 июля 2025  
**Статус:** ✅ Активная документация  
**Компоненты:** HeroSection, HeroExchangeForm, ExchangeForm.Container

---

## 📋 Оглавление

1. [🏗️ Архитектура компонентов](#-архитектура-компонентов)
2. [📱 Мобильная адаптация](#-мобильная-адаптация)
3. [💻 Десктопная оптимизация](#-десктопная-оптимизация)
4. [🎯 Критические моменты](#-критические-моменты)
5. [🔧 Практические примеры](#-практические-примеры)
6. [⚠️ Частые ошибки](#️-частые-ошибки)
7. [🚀 Best Practices](#-best-practices)

---

## 🏗️ Архитектура компонентов

### **Структура Hero Section**

```tsx
HeroSection.tsx
├── Section wrapper (responsive spacing)
├── Hero Header (title + description)
└── HeroExchangeForm
    ├── ExchangeBenefits
    └── ExchangeForm
        ├── ExchangeForm.Container (hero variant)
        ├── ExchangeForm.CardPair (withArrow layout)
        └── ExchangeForm.ActionArea
```

### **Ключевые файлы**

| Файл                   | Ответственность           | Критичность        |
| ---------------------- | ------------------------- | ------------------ |
| `HeroSection.tsx`      | Общая структура и spacing | 🟡 Средняя         |
| `HeroExchangeForm.tsx` | Логика формы и композиция | 🟢 Низкая          |
| `exchange-form.tsx`    | **СТИЛИ КОНТЕЙНЕРА**      | 🔴 **КРИТИЧЕСКАЯ** |

---

## 📱 Мобильная адаптация

### **Проблемы, которые решались**

#### ❌ **Проблема 1: Широкий контейнер**

```css
/* НЕПРАВИЛЬНО - растягивается на всю ширину */
max-w-none w-full
```

#### ✅ **Решение: Ограниченная ширина**

```css
/* ПРАВИЛЬНО - адаптивные ограничения */
max-w-2xl lg:max-w-4xl mx-auto w-full
```

### **Responsive Breakpoints для Hero Container**

```css
/* packages/ui/src/components/exchange-form.tsx */
const CONTAINER_STYLES = {
  hero: '... max-w-2xl lg:max-w-4xl mx-auto w-full';
}
```

| Устройство     | Breakpoint | Max Width           | Описание                    |
| -------------- | ---------- | ------------------- | --------------------------- |
| 📱 **Mobile**  | `0-1023px` | `max-w-2xl` (672px) | Компактно, не растягивается |
| 💻 **Desktop** | `1024px+`  | `max-w-4xl` (896px) | Удобно для чтения контента  |

### **Отступы и Spacing**

```css
/* Контейнер */
p-4 sm:p-6 md:p-8  /* Прогрессивные внутренние отступы */

/* Карточки */
p-6 sm:p-8  /* Достаточно места для контента */

/* Gaps между карточками */
gap-6 sm:gap-8 md:gap-10  /* Больше места = лучше читаемость */
```

---

## 💻 Десктопная оптимизация

### **CardPair Layout - withArrow**

```css
/* Десктопная сетка с arrow */
flex flex-col md:grid md:grid-cols-[1fr_auto_1fr] gap-6 sm:gap-8 md:gap-10 items-center
```

**Поведение:**

- 📱 **Mobile:** Вертикальный стек (flex-col)
- 💻 **Desktop:** Горизонтальная сетка [карточка - стрелка - карточка]

### **Прогрессивное увеличение gaps**

```css
gap-6 sm:gap-8 md:gap-10
```

| Размер экрана | Gap      | Пиксели | Назначение |
| ------------- | -------- | ------- | ---------- |
| Mobile        | `gap-6`  | 24px    | Компактно  |
| Tablet        | `gap-8`  | 32px    | Комфортно  |
| Desktop       | `gap-10` | 40px    | Просторно  |

---

## 🎯 Критические моменты

### 🔴 **КРИТИЧНО: Container Width**

**Место:** `packages/ui/src/components/exchange-form.tsx` строка ~15

```tsx
const CONTAINER_STYLES = {
  hero: '... max-w-2xl lg:max-w-4xl mx-auto w-full',
};
```

**Что нельзя делать:**

- ❌ `max-w-none` - растягивается на всю ширину
- ❌ `max-w-5xl` или больше - слишком широко на десктопе
- ❌ Только `max-w-2xl` - слишком узко на больших экранах

**Правильно:**

- ✅ `max-w-2xl lg:max-w-4xl` - адаптивная ширина
- ✅ `mx-auto` - центрирование
- ✅ `w-full` - заполнение доступного места

### 🟡 **ВАЖНО: CardPair Breakpoints**

**Место:** `packages/ui/src/components/exchange-form.tsx` CardPair component

```tsx
case 'withArrow':
  return 'flex flex-col md:grid md:grid-cols-[1fr_auto_1fr] ...'
//                    ^^^ ВАЖНО: md: а не lg:
```

**Почему `md:` а не `lg:`:**

- На планшетах (768px+) уже достаточно места для горизонтального layout
- `lg:` (1024px+) слишком поздно - на планшетах layout ломается

### 🟢 **ПОЛЕЗНО: Padding Progression**

```css
/* Контейнер */
p-4 sm:p-6 md:p-8

/* Карточки */
p-6 sm:p-8

/* Gaps */
gap-6 sm:gap-8 md:gap-10
```

**Принцип:** Каждый уровень добавляет комфорта без перебора

---

## 🔧 Практические примеры

### **Пример 1: Добавление нового варианта контейнера**

```tsx
const CONTAINER_STYLES = {
  hero: '... max-w-2xl lg:max-w-4xl mx-auto w-full',
  full: '... space-y-6 sm:space-y-8',
  // Новый вариант
  compact: 'bg-card border rounded-lg p-3 sm:p-4 max-w-xl mx-auto w-full',
};
```

### **Пример 2: Изменение breakpoint для мобилки**

```tsx
// Если нужно раньше переключаться на горизонтальный layout
case 'withArrow':
  return 'flex flex-col sm:grid sm:grid-cols-[1fr_auto_1fr] ...'
//                    ^^^ sm: вместо md:
```

### **Пример 3: Увеличение максимальной ширины**

```tsx
// Для очень широких экранов
hero: '... max-w-2xl lg:max-w-4xl xl:max-w-5xl mx-auto w-full';
//                              ^^^^^^^^^ добавили xl: breakpoint
```

---

## ⚠️ Частые ошибки

### ❌ **Ошибка 1: Неправильный max-width**

```css
/* ПЛОХО */
max-w-none  /* Растягивается на весь экран */
max-w-5xl   /* Слишком широко, текст "плывет" */

/* ХОРОШО */
max-w-2xl lg:max-w-4xl  /* Адаптивно и читаемо */
```

### ❌ **Ошибка 2: Неправильные breakpoints**

```css
/* ПЛОХО */
md:grid-cols-2  /* На планшетах layout ломается */
lg:grid         /* Слишком поздно для переключения */

/* ХОРОШО */
flex flex-col md:grid md:grid-cols-[1fr_auto_1fr]  /* Плавный переход */
```

### ❌ **Ошибка 3: Недостаточные gaps**

```css
/* ПЛОХО */
gap-2 sm:gap-3  /* Контент сжатый, плохо читается */

/* ХОРОШО */
gap-6 sm:gap-8 md:gap-10  /* Достаточно пространства */
```

### ❌ **Ошибка 4: Забыли mx-auto**

```css
/* ПЛОХО */
max-w-2xl w-full  /* Не центрируется */

/* ХОРОШО */
max-w-2xl mx-auto w-full  /* Центрируется */
```

---

## 🚀 Best Practices

### **1. 📱 Mobile-First подход**

```css
/* Сначала мобильные стили, потом desktop */
p-4 sm:p-6 md:p-8  /* ✅ Прогрессивное улучшение */

/* Не наоборот */
p-8 md:p-4  /* ❌ Деградация опыта */
```

### **2. 🎯 Логичные breakpoints**

| Breakpoint      | Назначение        | Пример                |
| --------------- | ----------------- | --------------------- |
| `sm:` (640px+)  | Большие мобильные | Увеличиваем padding   |
| `md:` (768px+)  | Планшеты          | Переключаемся на grid |
| `lg:` (1024px+) | Десктопы          | Увеличиваем max-width |
| `xl:` (1280px+) | Широкие экраны    | Доп. оптимизации      |

### **3. 🔄 Тестирование responsive**

```bash
# Тестовые размеры
- 360px  # Маленький мобильный
- 768px  # Планшет
- 1024px # Десктоп
- 1440px # Широкий экран
```

### **4. 📏 Пропорциональные отступы**

```css
/* Пропорции 1:1.5:2 */
p-4 sm:p-6 md:p-8     /* 16px → 24px → 32px */
gap-4 sm:gap-6 md:gap-8   /* 16px → 24px → 32px */
```

### **5. 🎨 Консистентность стилей**

```tsx
// Переиспользуем существующие варианты
<ExchangeForm.Container variant="hero" />

// Вместо кастомных стилей
<div className="bg-card p-4 max-w-2xl mx-auto rounded-xl" />
```

---

## 🧪 Тестирование изменений

### **Чек-лист перед внесением изменений**

- [ ] 📱 Проверил на 360px (мобильный)
- [ ] 📱 Проверил на 768px (планшет)
- [ ] 💻 Проверил на 1024px (десктоп)
- [ ] 💻 Проверил на 1440px+ (широкий экран)
- [ ] 🎯 Текст читается без напряжения
- [ ] 🎯 Карточки не сжаты
- [ ] 🎯 Gaps достаточные
- [ ] 🎯 Контейнер центрирован

### **Команды для тестирования**

```bash
# Сборка для проверки
npm run build:web

# Запуск dev сервера
npm run dev:web

# Проверка в браузере
# F12 → Device Toolbar → Responsive режим
```

---

## 📚 Связанные документы

- [`MOBILE_ADAPTATION_GUIDELINES.md`](./MOBILE_ADAPTATION_GUIDELINES.md) - Общие принципы мобильной адаптации
- [`CODE_STYLE_GUIDE.md`](./CODE_STYLE_GUIDE.md) - Стиль кода проекта
- [`COMPONENT_ARCHITECTURE.md`](./COMPONENT_ARCHITECTURE.md) - Архитектура компонентов

---

## 🔄 История изменений

| Дата       | Изменение                                 | Проблема                         | Решение                     |
| ---------- | ----------------------------------------- | -------------------------------- | --------------------------- |
| 15.07.2025 | `max-w-none` → `max-w-2xl lg:max-w-4xl`   | Широкий контейнер на мобилке     | Ограничили ширину адаптивно |
| 15.07.2025 | `gap-4` → `gap-6 sm:gap-8 md:gap-10`      | Текст "плыл", мало места         | Увеличили gaps прогрессивно |
| 15.07.2025 | `p-3 sm:p-4 md:p-6` → `p-4 sm:p-6 md:p-8` | Недостаточно внутренних отступов | Увеличили padding           |
| 15.07.2025 | `p-6` → `p-6 sm:p-8` в ExchangeCard       | Сжатый контент в карточках       | Больше места для текста     |

---

## 🎯 Итоговая формула успеха

```tsx
// GOLDEN FORMULA для Hero Section
const HERO_SUCCESS_FORMULA = {
  containerWidth: 'max-w-2xl lg:max-w-4xl mx-auto w-full',
  containerPadding: 'p-4 sm:p-6 md:p-8',
  cardPadding: 'p-6 sm:p-8',
  gaps: 'gap-6 sm:gap-8 md:gap-10',
  breakpoint: 'md:grid', // Не lg:!
  centeringAlways: 'mx-auto',
};
```

**Принцип:** Мобильная компактность + десктопная просторность = идеальная адаптация

---

**⚡ Помните:** Эта документация создана на основе реального опыта решения проблем. Следуйте ей, чтобы избежать тех же ошибок!
