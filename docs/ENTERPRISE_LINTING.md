# Enterprise Code Quality Rules

## 📊 Анализ качества линтеров

### ✅ **Что проверяется эффективно:**

#### 🔢 **Количественные метрики:**
- **Максимальная сложность функций**: 10 (циклическая сложность)
- **Максимальная длина функций**: 100 строк (без комментариев и пустых строк)
- **Максимальная вложенность**: 4 уровня
- **Максимальное количество параметров**: 5
- **Максимальная длина файла**: 500 строк
- **Максимальная длина строки CSS**: 120 символов

#### 🧠 **Когнитивная сложность (SonarJS):**
- **Cognitive complexity**: максимум 15 (как код воспринимается человеком)
- **Дублирование строк**: предупреждение при 3+ повторениях
- **Идентичные функции**: обнаружение копи-пасты

#### 🔒 **Безопасность:**
- **Object injection**: обнаружение `obj[userInput]`
- **Unsafe RegExp**: поиск ReDoS уязвимостей  
- **Non-literal RegExp**: динамические регулярки
- **Script injection**: `javascript:` URLs

#### 💎 **Hard-coded значения:**
- **Magic numbers**: числа кроме [-1, 0, 1, 2, 24, 60, 100, 1000]
- **No-console/alert**: забытые debug'и
- **Hardcoded strings**: через SonarJS duplicate detection

#### ⚛️ **React Performance & Best Practices:**
- **JSX bind detection**: `onClick={() => {}}` в render
- **Leaked renders**: `{count && <Component />}` вместо `{count > 0 && <Component />}`
- **Array index keys**: `key={index}` вместо уникальных ID
- **Button type**: кнопки без `type="button"`
- **Unstable nested components**: компоненты внутри render

#### 📦 **Import/Export организация:**
- **Правильный порядок импортов**: builtin → external → internal → relative
- **Алфавитная сортировка**: внутри групп
- **Пустые строки**: между группами импортов
- **Дублирование импортов**: одинаковые модули

#### 🎨 **CSS/Styles качество:**
- **Селектор specificity**: максимум `0,4,0`
- **Вложенность**: максимум 4 уровня
- **Vendor prefixes**: предупреждения о ненужных
- **Color/font consistency**: проверка соглашений
- **Property ordering**: алфабетный порядок

### ⚠️ **Найденные проблемы в коде:**

#### 📈 **Complexity Issues:**
```typescript
// ❌ Плохо: Функция DataTable имеет сложность 14 (лимит 10)
function DataTable() {
  // 198 строк кода (лимит 100)
  if (condition1) {
    if (condition2) {
      if (condition3) {
        // Глубокая вложенность
      }
    }
  }
}

// ✅ Хорошо: Разбить на более мелкие функции
function DataTable() {
  return (
    <TableHeader />
    <TableBody />
    <TableFooter />
  )
}
```

#### 🎯 **Performance Issues:**
```tsx
// ❌ Плохо: Arrow function в render
<button onClick={() => handleClick(item.id)}>

// ✅ Хорошо: Мемоизированный callback
const handleItemClick = useCallback((id: string) => {
  handleClick(id)
}, [handleClick])

// ❌ Плохо: Leaked render
{items.length && <ItemsList items={items} />}

// ✅ Хорошо: Explicit boolean
{items.length > 0 && <ItemsList items={items} />}
```

#### 🔢 **Hard-coding Issues:**
```typescript
// ❌ Плохо: Magic number
const TIMEOUT = 16; // что это за 16?

// ✅ Хорошо: Named constant
const ANIMATION_FRAME_MS = 16; // 60fps = 16.67ms per frame
```

### 🔧 **Конфигурации по типам проектов:**

#### 1. **Base Rules** (`@repo/eslint-config/base`)
- Общие правила для всех JavaScript/TypeScript файлов
- Security, complexity, imports, TypeScript basics

#### 2. **React Internal** (`@repo/eslint-config/react-internal`) 
- Добавляет React-специфичные правила
- Performance, accessibility, component quality

#### 3. **UI Library** (`@repo/eslint-config/ui-library`)
- Смягченные правила для design system компонентов
- Разрешает prop spreading, увеличивает лимиты сложности

#### 4. **Stylelint Enterprise**
- CSS organization, performance, maintainability
- Tailwind CSS compatibility
- Naming conventions

### 📊 **Практичные лимиты (не идеализм):**

| Метрика | Лимит | Обоснование |
|---------|-------|-------------|
| Cyclomatic Complexity | 10 | Sweet spot между читаемостью и продуктивностью |
| Function Lines | 100 | Умещается на экране без скролла |
| File Lines | 500 | Разумный размер для code review |
| Max Params | 5 | После 5 лучше использовать объект |
| Nesting Depth | 4 | Больше = сложно читать |
| CSS Specificity | 0,4,0 | Баланс для поддерживаемости |

### 🚀 **Commands для проверки:**

```bash
# Полный линтинг (JS/TS + CSS)
npm run lint

# Показать все проблемы качества (включая warnings)
npm run lint -- --max-warnings 1000

# Автофикс что можно
npm run lint -- --fix

# CSS линтинг отдельно
npm run lint:styles
```

### 💡 **Рекомендации:**

1. **Не игнорируйте warnings** - они показывают техдолг
2. **Рефакторьте большие функции** - разбивайте на логические части  
3. **Используйте named константы** вместо magic numbers
4. **Мемоизируйте callbacks** в React компонентах
5. **Следите за порядком импортов** - это влияет на bundle size
6. **Проверяйте cognitive complexity** - код должен легко читаться

---

**Статус**: ✅ Enterprise-level качество настроено
**Обновлено**: ${new Date().toLocaleDateString()}
