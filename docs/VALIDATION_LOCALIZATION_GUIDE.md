# Руководство по валидации с локализацией

## 🚨 КРИТИЧЕСКИ ВАЖНО: Читать ДО создания форм с валидацией!

Этот документ содержит **обязательные к изучению** паттерны для работы с валидацией и локализацией в проекте. Несоблюдение этих паттернов приводит к многочасовой отладке.

## 📋 Быстрая справка (TL;DR)

### ✅ Правильный паттерн для полей формы

```tsx
// 1. Используй простой getFieldProps без переопределений
<Input
  {...form.getFieldProps('fieldName')}
  placeholder={t('placeholder')}
/>

// 2. НЕ переопределяй onChange/onBlur из getFieldProps!
// ❌ НЕПРАВИЛЬНО:
<Input
  {...form.getFieldProps('fieldName')}
  onChange={customHandler} // Это ломает валидацию!
  onBlur={customHandler}   // Это тоже ломает!
/>
```

### ✅ Правильная настройка формы с валидацией

```tsx
const form = useFormWithNextIntl<FormData>({
  initialValues: {
    /* ... */
  },
  validationSchema: mySchema,
  t: useTranslations('AdvancedExchangeForm'), // ВАЖНО: правильный namespace
  onSubmit: async values => {
    /* ... */
  },
});
```

### ✅ Правильная схема Zod с локализацией

```tsx
// 🚨 ВАЖНО: Используй только security-enhanced schemas для новых форм!
import {
  securityEnhancedLoginSchema,
  securityEnhancedCreateExchangeOrderSchema,
  securityEnhancedCreateTicketSchema,
} from '@repo/utils';

// ✅ Security-enhanced schema с XSS protection
const mySchema = securityEnhancedCreateExchangeOrderSchema;

// ✅ Для композитных форм используй security-enhanced building blocks
const customSchema = z.object({
  // Используй базовые схемы для building blocks
  email: emailSchema, // Базовая схема ОК
  password: passwordSchema, // Базовая схема ОК
  // Но для XSS-sensitive полей используй security-enhanced
  comment: createXSSProtectedString(0, 500),
});
```

### ❌ Устаревшие patterns (НЕ используй!)

```tsx
// ❌ НЕПРАВИЛЬНО: Legacy schemas без security enhancement
import {
  loginSchema, // DEPRECATED - уязвимо к XSS!
  createOrderSchema, // DEPRECATED - нет XSS protection!
  userProfileSchema, // DEPRECATED - устаревшие лимиты!
} from '@repo/utils/validation-schemas';

// ❌ НЕ используй legacy schemas!
const mySchema = z.object({
  amount: amountSchema, // ОК - базовая схема
  email: emailSchema, // ОК - базовая схема
  comment: z.string(), // ❌ НЕТ XSS protection!
});
```

### 📚 Дополнительная документация

> **🛡️ Для подробного руководства по security-enhanced schemas смотри:**  
> **[Security-Enhanced Validation Guide](SECURITY_ENHANCED_VALIDATION_GUIDE.md)**

---

## 🏗️ Архитектура валидации

### Трехслойная система валидации

Проект использует **строгое разделение** на три независимых слоя:

#### **1. UI Layer (Пользовательский интерфейс)**

- **Цель**: Помощь пользователю в правильном вводе
- **Инструменты**: React Hook Form + централизованные Zod схемы
- **Ответственность**: Форматирование, базовая проверка, UX feedback

#### **2. tRPC Layer (API валидация)**

- **Цель**: Защита API от некорректных данных
- **Инструменты**: tRPC `.input()` + те же Zod схемы
- **Ответственность**: Валидация структуры запроса, типизация

#### **3. Business Layer (Бизнес-логика)**

- **Цель**: Проверка бизнес-правил и ограничений
- **Инструменты**: Специализированные business validators
- **Ответственность**: Внешние зависимости, сложные бизнес-проверки

### **КРИТИЧНО: Единые схемы для UI и tRPC**

```tsx
// ✅ ПРАВИЛЬНО: Одна схема для UI и tRPC
import { passwordSchema } from '@repo/utils/validation-schemas';

// UI форма использует
const form = useFormWithNextIntl({
  validationSchema: z.object({ password: passwordSchema }),
  // ...
});

// tRPC процедура использует ТУ ЖЕ схему
export const authRouter = router({
  login: publicProcedure
    .input(z.object({ password: passwordSchema })) // Та же схема!
    .mutation(({ input }) => {
      // ...
    }),
});
```

### Централизованные схемы

**Расположение**: `packages/utils/src/validation-schemas.ts`

Все базовые схемы валидации должны быть импортированы отсюда:

```tsx
// ✅ ПРАВИЛЬНО: Импорт из централизованного места
import { passwordSchema, emailSchema, amountSchema } from '@repo/utils/validation-schemas';

// ❌ НЕПРАВИЛЬНО: Создание дублированной схемы
const myPasswordSchema = z.string().min(8); // НЕ ДЕЛАЙ ТАК!
```

### Поток валидации

```
Ввод → UI Schema → tRPC Schema → Business Validator → Результат
      ↑___________ Одни и те же Zod схемы ___________↑
```

---

## 📁 Структура переводов

### Namespace для форм

Все формы ДОЛЖНЫ использовать namespace `'AdvancedExchangeForm'`:

```tsx
// ✅ ПРАВИЛЬНО
const t = useTranslations('AdvancedExchangeForm');
const form = useFormWithNextIntl({ t /* ... */ });

// ❌ НЕПРАВИЛЬНО
const t = useTranslations('SomeOtherForm'); // Переводы не найдутся!
```

---

## 📁 Структура переводов

### Namespace для форм

Все формы ДОЛЖНЫ использовать namespace `'AdvancedExchangeForm'`:

```tsx
// ✅ ПРАВИЛЬНО
const t = useTranslations('AdvancedExchangeForm');
const form = useFormWithNextIntl({ t /* ... */ });

// ❌ НЕПРАВИЛЬНО
const t = useTranslations('SomeOtherForm'); // Переводы не найдутся!
```

### Структура в JSON файлах переводов

```json
{
  "AdvancedExchangeForm": {
    "validation": {
      "required": "Это поле обязательно",
      "invalid": "Неверное значение",
      "amount": {
        "format": "Некорректный формат суммы",
        "positive": "Сумма должна быть больше 0",
        "minValue": "Минимальная сумма: {min}",
        "maxValue": "Максимальная сумма: {max}",
        "required": "Введите сумму"
      },
      "email": {
        "invalid": "Неверный email адрес",
        "required": "Email обязателен"
      },
      "password": {
        "minLength": "Пароль должен содержать минимум {min} символов",
        "required": "Пароль обязателен"
      }
    }
  }
}
```

---

## 🔧 Создание Zod схем

### КРИТИЧНО: Используй централизованные схемы!

**Правило №1**: НЕ создавай новые схемы валидации! Используй существующие из `@repo/utils/validation-schemas`.

```tsx
// ✅ ПРАВИЛЬНО: Импорт из централизованного места
import {
  passwordSchema,
  emailSchema,
  amountSchema,
  phoneSchema,
} from '@repo/utils/validation-schemas';

// ❌ НЕПРАВИЛЬНО: Создание дублированной схемы
const myPasswordSchema = z.string().min(8); // НЕ ДЕЛАЙ ТАК!
const customEmailSchema = z.string().email(); // НЕ ДЕЛАЙ ТАК!
```

### Базовые правила для новых схем

**Создавай новую схему ТОЛЬКО если**:

1. Для этого типа данных нет централизованной схемы
2. Требуется специфическая бизнес-логика
3. Схема одобрена архитектором проекта

### Правила создания схем

1. **Используй константы** вместо магических чисел
2. **Сообщения об ошибках** должны быть ключами для переводов
3. **Параметры** передавай через двоеточие в сообщении
4. **Добавляй в централизованное место** сразу после создания

### Примеры правильного использования

#### Использование существующих схем

```tsx
import { amountSchema, emailSchema, passwordSchema } from '@repo/utils/validation-schemas';

const loginFormSchema = z.object({
  email: emailSchema, // Переиспользуем схему
  password: passwordSchema, // Переиспользуем схему
});

const orderFormSchema = z.object({
  amount: amountSchema, // Переиспользуем схему
  email: emailSchema, // Переиспользуем схему
});
```

#### Создание специфической схемы (редкий случай)

```tsx
// Только если нет подходящей централизованной схемы!
const orderNotesSchema = z.string().max(500, { message: 'ORDER_NOTES_MAX_LENGTH:500' }).optional();

// ОБЯЗАТЕЛЬНО: Сразу добавь в validation-schemas.ts для переиспользования!
```

---

## 🎛️ Работа с формами

### Создание формы

```tsx
function MyForm() {
  const t = useTranslations('AdvancedExchangeForm');

  const form = useFormWithNextIntl<MyFormData>({
    initialValues: {
      amount: '',
      email: '',
      // ...
    },
    validationSchema: mySchema,
    t, // КРИТИЧЕСКИ ВАЖНО: передать правильную функцию перевода
    onSubmit: async values => {
      // Обработка отправки
    },
  });

  return <form onSubmit={form.handleSubmit}>{/* Поля формы */}</form>;
}
```

### Создание полей

#### ✅ Правильный способ

```tsx
<FormField name="amount" error={form.errors.amount}>
  <FormLabel>{t('sending.amount')}</FormLabel>
  <FormControl>
    <Input {...form.getFieldProps('amount')} placeholder={t('sending.placeholder')} />
  </FormControl>
  <FormMessage />
</FormField>
```

#### ❌ Неправильные способы

```tsx
// НЕ переопределяй onChange/onBlur!
<Input
  {...form.getFieldProps('amount')}
  onChange={(e) => {
    // Это ломает валидацию!
    form.setValue('amount', e.target.value);
  }}
/>

// НЕ используй кастомные обработчики валидации!
<Input
  value={form.values.amount}
  onChange={(e) => {
    form.setValue('amount', e.target.value);
    form.validateField('amount'); // Это тоже неправильно!
  }}
/>
```

---

## 🔄 Обработчики валидации

### Как работают handlers

Handlers находятся в `packages/utils/src/validation/handlers.ts` и обрабатывают специальные случаи валидации.

### Пример handler'а для суммы

```tsx
function handleAmountValidation(
  issue: z.ZodIssueOptionalMessage,
  t: NextIntlValidationConfig['t']
): { message: string } | null {
  if (issue.message === 'AMOUNT_POSITIVE') {
    return { message: t('validation.amount.positive') };
  }
  if (issue.message?.startsWith('AMOUNT_MIN_VALUE:')) {
    const min = issue.message.split(':')[1] || '0.01';
    return { message: t('validation.amount.minValue', { min }) };
  }
  // ...
  return null;
}
```

---

## 🐛 Частые ошибки и их решения

### 1. Валидация не работает вообще

**Проблема**: Переопределение onChange/onBlur из getFieldProps

```tsx
// ❌ НЕПРАВИЛЬНО
<Input
  {...form.getFieldProps('field')}
  onChange={customHandler} // Это ломает всю валидацию!
/>
```

**Решение**: Убрать переопределения

```tsx
// ✅ ПРАВИЛЬНО
<Input {...form.getFieldProps('field')} />
```

### 2. Дублированная валидация в проекте

**Проблема**: Создание новых схем вместо использования централизованных

```tsx
// ❌ НЕПРАВИЛЬНО: Дублирование схемы
const myPasswordSchema = z.string().min(8);
const anotherPasswordSchema = z.string().min(6); // Разные требования!
```

**Решение**: Использовать централизованную схему

```tsx
// ✅ ПРАВИЛЬНО: Единственный источник правды
import { passwordSchema } from '@repo/utils/validation-schemas';
```

### 3. Сообщения об ошибках не переводятся

**Проблема**: Неправильный namespace для переводов

```tsx
// ❌ НЕПРАВИЛЬНО
const t = useTranslations('WrongNamespace');
```

**Решение**: Использовать правильный namespace

```tsx
// ✅ ПРАВИЛЬНО
const t = useTranslations('AdvancedExchangeForm');
```

### 4. Показывается не то сообщение об ошибке

**Проблема**: Неправильные ключи в Zod схеме или отсутствующие переводы

**Решение**:

1. Проверить, что ключ ошибки существует в JSON переводах
2. Проверить, что handler обрабатывает этот тип ошибки
3. Проверить соответствие параметров (например, `{min}`)

### 5. Валидация срабатывает, но UI не обновляется

**Проблема**: Нарушена связь между формой и компонентами

**Решение**: Проверить:

```tsx
// FormField должен получать ошибку из формы
<FormField name="field" error={form.errors.field}>
  {/* ... */}
</FormField>
```

### 6. Разные требования к валидации в разных формах

**Проблема**: Использование разных схем для одного типа данных

```tsx
// ❌ НЕПРАВИЛЬНО: Разные требования к паролю
const loginPasswordSchema = z.string().min(6);
const registerPasswordSchema = z.string().min(8);
```

**Решение**: Единые требования через централизованную схему

```tsx
// ✅ ПРАВИЛЬНО: Единые требования
import { passwordSchema } from '@repo/utils/validation-schemas';
// Используется везде с одинаковыми требованиями
```

---

## 📝 Чек-лист для новых форм

### Перед началом работы:

- [ ] Изучил этот документ полностью
- [ ] Понимаю трехслойную архитектуру валидации
- [ ] Знаю где найти централизованные схемы
- [ ] Понимаю принцип "единственного источника правды"

### При создании схемы:

- [ ] **ПЕРВЫМ ДЕЛОМ**: Проверил `validation-schemas.ts` на наличие нужной схемы
- [ ] Если схема есть - использую её, НЕ создаю новую
- [ ] Если схемы нет - согласовал создание с архитектором
- [ ] Использую константы вместо магических чисел
- [ ] Сообщения об ошибках - это ключи переводов
- [ ] Параметры передаю через двоеточие
- [ ] Проверил, что все ключи существуют в JSON переводах

### При создании формы:

- [ ] Использую `useTranslations('AdvancedExchangeForm')`
- [ ] Передаю функцию `t` в `useFormWithNextIntl`
- [ ] Импортирую схемы из `@repo/utils/validation-schemas`
- [ ] НЕ переопределяю onChange/onBlur из getFieldProps
- [ ] Использую правильную структуру FormField

### При создании tRPC процедур:

- [ ] Использую ТЕ ЖЕ схемы что и в UI
- [ ] НЕ создаю отдельные "API схемы"
- [ ] Импортирую из `@repo/utils/validation-schemas`

### При тестировании:

- [ ] Проверил пустые значения
- [ ] Проверил граничные значения (мин/макс)
- [ ] Проверил неверные форматы
- [ ] Проверил, что переводы отображаются правильно
- [ ] Проверил совместимость UI и tRPC валидации

### Архитектурная проверка:

- [ ] НЕТ дублированных схем в проекте
- [ ] НЕТ "legacy" или "alternative" версий схем
- [ ] UI и tRPC используют одинаковые схемы
- [ ] Бизнес-валидация отделена от структурной

---

## 🔗 Связанные файлы

### Основные файлы системы валидации:

- `packages/utils/src/validation/` - вся система валидации
- `packages/utils/src/validation/hooks.ts` - useFormWithNextIntl
- `packages/utils/src/validation/handlers.ts` - обработчики ошибок
- `packages/utils/src/validation/constants.ts` - константы валидации
- `apps/web/messages/ru/` - русские переводы (модульная структура)
- `apps/web/messages/en/` - английские переводы (модульная структура)

### Модульная структура переводов:

**📍 Расположение переводов**: `apps/web/messages/{locale}/{domain}.json`

**Домены переводов**:

- `advanced-exchange.json` - формы обмена и валидация (основной для форм)
- `server-errors.json` - серверные ошибки tRPC
- `common-ui.json` - общие UI элементы и ошибки
- `layout.json` - навигация и общие элементы интерфейса
- `notifications.json` - toast уведомления и alerts

**Namespace'ы для валидации**:

```typescript
// ✅ Основной namespace для всех форм
const t = useTranslations('AdvancedExchangeForm'); // из advanced-exchange.json

// ✅ Серверные ошибки
const serverT = useTranslations('server.errors'); // из server-errors.json

// ✅ Общие UI ошибки
const commonT = useTranslations('common'); // из common-ui.json
```

**Структура advanced-exchange.json для валидации**:

```json
{
  "AdvancedExchangeForm": {
    "validation": {
      "required": "Это поле обязательно",
      "invalid": "Неверное значение",
      "amount": {
        "format": "Некорректный формат суммы",
        "positive": "Сумма должна быть больше 0",
        "minValue": "Минимальная сумма: {min}",
        "maxValue": "Максимальная сумма: {max}",
        "required": "Введите сумму"
      },
      "email": {
        "invalid": "Неверный email адрес",
        "required": "Email обязателен"
      },
      "password": {
        "minLength": "Пароль должен содержать минимум {min} символов",
        "required": "Пароль обязателен"
      },
      "phone": {
        "invalid": "Неверный формат номера телефона",
        "required": "Номер телефона обязателен"
      }
    },
    "form": {
      "sending": {
        "title": "Отправляете",
        "amount": "Сумма",
        "currency": "Валюта",
        "placeholder": "Введите сумму"
      },
      "receiving": {
        "title": "Получаете",
        "amount": "Сумма",
        "currency": "Валюта"
      },
      "userInfo": {
        "title": "Ваши данные",
        "email": "Email",
        "phone": "Телефон"
      }
    }
  }
}
```

### 📖 Дополнительная документация по модульной структуре:

- **[I18N_ARCHITECTURE_GUIDE.md](I18N_ARCHITECTURE_GUIDE.md)** - Полная архитектура модульной системы переводов
- **[I18N_TROUBLESHOOTING.md](troubleshooting/I18N_TROUBLESHOOTING.md)** - Решение проблем с модульными переводами

### Примеры рабочих форм:

- `apps/web/src/components/forms/LoginForm.tsx` - форма логина
- `apps/web/src/components/forms/RegisterForm.tsx` - форма регистрации
- `apps/web/src/components/exchange-form/` - формы обмена

---

## ⚡ Быстрая отладка

### Если валидация не работает:

1. **Проверь консоль браузера** - там могут быть ошибки
2. **Проверь getFieldProps** - не переопределяешь ли onChange/onBlur
3. **Проверь namespace** - используешь ли 'AdvancedExchangeForm'
4. **Проверь JSON переводы** - существует ли ключ ошибки
5. **Проверь схему** - правильно ли заданы сообщения об ошибках

### Тестирование валидации в консоли:

```javascript
// В DevTools браузера
const schema = z.string().min(1, { message: 'REQUIRED' });
const result = schema.safeParse('');
console.log(result); // Покажет ошибку с сообщением 'REQUIRED'
```

---

## 🎯 Заключение

**Главное правило**: Следуй принципу **единственного источника правды (Single Source of Truth)** для валидации.

### 🔑 Ключевые принципы:

1. **Централизация**: Все базовые схемы в `@repo/utils/validation-schemas`
2. **Переиспользование**: НЕ создавай дублированные схемы
3. **Единообразие**: UI и tRPC используют одинаковые схемы
4. **Разделение**: Бизнес-валидация отдельно от структурной

### 🚫 Что делать НЕ нужно:

- Создавать новые схемы без проверки существующих
- Делать "легаси" или "альтернативные" версии схем
- Смешивать бизнес-логику со структурной валидацией
- Использовать разные требования для одного типа данных

### ✅ При малейших сомнениях:

1. **Сначала**: Проверь `validation-schemas.ts`
2. **Потом**: Копируй паттерн из рабочей формы (LoginForm, RegisterForm)
3. **Наконец**: Адаптируй под свои нужды

**Помни**: Эта архитектура предотвращает часы отладки и создает надежную, масштабируемую систему валидации!

**Помни**: Простота - залог надежности. Не изобретай велосипед, используй готовые решения.
