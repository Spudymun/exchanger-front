# AI Agent Verification Guidelines - Руководство по верификации

**Создано**: 29 сентября 2025  
**Назначение**: Предотвращение ложных выводов о "отсутствии" файлов/компонентов  
**Приоритет**: КРИТИЧЕСКИЙ - применять в каждом запросе

---

## 🚨 ОСНОВНАЯ ПРОБЛЕМА

**НИКОГДА не утверждай что файл/компонент "отсутствует" без ПОЛНОЙ верификации всеми доступными методами!**

### Типичные ошибки:

- ❌ "Файл не найден" после одного `file_search`
- ❌ "Компонент отсутствует" без проверки imports
- ❌ "Функция не существует" без `semantic_search`
- ❌ Выводы на основе предположений вместо фактов

---

## 📋 ОБЯЗАТЕЛЬНЫЕ ЭТАПЫ ВЕРИФИКАЦИИ

### **ЭТАП 1: Множественный поиск файлов**

```bash
# ВСЕГДА используй ВСЕ методы поиска:
1. file_search: "**/filename*", "**/*keyword*"
2. grep_search: "import.*filename|from.*filename|export.*filename"
3. semantic_search: "ключевые слова из функциональности"
4. list_dir: соседние папки где может быть файл
5. grep_search: широкие паттерны по типу (service|manager|factory|adapter)
```

### **ЭТАП 2: Контекстная проверка**

```bash
# Проверь архитектуру проекта:
1. Прочитай PROJECT_STRUCTURE_MAP.md
2. list_dir для понимания структуры папок
3. Проверь соседние файлы в той же директории
4. Найди аналогичные паттерны в проекте
5. Изучи imports/exports связанных файлов
```

### **ЭТАП 3: Фактическое подтверждение**

```bash
# Найди РЕАЛЬНЫЕ доказательства:
1. read_file - прочитай содержимое найденных файлов
2. Проверь imports/exports/dependencies
3. Найди использование в других частях кода
4. Подтверди функциональность через тесты/examples
```

---

## 🎯 АЛГОРИТМ ПОИСКА ФАЙЛОВ

### **Поиск по имени файла:**

```yaml
1. file_search: "**/*exact-filename*"
2. file_search: "**/*partial-name*"
3. grep_search: "filename|FILENAME" (case variations)
4. semantic_search: "функциональность файла"
```

### **Поиск компонентов/функций:**

```yaml
1. semantic_search: 'ComponentName function service'
2. grep_search: 'ComponentName|componentName'
3. grep_search: 'import.*Component|from.*component'
4. grep_search: 'export.*Component|class.*Component'
```

### **Поиск конфигураций/констант:**

```yaml
1. grep_search: "CONSTANT_NAME|constantName"
2. file_search: "**/constants*", "**/config*"
3. semantic_search: "тип константы значение"
4. grep_search: "export.*CONSTANT|const.*CONSTANT"
```

---

## ✅ ПРАВИЛА ФОРМУЛИРОВОК

### **РАЗРЕШЕННЫЕ утверждения (после полной проверки):**

- ✅ "ФАКТ: файл существует по пути X"
- ✅ "Проверил методами file_search, grep_search, semantic_search - файл подтвержден"
- ✅ "Использовал все доступные методы поиска - файл не обнаружен"
- ✅ "Компонент найден в файле X, строка Y"

### **ЗАПРЕЩЕННЫЕ фразы (без верификации):**

- ❌ "Файл не найден"
- ❌ "Компонент отсутствует"
- ❌ "Функция не существует"
- ❌ "Конфигурация не определена"
- ❌ "Вероятно", "Возможно", "Скорее всего"
- ❌ "По-видимому", "Кажется"

---

## 🔍 КОНТРОЛЬНЫЕ ВОПРОСЫ

### **ПЕРЕД любым заключением спрашивай:**

#### Полнота поиска:

- [ ] Использовал ли я ВСЕ доступные методы поиска?
- [ ] Проверил ли я соседние директории?
- [ ] Искал ли я по imports/exports этого файла?
- [ ] Проверил ли я разные варианты названий?

#### Архитектурное понимание:

- [ ] Понимаю ли я структуру проекта?
- [ ] Есть ли в проекте аналогичные паттерны?
- [ ] Где логически должен находиться такой файл?
- [ ] Какие соглашения именования в проекте?

#### Качество анализа:

- [ ] Применил ли я Rule 8 (100% уверенность)?
- [ ] Готов ли я отвечать за каждое утверждение?
- [ ] Основан ли мой вывод на фактах, а не предположениях?
- [ ] Прочитал ли я реальные файлы для подтверждения?

---

## 📊 МЕТОДОЛОГИЯ ОТЧЕТНОСТИ

### **Структура отчета о поиске:**

```markdown
## Верификация [НАЗВАНИЕ_КОМПОНЕНТА]

### Методы поиска:

- ✅ file_search: паттерны X, Y, Z
- ✅ grep_search: паттерны A, B, C
- ✅ semantic_search: ключевые слова M, N, O
- ✅ list_dir: директории P, Q, R

### Результаты:

- НАЙДЕНО: файл X по пути Y
- ПОДТВЕРЖДЕНО: импорт в файле Z
- ИСПОЛЬЗОВАНИЕ: в компонентах A, B, C

### Заключение:

ФАКТ: [компонент] существует и функционален
```

### **Пример отрицательного результата:**

```markdown
## Верификация [НАЗВАНИЕ_КОМПОНЕНТА]

### Полная проверка выполнена:

- ✅ file_search: "**/wallet-factory\*", "**/factory\*"
- ✅ grep_search: "WalletFactory|walletFactory"
- ✅ semantic_search: "wallet factory manager"
- ✅ list_dir: packages/exchange-core/src/
- ✅ grep_search: "import.*Factory|from.*factory"

### Результаты:

- НЕ НАЙДЕНО в file_search
- НЕ НАЙДЕНО в grep_search
- НЕ НАЙДЕНО в semantic_search
- НЕ НАЙДЕНО в смежных директориях

### Заключение:

После ПОЛНОЙ верификации всеми методами: компонент НЕ ОБНАРУЖЕН
```

---

## 🎯 СПЕЦИАЛЬНЫЕ СЛУЧАИ

### **Поиск seed данных:**

```yaml
Паттерны поиска:
- file_search: "**/seed*", "**/migration*", "**/*init*"
- grep_search: "seed|Seed|SEED|INSERT.*INTO|CREATE.*TABLE"
- list_dir: packages/*/scripts/, packages/*/prisma/
- semantic_search: "database seeding test data"
```

### **Поиск Factory классов:**

```yaml
Паттерны поиска:
- file_search: "**/*factory*", "**/*Factory*"
- grep_search: "Factory|factory.*create|\.create\(\)"
- semantic_search: "factory pattern manager creation"
- grep_search: "import.*Factory|export.*Factory"
```

### **Поиск константы кэширования:**

```yaml
Паттерны поиска:
- grep_search: "staleTime|cacheTime|TTL|CACHE.*TIME"
- file_search: "**/constants*", "**/config*", "**/providers*"
- semantic_search: "cache configuration react query"
- grep_search: "QUERY.*TIME|STALE.*TIME"
```

---

## 🚫 КРИТИЧЕСКИЕ ПРАВИЛА

### **НИКОГДА НЕ ДЕЛАЙ:**

1. ❌ Выводы на основе одного метода поиска
2. ❌ Предположения без проверки фактов
3. ❌ Заключения без чтения файлов
4. ❌ Анализ без понимания архитектуры
5. ❌ Утверждения о "отсутствии" без полной верификации

### **ВСЕГДА ДЕЛАЙ:**

1. ✅ Используй ВСЕ доступные методы поиска
2. ✅ Читай PROJECT_STRUCTURE_MAP.md в начале
3. ✅ Проверяй соседние директории
4. ✅ Ищи imports/exports перед выводами
5. ✅ Применяй Rule 8 (100% уверенность) из ai-agent-rules.yml

---

## 📋 ФИНАЛЬНЫЙ ЧЕКЛИСТ

**ПЕРЕД отправкой ответа с утверждениями проверь:**

- [ ] ✅ Использовал все методы поиска (file_search, grep_search, semantic_search, list_dir)
- [ ] ✅ Прочитал реальные файлы для подтверждения
- [ ] ✅ Проверил соседние директории и архитектуру
- [ ] ✅ Нашел imports/exports и использование
- [ ] ✅ Понял контекст и назначение искомого компонента
- [ ] ✅ Применил Rule 8 (запрет предположений)
- [ ] ✅ Готов отвечать за каждое утверждение
- [ ] ✅ Базирую выводы на фактах, а не догадках

---

## 🎯 ГЛАВНОЕ ПРАВИЛО

> **"ЕСЛИ НЕ МОЖЕШЬ ПОКАЗАТЬ КОНКРЕТНЫЙ ФАЙЛ/КОД - НЕ УТВЕРЖДАЙ"**

**Применяй эти правила СТРОГО в каждом запросе для предотвращения ложных выводов!**
