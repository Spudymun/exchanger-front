# 📋 ИСПРАВЛЕННЫЙ ПЛАН: Отображение банка и курса фиксации в ордере

> **На основе ФАКТИЧЕСКОГО анализа архитектуры проекта**
>
> **Создан:** 2 октября 2025  
> **Статус:** Готов к реализации
> **Архитектурный подход:** Использование существующих паттернов проекта

---

## 🎯 ЦЕЛЬ

Обеспечить отображение **названия банка получателя** и **зафиксированного курса обмена** на странице ордера в браузере.

---

## 🔍 ФАКТИЧЕСКИЙ АНАЛИЗ АРХИТЕКТУРЫ

### ✅ ЧТО УЖЕ СУЩЕСТВУЕТ В ПРОЕКТЕ

#### 1. **База данных ГОТОВА** ✅

```sql
-- Таблица banks уже создана (миграция 20250929225352_banks)
model Bank {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  externalId String   @unique @map("external_id") @db.VarChar(50)
  name       String   @db.VarChar(100)           -- ✅ ДЛЯ ОТОБРАЖЕНИЯ
  shortName  String   @map("short_name") @db.VarChar(50)
  // ...
}
```

#### 2. **API для банков РАБОТАЕТ** ✅

```typescript
// apps/web/src/server/trpc/routers/fiat.ts
getBanksForFiatCurrency: // ✅ Возвращает банки из БД
getBankInfo:            // ✅ Возвращает информацию о конкретном банке
```

#### 3. **UI компоненты ГОТОВЫ** ✅

```typescript
// packages/ui/src/components/exchange/
ExchangeBankSelector; // ✅ Селектор банков (СУЩЕСТВУЕТ)
ReceivingInfo; // ✅ Отображение банковской информации (СУЩЕСТВУЕТ)
```

#### 4. **Система курсов РАБОТАЕТ** ✅

```typescript
// packages/exchange-core/src/utils/calculations.ts
getExchangeRateAsync(); // ✅ Получение курса в реальном времени
SmartPricingService; // ✅ Гибридная система ценообразования
```

#### 5. **bankId УЖЕ ПЕРЕДАЕТСЯ в Order** ✅

```typescript
// apps/web/src/components/exchange/ExchangeContainer.tsx
recipientData: {
  cardNumber: values.cardNumber,
  bankId: values.selectedBankId || EXCHANGE_DEFAULTS.DEFAULT_BANK_ID, // ✅ УЖЕ ЕСТЬ!
}
```

### ❌ ЧТО ОТСУТСТВУЕТ

1. **Сохранение bankId** в таблице orders
2. **Сохранение fixedExchangeRate** в таблице orders
3. **Получение bank данных** в API order details
4. **Отображение bank информации** в UI order статуса
5. **Переводы** для новых полей

---

## 📊 ПЛАН РЕАЛИЗАЦИИ (Минимальные изменения)

### 🗂️ **ЭТАП 1: База данных** (5 минут)

**Миграция:** `20251002_add_bank_rate_to_orders.sql`

```sql
-- Добавляем поля в существующую таблицу orders
ALTER TABLE "public"."orders"
ADD COLUMN "bank_id" VARCHAR(50), -- externalId банка (НЕ UUID!)
ADD COLUMN "fixed_exchange_rate" DECIMAL(15,8); -- Курс на момент создания

-- Индекс для производительности (orders часто запрашиваются)
CREATE INDEX "orders_bank_id_idx" ON "public"."orders"("bank_id");

-- НЕТ FK constraint - bankId может быть пустым для старых ордеров
```

**Обоснование архитектурного решения:**

- ✅ Используем `externalId` (string) вместо UUID для простоты и совместимости
- ✅ Поля nullable для обратной совместимости со старыми ордерами
- ✅ Следуем паттерну проекта: `assignedOperatorId`, `walletId` - тоже optional

### 🏗️ **ЭТАП 2: TypeScript типы** (5 минут)

**Файл:** `packages/exchange-core/src/types/order.ts`

```typescript
export interface Order {
  // Существующие поля...

  // ✅ ДОБАВИТЬ (следуя паттерну assignedOperatorId):
  bankId?: string; // externalId банка получателя
  fixedExchangeRate?: number; // Курс на момент создания ордера
}

export interface OrderWithUIData extends Order {
  // Существующие поля...

  // ✅ ДОБАВИТЬ (для UI компонентов):
  bankName?: string; // Название банка (из API join)
  bankShortName?: string; // Короткое название банка
}
```

### 🔗 **ЭТАП 3: API изменения** (10 минут)

#### 3.1 **Сохранение данных при создании**

**Файл:** `apps/web/src/server/trpc/routers/exchange.ts`

```typescript
// В методе createOrder - ДОБАВИТЬ перед createOrderInSystem:
.mutation(async ({ input, ctx }) => {
  // Получаем текущий курс (уже существует)
  const currentRate = await getExchangeRateAsync(validCurrency);

  // Извлекаем bankId из существующего recipientData
  const bankId = orderRequest.recipientData?.bankId ||
                 EXCHANGE_DEFAULTS.DEFAULT_BANK_ID;

  const enhancedOrderRequest = {
    ...orderRequest,
    bankId,                              // ✅ ДОБАВИТЬ
    fixedExchangeRate: currentRate.uahRate, // ✅ ДОБАВИТЬ
  };

  const order = await createOrderInSystem(enhancedOrderRequest, sessionMetadata, userSession);
  // остальная логика без изменений...
});
```

#### 3.2 **PostgresOrderAdapter - добавить поля**

**Файл:** `packages/session-management/src/adapters/postgres-order-adapter.ts`

```typescript
// В методе create - ДОБАВИТЬ в data:
data: {
  // существующие поля...
  bankId: orderData.bankId || null,                    // ✅ ДОБАВИТЬ
  fixedExchangeRate: orderData.fixedExchangeRate || null, // ✅ ДОБАВИТЬ
}

// В методе findByPublicId - ДОБАВИТЬ include:
include: {
  wallet: true,
  bank: {                              // ✅ ДОБАВИТЬ
    select: {
      name: true,
      shortName: true,
    }
  }
}
```

#### 3.3 **API возвращает bank данные**

**Файл:** `apps/web/src/server/trpc/routers/user/orders.ts`

```typescript
// В методе getOrderDetails - ДОБАВИТЬ в return:
return {
  // существующие поля...

  // ✅ ДОБАВИТЬ:
  bankId: order.bankId,
  fixedExchangeRate: order.fixedExchangeRate ? Number(order.fixedExchangeRate) : undefined,
  bankName: order.bank?.name,
  bankShortName: order.bank?.shortName,
};
```

### 🎨 **ЭТАП 4: UI изменения** (10 минут)

**Файл:** `packages/ui/src/components/order/helpers/OrderStatusHelpers.tsx`

```typescript
// ОБНОВИТЬ компонент OrderFinancialInfo - объединить банк, курс и карту в одну сетку:
export function OrderFinancialInfo({ orderData, locale, t }) {
  return (
    <div className="space-y-4">
      {/* Сумма обмена - остается отдельно */}
      <AmountDisplayWithCopy orderData={orderData} locale={locale} t={t} />

      {/* ✅ НОВАЯ СЕТКА: банк + курс + карта на одном уровне */}
      <div className="grid grid-cols-1 gap-4 md:grid-cols-3">
        {/* Банк получателя */}
        {(orderData as OrderWithUIData).bankName && (
          <div>
            <p className={textStyles.heading.sm}>{t('recipientBank')}</p>
            <p className={textStyles.body.md}>
              {(orderData as OrderWithUIData).bankName}
            </p>
          </div>
        )}

        {/* Зафиксированный курс */}
        {orderData.fixedExchangeRate && (
          <div>
            <p className={textStyles.heading.sm}>{t('fixedRate')}</p>
            <p className={textStyles.body.md}>
              1 {orderData.currency} = {orderData.fixedExchangeRate.toLocaleString(locale)} UAH
            </p>
          </div>
        )}

        {/* Карта получателя - перемещена в общую сетку */}
        {orderData.recipientData?.cardNumber && (
          <div>
            <p className={textStyles.heading.sm}>{t('recipientCard')}</p>
            <p className={combineStyles(textStyles.body.md, 'font-mono')}>
              {maskCardNumber(orderData.recipientData.cardNumber)}
            </p>
          </div>
        )}
      </div>
    </div>
  );
}
```

### 🌐 **ЭТАП 5: Переводы** (5 минут)

**Файл:** `apps/web/messages/ru/order-page.json`

```json
{
  "OrderStatus": {
    // существующие переводы...

    "recipientBank": "Банк получателя",
    "fixedRate": "Зафиксированный курс"
  }
}
```

**Файл:** `apps/web/messages/en/order-page.json`

```json
{
  "OrderStatus": {
    // existing translations...

    "recipientBank": "Recipient Bank",
    "fixedRate": "Fixed Exchange Rate"
  }
}
```

---

## 🚀 ПРЕИМУЩЕСТВА ИСПРАВЛЕННОГО ПЛАНА

### ✅ **Использует существующую архитектуру**

- **Bank система** уже работает и протестирована
- **API endpoints** для банков уже существуют
- **UI компоненты** для банков уже созданы
- **Курсовая система** уже работает в реальном времени

### ✅ **Минимальные изменения**

- Только **2 новых поля** в базе данных
- **Нет breaking changes** - все поля optional
- **Переиспользование** существующих компонентов
- **Следует паттернам** проекта (assignedOperatorId, walletId)

### ✅ **Производительность**

- **bankId** - string поле с индексом (быстрые запросы)
- **fixedExchangeRate** - Decimal поле (точные вычисления)
- **join** только при необходимости через include
- **Оптимальные запросы** без дополнительной нагрузки

### ✅ **Обратная совместимость**

- Старые ордера **не сломаются** (nullable поля)
- **Graceful degradation** - если нет данных о банке, просто не показываем
- **Постепенная миграция** - новые ордера будут с полной информацией

---

## ⚡ АРХИТЕКТУРНЫЕ РЕШЕНИЯ

### 🔄 **Почему externalId вместо FK**

```sql
-- ✅ ПРАВИЛЬНО (следуем паттерну проекта):
bank_id VARCHAR(50)  -- externalId банка

-- ❌ НЕ НУЖНО (избыточная сложность):
bank_id UUID REFERENCES banks(id)
```

**Обоснование:**

- В проекте **bankId везде используется как externalId** (string)
- API **работает с externalId**, не с внутренними UUID
- **Простота интеграции** с существующим кодом
- **Нет каскадных проблем** при изменении банков

### 💰 **Почему сохранять fixedExchangeRate**

```typescript
// ✅ ПРАВИЛЬНО - сохраняем курс в момент создания:
fixedExchangeRate: currentRate.uahRate;

// ❌ НЕ НУЖНО - вычислять каждый раз:
// Курс может измениться, пользователь увидит неправильную информацию
```

**Обоснование:**

- **Юридическая точность** - пользователь должен видеть курс, по которому обменял
- **Избежание путаницы** - текущий курс может отличаться от курса создания
- **Соответствие бизнес-логике** - курс фиксируется в момент создания ордера

---

## 📋 ПРОВЕРОЧНЫЙ СПИСОК РЕАЛИЗАЦИИ

### ✅ **База данных**

- [ ] Создать миграцию для добавления полей `bank_id` и `fixed_exchange_rate`
- [ ] Добавить индекс для `bank_id`
- [ ] Проверить совместимость со старыми ордерами

### ✅ **Backend**

- [ ] Обновить TypeScript типы Order и OrderWithUIData
- [ ] Модифицировать PostgresOrderAdapter для сохранения новых полей
- [ ] Обновить API getOrderDetails для возврата bank данных
- [ ] Добавить сохранение bankId и fixedExchangeRate при создании ордера

### ✅ **Frontend**

- [ ] Обновить OrderFinancialInfo для отображения bank и rate
- [ ] Добавить переводы на русский и английский
- [ ] Протестировать отображение в браузере

### ✅ **Тестирование**

- [ ] Проверить создание новых ордеров с bank данными
- [ ] Проверить отображение bank и rate на странице ордера
- [ ] Проверить обратную совместимость со старыми ордерами
- [ ] Проверить работу API при отсутствии bank данных

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После реализации пользователь увидит на странице ордера:

**Desktop (>= 768px) - 3 колонки на одном уровне:**

```
💰 Сумма обмена
┌─────────────────────────────────────────┐
│ 1000 USDT → 41,250 ₴                   │
└─────────────────────────────────────────┘

┌─────────────────┬─────────────────┬─────────────────┐
│ 🏦 Банк получателя │ 💱 Курс фиксации   │ 💳 Карта получателя │
│ PrivatBank      │ 1 USDT = 41.25 UAH │ 4149•••••••••5678   │
└─────────────────┴─────────────────┴─────────────────┘
```

**Mobile (< 768px) - вертикальная стопка:**

```
💰 Сумма обмена
1000 USDT → 41,250 ₴

🏦 Банк получателя
PrivatBank

💱 Зафиксированный курс
1 USDT = 41.25 UAH

💳 Карта получателя
4149•••••••••5678
```

**Время реализации:** ~35 минут  
**Риски:** Минимальные (используются существующие паттерны)  
**Rollback:** Простой (поля optional, можно отключить в UI)
