# 📘 ЗАДАНИЕ: Расширение markdown-отчёта стилей компонентов

**Дата создания**: 2025-07-22  
**Статус**: ГОТОВО К РЕАЛИЗАЦИИ  
**Приоритет**: ВЫСОКИЙ

## 🎯 Цель

Модифицировать существующий генератор `.md`-отчётов по стилям компонентов, чтобы он отображал развернутую информацию о всех вложенных компонентах внутри каждого файла секции.

## 🚨 ВАЖНЫЕ ОГРАНИЧЕНИЯ

### ❌ ЧТО НЕ ТРОГАТЬ:

- **Структуру файлов** - остается как есть: `style-docs/project/page/section.md`
- **Логику создания файлов** - не создавать новые файлы для вложенных компонентов
- **Общую архитектуру** style-scanner

### ✅ ЧТО МОДИФИЦИРОВАТЬ:

- **Только содержимое внутри существующих `section.md` файлов**
- Развернуть все вложенные компоненты в рамках одного файла секции

## 📋 Конкретные требования

### 1. **Инлайновое раскрытие вложенных компонентов**

- **Все компоненты секции любого уровня вложенности** должны быть показаны в одном файле
- **Матрешечная структура**: компоненты могут содержать другие компоненты на произвольную глубину
- **Каждый компонент на любом уровне** получает свой отдельный блок стилей
- **Полное развертывание дерева**: от корневого компонента до самых глубоко вложенных
- НЕ создавать отдельные файлы для вложенных компонентов

### 2. **Визуальная иерархия компонентов**

- ASCII дерево компонентов с отступами
- Иконки для разных уровней (`🧱`, `🔸`, `⚪`)
- Показ глубины вложенности

### 3. **Привязка стилей к каждому компоненту**

- Отдельный блок `### 🧱 ComponentName` для каждого компонента
- Разделение на Static и Dynamic классы
- CSS Modules и CSS-in-JS для каждого компонента

### 4. **Отметка динамически вычисляемых классов**

- Детект `cn()`, `clsx()`, `twMerge()` вызовов
- Пометка Dynamic classes с исходным JSX
- Разделение Static vs Dynamic в каждом блоке

## 🧱 Структура итогового файла

```markdown
# HeroSection

**File**: `apps/web/src/components/herosection.tsx`
**Page**: `apps/web/app/[locale]/page.tsx`
**Generated**: `2025-07-22T13:43:25.596Z`

---

## 📋 Section Summary

- **Direct Children**: 1
- **Total Nested Components**: 21
- **Max Nesting Depth**: 6 levels
- **Dynamic Classes Detected**: ✅
- **Named Imports**: `HeroExchangeForm`, `type HeroExchangeFormData`
- **Named Exports**: `HeroSection`

---

## 🧩 Component Tree
```

- HeroSection
  - HeroExchangeForm
    - ExchangeFormCards
      - ExchangeBenefits
        - BenefitItem
          - BenefitIcon
          - BenefitText
      - ReceivingCard
        - ReceivingInfo
          - CurrencyIcon
          - CurrencyLabel
      - SendingCard
        - AmountInput
          - InputField
          - ValidationMessage
        - CurrencySelector
          - DropdownTrigger
          - DropdownContent
            - CurrencyOption
              - FlagIcon
              - CurrencyCode

````

---

## 🎨 Styles Per Component

### 🧱 HeroSection

**File**: `apps/web/src/components/herosection.tsx`
**Export**: `HeroSection`

#### 🎨 Tailwind Classes
**Static (24)**:
```css
font-bold
text-lg
sm:space-y-8
...
````

**Dynamic**:

```tsx
cn('text-foreground', isDark && 'text-white');
```

#### 🧩 CSS Modules

_None_

#### 🧬 CSS-in-JS

_None_

### 🔸 HeroExchangeForm

**File**: `apps/web/src/components/HeroExchangeForm.tsx`  
**Export**: `HeroExchangeForm`

#### 🎨 Tailwind Classes

**Static (15)**:

```css
w-full
max-w-full
overflow-hidden
...
```

**Dynamic**:

```tsx
cn('w-full max-w-full overflow-hidden', className);
```

### ⚪ ExchangeFormCards

[... и так далее для всех компонентов]

---

## 🔍 Complete Tailwind Summary

- **HeroSection**: font-bold, text-lg, sm:space-y-8, ...
- **HeroExchangeForm**: w-full, max-w-full, overflow-hidden, ...
- **ExchangeFormCards**: border-input, text-muted-foreground, ...
- ...

````

## 🛠️ Технические детали

### Файлы для модификации:
1. **`packages/style-scanner/src/utils/markdown-generator.ts`**
   - Функция: `createSectionMarkdown()`
   - Добавить: развернутое дерево компонентов
   - Добавить: блоки стилей для каждого компонента

2. **`packages/style-scanner/src/utils/style-extractor.ts`** (если нужно)
   - Добавить: детект динамических классов
   - Методы: `detectDynamicClasses()`, `parseCnCalls()`

### Алгоритм реализации:
1. **Получить полное дерево компонентов секции на любую глубину**
2. **Рекурсивно обойти все вложенные компоненты** (матрешечная структура)
3. **Для каждого компонента на любом уровне** создать отдельный блок стилей
4. **Детектировать динамические классы в каждом компоненте**
5. **Сгруппировать итоговый summary по всем компонентам дерева**

## 🔍 Примеры динамических классов

### Найденные паттерны:
- `cn('w-full max-w-full overflow-hidden', className)` в `HeroExchangeForm.tsx:124`
- Импорт `cn` из `@repo/ui`
- Функция `cn()` = `twMerge(clsx(inputs))`

### Что детектировать:
- Вызовы `cn()`, `clsx()`, `twMerge()`
- Template literals с переменными: `className={`${base} ${active && 'text'}`}`
- Условные классы: `className={isActive ? 'active' : 'inactive'}`

## 📊 Критерии готовности

- [ ] Компонентное дерево визуализировано с отступами
- [ ] Все компоненты из дерева представлены в файле
- [ ] У каждого компонента есть отдельный блок стилей
- [ ] В каждом компоненте — указаны динамические классы
- [ ] Есть итоговая таблица Tailwind по компонентам
- [ ] Структура Markdown удобна для просмотра в IDE и GitHub
- [ ] Файловая структура НЕ изменилась
- [ ] НЕ создаются новые файлы для вложенных компонентов

## 🔧 Команды для тестирования

```bash
# Запуск сканирования
npm run scan-styles

# Проверка результата
cat style-docs/web/home-page/herosection.md

# Verbose режим
npm run scan-styles:verbose
````

## 📝 Примечания

- **Структура файлов остается неизменной** - это ключевое требование
- **Все изменения только внутри содержимого файлов** секций
- **Развернуть ВСЕ вложенные компоненты** в рамках файла секции
- **НЕ создавать отдельные файлы** для компонентов

## 🎯 Результат

После реализации каждый файл секции (например, `herosection.md`) будет содержать:

1. Развернутое ASCII дерево всех компонентов
2. Отдельные блоки стилей для каждого компонента
3. Детект и пометку динамических классов
4. Сгруппированный summary по компонентам

---

_Файл создан: 2025-07-22_  
_Для сессии GitHub Copilot_
