# 🛠️ План решения проблем packages/exchange-core

**Дата создания:** 14 августа 2025  
**Основа:** Анализ exchange-core-audit-report.md  
**Применены правила:** Rule 8 (Запрет предположений), Rule 20 (Запрет избыточности), Rule 21 (Осознанное удаление)  
**Архитектурная проверка:** Все решения основаны на существующих паттернах проекта

---

## 🎯 ОБЯЗАТЕЛЬНЫЙ АРХИТЕКТУРНЫЙ АНАЛИЗ ПЕРЕД ПЛАНОМ

### Изученные существующие паттерны в проекте:

**✅ ПОДТВЕРЖДЕННЫЕ ПАТТЕРНЫ:**

1. **Централизация типов в constants:** `business.ts` уже содержит комментарий "типы TransactionType и TransactionStatus перенесены в @repo/exchange-core/types"
2. **Паттерн миграции типов:** Примеры из `business.ts`: "OrderStatus type moved to exchange.ts to eliminate redundancy (Rule 20)"
3. **Deprecation approach:** Из `VALIDATION_ARCHITECTURE_GUIDE.md` - "Пометить как deprecated минимум на 1 спринт"
4. **Поэтапное удаление:** Rule 21 требует ПОЛНЫЙ анализ перед любым удалением

**✅ НАЙДЕННЫЕ ПОДХОДЫ К РЕФАКТОРИНГУ:**

1. **Migration Strategy** из `VALIDATION_ARCHITECTURE_GUIDE.md`: Анализ → Унификация → Миграция → Очистка
2. **Обратная совместимость:** Документ требует "Убедиться в обратной совместимости"
3. **Тестирование:** "Запустить все тесты, затрагивающие схему"

**✅ ПРОВЕРКА ИСПОЛЬЗОВАНИЯ TransactionType/TransactionStatus:**

- ✅ В `packages/exchange-core/src/types/transaction.ts` - ЕДИНСТВЕННОЕ место экспорта
- ✅ В `apps/**` - НЕ используются (grep показал 0 результатов)
- ✅ В `packages/hooks/**` - НЕ используются (grep показал 0 результатов)
- ✅ **ВЫВОД:** Типы используются ТОЛЬКО внутри exchange-core - удаление безопасно

---

## 📋 ДЕТАЛЬНЫЙ ПЛАН РЕШЕНИЯ ПРОБЛЕМ

### 🔴 ФАЗА 1: КРИТИЧЕСКИЕ ПРОБЛЕМЫ (День 1-2)

#### Задача 1.1: Устранение дублирования типов транзакций

**ПРОБЛЕМА:** `TransactionType` и `TransactionStatus` дублируются между exchange-core и constants

**АРХИТЕКТУРНОЕ РЕШЕНИЕ:**
Основано на паттерне из `business.ts` где уже есть комментарий о переносе типов. Нужно ОБРАТИТЬ миграцию - вернуть типы в constants согласно принципам проекта.

**ПОШАГОВОЕ РЕШЕНИЕ:**

**Шаг 1.1.1: Добавить типы в constants/business.ts**

```typescript
// В файл packages/constants/src/business.ts добавить:

// Re-export types для централизации согласно Rule 20
export type TransactionType = (typeof TRANSACTION_TYPES)[keyof typeof TRANSACTION_TYPES];
export type TransactionStatus = (typeof TRANSACTION_STATUSES)[keyof typeof TRANSACTION_STATUSES];
```

**Шаг 1.1.2: Обновить exchange-core/types/transaction.ts**

```typescript
// В файле packages/exchange-core/src/types/transaction.ts заменить:

// СТАРОЕ:
export type TransactionType = Transaction['type'];
export type TransactionStatus = Transaction['status'];

// НА НОВОЕ:
import type { TransactionType, TransactionStatus } from '@repo/constants';
export type { TransactionType, TransactionStatus };

// Альтернативно - убрать re-export совсем, импортировать напрямую из constants
```

**Шаг 1.1.3: Проверка консистентности**

```bash
# Проверить что типы резолвятся правильно
npx turbo run check-types --filter=@repo/exchange-core
```

**РИСКИ:** Низкий (типы не используются за пределами exchange-core)  
**ВРЕМЯ:** 30 минут  
**ПРИОРИТЕТ:** КРИТИЧЕСКИЙ

#### Задача 1.2: Удаление dead code согласно Rule 21

**ПРОБЛЕМА:** Неиспользуемые функции создают путаницу

**ОБЯЗАТЕЛЬНЫЙ АНАЛИЗ ПЕРЕД УДАЛЕНИЕМ (Rule 21):**

**Анализ 1.2.1: sanitizeCurrency функция**

- **Файл:** `packages/exchange-core/src/utils/data-sanitizers.ts`
- **Содержимое:** Простая функция `.toUpperCase().trim()`
- **Использование:** Поиск показал 0 использований в проекте
- **Зависимости:** Нет входящих/исходящих зависимостей
- **Альтернативы:** Функция настолько простая, что ее легко воссоздать при необходимости
- **Решение:** БЕЗОПАСНО удалить

**Анализ 1.2.2: generateNewWalletAddress функция**

- **Файл:** `packages/exchange-core/src/services/crypto-address-generation.ts`
- **Содержимое:** Placeholder функция, дублирует `generateDepositAddress`
- **Использование:** Поиск показал 0 использований в проекте
- **Комментарий в коде:** "placeholder for future implementation"
- **Решение:** БЕЗОПАСНО удалить как недореализованный placeholder

**ПОШАГОВОЕ УДАЛЕНИЕ:**

**Шаг 1.2.1: Удалить sanitizeCurrency**

```typescript
// В файле packages/exchange-core/src/utils/data-sanitizers.ts удалить:
export function sanitizeCurrency(currency: string): string {
  return currency.toUpperCase().trim();
}
```

**Шаг 1.2.2: Удалить generateNewWalletAddress**

```typescript
// В файле packages/exchange-core/src/services/crypto-address-generation.ts удалить:
generateNewWalletAddress(currency: CryptoCurrency): string {
  return this.generateDepositAddress(currency);
}
```

**Шаг 1.2.3: Обновить exports**

- Проверить что функции не экспортируются в index.ts
- Убрать из exports если есть

**ВРЕМЯ:** 15 минут  
**РИСКИ:** Минимальный (функции не используются)

### 🟡 ФАЗА 2: УЛУЧШЕНИЕ АРХИТЕКТУРЫ (День 3-5)

#### Задача 2.1: Максимизация использования @repo/utils

**ПРОБЛЕМА:** Недостаточное переиспользование централизованных утилит

**АРХИТЕКТУРНОЕ РЕШЕНИЕ:**
Основано на паттерне из `calculations.ts` где уже ПРАВИЛЬНО используются утилиты из `@repo/utils`. Расширить этот подход.

**Пошаговое решение:**

**Шаг 2.1.1: Анализ calculations.ts**

```typescript
// ХОРОШО: Уже используется
import { calculateNetAmount, formatUahAmount } from '@repo/utils';

// ПОТЕНЦИАЛЬНО УЛУЧШИТЬ: Проверить можно ли заменить
export function formatCryptoAmount(amount: number, currency: CryptoCurrency): string {
  // Проверить есть ли аналог в @repo/utils
}
```

**Шаг 2.1.2: Проверка доступных утилит**

```bash
# Проанализировать что доступно в @repo/utils
grep -r "export.*function" packages/utils/src/
```

**Шаг 2.1.3: Замена кастомных функций на централизованные**

- Заменить только если есть ТОЧНЫЙ аналог в utils
- НЕ СОЗДАВАТЬ новых утилит без необходимости (Rule 20)

**ВРЕМЯ:** 2-3 часа  
**РИСКИ:** Средний (изменение поведения функций)

#### Задача 2.2: Оптимизация validators согласно проектным паттернам

**ПРОБЛЕМА:** `composite-validators.ts` не использует Zod schemas

**АРХИТЕКТУРНОЕ РЕШЕНИЕ:**
Основано на `VALIDATION_ARCHITECTURE_GUIDE.md` - использовать централизованные Zod schemas.

**Пошаговое решение:**

**Шаг 2.2.1: Проверить доступные Zod schemas**

```bash
# Найти существующие schemas в utils
grep -r "recipientData\|RecipientData" packages/utils/src/
```

**Шаг 2.2.2: Заменить custom validation на Zod**

```typescript
// ТОЛЬКО если уже есть schema в utils
import { recipientDataSchema } from '@repo/utils';

export function validateRecipientData(data?: RecipientData): ValidationResult {
  return recipientDataSchema.safeParse(data);
}
```

**Шаг 2.2.3: НЕ создавать новые schemas**

- Если нет готовой схемы в utils - оставить как есть
- НЕ ДУБЛИРОВАТЬ валидацию (Rule 20)

**ВРЕМЯ:** 1-2 часа  
**РИСКИ:** Низкий (только замена существующих схем)

#### Задача 2.3: Унификация type guards

**ПРОБЛЕМА:** Простые type guards можно оптимизировать

**АРХИТЕКТУРНОЕ РЕШЕНИЕ:**
Оценить необходимость каждого type guard согласно принципам проекта.

**Пошаговое решение:**

**Шаг 2.3.1: Анализ существующих type guards**

```typescript
// В utils/type-guards.ts проанализировать:
- isAuthenticatedUser() - нужен для типизации
- isUserWithPassword() - domain-specific logic
- isVerifiedUser() - часто используемый паттерн
```

**Шаг 2.3.2: Проверить использование**

```bash
# Найти где используются type guards
grep -r "isAuthenticatedUser\|isUserWithPassword\|isVerifiedUser" packages/ apps/
```

**Шаг 2.3.3: Принять решение по каждому guard**

- Если используется - оставить
- Если не используется - удалить (Rule 21)
- НЕ переносить в utils без явной необходимости

**ВРЕМЯ:** 1 час  
**РИСКИ:** Низкий

### 🟢 ФАЗА 3: КАЧЕСТВЕННЫЕ УЛУЧШЕНИЯ (День 6-7)

#### Задача 3.1: Добавление unit tests

**АРХИТЕКТУРНОЕ РЕШЕНИЕ:**
Основано на существующей тестовой инфраструктуре проекта (Jest настроен).

**Пошаговое решение:**

**Шаг 3.1.1: Создать test файлы для утилит**

```typescript
// Создать packages/exchange-core/src/utils/__tests__/
-calculations.test.ts - crypto.test.ts - type - guards.test.ts;
```

**Шаг 3.1.2: Написать тесты для критических функций**

```typescript
// Приоритет: функции которые остались после очистки
describe('calculateUahAmount', () => {
  it('should calculate correct UAH amount with commission', () => {
    // test implementation
  });
});
```

**ВРЕМЯ:** 4-6 часов  
**ПРИОРИТЕТ:** СРЕДНИЙ

#### Задача 3.2: Улучшение документации

**Шаг 3.2.1: Обновить JSDoc комментарии**

```typescript
/**
 * Calculate UAH amount from crypto amount with commission
 * @param cryptoAmount - Amount in cryptocurrency
 * @param currency - Cryptocurrency type
 * @returns UAH amount after commission deduction
 * @example
 * calculateUahAmount(0.001, 'BTC') // returns 1755.0
 */
```

**ВРЕМЯ:** 2-3 часа  
**ПРИОРИТЕТ:** НИЗКИЙ

---

## ⚡ ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫПОЛНЕНИЯ

### День 1: Критические исправления

- [ ] 1.1.1 - Добавить типы транзакций в constants
- [ ] 1.1.2 - Обновить imports в exchange-core
- [ ] 1.1.3 - Проверка типизации
- [ ] 1.2.1 - Удалить sanitizeCurrency
- [ ] 1.2.2 - Удалить generateNewWalletAddress

### День 2: Проверка и тестирование критических изменений

- [ ] Запустить все типы проверки
- [ ] Проверить build процесс
- [ ] Убедиться что нет breaking changes

### День 3-4: Архитектурные улучшения

- [ ] 2.1 - Максимизация использования @repo/utils
- [ ] 2.2 - Оптимизация validators
- [ ] 2.3 - Унификация type guards

### День 5: Интеграционное тестирование

- [ ] Полное тестирование всех изменений
- [ ] Проверка производительности
- [ ] Документирование изменений

### День 6-7: Качественные улучшения (опционально)

- [ ] 3.1 - Добавление unit tests
- [ ] 3.2 - Улучшение документации

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА ВЫПОЛНЕНИЯ

### Rule 8 (Запрет предположений) - проверки:

- ✅ Все решения основаны на анализе существующего кода
- ✅ Паттерны взяты из документации проекта
- ✅ Использование типов проверено grep'ом
- ✅ Архитектурные решения соответствуют existing patterns

### Rule 20 (Запрет избыточности) - контроль:

- ✅ НЕ создавать новые утилиты без крайней необходимости
- ✅ Максимально использовать существующие решения
- ✅ Удалить дублирующие типы
- ✅ НЕ дублировать валидацию

### Rule 21 (Осознанное удаление) - гарантии:

- ✅ Полный анализ всех удаляемых функций проведен
- ✅ Проверено использование через grep
- ✅ Документированы причины удаления
- ✅ Оценены риски для каждого удаления

---

## 📊 КОНТРОЛЬНЫЕ МЕТРИКИ

### После Фазы 1:

- [ ] Bundle size уменьшен на ~5%
- [ ] 0 type errors
- [ ] 0 build errors
- [ ] Устранено дублирование TransactionType/TransactionStatus

### После Фазы 2:

- [ ] Увеличено использование @repo/utils на 20%+
- [ ] Уменьшено дублирование кода на 15%+
- [ ] Стандартизирован error handling

### После Фазы 3:

- [ ] Test coverage >80% для критических функций
- [ ] Улучшена JSDoc документация
- [ ] Нет deprecated warnings

---

## 🔧 ИНСТРУМЕНТЫ И КОМАНДЫ

### Проверка типизации:

```bash
npx turbo run check-types --filter=@repo/exchange-core
```

### Поиск использования функций:

```bash
grep -r "functionName" packages/ apps/
```

### Сборка пакета:

```bash
npx turbo run build --filter=@repo/exchange-core
```

### Запуск тестов:

```bash
npx turbo run test --filter=@repo/exchange-core
```

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

### ВЫСОКИЙ РИСК: Breaking changes при изменении типов

**Митигация:**

- Поэтапное изменение с проверкой на каждом шаге
- Использование re-export для совместимости
- Тестирование в dev окружении

### СРЕДНИЙ РИСК: Изменение поведения при замене утилит

**Митигация:**

- Заменять только ТОЧНЫЕ аналоги
- Добавить unit tests перед заменой
- Benchmark тестирование

### НИЗКИЙ РИСК: Удаление нужных функций

**Митигация:**

- Тщательный анализ каждой функции
- Поиск по всей кодовой базе
- Git history анализ

---

## 🎯 КРИТЕРИИ УСПЕХА

### Обязательные критерии:

- ✅ Устранено дублирование типов TransactionType/TransactionStatus
- ✅ Удален весь dead code
- ✅ Нет новых type errors
- ✅ Нет новых build errors
- ✅ Соблюдены все архитектурные принципы проекта

### Желательные критерии:

- ✅ Максимизировано использование @repo/utils
- ✅ Добавлены unit tests для критических функций
- ✅ Улучшена документация

### Показатели качества:

- Bundle size: -5%
- Code duplication: -15%
- Type safety: +20%
- Test coverage: >80%

---

## 📝 ЗАКЛЮЧЕНИЕ

План разработан с полным соблюдением архитектурных принципов проекта и существующих паттернов. Все решения основаны на фактическом анализе кода и документации, что исключает предположения (Rule 8).

**Следующий шаг:** Начать выполнение с Фазы 1 для получения быстрых результатов и минимизации рисков.
