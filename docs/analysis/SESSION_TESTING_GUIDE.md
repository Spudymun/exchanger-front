# 🔐 Практический гайд тестирования системы сессий

> **Основан на реальной кодовой базе** - все команды и действия проверены в коде

## 📋 Обзор системы

Ваша система сессий использует **UserManagerFactory** с автоматическим переключением режимов через `NODE_ENV`:

- **Mock режим** (`NODE_ENV` не установлен) - использует MockUserManagerWrapper
- **Development режим** (`NODE_ENV=development`) - MockUserManagerWrapper + возможность PostgreSQL/Redis
- **Production режим** (`NODE_ENV=production`) - PostgreSQL + Redis обязательны

## 🧪 ТЕСТ 1: Mock режим (по умолчанию)

### Настройка окружения

```powershell
# Убеждаемся что NODE_ENV не установлен
Remove-Item Env:NODE_ENV -ErrorAction SilentlyContinue
Write-Output "NODE_ENV: $env:NODE_ENV"  # Должно быть пустым

# Запускаем сервер
npm run dev
```

### Проверка что сервер запустился

```powershell
# Проверяем порт 3000
Test-NetConnection -ComputerName localhost -Port 3000

# Смотрим логи если есть проблемы
# В терминале где запущен npm run dev будут логи
```

### Тестирование на фронтенде

**1. Откройте браузер: http://localhost:3000**

**2. Тестирование регистрации:**

- Нажмите кнопку "Войти" в хедере
- Переключитесь на вкладку "Регистрация"
- Заполните форму:
  - Email: `test@example.com`
  - Пароль: `TestPassword123!`
  - Подтверждение пароля: `TestPassword123!`
  - Captcha: любой текст (например: `test`)
- Нажмите "Зарегистрироваться"

**3. Проверка сессии:**

- После успешной регистрации диалог должен закрыться
- В хедере должен появиться email пользователя вместо кнопки "Войти"
- Проверьте в Dev Tools → Application → Cookies: должен быть `sessionId`

**4. Тестирование логина:**

- Откройте новую вкладку браузера (или инкогнито)
- Перейдите на http://localhost:3000
- Нажмите "Войти"
- Используйте те же данные что при регистрации
- Email: `test@example.com`
- Пароль: `TestPassword123!`
- Captcha: любой текст

### ✅ Ожидаемые результаты Mock режима

- ✅ Регистрация создает пользователя в памяти
- ✅ Логин успешен с правильными данными
- ✅ В хедере показывается email пользователя
- ✅ Cookie `sessionId` устанавливается автоматически
- ✅ Данные НЕ сохраняются между перезапусками сервера
- ✅ При перезапуске `npm run dev` нужно регистрироваться заново

---

## 🔧 ТЕСТ 2: Development режим с базой данных

### Настройка окружения

```powershell
# Устанавливаем development режим
$env:NODE_ENV = "development"
Write-Output "NODE_ENV: $env:NODE_ENV"

# Проверяем что .env файл существует
Get-Content ".env" | Select-String "DATABASE_URL|REDIS_URL"
```

### Запуск инфраструктуры

```powershell
# Запускаем PostgreSQL и Redis через Docker (PowerShell скрипт)
.\scripts\docker\dev-up.ps1

# Альтернативно, если скрипт не работает:
docker-compose up -d postgres redis

# Проверяем что контейнеры запустились
docker-compose ps
```

### Проверка подключений

```powershell
# Проверяем PostgreSQL
Test-NetConnection -ComputerName localhost -Port 5432

# Проверяем Redis
Test-NetConnection -ComputerName localhost -Port 6379

# Проверяем pgAdmin (для управления БД)
Test-NetConnection -ComputerName localhost -Port 5050
```

### Запуск приложения

```powershell
# Запускаем сервер в development режиме
npm run dev

# Приложение должно подключиться к PostgreSQL + Redis
# Смотрите логи в терминале для подтверждения подключений
```

### Тестирование на фронтенде

**Повторите те же шаги что в Mock режиме:**

1. Регистрация через форму на сайте
2. Логин через форму на сайте
3. Проверка сессии в хедере

### ✅ Ожидаемые результаты Development режима

- ✅ Пользователи сохраняются в PostgreSQL
- ✅ Сессии сохраняются в Redis
- ✅ Данные сохраняются между перезапусками сервера
- ✅ Можно зайти в pgAdmin (http://localhost:5050) и увидеть пользователей
- ✅ При перезапуске приложения сессия остается активной

### Проверка данных в БД

```powershell
# Подключение к pgAdmin
# URL: http://localhost:5050
# Email: admin@example.com (из docker-compose.yml)
# Password: admin (из docker-compose.yml)

# В pgAdmin можно посмотреть:
# - Базу данных exchanger_dev
# - Таблицу User с сохраненными пользователями
```

---

## 🚀 ТЕСТ 3: Production режим

### Настройка окружения

```powershell
# Устанавливаем production режим
$env:NODE_ENV = "production"

# Проверяем наличие production настроек
if (Test-Path ".env.production") {
    Write-Output "Production файл найден"
    Get-Content ".env.production" | Select-String "DATABASE_URL|REDIS_URL"
} else {
    Write-Output "Создайте .env.production из .env.production.example"
    Copy-Item ".env.production.example" ".env.production"
}
```

### Запуск production инфраструктуры

```powershell
# Запуск production контейнеров (PowerShell)
.\scripts\docker\prod-deploy.ps1

# Альтернативно:
docker-compose -f docker-compose.production.yml up -d

# Проверка статуса (PowerShell)
.\scripts\docker\prod-manage.ps1 status
```

### Тестирование на фронтенде

**Повторите те же шаги тестирования через браузер**

### ✅ Ожидаемые результаты Production режима

- ✅ UserManagerFactory создает ProductionUserManager
- ✅ Обязательные подключения к PostgreSQL + Redis
- ✅ Полная функциональность сессий с metadata
- ✅ Ошибка если DATABASE_URL или REDIS_URL отсутствуют

---

## 🔍 Диагностика проблем

### Проблема: "Cannot connect to server"

**Проверка:**

```powershell
# Проверяем что порт 3000 доступен
Test-NetConnection -ComputerName localhost -Port 3000

# Смотрим логи в терминале где запущен npm run dev
```

### Проблема: "Database connection failed"

**Проверка:**

```powershell
# Проверяем PostgreSQL контейнер
docker-compose ps postgres

# Проверяем порт
Test-NetConnection -ComputerName localhost -Port 5432

# Смотрим логи PostgreSQL
docker-compose logs postgres
```

### Проблема: "Redis connection failed"

**Проверка:**

```powershell
# Проверяем Redis контейнер
docker-compose ps redis

# Проверяем порт
Test-NetConnection -ComputerName localhost -Port 6379

# Смотрим логи Redis
docker-compose logs redis
```

### Проблема: Форма регистрации не отправляется

**Проверка:**

- Убедитесь что все поля заполнены
- Captcha поле должно содержать любой текст
- Проверьте консоль браузера (F12) на ошибки
- Проверьте Network tab на HTTP запросы к `/api/trpc/auth.register`

### Проблема: "Session not found" после логина

**Причина:** Cookie не сохраняется или sessionId некорректный
**Проверка:**

- Откройте Dev Tools → Application → Cookies
- Должен быть cookie `sessionId`
- Проверьте что домен localhost правильный

---

## 📊 Реальная архитектура

### Файлы использующие новый UserManagerFactory:

- ✅ `apps/web/src/server/trpc/context.ts`
- ✅ `apps/web/src/server/trpc/routers/auth.ts`
- ✅ `apps/web/src/server/trpc/routers/shared.ts`

### Фронтенд компоненты аутентификации:

- `apps/web/src/components/app-header.tsx` - хедер с кнопками логина
- `apps/web/src/components/auth-dialogs.tsx` - модальные окна форм
- `apps/web/src/components/forms/LoginForm.tsx` - форма логина
- `apps/web/src/components/forms/RegisterForm.tsx` - форма регистрации

### Реальные API endpoints:

- `/api/trpc/auth.register` - регистрация (POST)
- `/api/trpc/auth.login` - логин (POST)
- `/api/trpc/auth.getSession` - проверка сессии (GET)
- `/api/trpc/auth.logout` - выход (POST)

### Доступные npm скрипты:

```json
{
  "dev": "turbo run dev", // Запуск dev сервера
  "build": "turbo run build", // Сборка для production
  "check-types": "turbo run check-types", // Проверка TypeScript
  "test": "turbo run test" // Запуск тестов
}
```

### Docker скрипты:

- `.\scripts\docker\dev-up.ps1` - запуск development окружения (PowerShell)
- `.\scripts\docker\dev-down.ps1` - остановка development окружения (PowerShell)
- `.\scripts\docker\prod-deploy.ps1` - деплой production (PowerShell)
- `.\scripts\docker\prod-manage.ps1` - управление production (PowerShell)

---

## 🎯 Заключение

Система сессий полностью мигрирована на **единый асинхронный UserManagerFactory**:

1. **Mock режим** - для быстрой разработки без БД (по умолчанию)
2. **Development режим** - с PostgreSQL + Redis для реалистичного тестирования
3. **Production режим** - с обязательной PostgreSQL + Redis для production

**Все тестирование проводится через реальный фронтенд в браузере** - никаких curl запросов!

**Основной workflow (Windows PowerShell):**

1. `.\scripts\docker\dev-up.ps1` - запуск Docker окружения
2. `npm run dev` - запуск сервера
3. http://localhost:3000 - открыть в браузере
4. Кнопка "Войти" - тестирование форм регистрации/логина
5. Хедер показывает состояние сессии
6. `.\scripts\docker\dev-down.ps1` - остановка Docker окружения
