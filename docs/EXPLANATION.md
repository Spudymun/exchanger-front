# Обучающий гайд по проекту Exchanger

## 👤 Целевая аудитория

Этот документ предназначен для backend-разработчика или менеджера, который не имеет опыта во frontend или инфраструктурной части, но хочет понимать, как устроен проект и из чего он состоит.

---

## 1. Общая архитектура

Проект построен по принципам **Enterprise Monorepo** с использованием **Turborepo**:

- Один репозиторий — множество приложений и библиотек.
- Каждый модуль логически изолирован (например, `apps/web`, `packages/ui`).

**Основные папки:**

- `apps/` — конечные приложения (веб-интерфейсы, админки).
- `packages/` — переиспользуемые библиотеки: UI, хуки, API, константы.
- `docs/` — документация по проекту.
- `scripts/` — утилиты для анализа и чистки.
- `tests/` — автотесты.

---

## 2. Основные технологии

| Технология                     | Назначение                                             |
| ------------------------------ | ------------------------------------------------------ |
| **Next.js (v15)**              | React-фреймворк для frontend. Используется App Router. |
| **TypeScript**                 | Типизированный JavaScript — повышает надёжность кода.  |
| **Tailwind CSS**               | Утилитарный CSS-фреймворк.                             |
| **tRPC**                       | Типобезопасный вызов API (как RPC между FE ↔ BE).     |
| **React Query / Zustand**      | Управление состоянием (данные и UI-флаги).             |
| **shadcn/ui**                  | Готовые UI-компоненты на Tailwind.                     |
| **Vitest / Playwright**        | Юнит и E2E тесты.                                      |
| **Storybook**                  | Каталог UI-компонентов для визуального теста.          |
| **OpenTelemetry / Prometheus** | Мониторинг, трассировка, метрики.                      |
| **Husky + lint-staged**        | Git-хуки для автопроверок перед коммитом.              |

---

## 3. Основные приложения (папка apps/)

### 3.1. `apps/web`

Главное клиентское приложение. Использует **App Router**:

- `app/(dashboard)/...` — сегмент с маршрутом личного кабинета.
- `@modal`, `@sidebar` — параллельные маршруты для всплывающих окон и боковых панелей.
- `middleware.ts` — защита маршрутов (например, авторизация).
- `api/trpc/[trpc]/route.ts` — входная точка tRPC API.
- `messages/*.json` — переводы для локализации (i18n).

### 3.2. `apps/admin-panel`

Интерфейс администратора. Использует ту же архитектуру, но минимально подключён.

### 3.3. `apps/docs`

Автономное Next.js приложение для хостинга документации.

---

## 4. Библиотеки (packages/)

### 4.1. `ui/`

Готовые компоненты пользовательского интерфейса:

- `button.tsx`, `dialog.tsx`, `data-table.tsx`, `tree-view.tsx`.
- `__tests__/` — unit-тесты компонентов.
- `stories/` — Storybook-документация.

### 4.2. `api-client/`

Обёртки над вызовами API с сервера и клиента. Используется tRPC.

- `client.ts`, `server.ts` — клиентская и серверная часть вызовов.

### 4.3. `hooks/`

React-хуки для состояния:

- `state/ui-store.ts` — UI-состояние.
- `state/trading-store.ts` — логика обменов, выбор валют и пр.

### 4.4. `constants/`

Статические данные и бизнес-логика:

- `exchange.ts`, `rate-limits.ts`, `validation.ts`, `business.ts`.

### 4.5. `providers/`

Глобальные обёртки контекстов — тема, i18n и т.п.

### 4.6. `utils/`

Общие вспомогательные функции и обработка переменных окружения.

---

## 5. Документация (docs/)

Содержит Markdown-документы:

- `RPD.md` — спецификация требований проекта.
- `ARCHITECTURE.md` — принципы архитектуры.
- `API_DOCS.md` — структура API.
- `TASKS-PART-*.md` — последовательность задач для реализации.
- `CODE_STYLE_GUIDE.md`, `ENTERPRISE_LINTING.md` — правила оформления кода.

> Эти документы помогают как людям, так и ИИ-агентам (например, Cloud Sonnet) ориентироваться в проекте.

---

## 6. Тесты и DevOps

- `tests/` — файлы e2e (интеграционные) тестов.
- `scripts/` — скрипты для анализа проекта и очистки.
- `.husky/`, `.lintstagedrc.json` — прекоммит‑проверки.
- `turbo.json` — конфигурация pipeline от Turborepo.
- `playwright.config.ts`, `jest.config.js` — тестовые фреймворки.

---

## 7. Вывод

Ты смотришь на **грамотно структурированный frontend-инфраструктурный проект**, где всё:

- Разделено по ответственности.
- Поддерживает масштабируемость.
- Подходит как для ручной работы, так и для автоматизации через ИИ.

Если хочешь — я могу объяснить любой файл или концепт глубже (например, как работает `tRPC`, или что делает `turbo.json`).
