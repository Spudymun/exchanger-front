# 🚀 Быстрый старт: Тестирование P2P Fallback

## 📋 Что делать

1. **Добавить ручной курс в БД** (если нет):

   ```bash
   npm run insert-manual-usdt-rate
   ```

2. **Включить симуляцию ошибки**:

   Добавить в `apps/web/.env.local`:

   ```bash
   SIMULATE_P2P_ERROR=true
   ```

3. **Перезапустить dev сервер**:

   ```bash
   npm run dev
   ```

4. **Открыть приложение**:

   ```
   http://localhost:3000
   ```

5. **Проверить логи** в терминале:

   ```
   ⚠️ SIMULATION MODE: P2P API error simulated
   INFO[SmartPricingService] Manual rate found for USDT
   ```

6. **После тестирования отключить**:

   Удалить или закомментировать в `.env.local`:

   ```bash
   # SIMULATE_P2P_ERROR=true
   ```

## 🎯 Ожидаемый результат

- ✅ P2P API симулирует ошибку
- ✅ Система использует Manual DB курс
- ✅ Курс USDT отображается корректно (~44.07 UAH)
- ✅ Обмен работает нормально

## 📚 Подробности

Смотри полное руководство: [P2P_FALLBACK_TESTING_GUIDE.md](./P2P_FALLBACK_TESTING_GUIDE.md)

---

**⚠️ ВАЖНО:** Не использовать `SIMULATE_P2P_ERROR=true` в production!
