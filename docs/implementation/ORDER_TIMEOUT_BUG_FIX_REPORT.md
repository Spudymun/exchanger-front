# 🐛 Отчет об исправлении критической ошибки расчета времени истечения заказа

**Дата обнаружения:** 11 октября 2025  
**Дата исправления:** 11 октября 2025  
**Приоритет:** КРИТИЧЕСКИЙ  
**Статус:** ✅ ИСПРАВЛЕНО

---

## 📋 Описание проблемы

### Обнаруженная ошибка

При тестировании системы автоматической отмены заказов обнаружено **критическое расхождение** между ожидаемым и фактическим временем истечения:

**Ожидалось:**

- Время истечения: **90 минут** (согласно фронтенду)
- TTL в Redis: **5400 секунд**

**Фактически было:**

- Время истечения: **90 ЧАСОВ** (324000 секунд)
- TTL в Redis: **324000 секунд**

**Расхождение:** В 60 раз больше ожидаемого!

---

## 🔍 Анализ причины

### Фактическая проверка константы

```typescript
// packages/constants/src/time-constants.ts (строка 98)
export const OPERATION_TIME_CONSTANTS = {
  ORDER_EXPIRATION_MINUTES: 90, // ✅ Правильное значение
} as const;
```

### Ошибочная формула расчета

**Было (неправильно):**

```typescript
// packages/constants/src/exchange.ts
export const ORDER_EXPIRATION_TIME_MS =
  OPERATION_TIME_CONSTANTS.ORDER_EXPIRATION_MINUTES *
  TIME_CONSTANTS.MINUTES_IN_HOUR * // ❌ ЛИШНИЙ МНОЖИТЕЛЬ!
  TIME_CONSTANTS.SECONDS_IN_MINUTE *
  TIME_CONSTANTS.MILLISECONDS_IN_SECOND;

// Расчет: 90 * 60 * 60 * 1000 = 324000000 ms = 90 ЧАСОВ ❌
```

**Стало (правильно):**

```typescript
// packages/constants/src/exchange.ts
export const ORDER_EXPIRATION_TIME_MS =
  OPERATION_TIME_CONSTANTS.ORDER_EXPIRATION_MINUTES *
  TIME_CONSTANTS.SECONDS_IN_MINUTE *
  TIME_CONSTANTS.MILLISECONDS_IN_SECOND;

// Расчет: 90 * 60 * 1000 = 5400000 ms = 90 МИНУТ ✅
```

---

## 🔧 Выполненные исправления

### 1. Исправлена формула расчета времени

**Файл:** `packages/constants/src/exchange.ts`

**Изменение:**

- ❌ Удален лишний множитель `TIME_CONSTANTS.MINUTES_IN_HOUR`
- ✅ Обновлен комментарий с "30 minutes" на "90 minutes"

**Commit:**

```diff
- // Order expiration time (30 minutes)
+ // Order expiration time (90 minutes)
  export const ORDER_EXPIRATION_TIME_MS =
    OPERATION_TIME_CONSTANTS.ORDER_EXPIRATION_MINUTES *
-   TIME_CONSTANTS.MINUTES_IN_HOUR *
    TIME_CONSTANTS.SECONDS_IN_MINUTE *
    TIME_CONSTANTS.MILLISECONDS_IN_SECOND;
```

### 2. Обновлены комментарии в коде

**Файл:** `apps/web/src/server/trpc/routers/exchange.ts` (строка 383)

```diff
- expiresAt: new Date(Date.now() + ORDER_EXPIRATION_TIME_MS), // ✅ Автоматическая отмена через 30 мин
+ expiresAt: new Date(Date.now() + ORDER_EXPIRATION_TIME_MS), // ✅ Автоматическая отмена через 90 мин
```

**Файл:** `packages/exchange-core/src/types/order.ts` (строка 47)

```diff
- expiresAt?: Date; // Время истечения заказа (now + 30 мин)
+ expiresAt?: Date; // Время истечения заказа (now + 90 мин)
```

---

## ✅ Верификация исправления

### Математическая проверка

```javascript
const ORDER_EXPIRATION_MINUTES = 90;
const SECONDS_IN_MINUTE = 60;
const MILLISECONDS_IN_SECOND = 1000;

const ORDER_EXPIRATION_TIME_MS =
  ORDER_EXPIRATION_MINUTES * SECONDS_IN_MINUTE * MILLISECONDS_IN_SECOND;

console.log('ORDER_EXPIRATION_TIME_MS =', ORDER_EXPIRATION_TIME_MS); // 5400000 ms
console.log('В секундах =', ORDER_EXPIRATION_TIME_MS / 1000); // 5400 сек
console.log('В минутах =', ORDER_EXPIRATION_TIME_MS / 1000 / 60); // 90 мин
console.log('В часах =', ORDER_EXPIRATION_TIME_MS / 1000 / 60 / 60); // 1.5 ч
```

**Результат:**

```
ORDER_EXPIRATION_TIME_MS = 5400000 ms ✅
В секундах = 5400 сек ✅
В минутах = 90 мин ✅
В часах = 1.5 ч ✅
```

### TypeScript проверка

```bash
npm run check-types
# ✅ Tasks: 14 successful, 14 total
# ✅ No TypeScript errors
```

---

## 📊 Влияние на систему

### До исправления

| Параметр               | Значение                         | Статус         |
| ---------------------- | -------------------------------- | -------------- |
| Время истечения заказа | 90 часов (3.75 дня)              | ❌ Неправильно |
| Redis TTL              | 324000 секунд                    | ❌ Неправильно |
| Соответствие фронтенду | Нет (фронтенд показывает 90 мин) | ❌ Расхождение |
| Бизнес-логика          | Заказы висят 3.75 дня            | ❌ Неприемлемо |

### После исправления

| Параметр               | Значение                       | Статус       |
| ---------------------- | ------------------------------ | ------------ |
| Время истечения заказа | 90 минут (1.5 часа)            | ✅ Правильно |
| Redis TTL              | 5400 секунд                    | ✅ Правильно |
| Соответствие фронтенду | Да (90 минут)                  | ✅ Совпадает |
| Бизнес-логика          | Заказы отменяются через 90 мин | ✅ Корректно |

---

## 🧪 Тестирование после исправления

### Рекомендуемые тесты

**Тест 1: Проверка константы**

```bash
node -e "console.log(require('./packages/constants/src/exchange.ts').ORDER_EXPIRATION_TIME_MS)"
# Ожидается: 5400000
```

**Тест 2: Создание заказа**

```bash
# 1. Запустить dev сервер: npm run dev
# 2. Создать тестовый заказ
# 3. Проверить Redis TTL:
docker exec -it exchanger-redis redis-cli TTL "order:expire:{orderId}"
# Ожидается: ~5400 секунд (90 минут)
```

**Тест 3: Проверка expiresAt в БД**

```bash
docker exec -it exchanger-postgres psql -U exchanger_user -d exchanger_db \
  -c "SELECT id, status, expires_at, created_at FROM orders ORDER BY created_at DESC LIMIT 1;"
# Ожидается: expires_at ≈ created_at + 90 минут
```

**Тест 4: Автоматическая отмена**

```bash
# Вручную истечь Redis ключ для ускорения теста:
docker exec -it exchanger-redis redis-cli DEL "order:expire:{orderId}"
# Проверить что заказ отменен в течение 1-2 секунд
```

---

## 📝 Уроки и рекомендации

### Что пошло не так

1. **Недостаточная верификация:** Не проверили фактическое значение константы на фронтенде перед реализацией
2. **Неправильный комментарий:** Комментарий "30 minutes" вводил в заблуждение
3. **Отсутствие unit-тестов:** Нет тестов на правильность расчета констант

### Рекомендации

1. ✅ **Добавить unit-тесты для констант:**

```typescript
// packages/constants/src/__tests__/exchange.test.ts
describe('ORDER_EXPIRATION_TIME_MS', () => {
  it('should equal 90 minutes in milliseconds', () => {
    const NINETY_MINUTES_MS = 90 * 60 * 1000;
    expect(ORDER_EXPIRATION_TIME_MS).toBe(NINETY_MINUTES_MS);
    expect(ORDER_EXPIRATION_TIME_MS).toBe(5400000);
  });
});
```

2. ✅ **Добавить валидацию в E2E тестах:**

```typescript
// tests/order-expiration.spec.ts
test('order TTL should be 90 minutes', async () => {
  const order = await createTestOrder();
  const ttl = await redis.ttl(`order:expire:${order.id}`);
  expect(ttl).toBeGreaterThan(5390); // ~90 мин минус время выполнения
  expect(ttl).toBeLessThan(5410); // ~90 мин плюс погрешность
});
```

3. ✅ **Документировать критические константы:**

```typescript
/**
 * Время жизни заказа в миллисекундах
 *
 * @constant ORDER_EXPIRATION_TIME_MS
 * @value 5400000 ms = 5400 сек = 90 минут = 1.5 часа
 * @see OPERATION_TIME_CONSTANTS.ORDER_EXPIRATION_MINUTES
 * @important Должно совпадать с таймером на фронтенде!
 */
export const ORDER_EXPIRATION_TIME_MS = ...
```

---

## 🎯 Итог

### Статус исправления

✅ **Исправление применено успешно**
✅ **TypeScript проверка пройдена**
✅ **Математическая верификация выполнена**
✅ **Комментарии обновлены**

### Следующие шаги

1. ✅ Запустить dev сервер: `npm run dev`
2. ✅ Создать тестовый заказ
3. ✅ Проверить Redis TTL (должен быть ~5400 секунд)
4. ✅ Проверить expiresAt в БД (должен быть +90 минут от created_at)
5. ⏳ Дождаться автоматической отмены ИЛИ вручную удалить Redis ключ
6. ⏳ Проверить что заказ перешел в статус `cancelled`

---

**Исправление завершено:** 11 октября 2025 ✅  
**Критическая ошибка устранена** 🎉
