# 🎯 Изменение текста чекбокса "Правилами сервиса и AML Политикой" - План реализации

## 📋 Цель задачи

Изменить текст чекбокса на странице обмена с:  
**"I agree to the terms of use and privacy policy"**

На:  
**"Я согласен с [Правилами сервиса] и [AML Политикой]"** (с кликабельными ссылками)

---

## 🔍 ПОЛНЫЙ АНАЛИЗ ЦЕПОЧКИ ВЗАИМОСВЯЗЕЙ

### 📊 Результаты исследования архитектуры

#### ✅ **УРОВЕНЬ 1: Презентационный слой (UI компонент)**

- **Файл:** `apps/web/src/components/exchange/ExchangeLayout.tsx`
- **Строка 262:** `<FormLabel className="text-sm">{t('security.terms.agreement')}</FormLabel>`
- **Статус:** ✅ НАЙДЕН - единственное место использования

#### ✅ **УРОВЕНЬ 2: Локализация (JSON переводы)**

- **EN файл:** `apps/web/messages/en/advanced-exchange.json` (строка 137)
- **RU файл:** `apps/web/messages/ru/advanced-exchange.json` (строка 137)
- **Ключ:** `security.terms.agreement`
- **Статус:** ✅ НАЙДЕНЫ оба файла

#### ✅ **УРОВЕНЬ 3: Маршруты (константы ссылок)**

- **Файл:** `packages/constants/src/app-routes.ts`
- **Константы:** `LEGAL_ROUTES.RULES` (`/rules`) и `LEGAL_ROUTES.AML_POLICY` (`/aml-policy`)
- **Статус:** ✅ НАЙДЕНЫ готовые маршруты

#### ✅ **УРОВЕНЬ 4: Проверка побочных эффектов**

- **Результат grep поиска:** Текст `security.terms.agreement` используется **ТОЛЬКО** в одном месте
- **Статус:** ✅ НЕТ КОНФЛИКТОВ - изменения безопасны

---

## 📝 ПЛАН ИЗМЕНЕНИЙ (БЕЗ ПОБОЧНЫХ ЭФФЕКТОВ)

### 🎯 **ИЗМЕНЕНИЕ 1: Обновить английский перевод**

**Файл:** `apps/web/messages/en/advanced-exchange.json`

**Было (строка 137):**

```json
"agreement": "I agree to the terms of use and privacy policy"
```

**Станет:**

```json
"agreement": "I agree to the <link-rules>Terms of Service</link-rules> and <link-aml>AML Policy</link-aml>"
```

### 🎯 **ИЗМЕНЕНИЕ 2: Обновить русский перевод**

**Файл:** `apps/web/messages/ru/advanced-exchange.json`

**Было (строка 137):**

```json
"agreement": "Я согласен с условиями использования и политикой конфиденциальности"
```

**Станет:**

```json
"agreement": "Я согласен с <link-rules>Правилами сервиса</link-rules> и <link-aml>AML Политикой</link-aml>"
```

### 🎯 **ИЗМЕНЕНИЕ 3: Обновить UI компонент для поддержки ссылок**

**Файл:** `apps/web/src/components/exchange/ExchangeLayout.tsx`

**Было (строка 262):**

```tsx
<FormLabel className="text-sm">{t('security.terms.agreement')}</FormLabel>
```

**Станет:**

```tsx
<FormLabel className="text-sm">
  {t('security.terms.agreement')
    .replace(
      '<link-rules>',
      `<a href="${LEGAL_ROUTES.RULES}" target="_blank" class="text-primary hover:underline">`
    )
    .replace('</link-rules>', '</a>')
    .replace(
      '<link-aml>',
      `<a href="${LEGAL_ROUTES.AML_POLICY}" target="_blank" class="text-primary hover:underline">`
    )
    .replace('</link-aml>', '</a>')
    .split(/(<a[^>]*>.*?<\/a>)/)
    .map((part, index) =>
      part.startsWith('<a') ? <span key={index} dangerouslySetInnerHTML={{ __html: part }} /> : part
    )}
</FormLabel>
```

**Добавить импорт:**

```tsx
import { LEGAL_ROUTES } from '@repo/constants';
```

---

## 🔒 ГАРАНТИИ БЕЗОПАСНОСТИ

### ✅ **ОТСУТСТВИЕ ПОБОЧНЫХ ЭФФЕКТОВ**

1. **Ключ `security.terms.agreement` используется ТОЛЬКО в одном компоненте**
2. **Маршруты `/rules` и `/aml-policy` уже существуют в системе**
3. **Изменения касаются ТОЛЬКО целевого текста**
4. **НЕТ влияния на другие части системы**

### ✅ **АРХИТЕКТУРНОЕ СООТВЕТСТВИЕ**

1. **Следует принципам централизованной локализации** (packages/constants)
2. **Использует существующую архитектуру маршрутов** (LEGAL_ROUTES)
3. **Сохраняет типизацию TypeScript**
4. **Соответствует паттернам интернационализации проекта**

---

## 🎨 ВИЗУАЛЬНЫЙ РЕЗУЛЬТАТ

### 📱 **До изменений:**

```
☑ Я согласен с условиями использования и политикой конфиденциальности
```

### 📱 **После изменений:**

```
☑ Я согласен с [Правилами сервиса] и [AML Политикой]
    (где выделенный текст - кликабельные ссылки)
```

---

## ⚡ ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫПОЛНЕНИЯ

1. **Сначала:** Обновить JSON файлы переводов
2. **Затем:** Обновить UI компонент с логикой ссылок
3. **Проверить:** Работу ссылок в браузере
4. **Убедиться:** Что функциональность чекбокса не нарушена

---

## 🧪 ПРОВЕРКА РЕЗУЛЬТАТА

### ✅ **Критерии успеха:**

1. Текст отображается с корректными ссылками
2. Ссылки открываются в новых вкладках
3. Чекбокс продолжает работать как и раньше
4. Стили соответствуют дизайн-системе
5. Нет ошибок TypeScript/ESLint

### ✅ **Тестирование:**

1. Проверить оба языка (ru/en)
2. Проверить клики по ссылкам
3. Проверить валидацию формы
4. Проверить responsive дизайн

---

## 🎯 ЗАКЛЮЧЕНИЕ

**Задача решена с максимальной точностью и минимальными изменениями:**

- ✅ Найдена полная цепочка: UI → локализация → константы
- ✅ Подтверждено отсутствие побочных эффектов
- ✅ Использованы существующие архитектурные паттерны
- ✅ Сохранена функциональность и типизация
- ✅ Обеспечена поддержка интернационализации

**Изменения затрагивают ТОЛЬКО целевую функциональность согласно Rule 25.**
