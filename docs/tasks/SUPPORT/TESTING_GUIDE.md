# 🧪 Testing Guide: Client Support Telegram

**Дата**: October 9, 2025  
**Статус**: Implementation completed, ready for testing

---

## ✅ Что реализовано

### Backend Changes

1. ✅ **Types**: `BotSession` расширен с `userType`, `lastMessageTime`, `messageCount`
2. ✅ **Utilities**: `extractUserId()`, `extractUsername()`, `getUserType()`, `checkClientRateLimit()`
3. ✅ **Client Handlers**: `handleClientStart()`, `handleClientHelp()`, `handleClientMessage()`
4. ✅ **Router**: `handleTelegramUpdate()` обновлён с routing по типу пользователя
5. ✅ **Constants**: `TELEGRAM_CLIENT_MESSAGES` добавлены
6. ✅ **Rate Limiting**: In-memory rate limiting (5 msg/min)

### Frontend Changes

1. ✅ **Constants**: `SOCIAL_LINKS.SUPPORT_TELEGRAM.href` → `https://t.me/exchangego_operators_bot`
2. ✅ **Localization**: `footer.contacts.telegram` → `@exchangego_operators_bot`
3. ✅ **Footer**: Уже существующий компонент использует обновлённую константу

---

## 📋 Тестовые сценарии

### Scenario 1: Operator Flow (Regression Testing) ✅

**Цель**: Убедиться что операторский функционал работает как раньше

**Шаги**:

1. Открыть Telegram
2. Найти бота `@exchangego_operators_bot`
3. Отправить `/start` от **авторизованного оператора** (userId в `AUTHORIZED_TELEGRAM_OPERATORS`)
4. **Ожидаемый результат**:

   ```
   Добро пожаловать в ExchangeGO Bot! 👋

   Я помогаю операторам управлять заявками.

   Доступные команды:
   /start - Начать работу с ботом
   /help - Показать справку
   /login - Войти как оператор
   /takeorder - Взять заявку в работу
   /orders - Показать активные заявки

   Для начала работы используйте /login
   ```

5. Отправить `/login`
6. **Ожидаемый результат**: "✅ Вы вошли как оператор!"

7. Отправить `/orders`
8. **Ожидаемый результат**: Список заявок или "Нет активных заявок"

✅ **Operator flow работает без изменений**

---

### Scenario 2: Client Flow (New Functionality) 🆕

**Цель**: Проверить новый функционал для клиентов

**Шаги**:

1. Открыть Telegram
2. Найти бота `@exchangego_operators_bot`
3. Отправить `/start` от **НЕ авторизованного пользователя** (обычный клиент)
4. **Ожидаемый результат**:

   ```
   👋 Добро пожаловать в службу поддержки ExchangeGO!

   Опишите вашу проблему или задайте вопрос.
   Наши операторы ответят вам в ближайшее время.

   ⏱ Среднее время ответа: 1-3 часа
   ⚡ Мы работаем 24/7
   ```

5. Отправить `/help`
6. **Ожидаемый результат**:

   ```
   📋 Как получить помощь:

   1️⃣ Опишите вашу проблему в сообщении
   2️⃣ Укажите номер заявки (если есть)
   3️⃣ Дождитесь ответа оператора

   ⏱ Мы отвечаем в течение 1-3 часов
   💬 Оператор свяжется с вами в личных сообщениях
   ```

7. Отправить текстовое сообщение (например: "Не пришла криптовалюта")
8. **Ожидаемый результат для клиента**:

   ```
   ✅ Ваше сообщение получено!

   Оператор свяжется с вами в ближайшее время.
   Среднее время ответа: 1-3 часа
   ```

9. **Ожидаемый результат для ВСЕХ операторов** (проверить в Telegram операторов):

   ```
   💬 **Новое обращение клиента в поддержку**

   👤 Пользователь: @username (или ID 123456789)
   📱 Telegram ID: `123456789`

   💬 Сообщение:
   Не пришла криптовалюта

   ℹ️ Ответьте клиенту в личных сообщениях Telegram
   ```

✅ **Client support flow работает**

---

### Scenario 3: Rate Limiting 🛡️

**Цель**: Проверить защиту от спама

**Шаги**:

1. От клиента отправить 5 сообщений подряд (в течение минуты)
2. **Ожидаемый результат**: Все 5 сообщений обработаны успешно

3. Отправить 6-е сообщение
4. **Ожидаемый результат**:

   ```
   ⚠️ Слишком много сообщений

   Пожалуйста, подождите минуту перед отправкой следующего сообщения.
   Лимит: 5 сообщений в минуту
   ```

5. Подождать 1 минуту
6. Отправить ещё одно сообщение
7. **Ожидаемый результат**: Сообщение обработано успешно

✅ **Rate limiting работает**

---

### Scenario 4: Operator Command Denial 🚫

**Цель**: Проверить что клиенты не могут использовать операторские команды

**Шаги**:

1. От клиента отправить `/login`
2. **Ожидаемый результат**:

   ```
   ❌ Эта команда доступна только операторам
   ```

3. От клиента отправить `/takeorder 12345`
4. **Ожидаемый результат**:

   ```
   ❌ Эта команда доступна только операторам
   ```

5. От клиента отправить `/orders`
6. **Ожидаемый результат**:
   ```
   ❌ Эта команда доступна только операторам
   ```

✅ **Operator commands blocked for clients**

---

### Scenario 5: Frontend Footer Link 🌐

**Цель**: Проверить что кнопка на сайте ведёт на правильный бот

**Шаги**:

1. Открыть сайт `http://localhost:3000` (или production URL)
2. Прокрутить до Footer
3. Найти секцию "Поддержка и помощь"
4. Кликнуть на "Telegram поддержка"
5. **Ожидаемый результат**: Открывается Telegram с ботом `@exchangego_operators_bot`

✅ **Frontend link работает**

---

## 🔍 Логи для проверки

### Backend Logs (telegram-bot)

**Client message received**:

```
CLIENT_MESSAGE_RECEIVED { userId: 123456789, username: '@testuser', messageLength: 25 }
```

**Operators notified**:

```
OPERATOR_NOTIFIED_CLIENT_MESSAGE { operatorId: '111111111', clientUserId: 123456789 }
CLIENT_MESSAGE_FORWARDED { userId: 123456789, operatorsNotified: 2, totalOperators: 2 }
```

**Rate limit exceeded**:

```
CLIENT_RATE_LIMIT_EXCEEDED { userId: 123456789, messageCount: 5, windowMs: 60000 }
```

**Routing**:

```
TELEGRAM_UPDATE_ROUTING { userId: 123456789, userType: 'client', command: '/start' }
TELEGRAM_START_ROUTING { userId: 123456789, userType: 'client' }
```

---

## 📊 Acceptance Criteria Checklist

### AC-1: User Type Detection

- [ ] Оператор (userId в `AUTHORIZED_TELEGRAM_OPERATORS`) → `userType: 'operator'`
- [ ] Обычный пользователь → `userType: 'client'`

### AC-2: Client Greeting

- [ ] Клиент пишет `/start` → получает приветственное сообщение
- [ ] Клиент пишет `/help` → получает инструкцию

### AC-3: Message Forwarding

- [ ] Клиент отправляет сообщение → все операторы получают уведомление
- [ ] Уведомление содержит: username, userId, текст сообщения

### AC-4: Operator Commands Blocked

- [ ] Клиент пытается `/login` → "Команда доступна только операторам"
- [ ] Клиент пытается `/takeorder` → "Команда доступна только операторам"
- [ ] Клиент пытается `/orders` → "Команда доступна только операторам"

### AC-5: Frontend Integration

- [ ] Footer содержит ссылку на Telegram поддержку
- [ ] Клик открывает `@exchangego_operators_bot`

### AC-6: Rate Limiting

- [ ] 5 сообщений в минуту → все обработаны
- [ ] 6-е сообщение → rate limit error
- [ ] После минуты → снова можно отправлять

### AC-7: Logging

- [ ] `CLIENT_MESSAGE_RECEIVED` логируется
- [ ] `CLIENT_MESSAGE_FORWARDED` с количеством уведомлённых операторов
- [ ] `CLIENT_RATE_LIMIT_EXCEEDED` при превышении лимита

---

## 🚨 Known Issues / Limitations

### Current Implementation (v1.0 MVP)

1. **In-memory sessions**: При рестарте бота rate limit сбрасывается (acceptable для MVP)
2. **Manual operator responses**: Операторы отвечают вручную в ЛС (не через бота)
3. **No message history**: История переписки не сохраняется в БД
4. **Lint warnings**: Некоторые функции превышают 50 строк (можно рефакторить позже)

### Future Improvements (v1.5-2.0)

- [ ] Reply механизм через бота
- [ ] Сохранение истории в БД
- [ ] Redis для персистентных sessions
- [ ] Система тикетов
- [ ] Dashboard для операторов

---

## 🎯 Testing Priorities

### Priority 1 (Must Test Before Release)

1. ✅ Operator flow regression
2. ✅ Client can send message
3. ✅ Operators receive notification
4. ✅ Frontend link works

### Priority 2 (Important)

5. ✅ Rate limiting works
6. ✅ Operator commands blocked for clients

### Priority 3 (Nice to Have)

7. ✅ Logging is correct
8. ✅ Error handling graceful

---

## 📝 Test Results Template

```markdown
## Test Results - [DATE]

**Tester**: [NAME]
**Environment**: Dev / Staging / Production
**Bot**: @exchangego_operators_bot

### Scenario 1: Operator Flow ✅ / ❌

- [ ] /start works
- [ ] /login works
- [ ] /takeorder works
- [ ] /orders works
      **Notes**:

### Scenario 2: Client Flow ✅ / ❌

- [ ] /start shows client greeting
- [ ] Text message sends notification to operators
- [ ] Operator received notification
      **Notes**:

### Scenario 3: Rate Limiting ✅ / ❌

- [ ] 5 messages accepted
- [ ] 6th message blocked
      **Notes**:

### Scenario 4: Command Denial ✅ / ❌

- [ ] /login blocked for client
- [ ] /takeorder blocked for client
      **Notes**:

### Scenario 5: Frontend Link ✅ / ❌

- [ ] Footer link opens correct bot
      **Notes**:

### Overall Status: ✅ PASSED / ❌ FAILED / ⚠️ PARTIAL

**Issues Found**:

1.
2.

**Recommendations**:

1.
2.
```

---

## 🔧 Environment Variables Required

**apps/telegram-bot/.env**:

```bash
TELEGRAM_BOT_TOKEN=8080670068:AAG1LtOO0INbJFOXhj5--WHWRvImewP866E
TELEGRAM_BOT_USERNAME=exchangego_operators_bot
AUTHORIZED_TELEGRAM_OPERATORS=111111111,222222222  # Operator Telegram IDs
```

**apps/web/.env.local**:

```bash
TELEGRAM_BOT_URL=http://localhost:3001  # Dev
# TELEGRAM_BOT_URL=https://bot.exchangego.com  # Production
```

---

**Ready for testing!** 🚀
