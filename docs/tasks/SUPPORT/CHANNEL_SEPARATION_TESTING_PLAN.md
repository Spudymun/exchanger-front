# ✅ Тестирование разделения каналов уведомлений

## 📋 Настройка завершена!

### Конфигурация:

```bash
TELEGRAM_ORDERS_CHAT_ID=-1003161915296    # Группа для заказов
TELEGRAM_SUPPORT_CHAT_ID=-1003037178274   # Группа для поддержки
```

---

## 🧪 План тестирования

### Тест 1: Клиентское сообщение через бота

**Цель**: Проверить, что сообщение от клиента попадает в Support Group

**Шаги**:

1. Откройте бота `@exchangego_operators_bot` в Telegram
2. Отправьте команду: `/message Тестовое сообщение от клиента`
3. Проверьте группу **TELEGRAM_SUPPORT_CHAT_ID** (-1003037178274)

**Ожидаемый результат**:

- ✅ Сообщение появилось в Support Group
- ✅ В логах: `TELEGRAM_SUPPORT_GROUP_ROUTE`
- ❌ НЕ появилось в Orders Group

**Логи для проверки**:

```
INFO[telegram-bot] TELEGRAM_SUPPORT_GROUP_ROUTE
{
  "supportChatId": "-1003037178274",
  "orderId": "...",
  "messageLength": ...
}
```

---

### Тест 2: Уведомление о новом заказе

**Цель**: Проверить, что уведомление о заказе попадает в Orders Group

**Шаги**:

1. Откройте веб-приложение (http://localhost:3000)
2. Создайте новый заказ на обмен
3. Проверьте группу **TELEGRAM_ORDERS_CHAT_ID** (-1003161915296)

**Ожидаемый результат**:

- ✅ Уведомление появилось в Orders Group
- ✅ В логах: `TELEGRAM_ORDERS_GROUP_ROUTE`
- ❌ НЕ появилось в Support Group

**Логи для проверки**:

```
INFO[telegram-bot] TELEGRAM_ORDERS_GROUP_ROUTE
{
  "ordersChatId": "-1003161915296",
  "orderId": "...",
  "messageLength": ...
}
```

---

### Тест 3: Fallback режим (без переменных)

**Цель**: Проверить обратную совместимость при отсутствии настроек групп

**Шаги**:

1. Закомментируйте обе переменные в `.env`:
   ```bash
   # TELEGRAM_ORDERS_CHAT_ID=-1003161915296
   # TELEGRAM_SUPPORT_CHAT_ID=-1003037178274
   ```
2. Перезапустите бота: `npm run dev`
3. Отправьте `/message Тест fallback`
4. Создайте новый заказ

**Ожидаемый результат**:

- ✅ Уведомления отправляются **всем операторам** (broadcast)
- ✅ В логах: `TELEGRAM_ORDERS_FALLBACK_BROADCAST` или `TELEGRAM_SUPPORT_FALLBACK_BROADCAST`
- ✅ Каждый оператор получает личное сообщение

**Логи для проверки**:

```
INFO[telegram-bot] TELEGRAM_SUPPORT_FALLBACK_BROADCAST
{
  "reason": "TELEGRAM_SUPPORT_CHAT_ID not configured",
  "orderId": "..."
}
```

---

### Тест 4: Ошибка отправки в группу (fallback на broadcast)

**Цель**: Проверить автоматический fallback при недоступности группы

**Шаги**:

1. Временно удалите бота из одной из групп (например, Support Group)
2. Отправьте `/message Тест ошибки`
3. Проверьте логи и личные сообщения операторам

**Ожидаемый результат**:

- ⚠️ Попытка отправки в группу не удалась
- ✅ Автоматический fallback на broadcast
- ✅ В логах: `Failed to send to support group, falling back to broadcast`
- ✅ Каждый оператор получил сообщение

**Логи для проверки**:

```
WARN[telegram-bot] Failed to send to support group, falling back to broadcast
{
  "orderId": "...",
  "supportChatId": "-1003037178274"
}

INFO[telegram-bot] TELEGRAM_SUPPORT_FALLBACK_BROADCAST
{
  "reason": "Group send failed",
  "orderId": "..."
}
```

---

## 📊 Чеклист тестирования

- [ ] **Тест 1**: Клиентское сообщение → Support Group ✅
- [ ] **Тест 2**: Уведомление о заказе → Orders Group ✅
- [ ] **Тест 3**: Fallback режим без переменных ✅
- [ ] **Тест 4**: Fallback при ошибке отправки ✅

---

## 🔍 Мониторинг в продакшене

### Ключевые метрики для отслеживания:

1. **Успешность отправки в группы**:
   - Количество `TELEGRAM_ORDERS_GROUP_ROUTE`
   - Количество `TELEGRAM_SUPPORT_GROUP_ROUTE`

2. **Частота fallback**:
   - Количество `FALLBACK_BROADCAST`
   - Причины fallback (`reason` в логах)

3. **Ошибки отправки**:
   - `Failed to send to orders/support group`
   - Проверить права бота в группах

### PowerShell скрипт для мониторинга логов:

```powershell
# Следим за логами в реальном времени
npm run dev | Select-String -Pattern "TELEGRAM.*GROUP.*ROUTE|FALLBACK"

# Или через grep (если установлен):
npm run dev | grep -E "TELEGRAM.*(GROUP_ROUTE|FALLBACK)"
```

---

## ⚠️ Известные проблемы и решения

### Проблема: Бот не отправляет в группу (403 ошибка)

**Причина**: Бот не является администратором группы

**Решение**:

1. Откройте группу → Настройки → Администраторы
2. Добавьте бота как администратора
3. Включите право: **"Отправка сообщений"**

---

### Проблема: Chat ID не работает

**Причина**: Неправильный формат Chat ID

**Решение**:

- Supergroup ID начинается с `-100` (например: `-1003161915296`)
- Обычная группа начинается с `-` (например: `-4815058497`)
- Рекомендуется использовать **supergroup**

---

### Проблема: Сообщения дублируются

**Причина**: Переменные заданы, но fallback тоже срабатывает

**Решение**:

- Проверьте логи на наличие `Failed to send to group`
- Убедитесь, что Chat ID правильный
- Проверьте права бота в группе

---

## 📝 Откат изменений

Если нужно вернуться к старому поведению (broadcast всем операторам):

```bash
# В apps/telegram-bot/.env закомментируйте:
# TELEGRAM_ORDERS_CHAT_ID=-1003161915296
# TELEGRAM_SUPPORT_CHAT_ID=-1003037178274

# Перезапустите:
npm run dev
```

Код автоматически переключится на fallback режим (обратная совместимость сохранена).

---

## ✅ Критерии успеха

Реализация считается успешной, если:

1. ✅ Клиентские сообщения попадают **только** в Support Group
2. ✅ Уведомления о заказах попадают **только** в Orders Group
3. ✅ При отсутствии переменных работает broadcast (обратная совместимость)
4. ✅ При ошибке отправки в группу срабатывает автоматический fallback
5. ✅ В логах видны события роутинга и fallback
6. ✅ Нет дублирования сообщений

---

**Готово к тестированию!** 🚀

Перезапустите бота и начинайте тесты по порядку.
