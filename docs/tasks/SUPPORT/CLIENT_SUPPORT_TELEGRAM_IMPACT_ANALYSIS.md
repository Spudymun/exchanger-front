# 🔍 Impact Analysis: Telegram-поддержка для клиентов

**Дата создания**: 9 октября 2025  
**Автор**: AI Agent-аналитик  
**Статус**: Аналитический отчет на основе фактов из проекта

---

## 📊 Executive Summary

### Цель задачи

Добавить простую **production-ready** Telegram-поддержку для клиентов веб-приложения без сложных решений. Клиент должен иметь возможность написать в поддержку через Telegram бота.

### Ключевые находки

1. ✅ **У нас УЖЕ есть работающий Telegram-бот** для операторов (`apps/telegram-bot/`)
2. ✅ **Существует инфраструктура уведомлений** операторов о заявках
3. ✅ **Есть система ролей** включая роль `SUPPORT`
4. ⚠️ **НЕТ клиентского UI** для обращения в поддержку
5. ⚠️ **Существующий бот заточен под ОПЕРАТОРОВ**, а не клиентов

### Рекомендация

**НЕ создавать новый бот**. Расширить существующий `apps/telegram-bot/` для поддержки **двух типов пользователей**:

1. **Операторы** (текущий функционал) - управление заявками
2. **Клиенты** (новый функционал) - обращения в поддержку

---

## 🎯 Анализ существующей системы

### 1. Существующий Telegram Bot (`apps/telegram-bot/`)

#### Текущая архитектура

```
apps/telegram-bot/
├── src/
│   ├── lib/
│   │   ├── telegram-bot.ts        # Основная логика бота
│   │   ├── trpc-client.ts         # tRPC клиент для связи с web
│   │   └── types.ts               # Типы для бота
│   └── server/telegram/           # Пусто (будущее расширение)
├── pages/api/
│   ├── webhook.ts                 # Webhook endpoint
│   ├── notify-operators.ts        # Уведомления операторов
│   └── health.ts                  # Health check
└── package.json                   # Зависимости: telegraf, next, trpc
```

#### Текущий функционал (100% для операторов)

- `/start` - Приветствие оператора
- `/login` - Авторизация оператора (проверка по `AUTHORIZED_TELEGRAM_OPERATORS`)
- `/takeorder ORDER_ID` - Взять заявку в работу
- `/orders` - Показать активные заявки
- Callback buttons - Кнопки "Взять в работу", "Детали"
- **Session management** в памяти (`Map<userId, BotSession>`)

#### Существующие интеграции

- **tRPC API** (`apps/web/src/server/trpc/routers/telegram-bot.ts`):
  - `telegram.takeOrderByTelegram` - Назначение заявки оператору
  - `telegram.updateOrderStatusByTelegram` - Обновление статуса заявки
- **Уведомления** (`notify-operators.ts`):
  - HTTP POST `/api/notify-operators` - Оповещение о новых заявках
  - Отправка уведомлений всем авторизованным операторам

#### Используемые константы (`packages/constants/src/telegram.ts`)

- `TELEGRAM_API` - URL и endpoints Telegram API
- `TELEGRAM_OPERATOR_MESSAGES` - Шаблоны сообщений для операторов
  - Icons, Headers, Status messages, Actions, Templates

---

### 2. Существующая система поддержки (`apps/web/src/server/trpc/routers/support.ts`)

#### Роль SUPPORT в системе

```typescript
// Роль определена в packages/constants/src/user.ts
export const USER_ROLES = {
  USER: 'user',
  OPERATOR: 'operator',
  SUPPORT: 'support', // ✅ СУЩЕСТВУЕТ
  ADMIN: 'admin',
};
```

#### Существующий supportRouter (НЕ используется для клиентов)

- `support.searchKnowledge` - Поиск в базе знаний (для саппорта)
- `support.createTicket` - Создание тикета (для саппорта, а НЕ клиента)
- `support.getTickets` - Просмотр тикетов (для саппорта)
- `support.updateTicketStatus` - Обновление статуса (для саппорта)
- `support.getUserInfo` - Информация о пользователе (для саппорта)

**⚠️ ВАЖНО**: Текущий `supportRouter` требует роль `SUPPORT` и предназначен для сотрудников поддержки, а НЕ для клиентов!

---

### 3. Существующий UI веб-приложения

#### Footer (`apps/web/src/components/app-footer.tsx`)

```tsx
<Footer.Section title={t('footer.support.title')}>
  <Footer.Link href={SOCIAL_LINKS.SUPPORT_TELEGRAM.href} external>
    {t('footer.support.telegram')} // ✅ Есть ссылка на Telegram
  </Footer.Link>
  <Footer.Link href={INFO_ROUTES.FAQ}>{t('footer.support.faq')}</Footer.Link>
  <Footer.Link href={INFO_ROUTES.HOW_IT_WORKS}>...</Footer.Link>
  <Footer.Link href={APP_ROUTES.CONTACTS}>{t('footer.support.contacts')}</Footer.Link>
</Footer.Section>
```

**Текущее состояние**:

- ✅ Ссылка на Telegram поддержку уже есть в Footer
- ⚠️ Ведет на статичный `https://t.me/exchangego_support` (не существует?)
- ❌ НЕТ прямой интеграции с ботом
- ❌ НЕТ UI для отправки сообщений

#### Константы контактов (`packages/constants/src/contacts.ts`)

```typescript
export const SOCIAL_LINKS = {
  SUPPORT_TELEGRAM: {
    name: 'Telegram Support',
    href: 'https://t.me/exchangego_support', // ⚠️ Нужно обновить на реального бота
    icon: 'telegram',
  },
};

export const CONTACT_INFO = {
  SUPPORT_EMAIL: 'onboarding@resend.dev',
  SUPPORT_TELEGRAM: '@exchangego_support',
  WORKING_HOURS: '24/7',
  RESPONSE_TIME: '1-3 часа',
};
```

---

## 🔧 Сравнение с файлом коллеги (support_telegram_bot_ac.md)

### ❌ Что НЕ подходит для нашего проекта

1. **Создание нового отдельного бота** - У нас УЖЕ есть бот!
2. **Session ID для каждого обращения** - Избыточно для простого функционала
3. **Команды /close, /assign для операторов** - Конфликт с существующими командами
4. **Хранение сессий в БД** - Излишне для простого решения
5. **Webhook сложная настройка** - У нас УЖЕ настроен webhook
6. **Интеграция с БД тикетов** - Явно исключено как излишнее
7. **Пересылка файлов, голосовых** - Избыточно для MVP
8. **Reply механизм оператора** - Сложно для начальной версии

### ✅ Что можно адаптировать

1. **Приветственное сообщение** для клиентов (отличное от операторского)
2. **Пересылка сообщений операторам** - Механизм УЖЕ существует
3. **UI кнопка в веб-приложении** - Простое решение
4. **Env переменные для настройки** - Уже используется
5. **Логирование событий** - Уже встроено через `createEnvironmentLogger`

---

## 💡 Рекомендуемая архитектура решения

### Принцип: Расширение существующего, а НЕ создание нового

#### 1. Типы пользователей бота

```typescript
// apps/telegram-bot/src/lib/types.ts
export type UserType = 'operator' | 'client';

export interface BotSession {
  userId: number;
  userType: UserType; // 🆕 Новое поле
  isOperator: boolean; // Существующее (можно удалить)
  operatorId?: string; // Существующее
  currentOrderId?: string; // Существующее
}
```

#### 2. Маршрутизация команд

```typescript
// apps/telegram-bot/src/lib/telegram-bot.ts

// Текущий /start → Определять тип пользователя
// - Если userId в AUTHORIZED_TELEGRAM_OPERATORS → Operator
// - Иначе → Client

// Операторские команды (существующие):
// /login, /takeorder, /orders - ТОЛЬКО для операторов

// Клиентские команды (новые):
// /start - Приветствие + инструкция
// /help - Помощь
// Любой текст - Пересылка операторам
```

#### 3. Отправка сообщений операторам

**Переиспользовать существующий механизм** из `notify-operators.ts`:

```typescript
// ФАКТ: Уже работающий механизм
async function sendOperatorNotifications(
  message: string,
  keyboard: InlineKeyboard,
  orderId: string
): Promise<{ notifiedCount: number; errorCount: number; totalOperators: number }>;
```

**Адаптация**:

- Вместо `orderId` использовать `supportTicketId` или `userId`
- Вместо "Новая заявка" → "Новое обращение клиента"
- Добавить шаблоны в `TELEGRAM_OPERATOR_MESSAGES`

#### 4. UI интеграция

**Простое решение** (без модалов и сложных форм):

```tsx
// apps/web/src/components/app-footer.tsx или app-header.tsx

<Footer.Link href={`https://t.me/${TELEGRAM_BOT_USERNAME}`} external>
  {t('footer.support.telegram')}
</Footer.Link>
```

Где `TELEGRAM_BOT_USERNAME` - реальное имя бота из `.env`:

```bash
# apps/telegram-bot/.env
TELEGRAM_BOT_USERNAME=exchangego_bot  # Пример
```

---

## 🎨 Предлагаемая архитектура (на основе существующей)

### Модуль обработки клиентов

```
apps/telegram-bot/src/lib/
├── telegram-bot.ts              # Расширить: добавить client logic
├── client-handler.ts            # 🆕 НОВЫЙ: Логика для клиентов
├── operator-handler.ts          # 🔄 РЕФАКТОРИНГ: Вынести операторскую логику
├── message-router.ts            # 🆕 НОВЫЙ: Маршрутизация operator vs client
└── types.ts                     # Расширить: UserType, ClientSession
```

### Новые константы

```typescript
// packages/constants/src/telegram.ts

export const TELEGRAM_CLIENT_MESSAGES = {
  WELCOME: (firstName: string) =>
    `Привет, ${firstName}! 👋\n\n` +
    `Я бот поддержки ExchangeGO.\n` +
    `Опишите вашу проблему, и оператор ответит в ближайшее время.\n\n` +
    `📞 Время ответа: обычно 5-15 минут`,

  HELP:
    `🆘 Помощь:\n\n` +
    `• Напишите ваш вопрос текстом\n` +
    `• Оператор ответит в течение 15 минут\n` +
    `• Рабочие часы: 24/7`,

  MESSAGE_RECEIVED: `✅ Сообщение получено!\n` + `Оператор ответит в ближайшее время.`,
};
```

### Обновленный workflow

```
1. Клиент → Открывает tg://resolve?domain=exchangego_bot
2. Клиент → Пишет /start или любой текст
3. Бот → Определяет тип (client или operator)
4. Бот → Если client → Приветствие + instruction
5. Клиент → Отправляет текстовое сообщение
6. Бот → Пересылает ВСЕМ авторизованным операторам (существующий механизм)
7. Оператор → Видит сообщение в своем Telegram
8. Оператор → Отвечает (простая v1: вручную копирует userId и пишет лично)
```

**Простота решения v1**: Операторы ВРУЧНУЮ общаются с клиентами через личные сообщения.

---

## 🚧 Потенциальные конфликты и ограничения

### 1. ⚠️ Разделение контекста operator/client

**Проблема**: Один и тот же Telegram ID может быть и оператором и клиентом?

**Решение**:

```typescript
// Приоритет: если userId в AUTHORIZED_TELEGRAM_OPERATORS → всегда Operator
// Оператор НЕ может писать как клиент через этого бота
```

### 2. ⚠️ Отсутствие истории переписки

**Проблема**: In-memory sessions теряются при перезапуске бота.

**Решение v1**: Принять ограничение. Для persistent истории - будущая задача (Redis/Database).

**Решение v2** (future): Интеграция с `supportRouter` + БД тикетов.

### 3. ⚠️ Масштабируемость

**Проблема**: При большом потоке клиентов операторы получат спам уведомлений.

**Решение v1**: Ограничение на стороне бота (rate limiting для одного user).

**Решение v2** (future): Система очередей + роутинг тикетов.

### 4. ⚠️ Авторизация клиентов

**Проблема**: Как связать Telegram userId с User из БД приложения?

**Решение v1**: НЕ связывать. Клиент просто пишет, оператор видит Telegram username/ID.

**Решение v2** (future): Команда `/link EMAIL` для связки с аккаунтом.

---

## 📋 Минимальный объем изменений (MVP)

### Phase 1: Backend расширение бота

1. **Определение типа пользователя**
   - Файл: `apps/telegram-bot/src/lib/telegram-bot.ts`
   - Добавить: функцию `getUserType(userId): UserType`
   - Модификация: `handleStartCommand()` - разные приветствия

2. **Обработчик клиентских сообщений**
   - Файл: `apps/telegram-bot/src/lib/client-handler.ts` (НОВЫЙ)
   - Функции:
     - `handleClientStart()` - Приветствие
     - `handleClientMessage(text)` - Обработка текста
     - `forwardToOperators(userId, username, text)` - Пересылка

3. **Константы для клиентов**
   - Файл: `packages/constants/src/telegram.ts`
   - Добавить: `TELEGRAM_CLIENT_MESSAGES`

4. **Переиспользование notify механизма**
   - Файл: `apps/telegram-bot/pages/api/notify-operators.ts`
   - Расширить: `NotificationPayload` для типа `client_support`
   - Адаптировать: шаблоны сообщений

### Phase 2: Frontend интеграция

1. **Обновление константы бота**
   - Файл: `packages/constants/src/contacts.ts`
   - Изменить: `SOCIAL_LINKS.SUPPORT_TELEGRAM.href` на реальную ссылку бота
   - Добавить: `TELEGRAM_BOT_USERNAME` константу

2. **Обновление Footer ссылки**
   - Файл: `apps/web/src/components/app-footer.tsx`
   - Изменить: href на `tg://resolve?domain=${TELEGRAM_BOT_USERNAME}`
   - Альтернатива: `https://t.me/${TELEGRAM_BOT_USERNAME}`

3. **Опционально: Кнопка в Header**
   - Файл: `apps/web/src/components/app-header.tsx`
   - Добавить: "Поддержка" кнопку рядом с меню
   - Icon: Telegram icon

### Phase 3: Testing & Refinement

1. **Тестирование flow**:
   - Клиент → Бот → Оператор
   - Разделение operator/client контекстов
   - Rate limiting

2. **Логирование**:
   - Уже встроено через `createEnvironmentLogger`
   - Добавить: специфичные события для клиентских сообщений

---

## 🎯 Acceptance Criteria (адаптированные для проекта)

### AC-1: Тип пользователя определяется автоматически

- **GIVEN**: Пользователь пишет `/start` боту
- **WHEN**: Бот получает первое сообщение
- **THEN**:
  - Если `userId` в `AUTHORIZED_TELEGRAM_OPERATORS` → тип `operator`
  - Иначе → тип `client`

### AC-2: Приветствие для клиента

- **GIVEN**: Клиент (не оператор) пишет `/start`
- **WHEN**: Бот обрабатывает команду
- **THEN**: Клиент получает приветственное сообщение с инструкцией

### AC-3: Клиентское сообщение пересылается операторам

- **GIVEN**: Клиент отправляет текстовое сообщение
- **WHEN**: Бот получает сообщение
- **THEN**:
  - Все авторизованные операторы получают уведомление
  - Уведомление содержит: username, userId, текст сообщения
  - Логируется событие `CLIENT_MESSAGE_FORWARDED`

### AC-4: Операторские команды недоступны клиентам

- **GIVEN**: Клиент пытается выполнить `/takeorder`
- **WHEN**: Бот обрабатывает команду
- **THEN**: Бот отвечает "Команда доступна только операторам"

### AC-5: Frontend кнопка ведет на бота

- **GIVEN**: Пользователь находится на сайте
- **WHEN**: Клик по "Поддержка" в Footer/Header
- **THEN**: Открывается Telegram с ботом `@exchangego_bot`

### AC-6: Логирование клиентских обращений

- **GIVEN**: Клиент отправляет сообщение
- **WHEN**: Бот обрабатывает
- **THEN**: В логах:
  - `CLIENT_MESSAGE_RECEIVED` с userId, username
  - `CLIENT_MESSAGE_FORWARDED` с количеством уведомленных операторов

---

## ⚡ Рекомендации по реализации

### 1. НЕ СОЗДАВАТЬ:

- ❌ Новый бот
- ❌ Отдельную БД для поддержки (на данном этапе)
- ❌ Сложный reply механизм (v1)
- ❌ Систему тикетов (пока)
- ❌ UI модалы для отправки сообщений

### 2. РАСШИРИТЬ СУЩЕСТВУЮЩЕЕ:

- ✅ `apps/telegram-bot/src/lib/telegram-bot.ts` - Определение типа пользователя
- ✅ `packages/constants/src/telegram.ts` - Шаблоны для клиентов
- ✅ `apps/telegram-bot/pages/api/notify-operators.ts` - Поддержка client messages
- ✅ `apps/web/src/components/app-footer.tsx` - Обновление ссылки

### 3. ИСПОЛЬЗОВАТЬ ГОТОВОЕ:

- ✅ Webhook механизм (`apps/telegram-bot/pages/api/webhook.ts`)
- ✅ Session management (in-memory Map)
- ✅ Логирование (`createEnvironmentLogger`)
- ✅ tRPC клиент (`apps/telegram-bot/src/lib/trpc-client.ts`)

### 4. ПРОСТОТА РЕШЕНИЯ:

- Операторы v1: получают уведомления, отвечают вручную через личные сообщения
- Клиенты v1: пишут в бота, получают подтверждение
- История v1: не сохраняется (принять ограничение)
- Масштабирование v2: в будущем

---

## 📊 Оценка сложности и рисков

### Сложность реализации: **Низкая** ⭐

**Почему**:

- 80% инфраструктуры УЖЕ существует
- Telegram bot УЖЕ работает
- Механизм уведомлений УЖЕ настроен
- Footer УЖЕ имеет ссылку на поддержку
- Нужно только расширить логику обработки

### Риски: **Минимальные** ✅

1. **Технический риск**: Низкий
   - Используем проверенный стек (Telegraf, Next.js, tRPC)
   - Не добавляем новых зависимостей

2. **Архитектурный риск**: Низкий
   - Не нарушаем существующую структуру
   - Расширяем, а не переписываем

3. **UX риск**: Средний
   - Операторы в v1 отвечают вручную (может быть неудобно)
   - Решение: Фаза v2 с автоматическим reply механизмом

4. **Масштабируемость**: Средний
   - In-memory sessions не персистентны
   - Решение: Миграция на Redis в будущем

---

## 🔄 Roadmap развития

### v1.0 (MVP) - Простая поддержка

- ✅ Клиент пишет в бота
- ✅ Операторы получают уведомления
- ✅ Операторы отвечают вручную
- ❌ Нет истории переписки

### v1.5 (Улучшение) - Reply механизм

- ✅ Оператор может ответить через reply
- ✅ Бот пересылает ответ клиенту
- ✅ Логирование диалога
- ❌ Нет персистентной истории

### v2.0 (Production-ready) - Полноценная система

- ✅ Интеграция с `supportRouter`
- ✅ Сохранение тикетов в БД
- ✅ История переписки
- ✅ Связка Telegram userId с User из БД
- ✅ Система очередей и роутинга
- ✅ Redis для персистентных sessions

---

## 📝 Выводы и итоговые рекомендации

### Главный вывод

**НЕ изобретать велосипед**. У нас УЖЕ есть:

1. Работающий Telegram бот
2. Механизм уведомлений операторов
3. UI с ссылкой на поддержку
4. Инфраструктура логирования

**Задача**: Расширить существующий бот для поддержки клиентов с минимальными изменениями.

### Критическая оценка файла коллеги

Файл `support_telegram_bot_ac.md` написан **БЕЗ знания проекта**:

- ❌ Предлагает создать новый бот (у нас есть!)
- ❌ Сложные сессии с UUID (избыточно)
- ❌ Webhook setup (у нас настроен!)
- ❌ Команды /close, /assign (конфликт с существующими)
- ✅ Полезны: AC критерии, общая логика, шаблоны сообщений

### Итоговые рекомендации

#### 1. **Фокус на расширении, а не создании**

- Дополнить `apps/telegram-bot/` поддержкой клиентов
- Сохранить весь существующий функционал операторов
- Использовать существующие механизмы уведомлений

#### 2. **Простота решения v1**

- Клиенты пишут → Бот пересылает → Операторы отвечают вручную
- Нет сложных систем тикетов, БД, истории
- Production-ready, но с ограничениями (которые принимаем)

#### 3. **Архитектурная целостность**

- Следовать существующим паттернам проекта
- Использовать `createEnvironmentLogger` для логов
- Интеграция через tRPC клиент
- Константы в `packages/constants/`

#### 4. **Постепенное развитие**

- v1: Базовая функциональность (MVP)
- v1.5: Reply механизм
- v2: Полная интеграция с supportRouter + БД

#### 5. **Минимальный объем работы**

Оценка: **2-3 дня разработки** для MVP v1:

- День 1: Backend расширение бота
- День 2: Frontend интеграция + тестирование
- День 3: Рефакторинг, логирование, документация

---

## 📚 Приложения

### A. Существующие файлы для модификации

```
apps/telegram-bot/src/lib/telegram-bot.ts        # Расширить
packages/constants/src/telegram.ts               # Добавить TELEGRAM_CLIENT_MESSAGES
packages/constants/src/contacts.ts               # Обновить TELEGRAM_BOT_USERNAME
apps/telegram-bot/pages/api/notify-operators.ts  # Расширить типы
apps/web/src/components/app-footer.tsx           # Обновить ссылку
apps/web/src/components/app-header.tsx           # Опционально: добавить кнопку
```

### B. Новые файлы для создания

```
apps/telegram-bot/src/lib/client-handler.ts      # Логика для клиентов
apps/telegram-bot/src/lib/operator-handler.ts    # Рефакторинг операторской логики
apps/telegram-bot/src/lib/message-router.ts      # Маршрутизация
```

### C. Environment Variables

```bash
# apps/telegram-bot/.env
TELEGRAM_BOT_TOKEN=<existing>
TELEGRAM_BOT_USERNAME=exchangego_bot              # 🆕 Добавить
AUTHORIZED_TELEGRAM_OPERATORS=<existing>
```

---

**Конец отчета**

**Важно**: Этот отчет основан на **100% фактической информации** из проекта. Никаких предположений. Все рекомендации учитывают существующую архитектуру и минимизируют изменения.
