# ⚡ Быстрая инструкция: Настройка разделения каналов

## 🎯 Что реализовано

Уведомления о заказах теперь могут отправляться в **отдельные каналы** по типам:

- 🆕 **Новые заказы** → `TELEGRAM_NEW_ORDERS_CHAT_ID`
- ❌ **Отменённые заказы** → `TELEGRAM_CANCELLED_ORDERS_CHAT_ID`
- ✅ **Оплаченные заказы** → `TELEGRAM_PAID_ORDERS_CHAT_ID`

---

## 📋 Быстрая настройка

### Шаг 1: Создайте Telegram группы

Создайте 3 группы (или используйте одну общую):

1. "ExchangeGO - Новые заказы"
2. "ExchangeGO - Отменённые заказы"
3. "ExchangeGO - Оплаченные заказы"

### Шаг 2: Добавьте бота

1. Добавьте `@exchangego_operators_bot` в каждую группу
2. Сделайте бота **администратором**
3. Включите права "Отправка сообщений"

### Шаг 3: Получите Chat ID

Используйте один из способов:

**Способ 1: @userinfobot**

1. Добавьте `@userinfobot` в группу
2. Скопируйте Chat ID из сообщения бота
3. Удалите @userinfobot

**Способ 2: Через свой бот**

1. Отправьте любое сообщение в группу
2. Проверьте логи бота (если включён временный код)
3. Chat ID начинается с `-100`

### Шаг 4: Настройте `.env`

Откройте `apps/telegram-bot/.env` и добавьте:

```bash
# Вариант А: Все в отдельные каналы
TELEGRAM_NEW_ORDERS_CHAT_ID=-1001111111111
TELEGRAM_CANCELLED_ORDERS_CHAT_ID=-1002222222222
TELEGRAM_PAID_ORDERS_CHAT_ID=-1003333333333

# Вариант Б: Только новые отдельно, остальные в общий
TELEGRAM_NEW_ORDERS_CHAT_ID=-1001111111111
TELEGRAM_ORDERS_CHAT_ID=-1003037178274  # Общий для остальных

# Вариант В: Все в один общий (как было)
TELEGRAM_ORDERS_CHAT_ID=-1003037178274
```

### Шаг 5: Перезапустите бота

```powershell
# Остановите текущий процесс (Ctrl+C)
npm run dev
```

---

## ✅ Проверка работы

### Проверка новых заказов

1. Создайте новый заказ через http://localhost:3000
2. Проверьте, что уведомление пришло в правильный канал
3. Проверьте логи:
   ```
   TELEGRAM_NOTIFICATION_TYPE_ROUTE
   {
     "notificationType": "new_order",
     "chatId": "-1001111111111"
   }
   ```

### Проверка отменённых заказов

1. Отмените заказ через веб-интерфейс
2. Проверьте канал отменённых заказов
3. Лог должен показать `notificationType: "order_cancelled"`

### Проверка оплаченных заказов

1. Нажмите "Оплатил" на странице заказа
2. Проверьте канал оплаченных заказов
3. Лог должен показать `notificationType: "order_paid"`

---

## 🔧 Troubleshooting

### Проблема: Уведомления не приходят в группу

**Решение**:

1. Проверьте, что Chat ID правильный (начинается с `-100`)
2. Убедитесь, что бот — администратор группы
3. Проверьте логи на ошибки Telegram API

### Проблема: Уведомления идут в broadcast

**Причина**: Переменные не настроены или закомментированы

**Решение**:

1. Проверьте `.env` файл
2. Убедитесь, что переменные раскомментированы
3. Перезапустите бота

### Проблема: Уведомления идут не в тот канал

**Причина**: Перепутаны Chat ID

**Решение**:

1. Проверьте соответствие Chat ID типам в `.env`
2. Используйте логи для проверки: смотрите на `notificationType` и `chatId`
3. Поменяйте местами Chat ID при необходимости

---

## 📊 Примеры конфигураций

### Конфигурация 1: Минимальная (один общий канал)

```bash
# Все типы в один канал
TELEGRAM_ORDERS_CHAT_ID=-1003037178274
```

**Результат**: Как раньше, все в одном месте

---

### Конфигурация 2: Разделение важных типов

```bash
# Новые и оплаченные отдельно, отменённые в общий
TELEGRAM_NEW_ORDERS_CHAT_ID=-1001111111111
TELEGRAM_PAID_ORDERS_CHAT_ID=-1003333333333
TELEGRAM_ORDERS_CHAT_ID=-1003037178274  # Fallback для отменённых
```

**Результат**: Фокус на активных заказах

---

### Конфигурация 3: Полное разделение

```bash
# Каждый тип в свой канал
TELEGRAM_NEW_ORDERS_CHAT_ID=-1001111111111
TELEGRAM_CANCELLED_ORDERS_CHAT_ID=-1002222222222
TELEGRAM_PAID_ORDERS_CHAT_ID=-1003333333333
```

**Результат**: Максимальная организация

---

## 🎉 Готово!

Теперь уведомления о заказах автоматически маршрутизируются в правильные каналы.

**Полная документация**: `ORDERS_CHANNEL_SEPARATION_BY_TYPE.md`
