# План реализации: Предупреждение о проверке адреса на странице заказа

**Дата создания:** 4 октября 2025  
**Автор:** AI Agent (Refactoring & Patterns)  
**Задача:** Добавить предупреждающий текст рядом с адресом кошелька о необходимости сверки адреса с email

---

## 📋 Анализ существующей архитектуры

### 1. Текущая структура страницы заказа

**Путь к компонентам:**

```
apps/web/app/[locale]/order/[orderId]/
  ├── page.tsx                    # Server component с metadata
  └── OrderPageClient.tsx         # Client component с OrderStatus

packages/ui/src/components/order/
  ├── OrderStatus.tsx            # Главный компонент отображения заказа
  └── helpers/
      └── OrderStatusHelpers.tsx # Вспомогательные компоненты (OrderCryptoInfo, DepositAddressBlock)
```

**Текущий flow:**

1. `page.tsx` → валидация orderId → рендер `OrderPageClient`
2. `OrderPageClient` → использует `OrderStatus` из `@repo/ui`
3. `OrderStatus` → рендерит `OrderStatusDetails` → внутри `OrderCryptoInfo`
4. `OrderCryptoInfo` → отображает `DepositAddressBlock` с адресом кошелька

### 2. Система локализации

**Используется:** `next-intl` с модульной структурой

**Файлы переводов:**

```
apps/web/messages/
  ├── en/
  │   └── order-page.json        # Английские переводы для OrderStatus
  └── ru/
      └── order-page.json        # Русские переводы для OrderStatus
```

**Паттерн использования:**

```tsx
const t = useTranslations('OrderStatus');
const text = t('depositAddress'); // "Адрес депозита" или "Deposit address"
```

### 3. Существующие UI компоненты

**Notification компонент** (`packages/ui/src/components/ui/notification.tsx`):

- ✅ Поддерживает варианты: `default`, `success`, `error`, `warning`, `info`
- ✅ Имеет иконки из `lucide-react` (AlertTriangle для warning)
- ✅ Поддерживает `title` и `description` props
- ✅ Использует дизайн-систему проекта (border-warning, bg-warning/10, text-warning)

**Существующие стили:**

```tsx
// Из shared-styles.ts
statusStyles = {
  warning: 'text-warning bg-warning/10',
};
```

### 4. Email конфигурация

**Путь:** `packages/constants/src/email-config.ts`

**Текущая структура:**

```typescript
export const EMAIL_SENDING_CONFIG = {
  GLOBAL_ENABLED: {
    development: false,
    staging: true,
    production: true,
  },
  BY_TYPE: {
    CRYPTO_ADDRESS: { development: true, staging: true, production: true },
    ORDER_STATUS: { ... },
    // и т.д.
  }
}
```

---

## 🎯 Цель реализации

**Добавить предупреждающий блок** рядом с адресом кошелька на странице заказа со следующим текстом:

> **RU:** "Просим сверять адрес оплаты который мы генерируем для оплаты заявки, с тем что отправляется на указанную в заявке почту. Проверять почту рекомендуем с другого устройства (смартфона)."
>
> **EN:** "Please verify the payment address we generate for the order with the one sent to the email specified in the order. We recommend checking your email from another device (smartphone)."

**Требования:**

1. ✅ Использовать существующий `Notification` компонент
2. ✅ Следовать паттернам локализации проекта
3. ✅ Не нарушать выравнивание и layout
4. ✅ Видимость должна управляться через конфигурацию в `email-config.ts`
5. ✅ Минимальные изменения в существующем коде (агент-рефакторинг подход)

---

## 📐 Архитектурное решение

### Принцип: Встроить как пазл в существующую структуру

**Не создавать новую архитектуру**, а использовать существующую:

- ✅ Использовать `Notification` компонент (уже есть в UI библиотеке)
- ✅ Добавить конфигурацию в `email-config.ts` (паттерн уже есть)
- ✅ Расширить переводы в `order-page.json` (паттерн уже есть)
- ✅ Интегрировать в `OrderCryptoInfo` (модификация, не переписывание)

---

## 🔧 Детальный план реализации

### ЭТАП 1: Расширение email-config.ts

**Файл:** `packages/constants/src/email-config.ts`

**Что добавить:**

```typescript
/**
 * UI Configuration для email-связанных компонентов
 */
export const EMAIL_UI_CONFIG = {
  /**
   * Показывать ли предупреждение о проверке адреса на странице заказа
   */
  SHOW_ADDRESS_VERIFICATION_WARNING: {
    development: true,
    staging: true,
    production: true,
    description: 'Предупреждение о необходимости сверки адреса из email с адресом на сайте',
  },
} as const;

export type EmailUIConfig = typeof EMAIL_UI_CONFIG;
```

**Обоснование:**

- Следует паттерну `EMAIL_SENDING_CONFIG.BY_TYPE` с описанием
- Позволяет гибко управлять отображением по окружениям
- Централизованное место для всех email-связанных конфигов

### ЭТАП 2: Добавление переводов

**Файл 1:** `apps/web/messages/en/order-page.json`

```json
{
  "OrderStatus": {
    // ... существующие ключи
    "addressVerificationWarning": {
      "title": "⚠️ Important: Verify Payment Address",
      "description": "Please verify the payment address we generate for the order with the one sent to the email specified in the order. We recommend checking your email from another device (smartphone)."
    }
  }
}
```

**Файл 2:** `apps/web/messages/ru/order-page.json`

```json
{
  "OrderStatus": {
    // ... существующие ключи
    "addressVerificationWarning": {
      "title": "⚠️ Важно: Проверьте адрес оплаты",
      "description": "Просим сверять адрес оплаты который мы генерируем для оплаты заявки, с тем что отправляется на указанную в заявке почту. Проверять почту рекомендуем с другого устройства (смартфона)."
    }
  }
}
```

**Обоснование:**

- Следует существующей структуре с `title` и `description`
- Использует вложенный объект для группировки связанных текстов
- Иконка ⚠️ в title для визуальной важности

### ЭТАП 3: Создание компонента AddressVerificationWarning

**Файл:** `packages/ui/src/components/order/helpers/AddressVerificationWarning.tsx`

````tsx
'use client';

import { EMAIL_UI_CONFIG } from '@repo/constants';
import { Notification } from '@repo/ui';
import { useTranslations } from 'next-intl';

/**
 * Определение текущего окружения
 * Внутренняя функция для определения окружения из NODE_ENV
 */
function getEnvironment(): 'development' | 'staging' | 'production' {
  const nodeEnv = process.env.NODE_ENV;

  if (nodeEnv === 'production') return 'production';
  if (nodeEnv === 'development') return 'development';

  // По умолчанию staging для тестирования
  return 'staging';
}

/**
 * Компонент предупреждения о необходимости проверки адреса
 * Отображается на странице заказа рядом с deposit address
 *
 * Использует EMAIL_UI_CONFIG для определения видимости в зависимости от окружения.
 * В production всегда показывается (согласно конфигу).
 *
 * @component
 * @example
 * ```tsx
 * <AddressVerificationWarning />
 * ```
 */
export function AddressVerificationWarning() {
  const t = useTranslations('OrderStatus');

  // Автоматическое определение окружения
  const environment = getEnvironment();

  // Проверяем конфигурацию для текущего окружения
  const shouldShow = EMAIL_UI_CONFIG.SHOW_ADDRESS_VERIFICATION_WARNING[environment];

  if (!shouldShow) {
    return null;
  }

  return (
    <Notification
      variant="warning"
      title={t('addressVerificationWarning.title')}
      description={t('addressVerificationWarning.description')}
      showIcon={true}
      closable={false}
      className="border-2"
    />
  );
}
````

**Обоснование:**

- ✅ Использует существующий `Notification` компонент
- ✅ Следует паттерну локализации с `useTranslations`
- ✅ Конфигурируемое поведение через `environment` prop
- ✅ Возвращает `null` если не должен отображаться (React-паттерн)
- ✅ JSDoc комментарии для документации

### ЭТАП 4: Экспорт нового компонента

**Файл:** `packages/ui/src/components/order/helpers/OrderStatusHelpers.tsx`

**Добавить в конец файла:**

```tsx
export { AddressVerificationWarning } from './AddressVerificationWarning';
```

**Альтернативный вариант** (если хотим экспортировать из главного index):

**Файл:** `packages/ui/src/components/index.ts`

**Найти секцию order компонентов и добавить:**

```typescript
// Order components
export { OrderStatus } from './order/OrderStatus';
export { OrderDevTools } from './order/OrderDevTools';
export { AddressVerificationWarning } from './order/helpers/AddressVerificationWarning'; // ← НОВОЕ
```

### ЭТАП 5: Интеграция в OrderCryptoInfo

**Файл:** `packages/ui/src/components/order/helpers/OrderStatusHelpers.tsx`

**Текущий код OrderCryptoInfo:**

```tsx
export function OrderCryptoInfo({
  orderData,
  userEmail,
  t,
}: {
  orderData: OrderWithUIData;
  userEmail?: string;
  t: ReturnType<typeof useTranslations>;
}) {
  return (
    <div className="space-y-4">
      {/* ИСПРАВЛЕНО: Используем переиспользуемый компонент вместо дублированного кода */}
      <DepositAddressBlock depositAddress={orderData.depositAddress} t={t} />

      {/* Email и Blockchain Network на одном уровне */}
      <div className="flex flex-col gap-4 sm:flex-row sm:gap-6">{/* ... */}</div>
    </div>
  );
}
```

**Модифицированный код:**

```tsx
import { AddressVerificationWarning } from './AddressVerificationWarning'; // ← НОВЫЙ ИМПОРТ

export function OrderCryptoInfo({
  orderData,
  userEmail,
  t,
}: {
  orderData: OrderWithUIData;
  userEmail?: string;
  t: ReturnType<typeof useTranslations>;
}) {
  return (
    <div className="space-y-4">
      {/* Deposit address с желтым фоном */}
      <DepositAddressBlock depositAddress={orderData.depositAddress} t={t} />

      {/* ✅ НОВОЕ: Предупреждение о проверке адреса 
          Компонент сам определяет окружение через NODE_ENV внутри */}
      <AddressVerificationWarning />

      {/* Email и Blockchain Network на одном уровне */}
      <div className="flex flex-col gap-4 sm:flex-row sm:gap-6">
        {/* Email - слева (всегда есть) */}
        <div className="flex-1">
          <p className={textStyles.heading.sm}>{t('email')}</p>
          <p className={textStyles.body.md}>{userEmail || 'unknown@unknown.com'}</p>
        </div>

        {/* Blockchain Network - справа (опционально для USDT) */}
        {orderData.currency === 'USDT' && (
          <div className="flex-1">
            <NetworkDisplay
              tokenStandard={
                (orderData.tokenStandard || DEFAULT_TOKEN_STANDARDS.USDT) as
                  | 'ERC-20'
                  | 'TRC-20'
                  | 'BEP-20'
              }
              showCopy={true}
              showDetails={true}
              t={t}
            />
          </div>
        )}
      </div>
    </div>
  );
}
```

**Обоснование изменений:**

- ✅ Предупреждение вставлено между `DepositAddressBlock` и блоком Email/Network
- ✅ Используется `space-y-4` родителя для правильного spacing
- ✅ Компонент сам определяет окружение через внутреннюю функцию `getEnvironment()`
- ✅ Минимальные изменения - только добавление, без переписывания
- ✅ Не требует передачи props - чистый API

### ЭТАП 6: Обновление типов констант (если нужно)

**Файл:** `packages/constants/src/email-config.ts`

**Добавить экспорт:**

```typescript
// В конец файла после EMAIL_LOG_MESSAGES

/**
 * UI Configuration для email-связанных компонентов
 */
export const EMAIL_UI_CONFIG = {
  /**
   * Показывать ли предупреждение о проверке адреса на странице заказа
   */
  SHOW_ADDRESS_VERIFICATION_WARNING: {
    development: true,
    staging: true,
    production: true,
    description: 'Предупреждение о необходимости сверки адреса из email с адресом на сайте',
  },
} as const;

export type EmailUIConfig = typeof EMAIL_UI_CONFIG;
export type EmailUIEnvironment = keyof typeof EMAIL_UI_CONFIG.SHOW_ADDRESS_VERIFICATION_WARNING;
```

---

## 🎨 Визуальный результат

### До изменений:

```
┌─────────────────────────────────────┐
│ ⚠️ Deposit address                  │
│ ┌─────────────────────────────────┐ │
│ │ TXYZabcd123456789...            │ │
│ │                          [Copy] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ Email              | Network        │
│ user@example.com   | TRC-20         │
└─────────────────────────────────────┘
```

### После изменений:

```
┌─────────────────────────────────────┐
│ ⚠️ Deposit address                  │
│ ┌─────────────────────────────────┐ │
│ │ TXYZabcd123456789...            │ │
│ │                          [Copy] │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ⚠️ Important: Verify Payment Address│ ← НОВОЕ
│ ┌─────────────────────────────────┐ │
│ │ Please verify the payment addr- │ │
│ │ ess we generate with the one    │ │
│ │ sent to your email. Check from  │ │
│ │ another device (smartphone).    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ Email              | Network        │
│ user@example.com   | TRC-20         │
└─────────────────────────────────────┘
```

---

## ✅ Чеклист выполнения

### Этап 1: Конфигурация

- [ ] Добавить `EMAIL_UI_CONFIG` в `packages/constants/src/email-config.ts`
- [ ] Добавить типы `EmailUIConfig` и `EmailUIEnvironment`
- [ ] Экспортировать новые константы

### Этап 2: Локализация

- [ ] Добавить `addressVerificationWarning` в `apps/web/messages/en/order-page.json`
- [ ] Добавить `addressVerificationWarning` в `apps/web/messages/ru/order-page.json`
- [ ] Проверить корректность JSON синтаксиса

### Этап 3: Компонент

- [ ] Создать `AddressVerificationWarning.tsx` в `packages/ui/src/components/order/helpers/`
- [ ] Добавить JSDoc комментарии
- [ ] Импортировать необходимые зависимости
- [ ] Реализовать условный рендеринг через конфиг

### Этап 4: Интеграция

- [ ] Импортировать `AddressVerificationWarning` в `OrderStatusHelpers.tsx`
- [ ] Добавить компонент в `OrderCryptoInfo` после `DepositAddressBlock`
- [ ] Передать правильный `environment` prop

### Этап 5: Экспорт

- [ ] Экспортировать `AddressVerificationWarning` из `OrderStatusHelpers.tsx`
- [ ] (Опционально) Добавить экспорт в `packages/ui/src/components/index.ts`

### Этап 6: Тестирование

- [ ] Проверить отображение в development
- [ ] Проверить отображение в production
- [ ] Проверить переводы (EN/RU)
- [ ] Проверить что layout не сломался
- [ ] Проверить адаптивность (мобильные устройства)
- [ ] Проверить работу конфига (выключение через `EMAIL_UI_CONFIG`)

---

## 🔍 Точки контроля качества

### 1. Соответствие архитектуре проекта

- ✅ Используются существующие компоненты из UI библиотеки
- ✅ Следует паттернам локализации (next-intl)
- ✅ Конфигурация в централизованном месте (constants)
- ✅ Компонент в правильном пакете (packages/ui/order/helpers)

### 2. Минимальность изменений

- ✅ Не переписываются существующие компоненты
- ✅ Только добавление нового функционала
- ✅ Один новый файл + несколько строк в существующих

### 3. Типобезопасность

- ✅ TypeScript типы для всех новых entity
- ✅ Правильные props interfaces
- ✅ Использование существующих типов из проекта

### 4. Дизайн-система

- ✅ Используется `Notification` компонент с `warning` вариантом
- ✅ Следует цветовой схеме (border-warning, bg-warning/10, text-warning)
- ✅ Правильный spacing через `space-y-4`

### 5. Производительность

- ✅ Условный рендеринг (возврат null если не показываем)
- ✅ Нет лишних вычислений
- ✅ Статическая проверка конфигурации

---

## 🚀 Альтернативные подходы (не выбраны)

### Альтернатива 1: Inline предупреждение в DepositAddressBlock

**Плюсы:** Меньше кода  
**Минусы:**

- Нарушает Single Responsibility Principle
- Делает DepositAddressBlock менее переиспользуемым
- Сложнее управлять конфигурацией

### Альтернатива 2: Создать новый Alert компонент

**Плюсы:** Специализированный компонент  
**Минусы:**

- Дублирование функциональности (Notification уже есть)
- Нарушает DRY принцип
- Больше кода для поддержки

### Альтернатива 3: Hardcoded текст в компоненте

**Плюсы:** Быстрее реализовать  
**Минусы:**

- Нарушает паттерн локализации проекта
- Не работает для мультиязычности
- Сложнее изменять тексты

---

## 📚 Связанные документы

- `docs/core/PROJECT_STRUCTURE_MAP.md` - Архитектура проекта
- `docs/core/DEVELOPER_GUIDE.md` - Руководство разработчика
- `packages/constants/src/email-config.ts` - Email конфигурация
- `packages/ui/src/components/ui/notification.tsx` - Notification компонент

---

## 🎯 Критерии успеха

1. ✅ Предупреждение отображается на странице заказа
2. ✅ Текст корректно переводится на RU/EN
3. ✅ Выравнивание и layout не нарушены
4. ✅ Работает управление через конфигурацию
5. ✅ Код следует архитектуре проекта
6. ✅ Минимальные изменения в существующем коде
7. ✅ TypeScript компилируется без ошибок
8. ✅ Адаптивный дизайн работает на всех устройствах

---

**Статус:** ✅ План готов к реализации  
**Следующий шаг:** Начать с ЭТАПА 1 (Конфигурация)
