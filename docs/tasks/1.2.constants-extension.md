# План реализации: Этап 1.2 - Расширение констант

**Базовые данные:**

- **Этап:** 1.2 из общего плана
- **Время выполнения:** 25 минут
- **Основа:** 100% ФАКТИЧЕСКИЙ анализ кода - БЕЗ ПРЕДПОЛОЖЕНИЙ
- **Дата создания:** 20 августа 2025

---

## 🎯 **ЦЕЛЬ ЭТАПА**

Создать ТОЛЬКО необходимые константы:

1. DEFAULT валюты для HeroExchangeForm (заменить хардкод)
2. Системное решение routing проблемы проекта

---

## 📋 **ФАКТИЧЕСКИЕ ОСНОВАНИЯ**

### **1. Хардкод в HeroExchangeForm (ГЛАВНАЯ СТРАНИЦА)**

```typescript
// apps/web/src/components/exchange-form/useHeroExchangeForm.ts
initialValues: {
  fromCurrency: 'USDT',  // 🚨 ХАРДКОД - нужна DEFAULT_FROM_CURRENCY
  toCurrency: 'UAH',     // 🚨 ХАРДКОД - нужна DEFAULT_TO_CURRENCY
}
```

### **2. Системная проблема routing (ВСЯ НАВИГАЦИЯ)**

```typescript
// apps/web/src/components/app-header.tsx
href = '/orders'; // 🚨 ХАРДКОД
href = '/contacts'; // 🚨 ХАРДКОД

// apps/web/src/components/app-footer.tsx
href = '/rates'; // 🚨 ХАРДКОД
href = '/reserves'; // 🚨 ХАРДКОД
href = '/limits'; // 🚨 ХАРДКОД
href = '/faq'; // 🚨 ХАРДКОД
href = '/how-it-works'; // 🚨 ХАРДКОД
```

### **3. Существующие константы (ПЕРЕИСПОЛЬЗУЕМ)**

```typescript
// packages/constants/src/time-constants.ts - УЖЕ ЕСТЬ:
CURRENCY_RATES_REFRESH: 10000,       // ✅ Используем
EXCHANGE_CALCULATION_DELAY: 300,     // ✅ Используем

// packages/constants/src/exchange-currencies.ts - УЖЕ ЕСТЬ:
CRYPTOCURRENCIES, TOKEN_STANDARDS    // ✅ Используем

// packages/constants/src/banks.ts - УЖЕ ЕСТЬ:
BANKS_BY_CURRENCY                    // ✅ Используем
```

---

## 🔧 **КОНСТАНТЫ К ДОБАВЛЕНИЮ**

### **1. Дополнение файла: `exchange.ts`**

```typescript
// packages/constants/src/exchange.ts - ДОПОЛНИТЬ существующий файл

/**
 * Дефолтные значения для форм обмена
 * Заменяет хардкод в useHeroExchangeForm.ts
 */
export const EXCHANGE_DEFAULTS = {
  /** Валюта по умолчанию для отправки - заменяет хардкод 'USDT' */
  FROM_CURRENCY: 'USDT' as const,
  /** Валюта по умолчанию для получения - заменяет хардкод 'UAH' */
  TO_CURRENCY: 'UAH' as const,
} as const;

export type DefaultFromCurrency = typeof EXCHANGE_DEFAULTS.FROM_CURRENCY;
export type DefaultToCurrency = typeof EXCHANGE_DEFAULTS.TO_CURRENCY;
```

### **2. Файл: `app-routes.ts`**

```typescript
// packages/constants/src/app-routes.ts

/**
 * Централизованные маршруты приложения
 * Устраняет хардкод в навигационных компонентах
 */

// === ОСНОВНЫЕ СТРАНИЦЫ ===
export const APP_ROUTES = {
  /** Главная страница */
  HOME: '/' as const,
  /** Страница заказов пользователя */
  ORDERS: '/orders' as const,
  /** Страница контактов */
  CONTACTS: '/contacts' as const,
  /** Обменник (новый) */
  EXCHANGE: '/exchange' as const,
} as const;

// === ИНФОРМАЦИОННЫЕ СТРАНИЦЫ ===
export const INFO_ROUTES = {
  /** Курсы валют */
  RATES: '/rates' as const,
  /** Резервы системы */
  RESERVES: '/reserves' as const,
  /** Лимиты обмена */
  LIMITS: '/limits' as const,
  /** Часто задаваемые вопросы */
  FAQ: '/faq' as const,
  /** Как это работает */
  HOW_IT_WORKS: '/how-it-works' as const,
} as const;

// === ПРАВОВЫЕ СТРАНИЦЫ ===
export const LEGAL_ROUTES = {
  /** Правила обмена */
  RULES: '/rules' as const,
  /** AML политика */
  AML_POLICY: '/aml-policy' as const,
  /** Возвраты */
  RETURNS: '/returns' as const,
  /** Политика конфиденциальности */
  PRIVACY: '/privacy' as const,
} as const;

// === ЯКОРНЫЕ ССЫЛКИ ===
export const ANCHOR_ROUTES = {
  /** Секция обменника на главной */
  EXCHANGE_SECTION: '#exchange-section' as const,
} as const;

// === ТИПЫ ===
export type AppRoute = (typeof APP_ROUTES)[keyof typeof APP_ROUTES];
export type InfoRoute = (typeof INFO_ROUTES)[keyof typeof INFO_ROUTES];
export type LegalRoute = (typeof LEGAL_ROUTES)[keyof typeof LEGAL_ROUTES];
export type AnchorRoute = (typeof ANCHOR_ROUTES)[keyof typeof ANCHOR_ROUTES];
```

---

## 📝 **ОБНОВЛЕНИЕ index.ts**

```typescript
// packages/constants/src/index.ts

// === СУЩЕСТВУЮЩИЕ ЭКСПОРТЫ (не трогаем) ===
export * from './api';
export * from './auth';
export * from './business';
export * from './ui';
export * from './validation';
export * from './user';
export * from './exchange'; // ДОПОЛНЕН новыми EXCHANGE_DEFAULTS
export * from './exchange-currencies';
export * from './fiat-currencies';
export * from './banks';
export * from './rate-limits';
export * from './order-statuses';
export * from './linter-limits';
export * from './decimal-precision';
export * from './validation-bounds';
export * from './percentage-calculations';
export * from './time-constants';
export * from './currency-formats';
export * from './business-limits';
export * from './contacts';
export * from './seo';

// === НОВЫЕ ЭКСПОРТЫ ===
export * from './app-routes'; // APP_ROUTES, INFO_ROUTES, LEGAL_ROUTES
```

---

## ⚡ **ПЛАН ВЫПОЛНЕНИЯ (25 минут)**

### **Шаг 1: Exchange defaults (5 минут)**

1. Дополнить `exchange.ts` новыми EXCHANGE_DEFAULTS
2. Добавить FROM_CURRENCY и TO_CURRENCY

### **Шаг 2: App routes (15 минут)**

1. Создать `app-routes.ts`
2. Централизовать ВСЕ маршруты из header/footer
3. Добавить типы для type safety

### **Шаг 3: Экспорты и проверка (5 минут)**

1. Обновить `index.ts` - добавить только app-routes
2. Интеграция EXCHANGE_DEFAULTS в useHeroExchangeForm.ts
3. Проверить TypeScript компиляцию

---

## ✅ **КРИТЕРИИ ГОТОВНОСТИ**

- [ ] Файл `exchange-defaults.ts` создан
- [ ] Файл `app-routes.ts` создан с ВСЕМИ маршрутами
- [ ] Экспорты добавлены в `index.ts`
- [ ] TypeScript компиляция проходит
- [ ] Подготовлен рефакторинг HeroExchangeForm и навигации

---

## 📊 **РЕЗУЛЬТАТ**

| Константа                         | Статус        | Назначение               |
| --------------------------------- | ------------- | ------------------------ |
| `EXCHANGE_DEFAULTS.FROM_CURRENCY` | 🆕 Создаем    | Заменит 'USDT' хардкод   |
| `EXCHANGE_DEFAULTS.TO_CURRENCY`   | 🆕 Создаем    | Заменит 'UAH' хардкод    |
| `APP_ROUTES.*`                    | 🆕 Создаем    | Заменит хардкод в header |
| `INFO_ROUTES.*`                   | 🆕 Создаем    | Заменит хардкод в footer |
| `LEGAL_ROUTES.*`                  | 🆕 Создаем    | Заменит хардкод в footer |
| `CURRENCY_RATES_REFRESH`          | ✅ Используем | Существующая константа   |
| `EXCHANGE_CALCULATION_DELAY`      | ✅ Используем | Существующая константа   |

**АРХИТЕКТУРНЫЙ РЕЗУЛЬТАТ:**

- ✅ Решает хардкод в HeroExchangeForm
- ✅ Системно решает routing проблему проекта
- ✅ Максимально переиспользует существующие константы
- ✅ Соответствует принципу "единого источника истины"
