# 🔍 Impact Analysis: Автоматическая установка минимальной суммы

**Дата анализа:** 5 сентября 2025  
**Агент-аналитик:** Анализ воздействия на существующую систему  
**Статус:** ✅ ЗАВЕРШЕН - Готов к архитектурному этапу

---

## 📋 **ТРЕБОВАНИЕ (УТОЧНЕННОЕ)**

Автоматически устанавливать минимальную сумму для выбранной криптовалюты **ТОЛЬКО:**

- При пустом поле суммы
- При открытии страницы
- БЕЗ UI индикации
- БЕЗ дополнительных валидаций (используем существующие)

**Навигация:** Главная страница → страница обмена с передачей данных формы

---

## ✅ **НАЙДЕННЫЙ СУЩЕСТВУЮЩИЙ ФУНКЦИОНАЛ**

### 🎯 **Готовые к переиспользованию компоненты:**

1. **`getMinCryptoAmountForUI(currency): number`** ✅
   - **Местоположение:** `packages/exchange-core/src/utils/calculations.ts`
   - **Назначение:** Возвращает минимальную сумму крипты для UI
   - **Пример:** `getMinCryptoAmountForUI('BTC')` → 0.00025
   - **Статус:** Готов к использованию

2. **`getCurrencyLimits(currency)` с полем `minCrypto`** ✅
   - Уже используется в валидации обеих форм
   - Возвращает тот же результат что и `getMinCryptoAmountForUI`

3. **Константы `AMOUNT_LIMITS`** ✅
   - **Значения:** MIN_USD: 10, MAX_USD: 5000
   - **Расчет:** минимум = $10 / текущий USD курс криптовалюты

### 🏗️ **Существующая архитектура форм:**

**Главная страница:**

```
HeroSection → HeroExchangeForm → SendingCard → CryptoAmountInput
Hook: useHeroExchangeForm (управляет состоянием)
Initial: fromAmount: ''
```

**Страница обмена:**

```
ExchangeContainer → ExchangeLayout → SendingSection → CryptoAmountInput
Hook: useFormWithNextIntl + useExchangeCalculations
Initial: fromAmount: initialParams.amount?.toString() || ''
```

---

## ⚠️ **ВЫЯВЛЕННЫЕ РИСКИ И КОНФЛИКТЫ**

### 🚨 **КРИТИЧЕСКИЕ - Требуют решения:**

1. **Навигация с данными формы:**
   - **Проблема:** Как передавать данные с главной на страницу обмена?
   - **Текущее состояние:** `initialParams.amount` предполагает URL параметры
   - **Требуется:** Определить механизм передачи данных

2. **Дублирование логики:**
   - **Риск:** Реализация автоподстановки в двух разных местах
   - **Решение:** Централизованный custom hook

### ⚡ **СРЕДНИЕ - Контролируемые:**

3. **Конфликт с существующими initial values:**
   - Главная: всегда пустая строка ✅
   - Обмен: может быть из URL параметров ⚠️

4. **Timing автоподстановки:**
   - Нужно срабатывать после инициализации формы
   - До первого рендера пользователю

---

## 🎯 **ОПРЕДЕЛЕННЫЕ ТОЧКИ РАСШИРЕНИЯ**

### ✅ **Оптимальные места для интеграции:**

1. **Custom Hook подход (РЕКОМЕНДУЕТСЯ):**

   ```typescript
   useAutoMinAmount(currency: string, currentAmount: string, shouldApply: boolean)
   ```

2. **Интеграция в существующие хуки:**
   - `useHeroExchangeForm` - добавить useEffect
   - `useExchangeFormData` - модифицировать initial values logic

3. **Точки внедрения:**
   - После инициализации формы
   - При смене валюты (но только если поле пустое)

---

## 📊 **ОТВЕТЫ НА КРИТИЧЕСКИЕ ВОПРОСЫ**

### ✅ **ПОЛУЧЕНЫ ОТ ПОЛЬЗОВАТЕЛЯ:**

1. **Триггеры:** ✅ Только пустое поле + открытие страницы
2. **URL параметры:** ✅ Используются для навигации главная→обмен
3. **UI индикация:** ✅ НЕ нужна
4. **Валидация:** ✅ Использовать существующую
5. **Навигация:** ✅ Главная должна передавать данные на страницу обмена

### ❓ **ОСТАЮЩИЕСЯ ТЕХНИЧЕСКИЕ ВОПРОСЫ ДЛЯ АРХИТЕКТОРА:**

6. **Механизм передачи данных:** Query params, state, localStorage, navigation state?
7. **Приоритет URL параметров:** Если есть amount в URL - игнорировать автоподстановку?
8. **Формат значения:** Как форматировать минимальную сумму для поля?

---

## 🎯 **РЕКОМЕНДАЦИИ ДЛЯ АРХИТЕКТУРНОГО ЭТАПА**

### 🏗️ **Предлагаемая архитектура:**

1. **Создать централизованный hook:** `useAutoMinAmount`
2. **Модифицировать навигацию:** HeroExchangeForm → ExchangeContainer
3. **Использовать существующие функции:** `getMinCryptoAmountForUI`
4. **Минимальные изменения** в существующий код

### 🔧 **Компоненты для изменения:**

- `apps/web/src/components/hero-exchange/useHeroExchangeForm.ts` (навигация)
- `apps/web/src/components/exchange/ExchangeContainer.tsx` (URL params)
- `apps/web/src/components/HeroSection.tsx` (обработка onExchange)
- Новый hook: `packages/hooks/src/business/useAutoMinAmount.ts`

### 📋 **Техническое решение:**

1. **Navigation:** router.push с search params или navigation state
2. **URL handling:** Проверка на наличие amount параметра
3. **Auto-fill logic:** useEffect отслеживающий currency + amount changes
4. **Format:** Использовать существующие форматтеры из utils

---

## 🚀 **СТАТУС: ГОТОВ К АРХИТЕКТУРНОМУ ЭТАПУ**

**Передача управления:** → **Агент-архитектор**

**Задача архитектора:**

- Спроектировать конкретные изменения в компонентах
- Определить механизм навигации с данными
- Создать схему интеграции без дублирования кода
- Обеспечить совместимость с существующей архитектурой

**Доступные ресурсы:**

- ✅ Готовые функции расчета минимума
- ✅ Существующие формы и хуки
- ✅ Валидационная система
- ✅ Четкие требования пользователя

---

**📁 Файл подготовлен для следующего этапа разработки**
