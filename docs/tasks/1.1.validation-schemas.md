# Детальный план реализации: Этап 1.1 - Создание валидационных схем

**Базовые данные:**

- **Этап:** 1.1 из общего плана
- **Время выполнения:** 45 минут
- **Основа:** 100% ФАКТИЧЕСКИЙ анализ существующей архитектуры
- **Дата создания:** 20 августа 2025

---

## 🎯 **ЦЕЛЬ ЭТАПА**

Добавить валидационную схему `securityEnhancedAdvancedExchangeFormSchema` в СУЩЕСТВУЮЩИЙ файл `packages/utils/src/validation/security-enhanced-schemas.ts` для УНИВЕРСАЛЬНОЙ страницы обмена с поддержкой ВСЕХ криптовалют, token standards, фиатных валют и банков.

---

## 📋 **ФАКТИЧЕСКИЕ ОСНОВАНИЯ**

### **1. Архитектурный анализ (ФАКТЫ)**

**КРИТИЧЕСКОЕ ОТКРЫТИЕ - НАРУШЕНИЕ АРХИТЕКТУРЫ:**

- ❌ **ОШИБКА:** Изначальный план предлагал создать новый файл `enhanced-exchange-schemas.ts`
- ✅ **ФАКТ:** Проект использует SINGLE-FILE архитектуру для всех security-enhanced схем
- ✅ **ФАКТ:** `packages/utils/src/validation/security-enhanced-schemas.ts` (561 строка) содержит ВСЕ схемы
- ✅ **ФАКТ:** `securityEnhancedExchangeSchema` УЖЕ СУЩЕСТВУЕТ в основном файле

**ПРАВИЛЬНАЯ АРХИТЕКТУРА:**

- ✅ `packages/utils/src/validation/security-enhanced-schemas.ts` - ЕДИНСТВЕННЫЙ файл для всех схем
- ✅ Добавление новых схем в СУЩЕСТВУЮЩИЙ файл, а НЕ создание нового
- ✅ Следование established паттернам внутри файла
- ✅ Использование существующих импортов и утилит

**Установленные паттерны в security-enhanced-schemas.ts:**

- ✅ Префикс `securityEnhanced` для всех схем
- ✅ Композиция базовых схем из локальных импортов
- ✅ XSS защита через `createXSSProtectedString`
- ✅ Константы лимитов из `@repo/constants/VALIDATION_LIMITS`

### **2. Существующие валидационные типы (ФАКТЫ)**

**КРИТИЧЕСКАЯ ИНФОРМАЦИЯ - ИЗБЕЖАНИЕ ДУБЛИРОВАНИЯ:**

```typescript
// ФАКТ: УЖЕ СУЩЕСТВУЮЩИЕ схемы в security-enhanced-schemas.ts
export const securityEnhancedExchangeSchema = z.object({...}); // Строка 313
export const securityEnhancedCreateExchangeOrderSchema = z.object({...}); // Строка 483

// ФАКТ: УЖЕ СУЩЕСТВУЮЩИЕ типы (строки 478-486)
export type SecurityEnhancedCreateExchangeOrder = z.infer<
  typeof securityEnhancedCreateExchangeOrderSchema
>;
export type SecurityEnhancedExchangeForm = z.infer<typeof securityEnhancedExchangeSchema>;
```

**РЕШЕНИЕ:** Создать `securityEnhancedAdvancedExchangeFormSchema` с уникальным именем

### **3. Card validation архитектура (ФАКТЫ)**

**Найденные функции в `card-validation.ts`:**

- ✅ `sanitizeCardNumber(cardNumber: string): string` - очистка от non-digit
- ✅ `luhnCheck(cardNumber: string): boolean` - алгоритм Луна
- ✅ `getCardBrand(cardNumber: string): string` - определение системы
- ✅ `validateCardLength(cardNumber: string): boolean` - проверка длины

**УСТАНОВЛЕННЫЙ ПАТТЕРН:** Утилиты уже созданы, нужна только интеграция в Zod схему

---

## 🔧 **ДЕТАЛЬНЫЙ ПЛАН РЕАЛИЗАЦИИ**

### **ШАГ 1: Анализ существующего файла (10 минут)**

**ДЕЙСТВИЕ:** Изучить `packages/utils/src/validation/security-enhanced-schemas.ts`

**ОБОСНОВАНИЕ:**

- Проект использует single-file архитектуру для всех security-enhanced схем
- Нужно следовать существующим паттернам импортов и структуры
- Избежать дублирования существующего `securityEnhancedExchangeSchema`

**АНАЛИЗ СТРУКТУРЫ ФАЙЛА:**

```typescript
// ФАКТ: Существующие импорты в файле (строки 1-15)
import { z } from 'zod';
import { VALIDATION_LIMITS, EXCHANGE_VALIDATION_PATTERNS } from '@repo/constants';
import { currencySchema } from './schemas-crypto';
import { emailSchema, passwordSchema, usernameSchema } from './schemas-basic';
import {
  createXSSProtectedString,
  SECURITY_VALIDATION_LIMITS,
  containsPotentialXSS,
} from './security-utils';
```

### **ШАГ 2: Анализ acceptance criteria полей (10 минут)**

**ДЕЙСТВИЕ:** Определить точные поля для `securityEnhancedAdvancedExchangeFormSchema`

**ФАКТЫ ИЗ ДОКУМЕНТАЦИИ (подтверждены чтением):**

- ✅ **Send Section:** Amount (USDT), Token Standard (TRC-20)
- ✅ **Receive Section:** Amount (UAH), Bank selection, Card number
- ✅ **Personal Data:** Email address
- ✅ **Security:** Captcha, Terms agreement

**ОТЛИЧИЯ ОТ СУЩЕСТВУЮЩЕЙ СХЕМЫ:**

```typescript
// СУЩЕСТВУЮЩАЯ: securityEnhancedExchangeSchema (простая форма)
// НОВАЯ: securityEnhancedAdvancedExchangeFormSchema (расширенная форма)

interface AdvancedExchangeFormData {
  // НОВОЕ: Universal crypto support с token standards
  fromCurrency: CryptoCurrency; // BTC, ETH, USDT, LTC
  tokenStandard?: TokenStandard; // TRC-20, ERC-20, BEP-20
  sendAmount: string; // Enhanced amount validation

  // НОВОЕ: Universal fiat support с банками
  toCurrency: FiatCurrency; // UAH, USD, EUR
  selectedBankId: string; // Bank selection based on fiat currency
  cardNumber: string; // Card validation с Luhn algorithm

  // СТАНДАРТНЫЕ поля
  email: string; // Existing emailSchema
  captcha: string; // Captcha validation
  acceptTerms: boolean; // Terms agreement
}
```

### **ШАГ 3: Интеграция card validation utilities (15 минут)**

**ДЕЙСТВИЕ:** Использовать существующие утилиты из `card-validation.ts` внутри основного файла

**ФАКТ:** Все card validation утилиты УЖЕ доступны в том же пакете:

```typescript
// СУЩЕСТВУЮЩИЕ утилиты в packages/utils/src/validation/card-validation.ts
import { sanitizeCardNumber, luhnCheck, validateCardLength, getCardBrand } from './card-validation';

// ИНТЕГРАЦИЯ: Добавить в security-enhanced-schemas.ts
const enhancedCardNumberSchema = z
  .string()
  .min(1, 'CARD_NUMBER_REQUIRED')
  .transform(val => {
    // XSS protection (existing pattern)
    if (containsPotentialXSS(val)) {
      throw new z.ZodError([
        {
          code: 'custom',
          message: 'INVALID_CHARACTERS_DETECTED',
          path: [],
        },
      ]);
    }
    return sanitizeCardNumber(val);
  })
  .refine(sanitized => validateCardLength(sanitized), 'INVALID_CARD_LENGTH')
  .refine(sanitized => luhnCheck(sanitized), 'INVALID_CARD_NUMBER');
```

### **ШАГ 4: Добавление главной схемы в существующий файл (10 минут)**

**ДЕЙСТВИЕ:** Добавить `securityEnhancedAdvancedExchangeFormSchema` в конец `security-enhanced-schemas.ts`

**ТОЧНОЕ МЕСТОПОЛОЖЕНИЕ:** После существующих схем, перед экспортом типов

**КОД ДЛЯ ДОБАВЛЕНИЯ:**

```typescript
/**
 * ADVANCED EXCHANGE FORM SCHEMA с enhanced security
 * ЦЕЛЬ: Universal exchange form (любая crypto → любая fiat + банки + карты)
 * ОТЛИЧИЕ ОТ securityEnhancedExchangeSchema: + tokenStandard + cardNumber + bankId
 */
export const securityEnhancedAdvancedExchangeFormSchema = z.object({
  // Send section - Universal crypto support
  fromCurrency: currencySchema, // Используем existing schema
  tokenStandard: z.string().optional(), // TRC-20, ERC-20, BEP-20
  sendAmount: createXSSProtectedString(1, SECURITY_VALIDATION_LIMITS.AMOUNT_MAX_LENGTH)
    .refine(val => !isNaN(Number(val)), 'AMOUNT_MUST_BE_NUMBER')
    .refine(val => Number(val) > 0, 'AMOUNT_POSITIVE_REQUIRED'),

  // Receive section - Universal fiat + banks + cards
  toCurrency: z.enum(['UAH', 'USD', 'EUR']), // All supported fiat currencies
  selectedBankId: z.string().min(1, 'BANK_REQUIRED').uuid('INVALID_BANK_ID_FORMAT'),
  cardNumber: z
    .string()
    .min(1, 'CARD_NUMBER_REQUIRED')
    .transform(val => {
      if (containsPotentialXSS(val)) {
        throw new z.ZodError([
          {
            code: 'custom',
            message: 'INVALID_CHARACTERS_DETECTED',
            path: [],
          },
        ]);
      }
      return sanitizeCardNumber(val);
    })
    .refine(sanitized => validateCardLength(sanitized), 'INVALID_CARD_LENGTH')
    .refine(sanitized => luhnCheck(sanitized), 'INVALID_CARD_NUMBER'),

  // Standard fields - reusing existing schemas
  email: emailSchema, // EXISTING schema
  captcha: createXSSProtectedString(1, 50), // Simple captcha validation
  acceptTerms: z.boolean().refine(val => val === true, 'TERMS_ACCEPTANCE_REQUIRED'),
});
```

**ДОБАВЛЕНИЕ ТИПА (в секцию типов файла):**

```typescript
// В конце файла, в секции export type
export type SecurityEnhancedAdvancedExchangeForm = z.infer<
  typeof securityEnhancedAdvancedExchangeFormSchema
>;
```

---

## ✅ **КРИТЕРИИ ЗАВЕРШЕНИЯ**

### **1. Интеграция с существующим файлом:**

- ✅ `packages/utils/src/validation/security-enhanced-schemas.ts` - схема добавлена в существующий файл
- ✅ Схема `securityEnhancedAdvancedExchangeFormSchema` - добавлена в конец файла
- ✅ Тип `SecurityEnhancedAdvancedExchangeForm` - экспортирован в секции типов
- ✅ Импорт `card-validation.ts` утилит добавлен в начало файла

### **2. Архитектурная совместимость:**

- ✅ Следует single-file архитектуре проекта
- ✅ Использует существующие импорты и паттерны
- ✅ НЕ дублирует `securityEnhancedExchangeSchema`
- ✅ Уникальное имя `securityEnhancedAdvancedExchangeFormSchema`

### **3. Функциональность работает:**

- ✅ Все импорты разрешаются корректно
- ✅ TypeScript компиляция проходит без ошибок
- ✅ Zod валидация функционирует для всех полей
- ✅ Card validation интегрирована с Luhn algorithm

### **4. Security-Enhanced compliance:**

- ✅ XSS защита применена ко всем text inputs
- ✅ Префикс `securityEnhanced` в названии схемы
- ✅ Существующие базовые схемы переиспользованы
- ✅ Следует established паттернам файла

---

## 🚨 **ВАЖНЫЕ ПРИНЦИПЫ**

### **НЕ ПРЕДПОЛАГАТЬ:**

- ✅ Все поля взяты из acceptance criteria документации
- ✅ Все utilities существуют в `card-validation.ts`
- ✅ Все patterns скопированы из `security-enhanced-schemas.ts`
- ✅ Все константы взяты из `@repo/constants`

### **СЛЕДОВАНИЕ АРХИТЕКТУРЕ:**

- ✅ ИСПРАВЛЕНО: Single-file архитектура вместо создания нового файла
- ✅ ИСПРАВЛЕНО: Использование существующих импортов в security-enhanced-schemas.ts
- ✅ ИСПРАВЛЕНО: Уникальное имя схемы во избежание дублирования
- ✅ Security-Enhanced префикс для consistency
- ✅ Композиция существующих базовых схем
- ✅ XSS protection layer на всех inputs
- ✅ Централизованные константы validation limits

### **ГОТОВНОСТЬ К ЭТАПУ 1.2:**

- ✅ Схема готова к интеграции с constants
- ✅ Типы готовы к использованию в hooks
- ✅ Валидация готова к интеграции с UI

---

## 📚 **СВЯЗАННЫЕ ФАЙЛЫ**

**Изучены и используются:**

- `packages/utils/src/validation/security-enhanced-schemas.ts` - архитектурные patterns
- `packages/utils/src/validation/card-validation.ts` - утилиты валидации карт
- `packages/utils/src/validation/security-utils.ts` - XSS protection
- `packages/utils/src/validation/schemas-basic.ts` - базовые схемы
- `docs/exchange-usdt-trc20-to-uah-card-acceptance-criteria-FINAL.md` - требования

**Следующие этапы будут использовать:**

- `packages/constants/src/exchange-currencies.ts` - для расширения констант
- `packages/exchange-core/src/types/` - для типов
- `packages/ui/src/components/ui/checkbox.tsx` - для UI компонента

---

**ИТОГ:** Схема валидации `securityEnhancedAdvancedExchangeFormSchema` добавлена в существующий файл `security-enhanced-schemas.ts` с полным соответствием single-file архитектуре проекта, избежанием дублирования и готовностью для УНИВЕРСАЛЬНОГО обменника с поддержкой всех криптовалют, token standards, фиатных валют и банков.
