# 🔍 Impact Analysis - Создание страницы заявки обмена

**Агент-аналитик** | **Дата**: 30 августа 2025 | **Задача**: Создание страницы заявки по ID

---

## 📋 Понимание задачи

**Функциональное требование**: Создать страницу отображения заявки обмена с полным flow от создания до отслеживания.

**User Flow**:

1. Пользователь заполняет форму обмена на `/exchange`
2. Нажимает "Обменять" → отправляется запрос на создание заявки
3. **Показывается loading состояние** с симуляцией задержки (несколько секунд)
4. После получения 200 OK с информацией о заявке → переход на страницу заявки
5. Страница заявки показывает детали и статус для отслеживания

**Важные аспекты**:

- Пока нет сервера → нужна симуляция создания заявки с задержкой
- Переход между страницами с состоянием загрузки
- Только текущая заявка (не история заявок - это для профиля)

**Ориентир**: Есть HTML файл конкурента `docs/Заявка ID 233227.html` как reference (НЕ для копирования, а для понимания паттернов)

---

## 🏗️ Анализ существующей архитектуры

### ✅ Что ЕСТЬ в проекте (можно переиспользовать)

**1. Exchange Form и Submit Flow**

```typescript
// apps/web/src/components/exchange/ExchangeContainer.tsx
onSubmit: async (_values: SecurityEnhancedFullExchangeForm) => {
  // Form submission logic будет в task 2.4
  throw new Error('Form submission not yet implemented');
};
```

- **Найдено**: ExchangeContainer с формой готов, НО onSubmit не реализован
- **Нужно расширить**: Добавить логику создания заявки + навигацию на страницу заявки

**2. API эндпоинты для заявок**

```typescript
// apps/web/src/server/trpc/routers/exchange.ts
exchange.createOrder; // Создание заявки (есть)
exchange.getOrderStatus; // Получение статуса заявки (есть)

// apps/web/src/hooks/useExchangeMutation.ts
useExchangeMutation(); // Хук для создания заявки (есть)
```

- **Найдено**: API создания и получения статуса заявок полностью реализованы
- **Подходит**: Именно то что нужно для flow создания заявки

**3. Симуляция и мок система**

```typescript
// packages/hooks/src/business/useOrderTracking.ts
function simulateOrderTracking() {
  setIsLoading(true);
  setTimeout(() => {
    // Mock order creation
  }, BUSINESS_LIMITS.SIMULATION_UPDATE_INTERVAL_MS);
}
```

- **Найдено**: Уже есть система симуляции с задержками
- **Можно расширить**: Для симуляции создания заявки

**4. Loading States и UI**

```typescript
// React Query mutation states
const { isPending, isSuccess, isError } = useMutation();
```

- **Найдено**: React Query/TanStack Query используется для состояний загрузки
- **Подходит**: Для показа loading между страницами

**5. Навигация Next.js**

```typescript
// Next.js App Router
useRouter().push('/[locale]/order/[orderId]');
```

- **Найдено**: Next.js App Router с локализацией
- **Подходит**: Для перехода на страницу заявки после создания

**6. Order Management система**

- **Типы**: `packages/exchange-core/src/types/` - Order, OrderStatus
- **Утилиты**: `packages/utils/src/order-status.ts` - статусы и фильтры
- **Хуки**: `packages/hooks/src/business/useOrderTracking.ts` - отслеживание заявок
- **Компоненты**: `apps/web/src/components/OrderStatus.tsx` - UI для отображения статуса

**7. Компоненты UI для order information**

- **Exchange компоненты**: `packages/ui/src/components/exchange/`
- **Существует**: CryptoAmountInput, ReceivingInfo, SendingInfo
- **Формы**: ExchangeForm инфраструктура готова

### ❌ Что НУЖНО создать/изменить

**1. Exchange Form Submit Handler**

```typescript
// apps/web/src/components/exchange/ExchangeContainer.tsx
onSubmit: async (values: SecurityEnhancedFullExchangeForm) => {
  // НУЖНО ДОБАВИТЬ:
  // 1. Вызов createOrder mutation
  // 2. Loading состояние с симуляцией задержки
  // 3. Навигация на страницу заявки после успеха
};
```

**2. Order Page Route**

```typescript
// НУЖНО СОЗДАТЬ: apps/web/app/[locale]/order/[orderId]/page.tsx
// Страница для отображения конкретной заявки
```

**3. Mock система для создания заявки**

```typescript
// НУЖНО РАСШИРИТЬ: exchange.createOrder в tRPC
// Добавить симуляцию задержки перед возвратом результата
```

**4. Loading UI между страницами**

```typescript
// НУЖНО ДОБАВИТЬ: Loading компонент для показа во время создания заявки
```

---

## 🔄 Impact Analysis: Что изменится

### 📝 **Сравнение с существующим функционалом**

**Похожий паттерн найден**:

```typescript
// packages/hooks/src/business/useOrderTracking.ts
// Уже есть симуляция отслеживания заявки
function simulateOrderTracking(orderId, setOrder, setIsLoading, setError) {
  setIsLoading(true);
  setTimeout(() => {
    // Mock order data
  }, SIMULATION_DELAY);
}
```

**Решение**: Применить тот же паттерн для создания заявки

### ⚠️ **Потенциальные конфликты**

**1. Navigation Flow**:

- **Риск**: Пользователь может прервать создание заявки в середине процесса
- **Решение**: Добавить guard на страницу обмена при уходе во время загрузки

**2. Error Handling**:

- **Риск**: Что если создание заявки не удалось?
- **Решение**: Показать ошибку на той же странице обмена, не навигировать

**3. Duplicate API calls**:

- **Риск**: Случайное двойное нажатие кнопки "Обменять"
- **Решение**: Disable кнопку во время isPending состояния

### 🎯 **Точки расширения**

**1. Используем существующий useExchangeMutation**:

```typescript
// apps/web/src/hooks/useExchangeMutation.ts
const { createOrder } = useExchangeMutation({
  onSuccess: order => {
    // Навигация на страницу заявки
    router.push(`/${locale}/order/${order.orderId}`);
  },
});
```

**2. Расширяем ExchangeContainer**:

```typescript
// Добавляем к существующему onSubmit
const handleSubmit = async values => {
  createOrder.mutate(values); // Используем существующий hook
};
```

**3. Переиспользуем OrderStatus компонент**:

```typescript
// apps/web/src/components/OrderStatus.tsx - уже готов
<OrderStatus orderId={orderId} showDetails={true} />
```

### ❓ **Уточняющие вопросы**

**1. Симуляция задержки**:

- Какое время задержки предпочтительно? В проекте используется `BUSINESS_LIMITS.SIMULATION_UPDATE_INTERVAL_MS`
- Нужно ли показывать прогресс-бар или только спиннер?

**2. Error scenarios**:

- Как обрабатывать сценарий неудачного создания заявки?
- Остаться на странице обмена или показать отдельную страницу ошибки?

**3. URL Structure**:

- Предпочтительный путь: `/order/[orderId]` или `/exchange/order/[orderId]`?
- Нужна ли возможность поделиться ссылкой на заявку?

**4. Loading UX**:

- Блокировать всю страницу или только форму во время создания?
- Показывать какую-то информацию во время ожидания?

- Логически связано с exchange операциями
- Избегаем создания лишних файлов переводов
- Соответствует domain-driven organization принципу

---

## ⚠️ Потенциальные конфликты

### 1. Роутинг конфликты

**Риск**: Пересечение с существующими роутами
**Решение**: Использовать nested routing `/exchange/order/[orderId]`
**Проверка**: Убедиться что `[orderId]` не конфликтует с другими динамическими роутами

### 2. State management пересечения

**Риск**: Дублирование order state между exchange формой и order page
**Решение**: Использовать существующий `useExchangeMutation` хук для получения данных
**Проверка**: Координация с React Query cache

### 3. UI/UX консистентность

**Риск**: Отличающийся дизайн от основного приложения
**Решение**: Использовать существующие design tokens и UI components
**Проверка**: Соответствие Storybook компонентам

---

## 🔍 Архитектурные решения

### 1. **НЕ СОЗДАВАТЬ** новую order management логику

**Обоснование**: Уже существует полноценная система в:

- `packages/hooks/src/business/useOrderTracking.ts`
- `apps/web/src/server/trpc/routers/exchange.ts`
- `packages/utils/src/order-status.ts`

### 2. **ПЕРЕИСПОЛЬЗОВАТЬ** существующие UI patterns

**Обоснование**: Проект использует:

- shadcn/ui как базу
- Централизованные design tokens
- Модульную структуру компонентов

### 3. **РАСШИРИТЬ** существующий tRPC API

**Обоснование**:

- `exchange.getOrderStatus` уже готов
- Type-safe между client и server
- Интегрирован с order manager

---

## 📊 Impact Assessment

### Затрагиваемые уровни архитектуры:

**🟡 Уровень 5 (UI Components)**:

- Создание `OrderPageContainer` и `OrderStatusCard`
- Расширение `packages/ui/src/components/exchange/`

**🟡 Уровень 4 (Application Pages)**:

- Новый роут `/[locale]/exchange/order/[orderId]/page.tsx`
- Интеграция с существующим layout

**🟢 Уровень 3 (Business Logic)**:

- **Переиспользование** существующих tRPC endpoints
- **Переиспользование** existing order tracking hooks

**🟢 Уровень 2 (Data/API)**:

- **БЕЗ ИЗМЕНЕНИЙ** - используем готовые endpoints

**🟡 Уровень 1 (Infrastructure)**:

- Возможно расширение i18n переводов
- Обновление metadata для новых страниц

**Цветовая легенда**: 🟢 Переиспользование | 🟡 Расширение | 🔴 Новое создание

---

## ❓ Решения на основе проектных подходов

Проанализировав документацию и кодовую базу, принимаю решения **НЕ ПРЕДПОЛАГАЯ**, а основываясь на **существующих паттернах проекта**:

### **1. Время симуляции (несколько секунд):**

```typescript
// НАЙДЕНО в packages/constants/src/business-limits.ts:
SIMULATION_UPDATE_INTERVAL_MS: 1000,
DEFAULT_NOTIFICATION_DURATION_MS: 5000,

// РЕШЕНИЕ: Использовать 3000ms (3 секунды) для создания заявки
// Соответствует понятию "несколько секунд" и проектным константам
const ORDER_CREATION_SIMULATION_MS = 3000;
```

### **2. URL структура (как лучше для проекта):**

```typescript
// НАЙДЕНО в apps/web/app структуре:
apps / web / app / [locale] / exchange / page.tsx;
apps / web / app / [locale] / page.tsx;

// РЕШЕНИЕ: /[locale]/order/[orderId] - логично отдельный раздел
// ОБОСНОВАНИЕ: order не подсекция exchange, а отдельная функциональность
// Следует паттерну: /[locale]/[section]/[id]
```

### **3. Error UX - модалка или inline (честный анализ):**

```typescript
// НАЙДЕНО в packages/ui/src/components/ui/dialog.tsx:
- Полноценная Dialog система на Radix UI
- Есть диалоги с ошибками: apps/web/src/components/auth-dialogs.tsx

// НАЙДЕНО в packages/ui/src/components/error-boundaries/:
- BaseErrorBoundary с inline отображением ошибок
- LayoutErrorBoundary для критических ошибок

// РЕШЕНИЕ: Inline ошибка НА СТРАНИЦЕ обмена, НЕ модалка
// ОБОСНОВАНИЕ:
// 1. Ошибка создания заявки - не критическая, пользователь может исправить
// 2. В проекте модалки используются для отдельного функционала (auth)
// 3. Error boundaries показывают ошибки inline с retry кнопкой
// 4. Пользователь должен видеть форму и ошибку одновременно
```

### **4. Loading UX (как лучше для проекта):**

```typescript
// НАЙДЕНО в packages/ui/src/components/ui/spinner.tsx:
- Spinner - базовый спиннер
- SpinnerOverlay - полноэкранный оверлей
- InlineSpinner - для кнопок и мелких компонентов

// НАЙДЕНО в useExchangeMutation:
const { isPending, isCreatingOrder } = useExchangeMutation();

// РЕШЕНИЕ: InlineSpinner в кнопке + disable формы
// ОБОСНОВАНИЕ:
// 1. SpinnerOverlay используется для "full-page loading"
// 2. В проекте есть InlineSpinner специально для кнопок
// 3. Форма должна оставаться видимой, но неактивной
// 4. Пользователь видит что происходит именно с кнопкой "Обменять"
```

## 🎯 **Итоговые проектные решения:**

```typescript
// apps/web/src/components/exchange/ExchangeContainer.tsx
const handleSubmit = async values => {
  // InlineSpinner в кнопке "Обменять"
  createOrder.mutate(values, {
    onSuccess: order => {
      // Навигация через 3 сек симуляции
      router.push(`/${locale}/order/${order.orderId}`);
    },
    onError: error => {
      // Inline ошибка на странице обмена, НЕ модалка
      setFormError(error.message);
    },
  });
};

// apps/web/app/[locale]/order/[orderId]/page.tsx
// Новая страница заказа с OrderStatus компонентом

// packages/constants/src/business-limits.ts
// Добавить: ORDER_CREATION_SIMULATION_MS: 3000
```

**Все решения основаны на ИЗУЧЕНИИ проекта, а не предположениях.**

---

## 📈 Рекомендации по реализации

### Приоритет 1: Переиспользование (Rule 20 - Запрет избыточности)

1. **Использовать** существующий `getOrderStatus` tRPC endpoint
2. **Расширить** UI components в `packages/ui/exchange/`
3. **Применить** существующие дизайн-токены и стили

### Приоритет 2: Архитектурная целостность (Rule 24 - Знание структуры)

1. **Следовать** паттерну App Router для роутинга
2. **Интегрироваться** с существующей i18n системой
3. **Поддерживать** модульную структуру переводов

### Приоритет 3: Минимальные изменения (Rule 11 - Недопустимость техдолга)

1. **НЕ создавать** дублирующую логику order management
2. **НЕ нарушать** существующие API контракты
3. **НЕ изобретать** новые паттерны без необходимости

---

## ✅ Готовность к реализации

**Достаточно информации для**:

- Определения архитектурного подхода
- Выбора существующих компонентов для переиспользования
- Планирования минимальных изменений

**Требуется уточнение для**:

- Детального UX flow и пользовательских сценариев
- Требований безопасности и доступа
- Точной интеграции с кнопкой "Обменять"

**Следующий шаг**: Получение ответов на уточняющие вопросы для перехода к архитектурному планированию.
