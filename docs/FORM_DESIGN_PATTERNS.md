# 🎨 Form Design Patterns - Руководство по дизайн-системе форм

**Дата создания:** 12 июля 2025  
**Статус:** Активный  
**Цель:** Решение проблем визуальной группировки в сложных формах

---

## 📋 Проблема

При анализе текущего обменника выявлены критические проблемы дизайна:

1. **Висящая кнопка "Обменять"** - нет визуальной связи с формой
2. **Отсутствие группировки** - элементы не воспринимаются как единое целое
3. **Неясная иерархия** - пользователь не понимает логическую связь элементов

## 🎯 Решение

Разработана система **form-patterns** для создания визуально связанных групп элементов.

### 🔧 Основные паттерны

#### 1. Контейнеризация форм (`formContainers`)

```javascript
// Основной контейнер для всей формы обмена
const formContainer = 'bg-white rounded-2xl border border-gray-200/60 shadow-lg p-6 space-y-6';

// Варианты для разных контекстов
const variants = {
  compact:
    'bg-white/95 backdrop-blur-sm rounded-xl border border-gray-200/50 shadow-md p-4 space-y-4',
  full: 'bg-white rounded-2xl border border-gray-200/60 shadow-xl p-8 space-y-8',
  mobile: 'bg-white rounded-lg border border-gray-200/50 shadow-md p-4 space-y-4',
};
```

#### 2. Визуальные соединители (`visualConnectors`)

```javascript
// Иконка обмена между карточками
const exchangeIcon = {
  base: 'absolute left-1/2 top-1/2 transform -translate-x-1/2 -translate-y-1/2 z-10',
  background: 'bg-white border-2 border-gray-200 rounded-full p-2 shadow-sm',
  variants: {
    primary: 'bg-blue-50 border-blue-200 text-blue-600',
    minimal: 'bg-white border-gray-200 text-gray-600',
  },
};
```

#### 3. Группировка компонентов (`componentGroups`)

```javascript
// Группа действий с четким разделением
const actionGroup = {
  container: 'border-t border-gray-100 pt-6 mt-6',
  primary: 'space-y-3',
  secondary: 'flex justify-center mt-4',
};
```

### 📝 Пример использования

**До (проблема):**

```tsx
<form onSubmit={form.handleSubmit} className="space-y-6">
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
    <SendingCard />
    <ReceivingCard />
  </div>
  <div className="flex justify-center">
    <Button>Обменять</Button> {/* Висит без связи */}
  </div>
</form>
```

**После (решение):**

```tsx
{
  /* Контейнер группирует всю форму */
}
<div className="bg-white rounded-2xl border border-gray-200/60 shadow-lg p-6 space-y-6">
  <form onSubmit={form.handleSubmit} className="space-y-6">
    {/* Группа карточек с визуальной связью */}
    <div className="space-y-4 relative">
      <SendingCard />

      {/* Визуальный соединитель */}
      <div className="flex items-center justify-center -my-2 relative z-10">
        <div className="bg-white border-2 border-gray-200 rounded-full p-2 shadow-sm">
          <RefreshCw className="w-4 h-4 text-gray-600" />
        </div>
      </div>

      <ReceivingCard />
    </div>

    {/* Область действий с четким разделением */}
    <div className="border-t border-gray-100 pt-6 mt-6">
      <div className="flex justify-center">
        <Button>Обменять</Button> {/* Теперь часть формы */}
      </div>
    </div>
  </form>
</div>;
```

### 🎨 Варианты стилизации

#### Компактная версия (для hero секции)

```javascript
const compact =
  'bg-white/95 backdrop-blur-sm rounded-xl border border-gray-200/50 shadow-md p-4 space-y-4';
```

#### Полная версия (для страницы exchange)

```javascript
const full = 'bg-white rounded-2xl border border-gray-200/60 shadow-xl p-8 space-y-8';
```

#### Мобильная версия

```javascript
const mobile = 'bg-white rounded-lg border border-gray-200/50 shadow-md p-4 space-y-4';
```

### 📐 Семантические отступы

```javascript
const formSpacing = {
  withinGroup: 'space-y-3', // Между элементами одной группы
  betweenGroups: 'space-y-6', // Между группами элементов
  betweenSections: 'space-y-8', // Между основными секциями
  aroundActions: 'pt-6 mt-6', // Вокруг action кнопок
};
```

## 🚀 Результат

### ✅ Решенные проблемы:

1. **Визуальная группировка** - все элементы формы объединены общим контейнером
2. **Логическая связь** - визуальный соединитель показывает направление обмена
3. **Четкая иерархия** - кнопка находится в выделенной области действий
4. **Семантические отступы** - правильные расстояния между элементами

### 🎯 Принципы применения:

- **Контейнеризация** - группируйте связанные элементы общими границами
- **Визуальные связи** - используйте соединители для показа логической связи
- **Иерархия действий** - выделяйте кнопки в отдельные области
- **Семантические отступы** - применяйте правильные расстояния

### 📦 Экспорт

Все паттерны доступны из дизайн-системы:

```javascript
import { formContainers, visualConnectors, componentGroups } from '@repo/design-tokens';
```

---

## 🔍 Диагностика проблем в формах

### Признаки плохой группировки:

- ❌ Кнопки "висят" между элементами
- ❌ Непонятно, какие элементы связаны
- ❌ Отсутствуют визуальные границы
- ❌ Неравномерные отступы

### Признаки хорошей группировки:

- ✅ Четкие границы групп элементов
- ✅ Логические связи видны визуально
- ✅ Кнопки привязаны к соответствующим формам
- ✅ Семантические отступы
