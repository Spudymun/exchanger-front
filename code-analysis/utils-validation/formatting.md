### Путь: packages/utils/src/formatting.ts

**Краткое назначение (1 предложение)**
Унифицированные утилиты форматирования для устранения дублирования логики отображения чисел и валют.

**Подробное описание (3–6 предложений)**
Файл содержит централизованные функции форматирования чисел, устраняя дублирование логики на уровне утилит. Использует константы из @repo/constants для обеспечения консистентности форматирования UAH и криптовалют. Включает функции для форматирования UAH сумм, комиссий, криптовалют с ограничением десятичных знаков. Предоставляет обратную функцию для парсинга отформатированных строк обратно в числа. Все функции являются чистыми без побочных эффектов.

**Экспортируемые сущности / API (все на 100%)**

- `formatUahAmount(amount)` - форматирование UAH с правильными десятичными знаками
- `formatCommissionAmount(amount)` - форматирование комиссии (аналогично UAH)
- `formatCryptoAmountForUI(amount, maxDecimals)` - форматирование криптовалют для UI с ограничением знаков
- `parseFormattedAmount(formattedAmount)` - парсинг отформатированной строки обратно в число

**Входы (expected inputs) / Параметры (все на 100%)**

- `amount: number` - числовая сумма для форматирования
- `maxDecimals: number` - максимальное количество десятичных знаков для криптовалют
- `formattedAmount: string` - отформатированная строка для парсинга

**Выходы / Побочные эффекты (все на 100%)**

- `formatUahAmount` возвращает `string` - UAH сумма с 2 десятичными знаками
- `formatCommissionAmount` возвращает `string` - комиссия с 2 десятичными знаками
- `formatCryptoAmountForUI` возвращает `string` - криптовалюта с ограниченными знаками
- `parseFormattedAmount` возвращает `number` - числовое значение
- Нет побочных эффектов - чистые функции

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: @repo/constants (DECIMAL_PRECISION, PERCENTAGE_CALCULATIONS)
- **Потенциальные потребители**:
  - Компоненты отображения валют
  - Калькуляторы обменных курсов
  - Формы ввода сумм
  - API ответы с форматированными данными
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Примеры использования:
const uahAmount = 1234.567;
const cryptoAmount = 0.123456789;

// UAH форматирование: "1234.57" (2 знака)
const formattedUah = formatUahAmount(uahAmount);

// Комиссия: "25.50" (2 знака)
const formattedCommission = formatCommissionAmount(25.5);

// Криптовалюта для UI: "0.1235" (4 знака максимум)
const formattedCrypto = formatCryptoAmountForUI(cryptoAmount, 4);

// Парсинг обратно: 1234.57
const parsed = parseFormattedAmount('1234.57');
```

**Риски и безопасность**

- **Средний риск**: влияет на отображение финансовых данных
- **Display accuracy**: неправильное форматирование может ввести в заблуждение
- **Precision loss**: потенциальная потеря точности при округлении
- **Parsing errors**: parseFormattedAmount может вернуть NaN для невалидных строк
- **Consistency**: обеспечивает единообразное отображение валют

**Тесты / рекомендации по покрытию**

- **Formatting accuracy тесты**: проверка корректности форматирования
- **Precision тесты**: проверка округления и точности
- **Parsing тесты**: корректность обратного парсинга
- **Edge case тесты**: граничные значения (0, очень большие числа)
- **Integration тесты**: использование в UI компонентах

**Оценка сложности (low/medium/high)**
**LOW** - простые функции форматирования с использованием констант

**TODO / Рефакторинг**

- ✅ Отличная централизация логики форматирования
- ✅ Использование констант из @repo/constants
- ✅ Чистые функции без побочных эффектов
- ✅ Устранение дублирования между модулями
- ⚠️ parseFormattedAmount может вернуть NaN - нужна валидация
- 💡 Рекомендация: добавить валидацию входных параметров
- 💡 Рекомендация: добавить обработку ошибок в parseFormattedAmount
- 💡 Рекомендация: рассмотреть добавление локализованного форматирования валют
