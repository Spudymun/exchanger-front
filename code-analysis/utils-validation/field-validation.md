### Путь: packages/utils/src/validation/field-validation.ts

**Краткое назначение (1 предложение)**
Система валидации отдельных полей формы с поддержкой next-intl локализации и специализированной обработкой различных типов полей.

**Подробное описание (3–6 предложений)**
Модуль реализует гибкую систему валидации полей с автоматическим определением типа поля и применением соответствующих правил валидации. Основная функция validateFieldWithNextIntl принимает Zod схему, значения формы, имя поля и конфигурацию локализации для выполнения валидации конкретного поля. Включает специализированную обработку для критически важных полей: email, password, confirmPassword и captcha через соответствующие функции из single-field модуля. Предоставляет fallback валидацию для общих полей через validateGenericField с проверкой на пустые значения. Использует модульную архитектуру с разделением логики по типам полей и интегрируется с константами валидации для консистентности сообщений об ошибках.

**Экспортируемые сущности / API (все на 100%)**

- `export function validateFieldWithNextIntl<T>({ validationSchema, values, fieldName, config })` — основная функция валидации поля

**Входы (expected inputs) / Параметры (все на 100%)**

- `validationSchema: z.ZodSchema<T>` — Zod схема для валидации
- `values: T extends Record<string, unknown>` — объект со значениями формы
- `fieldName: string` — имя валидируемого поля
- `config: NextIntlValidationConfig` — конфигурация с функцией локализации

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает `{ isValid: boolean; error: string | null }`
- `isValid: true` при успешной валидации
- `error: string` с локализованным сообщением об ошибке при неудаче
- `error: null` при успешной валидации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: формах для валидации отдельных полей при blur/change событиях
- **Импортирует**: NextIntlValidationConfig, VALIDATION_KEYS из constants
- **Использует**: validateSingle\* функции из single-field для специализированной валидации
- **Интегрируется**: с Zod схемами для структурной валидации
- **Часть**: системы валидации utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Параметры валидации
interface ValidateFieldParams<T extends Record<string, unknown>> {
  validationSchema: z.ZodSchema<T>;
  values: T;
  fieldName: string;
  config: NextIntlValidationConfig;
}

// Результат валидации
interface ValidationResult {
  isValid: boolean;
  error: string | null;
}

// Специализированные типы полей
type SpecializedFieldTypes = 'email' | 'password' | 'confirmPassword' | 'captcha';

// Конфигурация локализации
interface NextIntlValidationConfig {
  t: (key: string, values?: Record<string, any>) => string;
  locale: string;
}
```

**Риски и безопасность**

- **Type safety**: использует unknown для fieldValue, требует приведения типов
- **Field name matching**: строгое сравнение имен полей может быть хрупким
- **Validation consistency**: зависит от правильности single-field валидаторов
- **Localization dependency**: критически зависит от наличия переводов
- **Generic validation**: fallback валидация может быть недостаточной для сложных полей

**Тесты / рекомендации по покрытию**

- **Specialized field тесты**: корректность валидации email, password, confirmPassword, captcha
- **Generic field тесты**: правильность fallback валидации
- **Localization тесты**: корректность локализованных сообщений об ошибках
- **Edge cases тесты**: обработка null, undefined, empty values
- **Integration тесты**: совместимость с Zod схемами

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная логика определения типов полей и валидации

**TODO / Рефакторинг**

- ✅ Модульная архитектура с разделением по типам полей
- ✅ Интеграция с next-intl для локализации
- ✅ Специализированная обработка критически важных полей
- ✅ Fallback валидация для общих полей
- ✅ Использование констант для консистентности
- ✅ Generic типизация для гибкости
- 💡 Рекомендация: улучшить type safety для fieldValue
- 💡 Рекомендация: добавить регистрацию кастомных валидаторов
- 💡 Рекомендация: рассмотреть использование enum для типов полей
- 💡 Рекомендация: добавить кеширование результатов валидации
