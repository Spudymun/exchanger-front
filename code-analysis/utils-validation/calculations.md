### Путь: packages/utils/src/calculations.ts

**Краткое назначение (1 предложение)**
Унифицированные утилиты расчетов для устранения дублирования логики комиссий и процентов.

**Подробное описание (3–6 предложений)**
Файл содержит централизованные функции для финансовых расчетов, устраняя дублирование логики между exchange-core утилитами. Включает расчет суммы комиссии, чистой суммы после вычета комиссии и обратный расчет валовой суммы. Использует константы из @repo/constants для обеспечения консистентности процентных вычислений. Предоставляет функцию форматирования комиссионной ставки для отображения. Все функции являются чистыми без побочных эффектов.

**Экспортируемые сущности / API (все на 100%)**

- `calculateCommissionAmount(grossAmount, commissionRate)` - расчет суммы комиссии
- `calculateNetAmount(grossAmount, commissionRate)` - расчет чистой суммы после комиссии
- `calculateGrossAmountFromNet(netAmount, commissionRate)` - обратный расчет валовой суммы
- `formatCommissionRate(commissionRate)` - форматирование ставки комиссии для отображения

**Входы (expected inputs) / Параметры (все на 100%)**

- `grossAmount: number` - валовая сумма для расчета комиссии
- `commissionRate: number` - ставка комиссии в процентах (например, 2.5 для 2.5%)
- `netAmount: number` - чистая сумма для обратного расчета

**Выходы / Побочные эффекты (все на 100%)**

- `calculateCommissionAmount` возвращает `number` - сумма комиссии
- `calculateNetAmount` возвращает `number` - чистая сумма
- `calculateGrossAmountFromNet` возвращает `number` - валовая сумма
- `formatCommissionRate` возвращает `string` - отформатированная ставка (например, "2.5%")
- Нет побочных эффектов - чистые функции

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: @repo/constants (PERCENTAGE_CALCULATIONS)
- **Потенциальные потребители**:
  - exchange-core/utils/calculations.ts
  - apps/web/routers/\* (API роутеры)
  - Компоненты отображения комиссий
  - Калькуляторы обменных курсов
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Примеры использования:
const grossAmount = 1000; // $1000
const commissionRate = 2.5; // 2.5%

// Расчет комиссии: 1000 * (2.5 / 100) = 25
const commission = calculateCommissionAmount(grossAmount, commissionRate); // 25

// Чистая сумма: 1000 - 25 = 975
const netAmount = calculateNetAmount(grossAmount, commissionRate); // 975

// Обратный расчет: 975 / (1 - 2.5/100) = 1000
const originalGross = calculateGrossAmountFromNet(netAmount, commissionRate); // 1000

// Форматирование: "2.5%"
const formatted = formatCommissionRate(commissionRate); // "2.5%"
```

**Риски и безопасность**

- **Средний риск**: влияет на финансовые расчеты
- **Mathematical accuracy**: критически важна точность вычислений
- **Division by zero**: потенциальный риск в calculateGrossAmountFromNet при commissionRate = 100%
- **Floating point precision**: возможные ошибки округления
- **Business logic**: неправильные расчеты влияют на доходность

**Тесты / рекомендации по покрытию**

- **Mathematical accuracy тесты**: проверка точности всех расчетов
- **Edge case тесты**: граничные значения (0%, 100% комиссии)
- **Reverse calculation тесты**: проверка обратимости расчетов
- **Floating point тесты**: проверка округления
- **Integration тесты**: использование в реальных бизнес-сценариях

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важные финансовые расчеты

**TODO / Рефакторинг**

- ✅ Отличная централизация расчетной логики
- ✅ Устранение дублирования между модулями
- ✅ Использование констант из @repo/constants
- ✅ Чистые функции без побочных эффектов
- ⚠️ Потенциальный риск деления на ноль при commissionRate = 100%
- 💡 Рекомендация: добавить валидацию входных параметров
- 💡 Рекомендация: добавить защиту от деления на ноль
- 💡 Рекомендация: рассмотреть использование библиотеки для точных десятичных вычислений
