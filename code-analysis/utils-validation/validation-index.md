### Путь: packages/utils/src/validation/index.ts

**Краткое назначение (1 предложение)**
Центральная точка экспорта модульной системы валидации с next-intl интеграцией.

**Подробное описание (3–6 предложений)**
Файл служит единой точкой входа для модульной архитектуры валидации, разделенной на специализированные модули. Реэкспортирует все компоненты системы валидации включая константы, основные функции, обработчики полей, хуки и схемы. Обеспечивает удобный доступ к полной функциональности валидации через единый импорт. Следует принципу barrel exports для модульной архитектуры. Позволяет импортировать любые функции валидации из одного места.

**Экспортируемые сущности / API (все на 100%)**

- **Все экспорты из модулей**:
  - `constants` - константы валидации и конфигурация
  - `core` - основные функции (createNextIntlZodErrorMap, validateFormWithNextIntl)
  - `field-validation` - валидация полей форм
  - `handlers` - обработчики различных типов валидации
  - `hooks` - React хуки для валидации
  - `single-field` - валидация отдельных полей
  - `schemas-basic` - базовые Zod схемы
  - `schemas-crypto` - схемы для криптовалют
  - `schemas-composed` - составные схемы
  - `schemas-utils` - утилиты схем
  - `schema-helpers` - помощники для работы со схемами

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет собственных параметров - только реэкспорт
- Параметры зависят от конкретных функций из модулей

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет всю функциональность валидации через единый интерфейс
- Нет собственных побочных эффектов - только реэкспорт
- Обеспечивает доступ к модульной системе валидации
- Упрощает импорты для потребителей

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Реэкспортирует из**: всех модулей validation/
- **Импортируется в**: packages/utils/src/next-intl-validation.ts
- **Потенциальные потребители**: любой код, использующий валидацию с next-intl

**Домен данных / типы (все на 100%)**

```typescript
// Доступ ко всей системе валидации:
import {
  // Константы
  VALIDATION_KEYS,
  NextIntlValidationConfig,

  // Основные функции
  createNextIntlZodErrorMap,
  validateFormWithNextIntl,

  // Схемы
  emailSchema,
  passwordSchema,
  cryptoAmountSchema,

  // Хуки
  useFormWithNextIntl,

  // Обработчики
  handleEmailValidation,
  handlePasswordValidation,
} from '@repo/utils/validation';
```

**Риски и безопасность**

- **Низкий риск**: только реэкспорт без собственной логики
- **Module organization**: улучшает организацию валидации
- **Dependency management**: централизует доступ к валидации
- **Backward compatibility**: обеспечивает стабильный интерфейс

**Тесты / рекомендации по покрытию**

- **Export тесты**: проверка доступности всех реэкспортов
- **Module loading тесты**: правильность загрузки всех модулей
- **Integration тесты**: корректность работы модульной системы

**Оценка сложности (low/medium/high)**
**LOW** - простой реэкспорт для модульной архитектуры

**TODO / Рефакторинг**

- ✅ Отличная модульная организация валидации
- ✅ Единая точка доступа к системе валидации
- ✅ Следует принципу barrel exports
- 💡 Рекомендация: добавить JSDoc с описанием модулей
- 💡 Рекомендация: рассмотреть группировку экспортов по категориям
