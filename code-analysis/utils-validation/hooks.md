### Путь: packages/utils/src/validation/hooks.ts

**Краткое назначение (1 предложение)**
React хук для централизованного управления валидацией форм с next-intl локализацией и интеграцией с Zod схемами.

**Подробное описание (3–6 предложений)**
Хук useNextIntlValidation предоставляет централизованное управление валидацией форм с поддержкой локализации через next-intl и интеграцией с Zod схемами. Включает функции validateForm для валидации всей формы и validateField для валидации отдельных полей с автоматическим обновлением состояния ошибок. Использует мемоизацию через React.useMemo и React.useCallback для оптимизации производительности и предотвращения лишних ре-рендеров. Преобразует ошибки Zod в формат, подходящий для форм, через convertZodErrorsToFormErrors с правильной обработкой путей ошибок. Реализует иммутабельное обновление ошибок через applyFieldErrorUpdate без использования динамического индексирования для type safety.

**Экспортируемые сущности / API (все на 100%)**

- `export function useNextIntlValidation<T>(params)` — основной хук валидации
- `export interface UseNextIntlValidationParams<T>` — интерфейс параметров хука

**Входы (expected inputs) / Параметры (все на 100%)**

- `validationSchema?: z.ZodSchema<T>` — опциональная Zod схема для валидации
- `values: T` — текущие значения формы
- `setErrors: (errors) => void` — функция обновления ошибок формы
- `t: (key, values?) => string` — функция локализации из next-intl
- `locale?: string` — опциональная локаль для конфигурации

**Выходы / Побочные эффекты (все на 100%)**

- `validateForm: () => boolean` — функция валидации всей формы
- `validateField: (field: keyof T) => boolean` — функция валидации отдельного поля
- Обновляет состояние ошибок через setErrors при валидации
- Возвращает true при успешной валидации, false при ошибках

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: React компонентах форм для управления валидацией
- **Использует**: validateFormWithNextIntl из core для валидации формы
- **Использует**: validateFieldWithNextIntl из field-validation для полей
- **Интегрируется**: с NextIntlValidationConfig для локализации
- **Часть**: React хуков системы валидации utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Параметры хука
interface UseNextIntlValidationParams<T> {
  validationSchema?: z.ZodSchema<T>;
  values: T;
  setErrors: (
    errors:
      | Partial<Record<keyof T, string>>
      | ((prev: Partial<Record<keyof T, string>>) => Partial<Record<keyof T, string>>)
  ) => void;
  t: (key: string, values?: Record<string, string | number>) => string;
  locale?: string;
}

// Возвращаемый объект
interface UseNextIntlValidationReturn<T> {
  validateForm: () => boolean;
  validateField: (field: keyof T) => boolean;
}

// Внутренние типы
type FormErrors<T> = Partial<Record<keyof T, string>>;
type ErrorUpdateFunction<T> = (prev: FormErrors<T>) => FormErrors<T>;
```

**Риски и безопасность**

- **Performance**: мемоизация может не работать при частых изменениях t функции
- **Type safety**: использует type assertions для преобразования ошибок
- **State management**: сложная логика обновления ошибок может приводить к багам
- **Memory leaks**: callback функции могут создавать утечки памяти
- **Validation consistency**: зависит от правильности core и field-validation модулей

**Тесты / рекомендации по покрытию**

- **Form validation тесты**: корректность validateForm для различных схем
- **Field validation тесты**: правильность validateField для отдельных полей
- **Error conversion тесты**: корректность convertZodErrorsToFormErrors
- **Memoization тесты**: эффективность мемоизации callback функций
- **State update тесты**: правильность иммутабельного обновления ошибок

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная логика React хука с мемоизацией и state management

**TODO / Рефакторинг**

- ✅ Централизованное управление валидацией с локализацией
- ✅ Мемоизация для оптимизации производительности
- ✅ Иммутабельное обновление ошибок без динамического индексирования
- ✅ Интеграция с core и field-validation модулями
- ✅ Type safety через generic параметры
- ✅ Поддержка как полной, так и частичной валидации
- 💡 Рекомендация: добавить debounce для validateField
- 💡 Рекомендация: улучшить type safety для error conversion
- 💡 Рекомендация: рассмотреть использование useReducer для сложного state
- 💡 Рекомендация: добавить поддержку async валидации
