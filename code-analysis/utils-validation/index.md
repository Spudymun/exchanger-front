### Путь: packages/utils/src/index.ts

**Краткое назначение (1 предложение)**
Центральная точка экспорта всех утилит пакета, обеспечивающая единый интерфейс для импорта.

**Подробное описание (3–6 предложений)**
Файл служит единой точкой входа для всех утилит проекта, реэкспортируя содержимое всех модулей пакета. Организован по логическим группам: базовые утилиты (env, formatting, calculations), валидация (validation-helpers, validation-schemas, next-intl-validation), бизнес-логика (order-utils, order-status), технические утилиты (trpc-errors, store-factory, input-validation, scroll-utils). Следует принципу barrel exports для удобного импорта из других пакетов. Содержит комментарий об удаленном файле server-i18n-errors.ts с рекомендацией использовать next-intl/server.

**Экспортируемые сущности / API (все на 100%)**

- **Базовые утилиты**: env, formatting, calculations
- **Валидация**: validation-helpers, validation-schemas, next-intl-validation, validation
- **Бизнес-логика**: order-utils, order-status
- **Технические**: trpc-errors, store-factory, input-validation, scroll-utils

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - только реэкспорты
- Импортируется как: `import { formatCurrency, validateEmail } from '@repo/utils'`

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет все утилиты через единый интерфейс
- Нет побочных эффектов - только реэкспорты
- Обеспечивает tree-shaking для неиспользуемых утилит
- Упрощает импорты в других пакетах

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Реэкспортирует из**: всех файлов в packages/utils/src/
- **Импортируется в**: @repo/ui, @repo/hooks, @repo/exchange-core, apps/\*
- **Основная точка доступа** для внешних пакетов к утилитам

**Домен данных / типы (все на 100%)**

```typescript
// Все экспорты доступны через единый интерфейс:
import {
  // Базовые утилиты
  getEnvVar,
  formatCurrency,
  calculateCommissionAmount,
  // Валидация
  validateEmail,
  createNextIntlZodErrorMap,
  loginSchema,
  // Бизнес-логика
  getOrderStatusLabel,
  createOrderId,
  // Технические
  createTRPCError,
  createStore,
  validateNumericInput,
} from '@repo/utils';
```

**Риски и безопасность**

- **Низкий риск**: только реэкспорты без логики
- **Dependency management**: централизованный доступ к утилитам
- **Version control**: изменения в одном месте влияют на весь проект
- **Tree-shaking**: неиспользуемые утилиты исключаются из бандла

**Тесты / рекомендации по покрытию**

- **Export тесты**: проверка доступности всех экспортов
- **Integration тесты**: проверка импортов в других пакетах
- **Bundle тесты**: проверка tree-shaking
- **Type тесты**: проверка корректности типов экспортов

**Оценка сложности (low/medium/high)**
**LOW** - простые реэкспорты без логики

**TODO / Рефакторинг**

- ✅ Хорошо организовано по логическим группам
- ✅ Следует принципу barrel exports
- ✅ Поддерживает tree-shaking
- ✅ Комментарий об удаленном файле
- 💡 Рекомендация: рассмотреть группировку экспортов по namespace'ам для больших проектов
