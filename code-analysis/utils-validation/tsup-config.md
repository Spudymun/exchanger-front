### Путь: packages/constants/tsup.config.ts

**Краткое назначение (1 предложение)**
Конфигурация tsup для сборки constants пакета в CommonJS и ESM форматы с генерацией типов.

**Подробное описание (3–6 предложений)**
Файл содержит конфигурацию сборки для constants пакета с использованием tsup bundler. Настроен на сборку в оба формата (CommonJS и ESM) для максимальной совместимости с различными системами модулей. Включает генерацию TypeScript деклараций (.d.ts файлов) и source maps для отладки. Исключает React зависимости как external, поскольку constants пакет не должен их содержать. Использует @ts-nocheck для подавления проверок типов в конфигурационном файле.

**Экспортируемые сущности / API (все на 100%)**

- Экспортирует конфигурацию tsup через `defineConfig`
- Настройки сборки: entry, format, dts, splitting, sourcemap, clean, external

**Входы (expected inputs) / Параметры (все на 100%)**

- `entry: ['src/index.ts']` - точка входа для сборки
- `format: ['cjs', 'esm']` - форматы вывода
- `external: ['react', 'react-dom']` - внешние зависимости

**Выходы / Побочные эффекты (все на 100%)**

- Генерирует скомпилированные файлы в dist/ директории
- Создает .d.ts файлы для TypeScript типов
- Генерирует source maps для отладки
- Очищает dist/ перед сборкой (clean: true)

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: npm run build в constants пакете
- **Импортирует**: tsup (defineConfig)
- **Конфигурирует**: сборку constants пакета

**Домен данных / типы (все на 100%)**

```typescript
// Конфигурация tsup
{
  entry: ['src/index.ts'],        // Входной файл
  format: ['cjs', 'esm'],         // CommonJS и ESM
  dts: true,                      // Генерация .d.ts
  splitting: false,               // Без code splitting
  sourcemap: true,                // Source maps
  clean: true,                    // Очистка dist/
  external: ['react', 'react-dom'] // Внешние зависимости
}
```

**Риски и безопасность**

- **Низкий риск**: конфигурационный файл сборки
- **Build consistency**: обеспечивает консистентную сборку
- **Type safety**: генерация .d.ts файлов для типобезопасности
- **Compatibility**: поддержка обоих форматов модулей

**Тесты / рекомендации по покрытию**

- **Build тесты**: проверка успешной сборки в оба формата
- **Type generation тесты**: корректность генерируемых .d.ts файлов
- **External dependencies тесты**: правильность исключения внешних зависимостей

**Оценка сложности (low/medium/high)**
**LOW** - простая конфигурация сборки

**TODO / Рефакторинг**

- ✅ Правильная конфигурация для library пакета
- ✅ Поддержка обоих форматов модулей
- ✅ Генерация типов для TypeScript
- ✅ Исключение React зависимостей (правильно для constants)
- 💡 Рекомендация: рассмотреть удаление @ts-nocheck при исправлении типов
- 💡 Рекомендация: добавить minification для production сборки
