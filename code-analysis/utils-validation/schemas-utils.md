### Путь: packages/utils/src/validation/schemas-utils.ts

**Краткое назначение (1 предложение)**
Утилитарные Zod схемы для общих операций: быстрые действия администратора, получение по ID и фильтрация по датам.

**Подробное описание (3–6 предложений)**
Модуль предоставляет простые и переиспользуемые схемы валидации для часто встречающихся операций в приложении. Включает схему quickActionsSchema для быстрых административных действий с enum типами действий и опциональными параметрами. Содержит универсальные схемы для операций с ID: getByIdSchema для общих случаев и orderByIdSchema для специфичных операций с заказами. Предоставляет dateRangeSchema для фильтрации данных по диапазону дат с опциональными границами. Использует базовую схему idSchema из schemas-basic для обеспечения консистентности валидации идентификаторов во всем приложении.

**Экспортируемые сущности / API (все на 100%)**

- `export const quickActionsSchema` — схема быстрых административных действий
- `export const getByIdSchema` — универсальная схема получения по ID
- `export const orderByIdSchema` — схема операций с заказом по ID
- `export const dateRangeSchema` — схема фильтрации по диапазону дат

**Входы (expected inputs) / Параметры (все на 100%)**

- Схемы не принимают параметры - это статические определения
- Используют базовую схему idSchema из schemas-basic

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают соответствующие Zod схемы для валидации утилитарных операций
- Обеспечивают консистентную валидацию ID операций
- Поддерживают гибкую валидацию административных действий
- Предоставляют опциональную валидацию диапазонов дат

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: API endpoints, административных панелях, фильтрах данных
- **Импортирует**: idSchema из schemas-basic для консистентности
- **Используется**: в tRPC роутерах для валидации входных параметров
- **Часть**: утилитарной системы валидации utils пакета
- **Переиспользуется**: в различных частях приложения для общих операций

**Домен данных / типы (все на 100%)**

```typescript
// Схема быстрых действий
interface QuickActions {
  action: 'REFRESH_RATES' | 'CLEAR_CACHE' | 'SEND_NOTIFICATION';
  params?: Record<string, any>;
}

// Схема получения по ID
interface GetById {
  id: string;
}

// Схема операций с заказом
interface OrderById {
  orderId: string;
}

// Схема диапазона дат
interface DateRange {
  dateFrom?: string;
  dateTo?: string;
}

// Типы действий администратора
type AdminAction = 'REFRESH_RATES' | 'CLEAR_CACHE' | 'SEND_NOTIFICATION';

// Параметры действий
type ActionParams = Record<string, any>;
```

**Риски и безопасность**

- **Минимальный риск**: простые утилитарные схемы без сложной логики
- **Admin actions**: quickActionsSchema может требовать дополнительной авторизации
- **Date validation**: dateRangeSchema не валидирует формат дат
- **Params validation**: использует z.any() что может быть небезопасно
- **ID consistency**: зависит от правильности базовой idSchema

**Тесты / рекомендации по покрытию**

- **Schema validation тесты**: корректность всех утилитарных схем
- **Admin actions тесты**: правильность валидации административных действий
- **ID operations тесты**: корректность схем операций с ID
- **Date range тесты**: валидация диапазонов дат
- **Integration тесты**: совместимость с API endpoints

**Оценка сложности (low/medium/high)**
**LOW** - простые утилитарные схемы без сложной логики

**TODO / Рефакторинг**

- ✅ Простые и переиспользуемые схемы для общих операций
- ✅ Использование базовых схем для консистентности
- ✅ Опциональные параметры для гибкости
- ✅ Enum типы для административных действий
- ✅ Четкие названия схем по назначению
- 💡 Рекомендация: улучшить валидацию params в quickActionsSchema
- 💡 Рекомендация: добавить валидацию формата дат в dateRangeSchema
- 💡 Рекомендация: рассмотреть добавление схем для пагинации
- 💡 Рекомендация: добавить схемы для сортировки и фильтрации
