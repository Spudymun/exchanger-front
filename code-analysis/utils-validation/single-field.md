### Путь: packages/utils/src/validation/single-field.ts

**Краткое назначение (1 предложение)**
Функции валидации отдельных полей с детальной обработкой ошибок и локализацией через next-intl.

**Подробное описание (3–6 предложений)**
Модуль предоставляет специализированные функции для валидации критически важных полей: email, password, confirmPassword и captcha с детальной обработкой различных типов ошибок Zod. Каждая функция использует соответствующую схему из validation-schemas, выполняет safeParse и возвращает структурированный результат с локализованным сообщением об ошибке. Включает сложную логику обработки ошибок паролей через getPasswordErrorMessage с проверкой минимальной длины, regex валидации и интеграцией с VALIDATION_LIMITS. Реализует специальную логику для confirmPassword с проверкой совпадения с основным паролем и для captcha с обработкой различных типов ошибок валидации. Использует константы VALIDATION_KEYS для консистентности ключей локализации и обеспечивает fallback сообщения при неопределенных ошибках.

**Экспортируемые сущности / API (все на 100%)**

- `export function validateSingleEmail(value, config)` — валидация email поля
- `export function validateSinglePassword(value, config)` — валидация password поля
- `export function validateSingleConfirmPassword(value, passwordValue, config)` — валидация подтверждения пароля
- `export function validateSingleCaptcha(value, config)` — валидация captcha поля

**Входы (expected inputs) / Параметры (все на 100%)**

- `value: string` — значение валидируемого поля
- `config: NextIntlValidationConfig` — конфигурация с функцией локализации
- `passwordValue: string` (для confirmPassword) — значение основного пароля для сравнения

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают `{ isValid: boolean; error: string | null }`
- `isValid: true` при успешной валидации
- `error: string` с локализованным сообщением об ошибке при неудаче
- `error: null` при успешной валидации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: field-validation модуле для специализированной валидации
- **Импортирует**: emailSchema, passwordSchema, captchaSchema из validation-schemas
- **Использует**: VALIDATION_LIMITS из @repo/constants для ограничений
- **Интегрируется**: с NextIntlValidationConfig и VALIDATION_KEYS для локализации
- **Часть**: системы детальной валидации отдельных полей utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Результат валидации
interface SingleFieldValidationResult {
  isValid: boolean;
  error: string | null;
}

// Конфигурация локализации
interface NextIntlValidationConfig {
  t: (key: string, values?: Record<string, any>) => string;
  locale?: string;
}

// Типы ошибок Zod
enum ZodIssueCode {
  too_small = 'too_small',
  invalid_string = 'invalid_string',
  custom = 'custom',
}

// Специализированные поля
type ValidatableField = 'email' | 'password' | 'confirmPassword' | 'captcha';

// Ключи валидации
const VALIDATION_KEYS = {
  EMAIL_REQUIRED: 'validation.email.required',
  EMAIL_INVALID: 'validation.email.invalid',
  PASSWORD_MIN_LENGTH: 'validation.password.minLength',
  PASSWORD_WEAK: 'validation.password.weak',
  CONFIRM_PASSWORD_REQUIRED: 'validation.confirmPassword.required',
  CONFIRM_PASSWORD_NO_MATCH: 'validation.confirmPassword.noMatch',
  CAPTCHA_REQUIRED: 'validation.captcha.required',
  CAPTCHA_NOT_VERIFIED: 'validation.captcha.notVerified',
};
```

**Риски и безопасность**

- **Password validation**: критически важна для безопасности аутентификации
- **Schema dependency**: зависит от правильности схем из validation-schemas
- **Error handling**: должна покрывать все возможные типы ошибок Zod
- **Localization dependency**: критически зависит от наличия переводов
- **Type safety**: использует type assertions для error properties

**Тесты / рекомендации по покрытию**

- **Email validation тесты**: корректность валидации всех типов email ошибок
- **Password validation тесты**: правильность обработки сложности паролей
- **Confirm password тесты**: корректность проверки совпадения паролей
- **Captcha validation тесты**: правильность валидации captcha полей
- **Localization тесты**: корректность всех локализованных сообщений

**Оценка сложности (low/medium/high)**
**MEDIUM** - детальная обработка различных типов ошибок валидации

**TODO / Рефакторинг**

- ✅ Специализированные функции для каждого типа поля
- ✅ Детальная обработка различных типов ошибок Zod
- ✅ Интеграция с константами для ограничений и ключей
- ✅ Локализация всех сообщений об ошибках
- ✅ Fallback сообщения при неопределенных ошибках
- ✅ Специальная логика для confirmPassword и captcha
- 💡 Рекомендация: улучшить type safety для error properties
- 💡 Рекомендация: добавить кеширование результатов валидации
- 💡 Рекомендация: рассмотреть добавление async валидации
- 💡 Рекомендация: добавить поддержку кастомных валидаторов
