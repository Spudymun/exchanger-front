### Путь: packages/utils/src/trpc-errors.ts

**Краткое назначение (1 предложение)**
Стандартизированные фабрики для создания tRPC ошибок с централизованными паттернами для обеспечения консистентности.

**Подробное описание (3–6 предложений)**
Файл содержит фабрики для создания типизированных tRPC ошибок, устраняя дублирование паттернов обработки ошибок в API роутерах. Включает стандартные функции для всех типов HTTP ошибок (NOT_FOUND, BAD_REQUEST, UNAUTHORIZED и др.) с готовыми сообщениями. Предоставляет legacy функции для обратной совместимости с доменно-специфичными ошибками (user, order, support, security). Исправлена зависимость от server-i18n-errors - теперь принимает готовые сообщения для большей гибкости. Обеспечивает консистентность обработки ошибок во всех tRPC процедурах.

**Экспортируемые сущности / API (все на 100%)**

- **Стандартные фабрики**:
  - `createNotFoundError(message)` - ошибка "Не найдено" (NOT_FOUND)
  - `createBadRequestError(message)` - ошибка "Неверный запрос" (BAD_REQUEST)
  - `createInternalServerError(message)` - внутренняя ошибка сервера
  - `createUnauthorizedError(message)` - ошибка "Не авторизован"
  - `createForbiddenError(message)` - ошибка "Доступ запрещен"
  - `createConflictError(message)` - ошибка "Конфликт"
  - `createValidationError(message)` - ошибка валидации (алиас для BAD_REQUEST)
  - `createRateLimitError(message)` - ошибка превышения лимитов
- **Legacy фабрики**:
  - `createUserError(type, identifier?)` - пользовательские ошибки
  - `createOrderError(type, identifier?)` - ошибки заказов
  - `createSupportError(type, identifier?)` - ошибки поддержки
  - `createSecurityError(type)` - ошибки безопасности

**Входы (expected inputs) / Параметры (все на 100%)**

- `message: string` - готовое сообщение об ошибке для стандартных фабрик
- `type: string` - тип ошибки для legacy фабрик ('not_found', 'already_exists', и др.)
- `identifier?: string` - опциональный идентификатор для контекста ошибки

**Выходы / Побочные эффекты (все на 100%)**

- Все функции возвращают `TRPCError` с соответствующим кодом и сообщением
- Нет побочных эффектов - чистые функции создания ошибок
- Legacy функции автоматически генерируют сообщения на основе типа и идентификатора

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: @trpc/server (TRPCError)
- **Потенциальные потребители**:
  - apps/web/src/server/trpc/routers/\* (все tRPC роутеры)
  - apps/web/src/server/trpc/middleware/\* (middleware авторизации)
  - Любые tRPC процедуры с обработкой ошибок
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Стандартные ошибки
throw createNotFoundError('User not found');
// TRPCError { code: 'NOT_FOUND', message: 'User not found' }

throw createUnauthorizedError('Invalid credentials');
// TRPCError { code: 'UNAUTHORIZED', message: 'Invalid credentials' }

throw createValidationError('Invalid email format');
// TRPCError { code: 'BAD_REQUEST', message: 'Invalid email format' }

// Legacy ошибки (обратная совместимость)
throw createUserError('not_found', 'user123');
// TRPCError { code: 'NOT_FOUND', message: 'User with ID "user123" not found' }

throw createOrderError('cannot_cancel');
// TRPCError { code: 'BAD_REQUEST', message: 'Order cannot be cancelled in current status' }

throw createSecurityError('invalid_password');
// TRPCError { code: 'UNAUTHORIZED', message: 'Invalid current password' }
```

**Риски и безопасность**

- **Средний риск**: влияет на обработку ошибок во всех API
- **Error consistency**: обеспечивает единообразие ошибок в API
- **Security**: правильные HTTP коды для различных типов ошибок
- **Information disclosure**: сообщения не должны раскрывать чувствительную информацию
- **Client handling**: консистентные коды ошибок упрощают обработку на клиенте

**Тесты / рекомендации по покрытию**

- **Error creation тесты**: проверка создания всех типов ошибок
- **HTTP codes тесты**: корректность кодов для каждого типа ошибки
- **Message formatting тесты**: правильность формирования сообщений
- **Legacy compatibility тесты**: обратная совместимость legacy функций
- **Integration тесты**: использование в реальных tRPC процедурах

**Оценка сложности (low/medium/high)**
**MEDIUM** - множественные фабрики с legacy поддержкой

**TODO / Рефакторинг**

- ✅ Отличная централизация паттернов ошибок
- ✅ Исправлена зависимость от server-i18n-errors
- ✅ Поддержка обратной совместимости через legacy функции
- ✅ Стандартизированные HTTP коды для всех типов ошибок
- ✅ Гибкость через принятие готовых сообщений
- 💡 Рекомендация: добавить поддержку структурированных ошибок с дополнительными полями
- 💡 Рекомендация: рассмотреть постепенную миграцию от legacy функций
- 💡 Рекомендация: добавить валидацию сообщений об ошибках
