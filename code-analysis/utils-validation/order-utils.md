### Путь: packages/utils/src/order-utils.ts

**Краткое назначение (1 предложение)**
Комплексные утилиты для обработки заказов включающие сортировку, фильтрацию, пагинацию и статистику.

**Подробное описание (3–6 предложений)**
Файл содержит полный набор утилит для работы с заказами в административных и пользовательских интерфейсах. Включает гибкую систему сортировки по дате и сумме, многокритериальную фильтрацию по статусу, email, датам и суммам. Предоставляет универсальную пагинацию с поддержкой как offset-based, так и cursor-based подходов. Содержит функцию комбинированной обработки заказов и генерации статистики. Использует типизированные интерфейсы для всех опций и результатов.

**Экспортируемые сущности / API (все на 100%)**

- **Типы**: `OrderSortOption`, `OrderFilterOptions`, `PaginationOptions`, `PaginatedOrdersResult<T>`
- **Сортировка**: `sortOrders(orders, sortBy)` - сортировка по различным критериям
- **Фильтрация**: `filterOrders(orders, filters)` - многокритериальная фильтрация
- **Пагинация**: `paginateOrders<T>(orders, pagination, getId?)` - универсальная пагинация
- **Комбинированная обработка**: `processOrders<T>(orders, options)` - фильтрация + сортировка + пагинация
- **Статистика**: `getOrdersStatistics(orders)` - подробная статистика заказов
- Вспомогательные функции: фильтры по статусу, email, дате, сумме

**Входы (expected inputs) / Параметры (все на 100%)**

- `orders: Order[]` или `T[]` - массив заказов для обработки
- `sortBy: OrderSortOption` - опция сортировки ('newest', 'oldest', 'amount-high', 'amount-low')
- `filters: OrderFilterOptions` - критерии фильтрации (status, email, dateRange, amountRange)
- `pagination: PaginationOptions` - настройки пагинации (limit, offset?, cursor?)
- `getId?: (item: T) => string` - функция получения ID для cursor-based пагинации

**Выходы / Побочные эффекты (все на 100%)**

- `sortOrders` возвращает `Order[]` - отсортированный массив
- `filterOrders` возвращает `Order[]` - отфильтрованный массив
- `paginateOrders` возвращает `PaginatedOrdersResult<T>` - результат с пагинацией
- `processOrders` возвращает `PaginatedOrdersResult<T>` - комбинированный результат
- `getOrdersStatistics` возвращает объект со статистикой (total, byStatus, totalVolume, averageAmount, today)
- Нет побочных эффектов - чистые функции

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**:
  - @repo/constants (OrderStatus, ORDER_STATUSES)
  - @repo/exchange-core (Order)
- **Потенциальные потребители**:
  - API роутеры для обработки запросов списков заказов
  - Админ-панель для управления заказами
  - Компоненты таблиц и списков заказов
  - Dashboard для отображения статистики
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Сортировка
const sorted = sortOrders(orders, 'newest'); // Order[] по дате создания

// Фильтрация
const filtered = filterOrders(orders, {
  status: 'pending',
  email: 'user@example.com',
  fromDate: new Date('2024-01-01'),
  minAmount: 100,
}); // Order[]

// Пагинация
const paginated = paginateOrders(orders, { limit: 20, offset: 0 });
// { items: Order[], total: number, hasMore: boolean, nextCursor?: string }

// Комбинированная обработка
const processed = processOrders(orders, {
  filters: { status: 'completed' },
  sortBy: 'amount-high',
  pagination: { limit: 10 },
}); // PaginatedOrdersResult<Order>

// Статистика
const stats = getOrdersStatistics(orders);
// {
//   total: 150,
//   byStatus: { pending: 10, completed: 140 },
//   totalVolume: 1500000,
//   averageAmount: 10000,
//   today: 5
// }
```

**Риски и безопасность**

- **Средний риск**: обрабатывает критически важные данные заказов
- **Performance**: неэффективная обработка больших массивов может замедлить UI
- **Data integrity**: фильтрация должна корректно обрабатывать все типы данных
- **Memory usage**: пагинация предотвращает загрузку всех данных сразу
- **Type safety**: generic функции обеспечивают типобезопасность

**Тесты / рекомендации по покрытию**

- **Sorting тесты**: корректность всех типов сортировки
- **Filtering тесты**: все комбинации фильтров
- **Pagination тесты**: offset и cursor-based пагинация
- **Statistics тесты**: правильность расчета статистики
- **Performance тесты**: обработка больших массивов данных
- **Edge case тесты**: пустые массивы, некорректные данные

**Оценка сложности (low/medium/high)**
**HIGH** - сложная система обработки данных с множественными алгоритмами

**TODO / Рефакторинг**

- ✅ Отличная типизация с generic функциями
- ✅ Поддержка как offset, так и cursor-based пагинации
- ✅ Комбинированная функция для удобства использования
- ✅ Comprehensive статистика заказов
- ✅ Модульная архитектура с отдельными функциями
- 💡 Рекомендация: добавить индексы для оптимизации сортировки больших массивов
- 💡 Рекомендация: рассмотреть добавление кеширования для часто используемых фильтров
- 💡 Рекомендация: добавить валидацию входных параметров
