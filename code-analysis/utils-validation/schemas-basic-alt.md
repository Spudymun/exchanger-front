### Путь: packages/utils/src/validation/schemas/basic.ts

**Краткое назначение (1 предложение)**
Альтернативная версия базовых Zod схем валидации с усиленными требованиями безопасности и интеграцией с VALIDATION_LIMITS.

**Подробное описание (3–6 предложений)**
Модуль предоставляет альтернативную реализацию базовых схем валидации с фокусом на безопасность и соответствие современным стандартам OWASP. Включает строгую email схему без хардкода сообщений для интеграции с createNextIntlZodErrorMap и усиленную схему паролей с комплексным regex для проверки всех требований безопасности. Реализует двухуровневую систему паролей: новую схему с полными требованиями и legacy схему для совместимости с существующими аккаунтами. Использует константы из VALIDATION_LIMITS для всех ограничений длины, обеспечивая централизованное управление лимитами. Предоставляет базовые схемы для ID и имен пользователей с соответствующими ограничениями и regex валидацией.

**Экспортируемые сущности / API (все на 100%)**

- `export const emailSchema` — строгая схема email без хардкода сообщений
- `export const passwordSchema` — усиленная схема пароля с OWASP требованиями
- `export const newPasswordSchema` — алиас passwordSchema для ясности
- `export const legacyPasswordSchema` — схема для совместимости с существующими паролями
- `export const usernameSchema` — схема имени пользователя с regex
- `export const idSchema` — базовая схема ID

**Входы (expected inputs) / Параметры (все на 100%)**

- Схемы не принимают параметры - это статические определения
- Используют константы из VALIDATION_LIMITS для ограничений

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают соответствующие Zod схемы для валидации
- Обеспечивают строгую валидацию согласно OWASP стандартам
- Поддерживают как новые, так и legacy требования
- Интегрируются с createNextIntlZodErrorMap для локализации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Альтернатива**: schemas-basic.ts в корне validation директории
- **Импортирует**: VALIDATION_LIMITS из @repo/constants
- **Используется**: в формах и API для строгой валидации
- **Интегрируется**: с createNextIntlZodErrorMap для сообщений об ошибках
- **Часть**: альтернативной системы базовых схем utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Схемы валидации
type EmailSchema = z.ZodString; // min, max, email
type PasswordSchema = z.ZodString; // min, max, complex regex
type NewPasswordSchema = z.ZodString; // алиас passwordSchema
type LegacyPasswordSchema = z.ZodString; // min, max только
type UsernameSchema = z.ZodString; // min, max, alphanumeric regex
type IdSchema = z.ZodString; // min(1)

// Константы из VALIDATION_LIMITS
const LIMITS = {
  EMAIL_MAX_LENGTH: number,
  PASSWORD_MIN_LENGTH: number,
  PASSWORD_MAX_LENGTH: number,
  USERNAME_MIN_LENGTH: number,
  USERNAME_MAX_LENGTH: number,
};

// OWASP compliant regex для паролей
const PASSWORD_REGEX =
  /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[!@#$%^&*()_+\-=[\]{};':"\\|,.<>/?])[A-Za-z\d!@#$%^&*()_+\-=[\]{};':"\\|,.<>/?]{8,}$/;

// Username regex
const USERNAME_REGEX = /^[a-zA-Z0-9_-]+$/;
```

**Риски и безопасность**

- **Password security**: комплексный regex может быть подвержен ReDoS атакам
- **Legacy compatibility**: legacy пароли менее безопасны
- **Regex complexity**: сложные паттерны могут быть трудны для понимания
- **OWASP compliance**: требует регулярного обновления согласно стандартам
- **Constants dependency**: критически зависит от VALIDATION_LIMITS

**Тесты / рекомендации по покрытию**

- **Schema validation тесты**: корректность всех базовых схем
- **OWASP compliance тесты**: соответствие стандартам безопасности
- **Regex security тесты**: проверка на ReDoS уязвимости
- **Legacy compatibility тесты**: совместимость с существующими данными
- **Constants integration тесты**: правильность использования VALIDATION_LIMITS

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложные regex паттерны и требования безопасности

**TODO / Рефакторинг**

- ✅ Интеграция с VALIDATION_LIMITS для централизованных лимитов
- ✅ OWASP compliant regex для паролей
- ✅ Двухуровневая система паролей (новые/legacy)
- ✅ Отсутствие хардкода сообщений для локализации
- ✅ Строгая email валидация с length ограничениями
- ✅ Username схема с alphanumeric regex
- 💡 Рекомендация: проверить regex на ReDoS уязвимости
- 💡 Рекомендация: рассмотреть упрощение password regex
- 💡 Рекомендация: добавить поддержку unicode в username
- 💡 Рекомендация: регулярно обновлять согласно OWASP стандартам
