### Путь: packages/utils/src/store-factory.ts

**Краткое назначение (1 предложение)**
Централизованная фабрика для создания Zustand stores с стандартизированной конфигурацией middleware и утилитами управления таймерами.

**Подробное описание (3–6 предложений)**
Файл содержит фабрику для создания Zustand stores, устраняя дублирование паттернов создания stores во всем проекте. Предоставляет стандартизированную конфигурацию middleware включая devtools и subscribeWithSelector с SSR-безопасностью. Включает утилиты управления таймерами для централизации паттернов очистки таймеров, найденных в различных stores. Содержит debounce утилиту для действий stores, централизуя паттерн из exchange-store. Обеспечивает консистентную настройку devtools и подписок для всех stores проекта.

**Экспортируемые сущности / API (все на 100%)**

- `StoreConfig` - интерфейс конфигурации store (name, version?, enableSubscriptions?, enableDevtools?)
- `createStore<T>(config, stateCreator)` - основная фабрика создания stores
- `TimerState` - интерфейс состояния с таймерами (timers: Map<string, NodeJS.Timeout>)
- `TimerActions` - интерфейс действий с таймерами (setTimer, clearTimer, clearAllTimers)
- `createTimerActions<T>(set, get)` - создание действий управления таймерами
- `createDebounceAction<T>(options)` - создание debounced действий
- Вспомогательные функции: `createStoreWithMiddleware`, `createStoreWithConfig`

**Входы (expected inputs) / Параметры (все на 100%)**

- `config: StoreConfig | string` - конфигурация store или просто имя
- `stateCreator: StateCreator<T, [], [], T>` - функция создания состояния Zustand
- `set: (fn: (state: T) => Partial<T>) => void` - функция обновления состояния
- `get: () => T` - функция получения текущего состояния
- `options` для debounce: `{ set, get, action, delay, key? }`

**Выходы / Побочные эффекты (все на 100%)**

- `createStore` возвращает Zustand store с настроенными middleware
- `createTimerActions` возвращает `TimerActions` объект с методами управления таймерами
- `createDebounceAction` возвращает debounced функцию
- **Побочные эффекты**: создание/очистка таймеров, обновление состояния stores
- SSR-безопасность: отключает middleware на сервере

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: zustand (create, devtools, subscribeWithSelector)
- **Потенциальные потребители**:
  - packages/hooks/src/state/\* (все Zustand stores)
  - Любые новые stores в проекте
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Создание store
interface MyStore {
  count: number;
  increment: () => void;
}

const useMyStore = createStore<MyStore>('my-store', set => ({
  count: 0,
  increment: () => set(state => ({ count: state.count + 1 })),
}));

// С полной конфигурацией
const useAdvancedStore = createStore<MyStore>(
  {
    name: 'advanced-store',
    version: 2,
    enableSubscriptions: true,
    enableDevtools: true,
  },
  stateCreator
);

// Управление таймерами
interface StoreWithTimers extends TimerState {
  data: string;
}

const timerActions = createTimerActions<StoreWithTimers>(set, get);
timerActions.setTimer(
  'refresh',
  setTimeout(() => {}, 1000)
);
timerActions.clearTimer('refresh');

// Debounce действие
const debouncedAction = createDebounceAction({
  set,
  get,
  action: () => console.log('Debounced!'),
  delay: 300,
  key: 'search',
});
```

**Риски и безопасность**

- **Средний риск**: влияет на все stores проекта
- **SSR safety**: корректно обрабатывает серверное окружение
- **Memory leaks**: утилиты таймеров предотвращают утечки памяти
- **Development tools**: devtools включены только в development
- **State consistency**: стандартизированная конфигурация обеспечивает консистентность

**Тесты / рекомендации по покрытию**

- **Store creation тесты**: проверка создания stores с различными конфигурациями
- **SSR тесты**: корректное поведение на сервере
- **Timer management тесты**: правильность управления таймерами
- **Debounce тесты**: корректность debounce логики
- **Middleware тесты**: правильность применения devtools и subscriptions

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная фабрика с множественными конфигурациями и утилитами

**TODO / Рефакторинг**

- ✅ Отличная централизация паттернов создания stores
- ✅ SSR-безопасность с проверкой окружения
- ✅ Comprehensive утилиты управления таймерами
- ✅ Debounce утилита для оптимизации производительности
- ✅ Стандартизированная конфигурация middleware
- 💡 Рекомендация: добавить поддержку persist middleware
- 💡 Рекомендация: рассмотреть добавление типизированных селекторов
- 💡 Рекомендация: добавить утилиты для тестирования stores
