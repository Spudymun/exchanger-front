### Путь: packages/utils/src/validation/schema-helpers.ts

**Краткое назначение (1 предложение)**
Утилитарные функции для создания параметризованных Zod схем валидации с настраиваемыми ограничениями.

**Подробное описание (3–6 предложений)**
Модуль предоставляет набор helper функций для создания часто используемых Zod схем с настраиваемыми параметрами, избегая дублирования кода при создании схем. Включает функции для создания схем временных меток, опциональных строк с минимальной длиной, массивов ID с ограничением количества элементов и числовых диапазонов. Каждая функция принимает параметры для настройки ограничений (минимальная длина, максимальное количество элементов, диапазоны значений) и возвращает соответствующую Zod схему. Использует базовые схемы из schemas-basic (например, idSchema) для обеспечения консистентности валидации. Предназначен для упрощения создания сложных схем валидации в различных частях приложения.

**Экспортируемые сущности / API (все на 100%)**

- `export function createTimestampSchema()` — схема для временных меток
- `export function createOptionalStringSchema(minLength?)` — схема опциональной строки
- `export function createIdsArraySchema(maxItems?)` — схема массива ID
- `export function createNumberRangeSchema(min, max)` — схема числового диапазона

**Входы (expected inputs) / Параметры (все на 100%)**

- `createTimestampSchema`: нет параметров
- `createOptionalStringSchema`: `minLength = 1` — минимальная длина строки
- `createIdsArraySchema`: `maxItems = 100` — максимальное количество элементов
- `createNumberRangeSchema`: `min: number, max: number` — границы диапазона

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают соответствующие Zod схемы для валидации
- `createTimestampSchema`: `z.number().int().positive()`
- `createOptionalStringSchema`: `z.string().min(minLength).optional()`
- `createIdsArraySchema`: `z.array(idSchema).max(maxItems)`
- `createNumberRangeSchema`: `z.number().min(min).max(max)`

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: сложных схемах валидации для создания компонентов
- **Импортирует**: idSchema из schemas-basic для консистентности
- **Использует**: Zod для создания схем валидации
- **Часть**: системы helper функций для валидации utils пакета
- **Упрощает**: создание параметризованных схем в приложении

**Домен данных / типы (все на 100%)**

```typescript
// Возвращаемые типы схем
type TimestampSchema = z.ZodNumber; // int().positive()
type OptionalStringSchema = z.ZodOptional<z.ZodString>; // string().min().optional()
type IdsArraySchema = z.ZodArray<z.ZodString>; // array(idSchema).max()
type NumberRangeSchema = z.ZodNumber; // number().min().max()

// Параметры функций
interface CreateOptionalStringParams {
  minLength?: number; // default: 1
}

interface CreateIdsArrayParams {
  maxItems?: number; // default: 100
}

interface CreateNumberRangeParams {
  min: number;
  max: number;
}

// Константы по умолчанию
const DEFAULT_MIN_LENGTH = 1;
const DEFAULT_MAX_ITEMS = 100;
```

**Риски и безопасность**

- **Минимальный риск**: простые utility функции без побочных эффектов
- **Parameter validation**: не валидирует входные параметры (min < max, etc.)
- **Schema consistency**: зависит от правильности базовых схем (idSchema)
- **Default values**: жестко заданные значения по умолчанию могут не подходить всем случаям
- **Type safety**: полагается на Zod для обеспечения типобезопасности

**Тесты / рекомендации по покрытию**

- **Schema creation тесты**: корректность создания всех типов схем
- **Parameter handling тесты**: правильность обработки параметров и значений по умолчанию
- **Validation тесты**: корректность валидации созданными схемами
- **Edge cases тесты**: обработка граничных значений параметров
- **Integration тесты**: совместимость с базовыми схемами

**Оценка сложности (low/medium/high)**
**LOW** - простые utility функции для создания схем

**TODO / Рефакторинг**

- ✅ Параметризованные функции для гибкости
- ✅ Разумные значения по умолчанию
- ✅ Использование базовых схем для консистентности
- ✅ Четкие JSDoc комментарии с назначением
- ✅ Простая и понятная API
- 💡 Рекомендация: добавить валидацию входных параметров
- 💡 Рекомендация: рассмотреть добавление более сложных helper функций
- 💡 Рекомендация: добавить примеры использования в комментариях
- 💡 Рекомендация: рассмотреть создание builder pattern для сложных схем
