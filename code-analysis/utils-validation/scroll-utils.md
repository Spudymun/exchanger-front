### Путь: packages/utils/src/scroll-utils.ts

**Краткое назначение (1 предложение)**
Утилиты для плавного скролла к элементам с поддержкой offset и React refs.

**Подробное описание (3–6 предложений)**
Файл содержит утилиты для управления скроллом страницы с поддержкой современных браузерных API. Включает функцию плавного скролла к элементу с настраиваемыми опциями поведения и выравнивания. Поддерживает дополнительный offset для точного позиционирования элементов относительно фиксированных заголовков. Предоставляет удобную обертку для работы с React refs. Использует современный scrollIntoView API с fallback на window.scrollTo для случаев с offset.

**Экспортируемые сущности / API (все на 100%)**

- `ScrollToElementOptions` - интерфейс опций скролла:
  - `behavior?: ScrollBehavior` - поведение скролла ('smooth', 'auto')
  - `block?: ScrollLogicalPosition` - выравнивание по вертикали ('start', 'center', 'end', 'nearest')
  - `inline?: ScrollLogicalPosition` - выравнивание по горизонтали
  - `offset?: number` - дополнительный отступ сверху
- `scrollToElement(element, options?)` - скролл к HTML элементу
- `scrollToRef(elementRef, options?)` - скролл к элементу через React ref

**Входы (expected inputs) / Параметры (все на 100%)**

- `element: HTMLElement | null` - целевой элемент для скролла
- `elementRef: React.RefObject<HTMLElement | null>` - React ref на элемент
- `options?: ScrollToElementOptions` - опции скролла с дефолтными значениями

**Выходы / Побочные эффекты (все на 100%)**

- Обе функции возвращают `void`
- **Побочные эффекты**: изменяют позицию скролла страницы
- `scrollToElement` вызывает `element.scrollIntoView()` или `window.scrollTo()`
- `scrollToRef` извлекает элемент из ref и вызывает `scrollToElement`

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**:
  - FloatingExchangeButton.tsx (подтверждено в документации)
  - Компоненты навигации
  - Формы с валидацией (скролл к ошибкам)
  - Компоненты с якорными ссылками
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Базовое использование
scrollToElement(document.getElementById('target')); // Плавный скролл к элементу

// С опциями
scrollToElement(element, {
  behavior: 'smooth',
  block: 'center',
  offset: 80 // Отступ для фиксированного заголовка
});

// С React ref
const targetRef = useRef<HTMLDivElement>(null);
scrollToRef(targetRef, {
  behavior: 'smooth',
  block: 'start',
  offset: 100
});

// Дефолтные значения
{
  behavior: 'smooth',
  block: 'start',
  inline: 'nearest',
  offset: 0
}
```

**Риски и безопасность**

- **Низкий риск**: простые утилиты без критической логики
- **Browser compatibility**: использует современные API (scrollIntoView, scrollTo)
- **Null safety**: корректно обрабатывает null элементы
- **Performance**: плавный скролл может влиять на производительность на слабых устройствах
- **User experience**: неожиданный скролл может дезориентировать пользователя

**Тесты / рекомендации по покрытию**

- **Functionality тесты**: проверка скролла к элементам
- **Options тесты**: корректность применения всех опций
- **Null handling тесты**: поведение с null элементами и refs
- **Offset тесты**: правильность расчета позиции с offset
- **Browser compatibility тесты**: работа в разных браузерах

**Оценка сложности (low/medium/high)**
**LOW** - простые утилиты скролла с минимальной логикой

**TODO / Рефакторинг**

- ✅ Хорошая типизация с TypeScript интерфейсами
- ✅ Поддержка React refs для удобства
- ✅ Корректная обработка null значений
- ✅ Использование современных браузерных API
- ✅ Поддержка offset для фиксированных заголовков
- 💡 Рекомендация: добавить проверку поддержки scrollIntoView в браузере
- 💡 Рекомендация: рассмотреть добавление анимационных easing функций
- 💡 Рекомендация: добавить callback для завершения скролла
