### Путь: packages/utils/src/validation/handlers.ts

**Краткое назначение (1 предложение)**
Специализированные обработчики валидации для различных типов полей с поддержкой next-intl локализации и graceful fallback.

**Подробное описание (3–6 предложений)**
Модуль предоставляет набор обработчиков для специфичных типов полей: CAPTCHA, email, password, confirmPassword, amount и общих полей с детальной обработкой различных типов ошибок Zod. Каждый обработчик анализирует ZodIssue объект, определяет тип ошибки и возвращает соответствующее локализованное сообщение через next-intl функцию перевода. Включает graceful fallback механизм для CAPTCHA сообщений с английскими fallback текстами при недоступности локализации. Использует константы из VALIDATION_LIMITS и VALIDATION_KEYS для консистентности и поддерживает сложную логику валидации паролей с проверкой минимальной длины и regex паттернов. Предоставляет специализированную обработку для полей сумм с проверкой формата и положительности значений.

**Экспортируемые сущности / API (все на 100%)**

- `export function handleCaptchaValidation(issue, t)` — обработчик валидации CAPTCHA
- `export function handleEmailValidation(issue, t)` — обработчик валидации email
- `export function handlePasswordValidation(issue, t)` — обработчик валидации password
- `export function handleConfirmPasswordValidation(issue, t)` — обработчик подтверждения пароля
- `export function handleAmountValidation(issue, t)` — обработчик валидации сумм
- `export function handleGeneralValidation(issue, t)` — обработчик общих случаев

**Входы (expected inputs) / Параметры (все на 100%)**

- `issue: z.ZodIssueOptionalMessage` — объект ошибки Zod с информацией о валидации
- `t: NextIntlValidationConfig['t']` — функция локализации из next-intl

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают `{ message: string } | null`
- `{ message: string }` с локализованным сообщением об ошибке при соответствии
- `null` если обработчик не применим к данному типу ошибки
- Graceful fallback на английский для CAPTCHA при недоступности локализации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: createNextIntlZodErrorMap для обработки Zod ошибок
- **Импортирует**: VALIDATION_LIMITS из @repo/constants для лимитов
- **Использует**: VALIDATION_KEYS из constants для ключей локализации
- **Интегрируется**: с Zod для обработки различных типов ошибок валидации
- **Часть**: системы локализованной валидации utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Zod issue типы
interface z.ZodIssueOptionalMessage {
  code: z.ZodIssueCode;
  path?: (string | number)[];
  message?: string;
  validation?: string;
  minimum?: number;
  type?: string;
  expected?: string;
}

// Результат обработки
interface ValidationHandlerResult {
  message: string;
}

// Типы ошибок Zod
enum ZodIssueCode {
  too_small = 'too_small',
  invalid_string = 'invalid_string',
  custom = 'custom',
  invalid_type = 'invalid_type'
}

// Специализированные поля
type SpecializedFields = 'captcha' | 'email' | 'password' | 'confirmPassword' | 'amount';
```

**Риски и безопасность**

- **Localization dependency**: критически зависит от наличия переводов
- **Graceful fallback**: только для CAPTCHA, другие поля могут показывать ключи
- **Type safety**: использует unknown для fieldName, требует проверок
- **Issue parsing**: зависит от структуры Zod issue объектов
- **Fallback consistency**: английские fallback могут не соответствовать дизайну

**Тесты / рекомендации по покрытию**

- **Handler specificity тесты**: корректность определения применимости обработчиков
- **Error message тесты**: правильность локализованных сообщений
- **Graceful fallback тесты**: корректность английских fallback для CAPTCHA
- **Zod integration тесты**: совместимость с различными типами Zod ошибок
- **Edge cases тесты**: обработка некорректных issue объектов

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная логика обработки различных типов ошибок

**TODO / Рефакторинг**

- ✅ Специализированные обработчики для всех типов полей
- ✅ Graceful fallback для CAPTCHA сообщений
- ✅ Интеграция с централизованными константами
- ✅ Детальная обработка различных типов Zod ошибок
- ✅ Поддержка сложной валидации паролей и сумм
- ✅ Модульная архитектура с отдельными функциями
- 💡 Рекомендация: добавить graceful fallback для всех обработчиков
- 💡 Рекомендация: улучшить type safety для issue parsing
- 💡 Рекомендация: добавить кеширование переводов для производительности
- 💡 Рекомендация: рассмотреть создание registry для кастомных обработчиков
