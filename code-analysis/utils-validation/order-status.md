### Путь: packages/utils/src/order-status.ts

**Краткое назначение (1 предложение)**
Централизованная бизнес-логика для работы со статусами заказов включающая валидацию переходов, фильтрацию и UI утилиты.

**Подробное описание (3–6 предложений)**
Файл содержит всю бизнес-логику работы со статусами заказов, перенесенную из constants согласно Rule 20. Включает функции проверки состояний заказов, валидации переходов между статусами и фильтрации заказов по различным критериям. Предоставляет утилиты для UI включая получение отображаемых названий и CSS классов статусов. Содержит сложную логику валидации переходов статусов с поддержкой дополнительных данных. Использует централизованные константы из @repo/constants и типы из @repo/exchange-core.

**Экспортируемые сущности / API (все на 100%)**

- **Проверки состояний**: `isActiveOrder`, `isCompletedOrder`, `isCancelledOrder`, `isFinalStatus`
- **Валидация переходов**: `canChangeOrderStatus`, `canTransitionStatus`, `getAvailableTransitions`, `validateStatusTransition`
- **Фильтрация заказов**: `filterOrdersForOperator`, `filterCompletedOrders`, `filterCancelledOrders`, `filterOrdersByStatus`
- **Группировка и подсчет**: `groupOrdersByStatus`, `countOrdersByStatus`
- **UI утилиты**: `getStatusDisplayName`, `getStatusColorClass`
- **Утилиты статусов**: `isActiveOrderStatus`, `isFinalOrderStatus`, `isSuccessfulOrderStatus`, `isFailedOrderStatus`
- **Дополнительные**: `requiresAdditionalData`

**Входы (expected inputs) / Параметры (все на 100%)**

- `order: Order` - объект заказа для проверки
- `orders: Order[]` - массив заказов для фильтрации
- `status: OrderStatus` - статус для проверки или фильтрации
- `fromStatus/toStatus: OrderStatus` - статусы для валидации перехода
- `additionalData?: Record<string, unknown>` - дополнительные данные для валидации

**Выходы / Побочные эффекты (все на 100%)**

- Функции проверки возвращают `boolean`
- Фильтры возвращают `Order[]` - отфильтрованные массивы
- `groupOrdersByStatus` возвращает `Record<OrderStatus, Order[]>`
- `countOrdersByStatus` возвращает `Record<OrderStatus, number>`
- `validateStatusTransition` возвращает `{ isValid: boolean; error?: string }`
- `getStatusDisplayName` возвращает `string` - локализованное название
- `getStatusColorClass` возвращает `string` - CSS классы
- Нет побочных эффектов - чистые функции

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**:
  - @repo/constants (ORDER_STATUSES, ORDER_STATUS_GROUPS, ORDER_STATUS_CONFIG, OrderStatus)
  - @repo/exchange-core (Order)
- **Потенциальные потребители**:
  - API роутеры для валидации переходов
  - Компоненты отображения заказов
  - Админ-панель для управления заказами
  - Фильтры и поиск заказов
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Проверки состояний
const isActive = isActiveOrder(order); // boolean
const isCompleted = isCompletedOrder(order); // boolean

// Валидация переходов
const canChange = canTransitionStatus('pending', 'processing'); // true
const transitions = getAvailableTransitions('pending'); // ['processing', 'cancelled']

// Валидация с дополнительными данными
const validation = validateStatusTransition(order, 'completed', { txHash: '0x123...' });
// { isValid: true } или { isValid: false, error: 'Transaction hash required' }

// Фильтрация
const activeOrders = filterOrdersForOperator(orders); // Order[]
const completedOrders = filterCompletedOrders(orders); // Order[]

// UI утилиты
const displayName = getStatusDisplayName('pending'); // 'Ожидание оплаты'
const cssClass = getStatusColorClass('completed'); // 'text-green-600 bg-green-50'
```

**Риски и безопасность**

- **Критический риск**: содержит ключевую бизнес-логику статусов
- **Business logic integrity**: неправильная валидация может нарушить workflow
- **State transitions**: критически важно для целостности данных
- **UI consistency**: влияет на отображение статусов пользователям
- **Data validation**: предотвращает некорректные переходы статусов

**Тесты / рекомендации по покрытию**

- **State transition тесты**: все возможные переходы статусов
- **Business logic тесты**: корректность проверок состояний
- **Validation тесты**: валидация с различными дополнительными данными
- **Filter тесты**: правильность фильтрации заказов
- **UI utility тесты**: корректность отображения статусов
- **Edge case тесты**: граничные случаи и некорректные данные

**Оценка сложности (low/medium/high)**
**HIGH** - сложная бизнес-логика с множественными проверками и валидацией

**TODO / Рефакторинг**

- ✅ Отличная централизация бизнес-логики статусов
- ✅ Правильное разделение констант и логики (Rule 20)
- ✅ Comprehensive набор функций для всех сценариев
- ✅ Интеграция с централизованными константами
- ✅ Поддержка дополнительных данных в валидации
- 💡 Рекомендация: добавить unit тесты для всех переходов статусов
- 💡 Рекомендация: рассмотреть добавление state machine для более строгой валидации
- 💡 Рекомендация: добавить логирование изменений статусов для аудита
