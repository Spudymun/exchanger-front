### Путь: packages/utils/src/validation-schemas.ts

**Краткое назначение (1 предложение)**
Централизованные Zod схемы валидации для всех форм и API endpoints с интеграцией констант и модульной архитектурой.

**Подробное описание (3–6 предложений)**
Файл содержит все основные Zod схемы валидации проекта, используя константы из @repo/constants для единообразия. Реэкспортирует базовые схемы из модульной системы validation/ и определяет специфичные схемы для различных доменов (заказы, аутентификация, поддержка). Включает схемы для пагинации (offset и cursor-based), финансовых данных, поиска и профилей пользователей. Поддерживает локализацию через createNextIntlZodErrorMap без хардкода сообщений. Следует принципу централизации валидации с возможностью переиспользования схем в API и формах.

**Экспортируемые сущности / API (все на 100%)**

- **Реэкспорты базовых схем**: `usernameSchema`, `phoneInternationalSchema`, `idSchema`, `emailSchema`, `passwordSchema`, `currencySchema`
- **Реэкспорты крипто схем**: `btcAddressSchema`, `ethAddressSchema`, `createCryptoAddressSchema`, `cryptoAmountStringSchema`
- **Пагинация**: `offsetPaginationSchema`, `cursorPaginationSchema`, `universalPaginationSchema`
- **Финансовые**: `cryptoAmountSchema`, `uahAmountSchema`, `uahAmountStringSchema`
- **Заказы**: `orderStatusSchema`, `createOrderBaseSchema`, `orderHistorySchema`, `operatorOrdersSchema`, `updateOrderStatusSchema`
- **Обмен**: `getCurrencyRateSchema`, `calculateAmountSchema`, `createExchangeOrderSchema`
- **Аутентификация**: `loginSchema`, `registerSchema`, `resetPasswordSchema`, `changePasswordSchema`, `captchaSchema`
- **Поиск**: `searchOrdersSchema`, `searchUsersSchema`, `searchKnowledgeSchema`
- **Поддержка**: `ticketPrioritySchema`, `ticketStatusSchema`, `createTicketSchema`, `getTicketsSchema`
- **Профиль**: `updateNotificationsSchema`
- **Реэкспорты составных**: `createOrderEnhancedSchema`, `changePasswordEnhancedSchema`

**Входы (expected inputs) / Параметры (все на 100%)**

- Схемы принимают соответствующие объекты данных для валидации
- Используют константы из @repo/constants для лимитов и паттернов
- Интегрируются с базовыми схемами из validation/ модулей

**Выходы / Побочные эффекты (все на 100%)**

- Все схемы возвращают Zod validation результаты
- Успешная валидация возвращает типизированные данные
- Ошибки валидации обрабатываются через createNextIntlZodErrorMap
- Нет побочных эффектов - чистые схемы валидации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**:
  - @repo/constants (множественные константы валидации)
  - ./validation/schemas-basic (базовые схемы)
  - ./validation/schemas-crypto (крипто схемы)
  - ./validation/schemas-composed (составные схемы)
- **Потенциальные потребители**:
  - tRPC роутеры для валидации API
  - Формы с React Hook Form + Zod
  - Middleware валидации
  - input-validation.ts (интеграция)
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Аутентификация
const loginData = loginSchema.parse({
  email: 'user@example.com',
  password: 'password123',
  captcha: '42',
}); // Типизированный результат

// Заказы
const orderData = createOrderBaseSchema.parse({
  email: 'user@example.com',
  cryptoAmount: 0.1,
  currency: 'BTC',
});

// Пагинация
const pagination = offsetPaginationSchema.parse({
  limit: 20,
  offset: 0,
}); // { limit: 20, offset: 0 }

// Поиск
const searchParams = searchOrdersSchema.parse({
  query: 'order123',
  status: 'pending',
  limit: 10,
  offset: 0,
});

// Финансовые данные
const amount = uahAmountStringSchema.parse('1000.50'); // Валидная UAH сумма
```

**Риски и безопасность**

- **Критический риск**: основа валидации всех данных в системе
- **Data integrity**: неправильная валидация может привести к некорректным данным
- **Security**: схемы предотвращают injection атаки и некорректные данные
- **Type safety**: обеспечивает типобезопасность на уровне API
- **Business rules**: интеграция с константами обеспечивает соответствие бизнес-правилам

**Тесты / рекомендации по покрытию**

- **Schema validation тесты**: проверка всех схем с валидными и невалидными данными
- **Integration тесты**: использование в tRPC роутерах и формах
- **Localization тесты**: корректность локализованных сообщений об ошибках
- **Business rules тесты**: соответствие валидации бизнес-требованиям
- **Performance тесты**: производительность сложных схем валидации

**Оценка сложности (low/medium/high)**
**HIGH** - сложная система с множественными доменами и интеграциями

**TODO / Рефакторинг**

- ✅ Отличная интеграция с константами из @repo/constants
- ✅ Модульная архитектура с реэкспортами
- ✅ Поддержка локализации без хардкода сообщений
- ✅ Comprehensive покрытие всех доменов приложения
- ✅ Правильная типизация и использование Zod
- 💡 Рекомендация: добавить unit тесты для всех схем
- 💡 Рекомендация: рассмотреть дальнейшее разделение на доменные файлы
- 💡 Рекомендация: добавить схемы для новых API endpoints по мере развития
