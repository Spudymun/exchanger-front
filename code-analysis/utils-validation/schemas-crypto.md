### Путь: packages/utils/src/validation/schemas-crypto.ts

**Краткое назначение (1 предложение)**
Специализированные Zod схемы валидации для криптовалют, адресов и сумм с поддержкой различных блокчейнов.

**Подробное описание (3–6 предложений)**
Модуль предоставляет схемы валидации для криптовалютных операций, включая адреса Bitcoin, Ethereum, Litecoin и валидацию криптовалютных сумм в строковом формате. Использует regex паттерны из schemas-basic для валидации форматов адресов различных блокчейнов с учетом их специфических требований. Включает динамическую функцию createCryptoAddressSchema для выбора подходящей схемы валидации адреса в зависимости от типа криптовалюты. Реализует строгую валидацию сумм через cryptoAmountStringSchema с проверкой формата (до 8 знаков после запятой), положительности и соответствия границам из VALIDATION_BOUNDS. Интегрируется с константами CRYPTOCURRENCIES для обеспечения консистентности поддерживаемых валют в системе.

**Экспортируемые сущности / API (все на 100%)**

- `export const btcAddressSchema` — схема валидации Bitcoin адресов
- `export const ethAddressSchema` — схема валидации Ethereum адресов
- `export const ltcAddressSchema` — схема валидации Litecoin адресов
- `export const createCryptoAddressSchema(currency)` — динамическая функция выбора схемы
- `export const cryptoAmountStringSchema` — схема валидации криптовалютных сумм
- `export const currencySchema` — схема валидации типа криптовалюты

**Входы (expected inputs) / Параметры (все на 100%)**

- `createCryptoAddressSchema`: `currency: CryptoCurrency` — тип криптовалюты для выбора схемы
- Остальные схемы не принимают параметры - это статические определения

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают соответствующие Zod схемы для валидации криптовалютных данных
- `createCryptoAddressSchema` возвращает подходящую схему адреса для валюты
- Обеспечивают строгую валидацию форматов адресов и сумм
- Поддерживают fallback схему для неизвестных валют

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: формах создания заказов, переводов, обменов криптовалют
- **Импортирует**: CRYPTOCURRENCIES, VALIDATION_BOUNDS из @repo/constants
- **Использует типы**: CryptoCurrency из @repo/exchange-core
- **Импортирует**: PATTERNS из schemas-basic для regex валидации
- **Часть**: специализированной системы валидации криптовалют utils пакета

**Домен данных / типы (все на 100%)**

```typescript
// Поддерживаемые криптовалюты
type CryptoCurrency = 'BTC' | 'ETH' | 'USDT' | 'LTC';

// Схемы адресов
type BtcAddressSchema = z.ZodString; // с BTC regex
type EthAddressSchema = z.ZodString; // с ETH regex
type LtcAddressSchema = z.ZodString; // с LTC regex

// Схема суммы
type CryptoAmountStringSchema = z.ZodString; // с regex, refine для amount, bounds

// Схема валюты
type CurrencySchema = z.ZodEnum<[string, ...string[]]>; // из CRYPTOCURRENCIES

// Regex паттерны (из schemas-basic)
const CRYPTO_PATTERNS = {
  BTC_ADDRESS: /^([13][a-km-zA-HJ-NP-Z1-9]{25,34}|bc1[a-z0-9]{39,59})$/u,
  ETH_ADDRESS: /^0x[a-fA-F0-9]{40}$/u,
  LTC_ADDRESS: /^[LM3][a-km-zA-HJ-NP-Z1-9]{26,33}$/u,
};

// Границы валидации
const VALIDATION_BOUNDS = {
  MIN_ORDER_AMOUNT: number,
  MAX_ORDER_AMOUNT: number,
};
```

**Риски и безопасность**

- **Address validation**: критически важна для предотвращения потери средств
- **Regex patterns**: могут устареть при изменении форматов адресов блокчейнов
- **Amount precision**: ограничение в 8 знаков может быть недостаточным для некоторых токенов
- **Currency support**: жестко привязан к CRYPTOCURRENCIES константе
- **Fallback schema**: может быть недостаточно строгой для новых валют

**Тесты / рекомендации по покрытию**

- **Address format тесты**: корректность валидации всех типов адресов
- **Amount validation тесты**: правильность валидации сумм и границ
- **Currency enum тесты**: соответствие поддерживаемым валютам
- **Dynamic schema тесты**: корректность createCryptoAddressSchema для всех валют
- **Edge cases тесты**: обработка граничных значений и некорректных форматов

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложные regex паттерны и криптовалютная специфика

**TODO / Рефакторинг**

- ✅ Извлечение из validation-schemas.ts для модульности
- ✅ Поддержка основных криптовалют (BTC, ETH, LTC, USDT)
- ✅ Динамическая функция выбора схемы по валюте
- ✅ Строгая валидация сумм с границами
- ✅ Интеграция с константами для консистентности
- ✅ Fallback схема для неизвестных валют
- 💡 Рекомендация: добавить поддержку новых форматов адресов (Taproot, etc.)
- 💡 Рекомендация: рассмотреть увеличение precision для некоторых токенов
- 💡 Рекомендация: добавить валидацию network-specific адресов
- 💡 Рекомендация: регулярно обновлять regex паттерны для новых форматов
