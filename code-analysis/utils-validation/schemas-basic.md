### Путь: packages/utils/src/validation/schemas-basic.ts

**Краткое назначение (1 предложение)**
Базовые Zod схемы валидации для основных примитивов с константами, паттернами и усиленными требованиями безопасности.

**Подробное описание (3–6 предложений)**
Модуль предоставляет фундаментальные схемы валидации для основных типов данных: email, пароли (новые и legacy), имена пользователей, поисковые запросы и международные номера телефонов. Включает константы для ограничений длины и regex паттерны для различных типов адресов криптовалют (BTC, ETH, LTC) и email валидации. Реализует двухуровневую систему паролей: усиленную схему для новых паролей с требованиями к сложности и legacy схему для совместимости с существующими пользователями. Использует RFC стандарты для ограничений (например, EMAIL_MAX_LENGTH = 254 по RFC 5321) и безопасные regex паттерны для предотвращения ReDoS атак. Извлечен из validation-schemas.ts для улучшения поддерживаемости и модульности системы валидации.

**Экспортируемые сущности / API (все на 100%)**

- `export const idSchema` — базовая схема для ID
- `export const emailSchema` — усиленная схема email с regex
- `export const passwordSchema` — схема для новых паролей с требованиями сложности
- `export const newPasswordSchema` — алиас для passwordSchema
- `export const legacyPasswordSchema` — схема для существующих паролей
- `export const usernameSchema` — схема имени пользователя
- `export const searchQuerySchema` — схема поискового запроса
- `export const phoneInternationalSchema` — схема международного телефона
- Константы: PASSWORD_MIN_LENGTH, EMAIL_MAX_LENGTH, USERNAME_MIN_LENGTH, etc.
- `export const PATTERNS` — regex паттерны для различных типов данных

**Входы (expected inputs) / Параметры (все на 100%)**

- Схемы не принимают параметры - это статические определения
- Константы определяют ограничения для схем

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают соответствующие Zod схемы для валидации
- Предоставляют константы для использования в других модулях
- Обеспечивают консистентную валидацию основных типов данных
- Поддерживают как строгие, так и legacy требования

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: сложных схемах валидации, формах, API endpoints
- **Импортируется**: в schema-helpers, schemas-composed и других модулях валидации
- **Базируется на**: RFC стандартах и best practices безопасности
- **Часть**: фундамента системы валидации utils пакета
- **Заменяет**: части validation-schemas.ts для лучшей модульности

**Домен данных / типы (все на 100%)**

```typescript
// Константы ограничений
const PASSWORD_MIN_LENGTH = 8;
const LEGACY_PASSWORD_MIN_LENGTH = 6;
const PASSWORD_MAX_LENGTH = 128;
const USERNAME_MIN_LENGTH = 3;
const USERNAME_MAX_LENGTH = 30;
const EMAIL_MAX_LENGTH = 254; // RFC 5321
const SEARCH_QUERY_MAX_LENGTH = 100;

// Regex паттерны
const PATTERNS = {
  EMAIL: /^[^\s@]+@[^\s@]+\.[^\s@]+$/u,
  BTC_ADDRESS: /^([13][a-km-zA-HJ-NP-Z1-9]{25,34}|bc1[a-z0-9]{39,59})$/u,
  ETH_ADDRESS: /^0x[a-fA-F0-9]{40}$/u,
  LTC_ADDRESS: /^[LM3][a-km-zA-HJ-NP-Z1-9]{26,33}$/u,
} as const;

// Типы схем
type EmailSchema = z.ZodString; // с email, regex, length валидацией
type PasswordSchema = z.ZodString; // с complexity требованиями
type LegacyPasswordSchema = z.ZodString; // с базовыми требованиями
type UsernameSchema = z.ZodString; // с alphanumeric + underscore
type SearchQuerySchema = z.ZodString; // с trim и length
type PhoneInternationalSchema = z.ZodString; // с international format
```

**Риски и безопасность**

- **Password security**: двухуровневая система может создавать уязвимости
- **Regex safety**: все паттерны проверены на ReDoS атаки
- **Email validation**: использует безопасный упрощенный regex
- **Legacy compatibility**: legacy пароли могут быть менее безопасными
- **Crypto addresses**: паттерны могут устареть при изменении форматов

**Тесты / рекомендации по покрытию**

- **Schema validation тесты**: корректность всех базовых схем
- **Regex pattern тесты**: правильность всех regex паттернов
- **Security тесты**: проверка на ReDoS и другие уязвимости
- **RFC compliance тесты**: соответствие стандартам (email length, etc.)
- **Legacy compatibility тесты**: совместимость legacy схем

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложные regex паттерны и требования безопасности

**TODO / Рефакторинг**

- ✅ Извлечение из validation-schemas.ts для модульности
- ✅ RFC стандарты для ограничений длины
- ✅ Безопасные regex паттерны без ReDoS уязвимостей
- ✅ Двухуровневая система паролей (новые/legacy)
- ✅ Константы для всех ограничений
- ✅ Поддержка криптовалютных адресов
- ✅ Усиленные требования безопасности для паролей
- 💡 Рекомендация: добавить поддержку новых форматов криптоадресов
- 💡 Рекомендация: рассмотреть миграцию legacy паролей на новые требования
- 💡 Рекомендация: добавить поддержку internationalized domain names
- 💡 Рекомендация: регулярно обновлять crypto address паттерны
