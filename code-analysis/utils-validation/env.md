### Путь: packages/utils/src/env.ts

**Краткое назначение (1 предложение)**
Типобезопасная система управления переменными окружения с валидацией и значениями по умолчанию.

**Подробное описание (3–6 предложений)**
Файл предоставляет типобезопасный доступ к переменным окружения с разделением на обязательные и опциональные. Определяет схему валидации для production окружения и значения по умолчанию для development. Включает функцию валидации всех обязательных переменных при запуске приложения. Предоставляет перегруженную функцию getEnvVar с типобезопасным доступом к переменным. В настоящее время содержит заготовку для будущих переменных окружения.

**Экспортируемые сущности / API (все на 100%)**

- `requiredEnvVars` - массив обязательных переменных (пока пустой)
- `optionalEnvVars` - объект опциональных переменных с дефолтами:
  - `PORT: '3000'`, `NODE_ENV: 'development'`
  - `VERCEL_URL: undefined`, `CI: undefined`, `PLAYWRIGHT_TEST_BASE_URL: undefined`
- `RequiredEnvVar` - тип для обязательных переменных
- `OptionalEnvVar` - тип для опциональных переменных
- `EnvVar` - объединенный тип переменных
- `validateEnvVars()` - функция валидации обязательных переменных
- `getEnvVar()` - перегруженная функция получения переменных с типобезопасностью

**Входы (expected inputs) / Параметры (все на 100%)**

- `validateEnvVars()` - нет параметров, читает process.env
- `getEnvVar(key: RequiredEnvVar)` - ключ обязательной переменной
- `getEnvVar(key: OptionalEnvVar, defaultValue?: string)` - ключ опциональной переменной и дефолт

**Выходы / Побочные эффекты (все на 100%)**

- `validateEnvVars()` - выбрасывает Error при отсутствии обязательных переменных
- `getEnvVar()` для обязательных - возвращает `string`
- `getEnvVar()` для опциональных - возвращает `string | undefined`
- Нет побочных эффектов кроме чтения process.env

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**:
  - Конфигурация приложений (Next.js config)
  - Инициализация серверов
  - Database connections
  - API клиенты
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/utils/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Текущая конфигурация
requiredEnvVars: [] // Пока пустой массив

optionalEnvVars: {
  PORT: '3000',                    // Порт сервера
  NODE_ENV: 'development',         // Окружение
  VERCEL_URL: undefined,           // URL Vercel
  CI: undefined,                   // CI окружение
  PLAYWRIGHT_TEST_BASE_URL: undefined // URL для тестов
}

// Примеры использования:
const port = getEnvVar('PORT'); // string | undefined
const nodeEnv = getEnvVar('NODE_ENV'); // string | undefined

// Валидация при запуске:
validateEnvVars(); // Проверяет обязательные переменные
```

**Риски и безопасность**

- **Средний риск**: управляет доступом к конфигурации
- **Configuration security**: неправильные переменные могут нарушить безопасность
- **Type safety**: предотвращает ошибки доступа к несуществующим переменным
- **Environment isolation**: разделение dev/prod конфигураций
- **Validation**: защита от запуска с неполной конфигурацией

**Тесты / рекомендации по покрытию**

- **Validation тесты**: проверка валидации обязательных переменных
- **Type safety тесты**: корректность типизации getEnvVar
- **Default values тесты**: правильность значений по умолчанию
- **Environment isolation тесты**: поведение в разных окружениях

**Оценка сложности (low/medium/high)**
**LOW** - простая система управления переменными окружения

**TODO / Рефакторинг**

- ✅ Отличная типобезопасность с перегруженными функциями
- ✅ Разделение обязательных и опциональных переменных
- ✅ Валидация при запуске приложения
- ✅ Значения по умолчанию для development
- 📋 TODO: добавить реальные обязательные переменные (DATABASE_URL, API_KEY, JWT_SECRET)
- 💡 Рекомендация: добавить схему валидации с Zod для типов переменных
- 💡 Рекомендация: добавить документацию для каждой переменной
- 💡 Рекомендация: рассмотреть добавление переменных для криптовалютных API
