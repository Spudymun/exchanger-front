### Путь: packages/constants/src/validation.ts

**Краткое назначение (1 предложение)**
Общие правила валидации и лимиты для всех приложений, включая пользовательские данные, финансовые операции и API ограничения.

**Подробное описание (3–6 предложений)**
Файл содержит универсальные константы валидации, применимые во всех приложениях проекта. Определяет лимиты для пользовательских данных (email, пароли, имена), финансовых операций и контента. Включает настройки безопасности паролей, ограничения файловых загрузок и API лимиты. Содержит константы аутентификации для сессий и сброса паролей. Удалены дублирующие системы валидации в пользу Zod схем и next-intl переводов, следуя принципу DRY.

**Экспортируемые сущности / API (все на 100%)**

- `VALIDATION_LIMITS` - основные лимиты валидации:
  - Пользовательские данные (EMAIL_MAX_LENGTH: 255, PASSWORD_MIN_LENGTH: 8, USERNAME_MIN_LENGTH: 3)
  - Безопасность паролей (BCRYPT_SALT_ROUNDS: 10)
  - Имена (FIRST_NAME_MIN_LENGTH: 1, LAST_NAME_MAX_LENGTH: 50)
  - Финансовые операции (MIN_ORDER_AMOUNT, MAX_WITHDRAWAL_AMOUNT: 100000)
  - Торговля (ORDER_ITEMS_MAX: 50, OPEN_ORDERS_MAX: 20)
  - Контент (DESCRIPTION_MAX_LENGTH: 500, COMMENT_MAX_LENGTH: 1000)
  - Файловые загрузки (AVATAR_MAX_SIZE_MB: 5, SUPPORTED_IMAGE_FORMATS)
  - API (REQUEST_TIMEOUT_MS, MAX_REQUESTS_PER_MINUTE: 100)
  - Пагинация (MIN_PAGE_SIZE: 10, MAX_PAGE_SIZE: 100)
- `AUTH_CONSTANTS` - константы аутентификации:
  - Сессии (SESSION_MAX_AGE_SECONDS: 604800, SESSION_COOKIE_NAME)
  - Задержки запросов (AUTH_REQUEST_DELAY_MS: 300, LOGIN_REQUEST_DELAY_MS: 500)
  - Сброс паролей (RESET_CODE_LENGTH: 6, RESET_CODE_BASE: 36)
  - HTTP заголовки (SET_COOKIE_HEADER)

**Входы (expected inputs) / Параметры (все на 100%)**

- Импортирует константы из других файлов:
  - `REQUEST_TIMEOUT_CONSTANTS` из './time-constants'
  - `VALIDATION_BOUNDS` из './validation-bounds'

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные лимиты валидации
- Нет побочных эффектов - чистые константы
- Переиспользует существующие константы (следует Rule 20)
- Предоставляет предвычисленные значения (SESSION_MAX_AGE_SECONDS)

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: time-constants.ts, validation-bounds.ts
- **Потенциальные потребители**: Zod схемы, валидаторы, middleware, формы
- **Заменяет**: удаленные VALIDATION_PATTERNS, VALIDATION_MESSAGES, VALIDATION_HELPERS
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Основные лимиты
VALIDATION_LIMITS: {
  EMAIL_MAX_LENGTH: 255,
  PASSWORD_MIN_LENGTH: 8,
  PASSWORD_MAX_LENGTH: 128,
  BCRYPT_SALT_ROUNDS: 10,
  MIN_ORDER_AMOUNT: number, // из VALIDATION_BOUNDS
  MAX_ORDER_AMOUNT: number, // из VALIDATION_BOUNDS
  SUPPORTED_IMAGE_FORMATS: ['jpg', 'jpeg', 'png', 'gif', 'webp'],
  MAX_REQUESTS_PER_MINUTE: 100
}

// Аутентификация
AUTH_CONSTANTS: {
  SESSION_MAX_AGE_SECONDS: 604800, // 7 дней
  SESSION_COOKIE_NAME: 'sessionId',
  RESET_CODE_LENGTH: 6,
  AUTH_REQUEST_DELAY_MS: 300
}
```

**Риски и безопасность**

- **Высокий риск**: содержит критически важные лимиты безопасности
- **Password security**: BCRYPT_SALT_ROUNDS для хеширования паролей
- **Session security**: лимиты времени жизни сессий
- **Rate limiting**: MAX_REQUESTS_PER_MINUTE для защиты от DDoS
- **File upload security**: ограничения размеров и форматов файлов
- **Financial security**: лимиты сумм операций

**Тесты / рекомендации по покрытию**

- **Security тесты**: проверка лимитов безопасности
- **Validation тесты**: проверка корректности лимитов
- **Integration тесты**: использование в Zod схемах
- **Performance тесты**: проверка таймаутов и лимитов запросов
- **Business logic тесты**: проверка финансовых лимитов

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важные лимиты безопасности и зависимости

**TODO / Рефакторинг**

- ✅ Успешная миграция от дублирующих систем к Zod + i18n
- ✅ Правильное переиспользование констант (Rule 20)
- ✅ Предвычисленные значения без вычислений в константах
- ✅ Комментарии объясняют миграцию и замены
- 💡 Рекомендация: добавить JSDoc для критически важных лимитов
- 💡 Рекомендация: рассмотреть вынос AUTH_CONSTANTS в отдельный файл
