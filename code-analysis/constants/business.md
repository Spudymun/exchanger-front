### Путь: packages/constants/src/business.ts

**Краткое назначение (1 предложение)**
Централизованные константы бизнес-домена для ролей пользователей, типов заказов, транзакций, валют и уровней KYC.

**Подробное описание (3–6 предложений)**
Файл содержит ключевые бизнес-константы для криптовалютной биржи ExchangeGO. Определяет роли пользователей (admin, operator, support, user) для системы авторизации. Включает типы и статусы транзакций, поддерживаемые валюты (фиат и криптовалюты), уровни KYC верификации. Содержит типы уведомлений для различных бизнес-событий. Все константы типизированы и следуют принципу единого источника истины для предотвращения дублирования.

**Экспортируемые сущности / API (все на 100%)**

- `USER_ROLES` - роли пользователей (ADMIN, OPERATOR, SUPPORT, USER)
- `USER_STATUS` - статусы пользователей (ACTIVE, INACTIVE, SUSPENDED, PENDING_VERIFICATION)
- `ORDER_TYPES` - типы заказов (MARKET, LIMIT, STOP, STOP_LIMIT)
- `TRANSACTION_TYPES` - типы транзакций (BUY, SELL, DEPOSIT, WITHDRAWAL, TRANSFER, EXCHANGE)
- `TRANSACTION_STATUSES` - статусы транзакций (PENDING, PROCESSING, COMPLETED, FAILED, CANCELLED)
- `CURRENCY_TYPES` - типы валют (FIAT, CRYPTO, STABLE)
- `SUPPORTED_CURRENCIES` - поддерживаемые валюты (USD, EUR, BTC, ETH, USDT и др.)
- `KYC_LEVELS` - уровни KYC (NONE: 0, BASIC: 1, INTERMEDIATE: 2, ADVANCED: 3)
- `NOTIFICATION_TYPES` - типы уведомлений (ORDER_FILLED, DEPOSIT_COMPLETED и др.)
- Соответствующие TypeScript типы для всех констант

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { USER_ROLES, TRANSACTION_TYPES } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные бизнес-константы
- Нет побочных эффектов - чистые константы
- Обеспечивает type safety через `as const` assertions
- Предотвращает использование магических строк в бизнес-логике

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортируется в**:
  - `packages/hooks/src/state/trading-store.ts` (TRANSACTION_TYPES)
  - `packages/exchange-core/src/types/transaction.ts` (TRANSACTION_TYPES, TRANSACTION_STATUSES)
  - `apps/web/src/server/trpc/middleware/auth.ts` (USER_ROLES)
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Роли и статусы пользователей
USER_ROLES: {
  ADMIN: 'admin',      // Только admin-panel
  OPERATOR: 'operator', // apps/web - обработка заявок
  SUPPORT: 'support',   // apps/web - техподдержка
  USER: 'user'         // Обычные пользователи
}

// Типы транзакций
TRANSACTION_TYPES: {
  BUY: 'buy',
  SELL: 'sell',
  DEPOSIT: 'deposit',
  WITHDRAWAL: 'withdrawal',
  TRANSFER: 'transfer',
  EXCHANGE: 'exchange'
}

// Поддерживаемые валюты
SUPPORTED_CURRENCIES: {
  USD: 'USD', EUR: 'EUR', RUB: 'RUB', // Fiat
  BTC: 'BTC', ETH: 'ETH', LTC: 'LTC', // Crypto
  USDT: 'USDT', USDC: 'USDC', DAI: 'DAI' // Stablecoins
}
```

**Риски и безопасность**

- **Низкий риск**: только бизнес-константы, нет секретов
- **Role-based security**: USER_ROLES используется в middleware авторизации
- **Type safety**: предотвращает ошибки в бизнес-логике
- **Centralization**: единый источник ролей снижает риск несоответствий

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка корректности значений констант
- **Integration тесты**: использование в middleware авторизации
- **Business logic тесты**: проверка соответствия бизнес-правилам
- **Type тесты**: проверка типизации ролей и статусов

**Оценка сложности (low/medium/high)**
**LOW** - простые бизнес-константы без сложной логики

**TODO / Рефакторинг**

- ✅ Хорошо структурировано по бизнес-доменам
- ✅ Правильная типизация с `as const`
- ✅ Комментарии объясняют назначение ролей
- ⚠️ Замечание: ORDER_STATUSES перенесены в exchange.ts (Rule 20 - устранение дублирования)
- 💡 Рекомендация: добавить JSDoc для лучшей документации бизнес-правил
