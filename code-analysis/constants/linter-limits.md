### Путь: packages/constants/src/linter-limits.ts

**Краткое назначение (1 предложение)**
Централизованные лимиты для ESLint конфигурации с дифференцированными значениями по архитектурным слоям.

**Подробное описание (3–6 предложений)**
Файл содержит все лимиты качества кода для централизованной ESLint архитектуры согласно CODE_STYLE_GUIDE.md. Определяет базовые лимиты сложности, размеров функций и файлов с увеличенными значениями для специфических слоев (UI, API, тесты). Включает лимиты глубины вложенности, количества параметров и архитектурные пути для различных типов файлов. Обеспечивает единообразное применение правил качества кода во всем монорепозитории. Следует принципу централизации констант согласно Rule 19.

**Экспортируемые сущности / API (все на 100%)**

- `COMPLEXITY_LIMITS` - цикломатическая сложность:
  - `BASE: 10` - базовая сложность
  - `API_LAYER: 15` - увеличенная для API слоя
  - `UTILS: 8` - уменьшенная для утилит
- `FUNCTION_SIZE_LIMITS` - размеры функций в строках:
  - `BASE: 50` - базовый лимит
  - `UI_COMPONENTS: 80` - для UI компонентов
  - `API_ENDPOINTS: 80` - для API endpoints
  - `TESTS: 100` - для тестов
  - `MAIN_PAGES: 70` - для основных страниц
  - `DASHBOARD: 65` - для dashboard
  - `HOOKS: 60` - для хуков
- `FILE_SIZE_LIMITS` - размеры файлов:
  - `BASE: 300` - базовый лимит
  - `UI_LIBRARY: 400` - для UI библиотеки
  - `MAIN_PAGES: 350` - для основных страниц
  - `ESLINT_CONFIG: 500` - для ESLint конфига
- `DEPTH_LIMITS` - глубина вложенности:
  - `BASE: 2` - базовая глубина
  - `SCRIPTS: 4` - для скриптов
- `PARAMETERS_LIMITS` - параметры функций:
  - `BASE: 4` - максимум параметров
  - `NESTED_CALLBACKS: 3` - максимум callbacks
  - `STATEMENTS_PER_LINE: 1` - statements в строке
- `DESCRIPTION_LENGTHS` - длина описаний:
  - `TS_IGNORE_COMMENT: 20` - минимум для @ts-ignore
- `ARCHITECTURAL_LAYERS` - пути архитектурных слоев
- Соответствующие TypeScript типы для всех лимитов

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется в ESLint конфигурации: `import { COMPLEXITY_LIMITS, FUNCTION_SIZE_LIMITS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные лимиты для линтеров
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность правил качества кода
- Используется в централизованной ESLint архитектуре

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Активно используется в**:
  - `eslint.config.mjs` - главная ESLint конфигурация
  - `packages/eslint-config/` - модульные конфигурации
- **Потенциальные потребители**: все ESLint правила проекта
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Сложность кода
COMPLEXITY_LIMITS: {
  BASE: 10,        // Базовая цикломатическая сложность
  API_LAYER: 15,   // Для tRPC middleware
  UTILS: 8         // Строже для утилит
}

// Размеры функций
FUNCTION_SIZE_LIMITS: {
  BASE: 50,           // Базовый лимит строк
  UI_COMPONENTS: 80,  // UI компоненты
  API_ENDPOINTS: 80,  // API endpoints
  TESTS: 100,         // Тесты
  MAIN_PAGES: 70,     // Основные страницы
  HOOKS: 60           // React хуки
}

// Архитектурные слои
ARCHITECTURAL_LAYERS: {
  UI: 'packages/ui',
  API: 'apps/web/src/server/trpc',
  UTILS: 'packages/utils',
  TESTS: '**/*.{test,spec}.{js,jsx,ts,tsx}',
  // ... остальные слои
}
```

**Риски и безопасность**

- **Средний риск**: влияет на качество всего кода проекта
- **Code quality**: неправильные лимиты могут снизить качество
- **Development velocity**: слишком строгие лимиты замедляют разработку
- **Architectural consistency**: обеспечивает единообразие архитектуры
- **Technical debt**: правильные лимиты предотвращают накопление долга

**Тесты / рекомендации по покрытию**

- **ESLint integration тесты**: проверка применения лимитов
- **Code quality тесты**: соответствие кода установленным лимитам
- **Performance тесты**: влияние лимитов на скорость линтинга
- **Architecture compliance тесты**: соответствие архитектурным слоям

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важные лимиты качества кода с архитектурной логикой

**TODO / Рефакторинг**

- ✅ Отличная дифференциация по архитектурным слоям
- ✅ Соответствие CODE_STYLE_GUIDE.md
- ✅ Правильная типизация всех лимитов
- ✅ Логическая группировка по типам лимитов
- ✅ Хорошие комментарии объясняющие назначение
- 💡 Рекомендация: добавить валидацию разумности лимитов
- 💡 Рекомендация: рассмотреть добавление метрик для мониторинга соблюдения лимитов
- 💡 Рекомендация: документировать обоснование каждого лимита
