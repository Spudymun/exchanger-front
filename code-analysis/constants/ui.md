### Путь: packages/constants/src/ui.ts

**Краткое назначение (1 предложение)**
Централизованные UI константы включающие числовые значения, варианты компонентов, темизацию, CSS классы и конфигурации статусов.

**Подробное описание (3–6 предложений)**
Файл содержит все UI-related константы для обеспечения консистентности интерфейса. Включает числовые константы для сеток, пагинации, размеров и таймаутов. Определяет варианты компонентов (кнопки, алерты, модалы) и их размеры. Содержит константы темизации (light/dark/system), брейкпоинты и Z-index слои. Включает конфигурации статусов транзакций с цветами и иконками. Поддерживает интернационализацию с настройками локалей.

**Экспортируемые сущности / API (все на 100%)**

- `UI_NUMERIC_CONSTANTS` - числовые константы (размеры, лимиты, таймауты)
- `ALERT_VARIANTS` - варианты алертов (SUCCESS, ERROR, WARNING, INFO)
- `BUTTON_VARIANTS` - варианты кнопок (PRIMARY, SECONDARY, DESTRUCTIVE, OUTLINE, GHOST, LINK)
- `BUTTON_SIZES` - размеры кнопок (SM, MD, LG, ICON)
- `BADGE_VARIANTS` - варианты бейджей (DEFAULT, SECONDARY, DESTRUCTIVE, OUTLINE)
- `LOADING_VARIANTS` - варианты загрузки (SPINNER, SKELETON, DOTS, PULSE)
- `MODAL_SIZES` - размеры модалов (SM, MD, LG, XL, FULL)
- `ICON_SIZES` - размеры иконок (XS, SM, MD, LG, XL)
- `THEME_MODES` - режимы темы (LIGHT, DARK, SYSTEM)
- `BREAKPOINTS` - брейкпоинты (SM, MD, LG, XL, 2XL)
- `CHART_INTERVALS` - интервалы графиков (1M, 5M, 15M, 30M, 1H, 4H, 1D, 1W)
- `TABLE_DENSITIES` - плотность таблиц (COMPACT, NORMAL, COMFORTABLE)
- `CSS_CLASSES` - CSS классы (размеры иконок)
- `Z_INDEX_LAYERS` - Z-index слои (BASE, DROPDOWN, MODAL, TOAST и др.)
- `COLOR_SCALE_KEYS` - ключи цветовой шкалы (50-950)
- `TRANSACTION_STATUS_CONFIG` - конфигурация статусов транзакций
- `SUPPORTED_LOCALES` - поддерживаемые локали (['en', 'ru'])
- `I18N_CONFIG` - конфигурация интернационализации
- Соответствующие TypeScript типы для всех констант

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { THEME_MODES, UI_NUMERIC_CONSTANTS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные UI константы
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность UI через единые значения
- Поддерживает type safety для UI компонентов

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Активно используется в**:
  - `packages/utils/src/validation-schemas.ts` (UI_NUMERIC_CONSTANTS)
  - `packages/ui/src/components/ui/dropdown-menu.tsx` (UI_NUMERIC_CONSTANTS)
  - `packages/ui/src/components/TreeNodeItem.tsx` (UI_NUMERIC_CONSTANTS)
  - `packages/ui/src/components/theme-toggle.tsx` (THEME_MODES)
  - `packages/ui/src/components/data-table/DataTablePagination.tsx` (UI_NUMERIC_CONSTANTS)
  - `packages/providers/src/theme-script.tsx` (THEME_MODES)
  - `packages/providers/src/theme-provider.tsx` (THEME_MODES)
  - `packages/hooks/src/useTheme.ts` (THEME_MODES)
  - `packages/hooks/src/state/ui-store.ts` (THEME_MODES)
  - `packages/exchange-core/src/services/id-generation.ts` (UI_NUMERIC_CONSTANTS)
  - `packages/hooks/src/business/useAuth.ts` (UI_NUMERIC_CONSTANTS)
- **Не импортирует**: никаких других модулей (zero dependencies)

**Домен данных / типы (все на 100%)**

```typescript
// Числовые константы
UI_NUMERIC_CONSTANTS: {
  DEFAULT_PAGE_SIZE: 10,
  MAX_PAGE_SIZE_SMALL: 20,
  MAX_PAGE_SIZE_LARGE: 50,
  TREE_LEVEL_PADDING: 16,
  NOTIFICATION_AUTO_REMOVE_TIMEOUT: 5000,
  // ... множество других числовых значений
}

// Темизация
THEME_MODES: {
  LIGHT: 'light',
  DARK: 'dark',
  SYSTEM: 'system'
}

// Варианты компонентов
BUTTON_VARIANTS: {
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
  DESTRUCTIVE: 'destructive',
  // ... остальные варианты
}

// Интернационализация
I18N_CONFIG: {
  DEFAULT_LOCALE: 'ru',
  FALLBACK_LOCALE: 'en',
  SUPPORTED_LOCALES: ['en', 'ru']
}
```

**Риски и безопасность**

- **Низкий риск**: только UI константы, нет чувствительных данных
- **Theme security**: безопасное переключение тем без XSS рисков
- **Type safety**: строгая типизация предотвращает UI ошибки
- **Consistency**: единые значения снижают риск UI несоответствий

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка корректности числовых значений
- **UI тесты**: проверка применения вариантов компонентов
- **Theme тесты**: проверка переключения тем
- **Integration тесты**: использование в реальных компонентах
- **Accessibility тесты**: проверка Z-index и контрастности

**Оценка сложности (low/medium/high)**
**MEDIUM** - большой объем констант с различными доменами (UI, темы, i18n)

**TODO / Рефакторинг**

- ✅ Хорошо структурировано по категориям
- ✅ Правильная типизация с `as const`
- ✅ Комментарии объясняют назначение секций
- ✅ Поддержка интернационализации
- ⚠️ Большой файл - можно рассмотреть разделение на подфайлы
- 💡 Рекомендация: добавить JSDoc для числовых констант
- 💡 Рекомендация: рассмотреть выделение theme.ts и i18n.ts отдельно
