### Путь: packages/constants/src/decimal-precision.ts

**Краткое назначение (1 предложение)**
Константы для точности десятичных дробей валют и длины случайных частей в идентификаторах.

**Подробное описание (3–6 предложений)**
Файл содержит константы для управления точностью отображения и вычислений с валютами. Определяет количество знаков после запятой для UAH (2 знака) и криптовалют (8 знаков). Включает максимальное количество знаков для UI отображения и длину случайной части в ID заказов. Централизует управление точностью вычислений согласно Rule 19. Обеспечивает консистентность отображения валют во всем приложении.

**Экспортируемые сущности / API (все на 100%)**

- `DECIMAL_PRECISION` - объект с константами точности:
  - `UAH_DECIMAL_PLACES: 2` - знаки после запятой для UAH
  - `CRYPTO_DECIMAL_PLACES: 8` - знаки после запятой для криптовалют
  - `UI_MAX_DECIMAL_PLACES: 8` - максимальные знаки для UI
  - `ORDER_ID_RANDOM_LENGTH: 9` - длина случайной части в ID заказа
- `DecimalPrecision` - тип для констант точности

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { DECIMAL_PRECISION } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные константы точности
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность форматирования валют
- Используется в вычислениях и отображении

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**:
  - Функции форматирования валют
  - Компоненты отображения сумм
  - Валидаторы числовых полей
  - Генераторы ID заказов (ORDER_ID_RANDOM_LENGTH)
- **Используется в**: packages/exchange-core/src/services/id-generation.ts (подтверждено ранее)
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Константы точности
DECIMAL_PRECISION: {
  UAH_DECIMAL_PLACES: 2,        // 100.50 UAH
  CRYPTO_DECIMAL_PLACES: 8,     // 0.12345678 BTC
  UI_MAX_DECIMAL_PLACES: 8,     // Максимум для отображения
  ORDER_ID_RANDOM_LENGTH: 9     // Длина случайной части ID
}

// Примеры использования:
// UAH: 1234.56 (2 знака)
// BTC: 0.12345678 (8 знаков)
// Order ID: order_1234567890_abc123def (9 символов в конце)
```

**Риски и безопасность**

- **Средний риск**: влияет на точность финансовых вычислений
- **Financial precision**: критически важно для денежных операций
- **UI consistency**: обеспечивает единообразное отображение
- **ID generation**: влияет на уникальность идентификаторов
- **Rounding errors**: неправильная точность может привести к ошибкам округления

**Тесты / рекомендации по покрытию**

- **Precision тесты**: проверка корректности округления
- **Financial calculation тесты**: точность денежных вычислений
- **UI display тесты**: корректность отображения валют
- **ID generation тесты**: уникальность генерируемых ID

**Оценка сложности (low/medium/high)**
**LOW** - простые числовые константы

**TODO / Рефакторинг**

- ✅ Хорошо документированные константы
- ✅ Логическое разделение по назначению
- ✅ Следует принципу централизации
- ✅ Используется в реальном коде (id-generation.ts)
- 💡 Рекомендация: добавить константы для других валют (EUR, USD)
- 💡 Рекомендация: рассмотреть добавление констант для минимальных единиц (сатоши для BTC)
- 💡 Рекомендация: добавить валидацию разумности значений
