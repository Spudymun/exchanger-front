### Путь: packages/constants/src/time-constants.ts

**Краткое назначение (1 предложение)**
Централизованные временные константы включающие базовые единицы времени, UI интервалы, таймауты запросов и пагинацию.

**Подробное описание (3–6 предложений)**
Файл содержит все временные константы проекта согласно Rule 19 (централизация констант). Включает базовые единицы времени (часы, минуты, секунды), константы для UI debounce и throttle, интервалы обновления компонентов. Определяет таймауты для HTTP запросов различных типов и константы для работы с датами и форматированием. Содержит настройки пагинации с лимитами и offset. Предотвращает использование магических чисел во временных вычислениях и обеспечивает консистентность поведения UI.

**Экспортируемые сущности / API (все на 100%)**

- `TIME_CONSTANTS` - базовые единицы времени:
  - `HOURS_IN_DAY: 24`, `MINUTES_IN_HOUR: 60`, `SECONDS_IN_MINUTE: 60`
  - `DAYS_IN_WEEK: 7`, `MILLISECONDS_IN_SECOND: 1000`
- `UI_DEBOUNCE_CONSTANTS` - задержки для UI:
  - `EXCHANGE_CALCULATION_DELAY: 300` - пересчет обмена
  - `SEARCH_INPUT_DELAY: 150` - поиск
  - `FORM_AUTOSAVE_DELAY: 500` - автосохранение формы
- `UI_REFRESH_INTERVALS` - интервалы обновления:
  - `ORDER_STATUS_REFRESH: 30000` - статус заказа (30 сек)
  - `DASHBOARD_REFRESH: 60000` - дашборд (60 сек)
  - `CURRENCY_RATES_REFRESH: 10000` - курсы валют (10 сек)
- `REQUEST_TIMEOUT_CONSTANTS` - таймауты запросов:
  - `DEFAULT_API_TIMEOUT: 30000` - стандартные API (30 сек)
  - `EXCHANGE_REQUEST_TIMEOUT: 30000` - обменные операции (30 сек)
  - `FILE_UPLOAD_TIMEOUT: 60000` - загрузка файлов (60 сек)
- `DATE_FORMAT_CONSTANTS` - форматирование дат:
  - `ISO_DATE_TIME_SEPARATOR: 'T'`, `DATE_PART_INDEX: 0`, `TIME_PART_INDEX: 1`
- `PAGINATION_CONSTANTS` - пагинация:
  - `DEFAULT_OFFSET: 0`, `DEFAULT_LIMIT: 20`, `MAX_LIMIT: 100`
- `TimeConstants` - тип для временных констант

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { UI_REFRESH_INTERVALS, REQUEST_TIMEOUT_CONSTANTS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные временные константы
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность временного поведения
- Используется в таймерах, интервалах и HTTP запросах

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Активно используется в**:
  - packages/constants/src/validation.ts (REQUEST_TIMEOUT_CONSTANTS)
  - packages/constants/src/exchange.ts (REQUEST_TIMEOUT_CONSTANTS)
- **Потенциальные потребители**:
  - Компоненты с debounce/throttle
  - HTTP клиенты и API запросы
  - Компоненты обновления данных
  - Форматтеры дат и времени
  - Компоненты пагинации
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Базовые единицы времени
TIME_CONSTANTS: {
  HOURS_IN_DAY: 24,
  MINUTES_IN_HOUR: 60,
  SECONDS_IN_MINUTE: 60,
  MILLISECONDS_IN_SECOND: 1000
}

// UI задержки (мс)
UI_DEBOUNCE_CONSTANTS: {
  EXCHANGE_CALCULATION_DELAY: 300,  // Пересчет при вводе суммы
  SEARCH_INPUT_DELAY: 150,          // Поиск при вводе
  FORM_AUTOSAVE_DELAY: 500          // Автосохранение формы
}

// Интервалы обновления (мс)
UI_REFRESH_INTERVALS: {
  ORDER_STATUS_REFRESH: 30000,      // Каждые 30 секунд
  DASHBOARD_REFRESH: 60000,         // Каждую минуту
  CURRENCY_RATES_REFRESH: 10000     // Каждые 10 секунд
}

// HTTP таймауты (мс)
REQUEST_TIMEOUT_CONSTANTS: {
  DEFAULT_API_TIMEOUT: 30000,       // 30 секунд
  EXCHANGE_REQUEST_TIMEOUT: 30000,  // 30 секунд
  FILE_UPLOAD_TIMEOUT: 60000        // 60 секунд
}
```

**Риски и безопасность**

- **Средний риск**: влияет на производительность и UX
- **Performance impact**: неправильные интервалы могут перегрузить систему
- **User experience**: слишком длинные задержки ухудшают UX
- **Timeout security**: защита от зависших запросов
- **Resource management**: правильные интервалы экономят ресурсы

**Тесты / рекомендации по покрытию**

- **Performance тесты**: влияние интервалов на производительность
- **UX тесты**: комфортность задержек для пользователей
- **Timeout тесты**: корректная обработка таймаутов
- **Integration тесты**: использование в компонентах
- **Load тесты**: поведение под нагрузкой

**Оценка сложности (low/medium/high)**
**MEDIUM** - множество различных временных констант с влиянием на производительность

**TODO / Рефакторинг**

- ✅ Отличная категоризация по назначению
- ✅ Хорошо документированные константы
- ✅ Активно переиспользуется в других файлах
- ✅ Предотвращает магические числа
- ✅ Разумные значения для UX и производительности
- 💡 Рекомендация: добавить константы для анимаций CSS
- 💡 Рекомендация: рассмотреть адаптивные интервалы на основе активности пользователя
- 💡 Рекомендация: добавить мониторинг производительности интервалов
