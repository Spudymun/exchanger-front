### Путь: packages/constants/src/auth.ts

**Краткое назначение (1 предложение)**
Централизованные константы для аутентификации включающие конфигурацию CAPTCHA, форм и уникальные ID элементов.

**Подробное описание (3–6 предложений)**
Файл содержит константы для системы аутентификации, устраняя хардкод в компонентах форм. Определяет конфигурацию CAPTCHA с уровнем сложности и настройками отображения. Включает настройки форм аутентификации для валидации. Содержит уникальные ID для элементов форм логина и регистрации, обеспечивая корректную работу в модальных окнах. Удалены сообщения уведомлений в пользу локализованных переводов.

**Экспортируемые сущности / API (все на 100%)**

- `AUTH_CAPTCHA_CONFIG` - конфигурация CAPTCHA (DIFFICULTY: 'medium', HIDE_LABEL: true)
- `AUTH_FORM_CONFIG` - конфигурация форм (VALIDATE_ON_BLUR: true)
- `AUTH_FIELD_IDS` - уникальные ID для полей форм (LOGIN/REGISTER с EMAIL/PASSWORD/CAPTCHA)
- `AuthCaptchaDifficulty` - тип для уровня сложности CAPTCHA
- `AuthFieldId` - тип для ID полей аутентификации

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { AUTH_CAPTCHA_CONFIG, AUTH_FIELD_IDS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные константы аутентификации
- Нет побочных эффектов - чистые константы
- Обеспечивает уникальность ID элементов форм
- Предотвращает конфликты ID в модальных окнах

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**: компоненты форм аутентификации в packages/ui/src/components/auth/
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Конфигурация CAPTCHA
AUTH_CAPTCHA_CONFIG: {
  DIFFICULTY: 'medium',
  HIDE_LABEL: true
}

// ID полей форм
AUTH_FIELD_IDS: {
  LOGIN: {
    EMAIL: 'auth-login-email',
    PASSWORD: 'auth-login-password',
    CAPTCHA: 'auth-login-captcha'
  },
  REGISTER: {
    EMAIL: 'auth-register-email',
    PASSWORD: 'auth-register-password',
    CONFIRM_PASSWORD: 'auth-register-confirm-password',
    CAPTCHA: 'auth-register-captcha'
  }
}
```

**Риски и безопасность**

- **Низкий риск**: только конфигурационные константы
- **ID uniqueness**: предотвращает конфликты ID в DOM
- **CAPTCHA security**: настройки для защиты от ботов
- **No sensitive data**: не содержит секретов или ключей

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка корректности ID и конфигураций
- **Integration тесты**: использование в компонентах форм
- **Accessibility тесты**: проверка уникальности ID для screen readers
- **Type тесты**: проверка типизации конфигураций

**Оценка сложности (low/medium/high)**
**LOW** - простые конфигурационные константы

**TODO / Рефакторинг**

- ✅ Хорошо структурировано по назначению
- ✅ Правильная типизация с `as const`
- ✅ Комментарии объясняют назначение
- ✅ Удалены дублирующие сообщения в пользу i18n
- 💡 Рекомендация: добавить JSDoc для лучшей документации
