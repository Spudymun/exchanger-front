### Путь: packages/constants/src/validation-bounds.ts

**Краткое назначение (1 предложение)**
Граничные значения для валидации с централизованными лимитами сумм заказов и обменов.

**Подробное описание (3–6 предложений)**
Файл содержит граничные значения для валидации согласно Rule 19 (централизация констант). Определяет базовые константы для валидации (минимальные значения, индексы поиска, максимальные части разделения строк). Включает критически важные лимиты сумм для заказов и обменов, централизованные для устранения дубликатов. Содержит максимальную сумму в UAH и производные константы для обратной совместимости. Предотвращает использование магических чисел в валидации и обеспечивает консистентность граничных проверок.

**Экспортируемые сущности / API (все на 100%)**

- `VALIDATION_BOUNDS` - граничные значения:
  - `MIN_VALUE: 0` - минимальное значение для валидации
  - `SINGLE_ELEMENT: 1` - единичное значение для массивов
  - `NOT_FOUND: -1` - значение "не найдено" для поиска
  - `MAX_SPLIT_PARTS: 2` - максимальные части при разделении строки
  - `MAX_UAH_AMOUNT: 100000000` - максимальная сумма в UAH
  - `MIN_ORDER_AMOUNT: 0.01` - минимальная сумма заказа/обмена
  - `MAX_ORDER_AMOUNT: 1000000` - максимальная сумма заказа/обмена
- `MAX_CRYPTO_AMOUNT` - производная константа для обратной совместимости (= MAX_ORDER_AMOUNT)
- `ValidationBounds` - тип для граничных значений

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { VALIDATION_BOUNDS, MAX_CRYPTO_AMOUNT } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные граничные значения
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность валидации
- Централизует лимиты для устранения дублирования

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Активно используется в**:
  - packages/constants/src/validation.ts (MIN_ORDER_AMOUNT, MAX_ORDER_AMOUNT)
  - packages/utils/src/validation-schemas.ts (MAX_UAH_AMOUNT)
- **Потенциальные потребители**:
  - Валидаторы форм
  - Схемы Zod
  - Бизнес-логика обменов
  - Компоненты ввода сумм
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Граничные значения
VALIDATION_BOUNDS: {
  MIN_VALUE: 0,              // Минимум для валидации
  SINGLE_ELEMENT: 1,         // Единичный элемент
  NOT_FOUND: -1,             // Не найдено в массиве
  MAX_SPLIT_PARTS: 2,        // Максимум частей при split
  MAX_UAH_AMOUNT: 100000000, // 100 млн UAH

  // Лимиты заказов (централизованные)
  MIN_ORDER_AMOUNT: 0.01,    // Минимум 1 копейка
  MAX_ORDER_AMOUNT: 1000000  // Максимум 1 млн
}

// Обратная совместимость
MAX_CRYPTO_AMOUNT = VALIDATION_BOUNDS.MAX_ORDER_AMOUNT

// Примеры использования:
// array.indexOf(item) !== VALIDATION_BOUNDS.NOT_FOUND
// amount >= VALIDATION_BOUNDS.MIN_ORDER_AMOUNT
// parts = string.split(delimiter, VALIDATION_BOUNDS.MAX_SPLIT_PARTS)
```

**Риски и безопасность**

- **Высокий риск**: содержит критически важные финансовые лимиты
- **Financial validation**: неправильные лимиты могут нарушить бизнес-логику
- **Data integrity**: граничные значения обеспечивают корректность данных
- **Business rules**: лимиты заказов влияют на доходность
- **Consistency**: централизация предотвращает расхождения в лимитах

**Тесты / рекомендации по покрытию**

- **Validation тесты**: проверка корректности граничных значений
- **Business logic тесты**: соответствие лимитов бизнес-требованиям
- **Integration тесты**: использование в валидаторах и схемах
- **Edge case тесты**: поведение на граничных значениях
- **Consistency тесты**: отсутствие дублирования лимитов

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важные финансовые лимиты и активно переиспользуется

**TODO / Рефакторинг**

- ✅ Отличная централизация граничных значений
- ✅ Активное переиспользование в других файлах
- ✅ Обратная совместимость через производные константы
- ✅ Предотвращение магических чисел
- ✅ Следует Rule 19 (централизация) и Rule 20 (устранение дублирования)
- 💡 Рекомендация: добавить валидацию разумности граничных значений
- 💡 Рекомендация: документировать обоснование каждого лимита
- 💡 Рекомендация: рассмотреть добавление констант для других валют
