### Путь: packages/constants/src/banks.ts

**Краткое назначение (1 предложение)**
Конфигурация банков и платежных систем для различных фиатных валют с резервами и вспомогательными функциями.

**Подробное описание (3–6 предложений)**
Файл содержит конфигурацию банков для ExchangeGO, организованную по валютам (UAH, USD, EUR). Определяет банки для каждой валюты с приоритетами, логотипами и статусами активности. Включает моковые резервы для каждого банка по валютам для тестирования и разработки. Предоставляет вспомогательные функции для получения банков по валюте, поиска банка по ID и получения резервов. Основан на требованиях из exchanger_AC.md для поддержки различных фиатных валют.

**Экспортируемые сущности / API (все на 100%)**

- `BANKS_BY_CURRENCY` - конфигурация банков по валютам (UAH: ПриватБанк, Монобанк и др.; USD: Wise, Payoneer, Revolut; EUR: Revolut, Wise, N26)
- `Bank` - интерфейс банка (id, name, shortName, logoUrl, isActive, priority)
- `ALL_BANK_IDS` - массив всех ID банков
- `MOCK_BANK_RESERVES` - моковые резервы банков по валютам
- `getBanksForCurrency(currency)` - функция получения банков для валюты
- `getBankById(bankId)` - функция поиска банка по ID
- `getBankReserve(bankId, currency)` - функция получения резерва банка
- `FiatCurrency` - тип для фиатных валют

**Входы (expected inputs) / Параметры (все на 100%)**

- Импортирует `FIAT_CURRENCIES` из './fiat-currencies'
- `getBanksForCurrency(currency: FiatCurrency)` - валюта для поиска банков
- `getBankById(bankId: string)` - ID банка для поиска
- `getBankReserve(bankId: string, currency: FiatCurrency)` - ID банка и валюта

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает конфигурации банков и резервы
- Нет побочных эффектов - чистые функции и константы
- `getBanksForCurrency` возвращает readonly Bank[] или пустой массив
- `getBankById` возвращает Bank | undefined
- `getBankReserve` возвращает number (резерв или 0 по умолчанию)

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: fiat-currencies.ts (FIAT_CURRENCIES)
- **Потенциальные потребители**: компоненты выбора банков, обменные формы, API роутеры
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Структура банка
interface Bank {
  id: string;           // 'privatbank', 'monobank'
  name: string;         // 'ПриватБанк', 'Монобанк'
  shortName: string;    // 'Приват', 'Моно'
  logoUrl: string;      // '/images/banks/privatbank.svg'
  isActive: boolean;    // true/false
  priority: number;     // 1, 2, 3, 4
}

// Резервы банков
MOCK_BANK_RESERVES: {
  privatbank: { UAH: 10000000, USD: 0, EUR: 0 },
  wise: { UAH: 0, USD: 150000, EUR: 0 },
  // ... остальные банки
}
```

**Риски и безопасность**

- **Средний риск**: содержит бизнес-конфигурацию банков
- **Mock data**: резервы являются тестовыми данными
- **Logo paths**: пути к логотипам должны быть защищены от XSS
- **Bank validation**: необходима валидация существования банков
- **Currency matching**: критически важно соответствие банков валютам

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка вспомогательных функций
- **Data integrity тесты**: соответствие банков валютам
- **Mock data тесты**: корректность резервов
- **Integration тесты**: использование в компонентах выбора банков

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит бизнес-логику, функции и зависимости

**TODO / Рефакторинг**

- ✅ Хорошо структурировано по валютам
- ✅ Типизированные интерфейсы и функции
- ✅ Комментарии объясняют назначение
- ⚠️ Mock резервы - заменить на реальные данные в продакшене
- 💡 Рекомендация: добавить валидацию банков при инициализации
- 💡 Рекомендация: рассмотреть вынос логотипов в отдельную конфигурацию
