### Путь: packages/constants/src/order-statuses.ts

**Краткое назначение (1 предложение)**
Централизованные статусы заказов и тикетов с UI конфигурацией и группировкой для бизнес-логики.

**Подробное описание (3–6 предложений)**
Файл содержит все статусы заказов ExchangeGO согласно Rule 20 (запрет избыточности). Определяет основные статусы заказов (pending, paid, processing, completed, cancelled, failed) с детальной UI конфигурацией включающей цвета, иконки и описания. Включает группировку статусов для фильтрации и бизнес-логики (активные, завершенные, неудачные). Содержит статусы тикетов поддержки и массивы значений для Zod схем. Обеспечивает единый источник истины для всех статусов в системе.

**Экспортируемые сущности / API (все на 100%)**

- `ORDER_STATUSES` - основные статусы заказов:
  - `PENDING: 'pending'` - ожидание оплаты
  - `PAID: 'paid'` - оплачено
  - `PROCESSING: 'processing'` - в обработке
  - `COMPLETED: 'completed'` - выполнено
  - `CANCELLED: 'cancelled'` - отменено
  - `FAILED: 'failed'` - неудачно
- `ORDER_STATUS_CONFIG` - UI конфигурация статусов (label, color, icon, description)
- `ORDER_STATUS_GROUPS` - группировка статусов:
  - `ACTIVE` - активные заказы
  - `COMPLETED` - завершенные
  - `FAILED` - неудачные
  - `NEEDS_PAYMENT` - требуют оплаты
  - `IN_PROGRESS` - в процессе
  - `FINAL` - финальные статусы
- `TICKET_STATUSES` - статусы тикетов (OPEN, IN_PROGRESS, RESOLVED, CLOSED)
- `OrderStatus` / `TicketStatus` - типы статусов
- `ORDER_STATUS_VALUES` / `TICKET_STATUS_VALUES` - массивы для Zod схем

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { ORDER_STATUSES, ORDER_STATUS_CONFIG } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные статусы и конфигурации
- Нет побочных эффектов - чистые константы
- Обеспечивает консистентность отображения статусов
- Предоставляет группировки для бизнес-логики

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**:
  - Компоненты отображения заказов
  - Бизнес-логика обработки заказов
  - Фильтры и поиск заказов
  - Валидаторы статусов (Zod схемы)
  - Компоненты тикетов поддержки
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Основные статусы
ORDER_STATUSES: {
  PENDING: 'pending',      // Ожидание оплаты
  PAID: 'paid',           // Оплачено
  PROCESSING: 'processing', // В обработке
  COMPLETED: 'completed',   // Выполнено
  CANCELLED: 'cancelled',   // Отменено
  FAILED: 'failed'         // Неудачно
}

// UI конфигурация
ORDER_STATUS_CONFIG: {
  pending: {
    label: 'Ожидание оплаты',
    color: 'warning',
    icon: 'clock',
    description: 'Переведите криптовалюту на указанный адрес'
  },
  completed: {
    label: 'Выполнено',
    color: 'success',
    icon: 'check-circle-2',
    description: 'Средства переведены на ваш счет'
  }
  // ... остальные статусы
}

// Группировка для бизнес-логики
ORDER_STATUS_GROUPS: {
  ACTIVE: ['pending', 'paid', 'processing'],
  COMPLETED: ['completed'],
  FAILED: ['cancelled', 'failed'],
  FINAL: ['completed', 'cancelled', 'failed']
}
```

**Риски и безопасность**

- **Высокий риск**: критически важно для бизнес-процессов
- **Business logic**: статусы определяют flow обработки заказов
- **UI consistency**: конфигурация влияет на пользовательский опыт
- **State transitions**: неправильные группировки могут нарушить логику переходов
- **Data integrity**: статусы должны соответствовать базе данных

**Тесты / рекомендации по покрытию**

- **Business logic тесты**: проверка корректности группировок статусов
- **State transition тесты**: валидные переходы между статусами
- **UI consistency тесты**: корректность отображения статусов
- **Zod schema тесты**: использование в валидации
- **Integration тесты**: использование в компонентах заказов

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важную бизнес-логику статусов

**TODO / Рефакторинг**

- ✅ Отличная централизация всех статусов системы
- ✅ Comprehensive UI конфигурация с цветами и иконками
- ✅ Логическая группировка для бизнес-логики
- ✅ Поддержка Zod схем через массивы значений
- ✅ Правильная типизация статусов
- 💡 Рекомендация: добавить валидацию допустимых переходов между статусами
- 💡 Рекомендация: рассмотреть добавление временных меток для статусов
- 💡 Рекомендация: добавить локализацию описаний статусов
