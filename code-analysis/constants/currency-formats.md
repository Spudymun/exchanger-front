### Путь: packages/constants/src/currency-formats.ts

**Краткое назначение (1 предложение)**
Зарезервированный файл для будущих констант форматов валют и кодировок.

**Подробное описание (3–6 предложений)**
Файл создан для централизации констант форматов валют согласно Rule 19 (централизация констант). В настоящее время содержит только пустую структуру, зарезервированную для будущего развития. Предназначен для хранения форматов отображения валют, локализованных форматов чисел, символов валют и правил кодировки. Следует архитектурному принципу подготовки инфраструктуры для будущих потребностей. Обеспечивает типизацию через TypeScript даже для пустой структуры.

**Экспортируемые сущности / API (все на 100%)**

- `CURRENCY_FORMATS` - пустой объект для будущих констант форматов валют
- `CurrencyFormats` - тип для форматов валют (в настоящее время пустой)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только пустую структуру
- Импортируется как ES модуль: `import { CURRENCY_FORMATS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает пустой типизированный объект
- Нет побочных эффектов - только структура
- Готов для добавления констант форматов валют
- Обеспечивает типизацию для будущих расширений

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**: компоненты форматирования валют, локализация
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Текущая структура (пустая)
CURRENCY_FORMATS: {
}

// Потенциальная будущая структура
// CURRENCY_FORMATS: {
//   USD: { symbol: '$', decimals: 2, locale: 'en-US' },
//   EUR: { symbol: '€', decimals: 2, locale: 'de-DE' },
//   UAH: { symbol: '₴', decimals: 2, locale: 'uk-UA' }
// }
```

**Риски и безопасность**

- **Низкий риск**: пустая структура без функциональности
- **Future planning**: готовность к добавлению форматов валют
- **Type safety**: типизация обеспечена даже для пустой структуры
- **No breaking changes**: добавление констант не нарушит существующий код

**Тесты / рекомендации по покрытию**

- **Structure тесты**: проверка существования экспорта
- **Type тесты**: проверка корректности типизации
- **Future readiness тесты**: готовность к добавлению констант
- **Integration тесты**: совместимость с системой импортов

**Оценка сложности (low/medium/high)**
**LOW** - пустая структура без логики

**TODO / Рефакторинг**

- ✅ Правильная архитектурная подготовка
- ✅ Типизация готова для расширения
- ✅ Следует принципу централизации констант
- 📋 TODO: добавить форматы для поддерживаемых валют (USD, EUR, UAH, BTC, ETH, USDT, LTC)
- 📋 TODO: добавить локализованные форматы чисел
- 📋 TODO: добавить символы валют и правила отображения
- 💡 Рекомендация: определить структуру формата валют перед добавлением
