### Путь: packages/constants/src/business-limits.ts

**Краткое назначение (1 предложение)**
Централизованные бизнес-лимиты и константы для устранения магических чисел в коде.

**Подробное описание (3–6 предложений)**
Файл содержит все бизнес-лимиты проекта с семантическими именами согласно Rule 20 (запрет избыточности). Включает денежные лимиты (минимальные суммы, максимальные количества заказов), временные интервалы (таймауты, интервалы обновления), размеры пагинации и API лимиты. Все константы имеют описательные комментарии JSDoc для понимания назначения. Предотвращает использование магических чисел в коде, обеспечивая централизованное управление бизнес-правилами. Следует принципу единого источника истины для всех числовых бизнес-констант.

**Экспортируемые сущности / API (все на 100%)**

- `BUSINESS_LIMITS` - объект с бизнес-лимитами:
  - Денежные: `MIN_WITHDRAWAL_AMOUNT: 10`, `MAX_ACTIVE_ORDERS_PER_USER: 20`, `MAX_ORDER_ITEMS: 50`, `MAX_PORTFOLIO_ASSETS: 100`
  - Временные: `ORDER_STATUS_UPDATE_INTERVAL_MS: 1000`, `DEFAULT_NOTIFICATION_DURATION_MS: 5000`, `ERROR_NOTIFICATION_DURATION_MS: 8000`
  - Пагинация: `MIN_PAGE_SIZE: 10`, `MAX_PAGE_SIZE: 100`, `DEFAULT_PAGE_SIZE: 20`
  - API: `MAX_REQUESTS_PER_MINUTE: 100`
  - Тестовые: `TEST_ORDER_AMOUNT: 1000`
  - Прочие: `PERCENTAGE_BASE: 100`, `MAX_NOTIFICATIONS: 8`
- `BusinessLimits` - тип для бизнес-лимитов

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { BUSINESS_LIMITS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные бизнес-лимиты
- Нет побочных эффектов - чистые константы
- Обеспечивает семантические имена для магических чисел
- Централизует управление бизнес-правилами

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**: валидаторы, бизнес-логика, UI компоненты, API роутеры
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Бизнес-лимиты
BUSINESS_LIMITS: {
  // Денежные лимиты
  MIN_WITHDRAWAL_AMOUNT: 10,
  MAX_ACTIVE_ORDERS_PER_USER: 20,
  MAX_ORDER_ITEMS: 50,
  MAX_PORTFOLIO_ASSETS: 100,

  // Временные интервалы (мс)
  ORDER_STATUS_UPDATE_INTERVAL_MS: 1000,
  DEFAULT_NOTIFICATION_DURATION_MS: 5000,
  ERROR_NOTIFICATION_DURATION_MS: 8000,
  WARNING_NOTIFICATION_DURATION_MS: 7000,

  // Пагинация
  MIN_PAGE_SIZE: 10,
  MAX_PAGE_SIZE: 100,
  DEFAULT_PAGE_SIZE: 20,

  // API лимиты
  MAX_REQUESTS_PER_MINUTE: 100
}
```

**Риски и безопасность**

- **Высокий риск**: содержит критически важные бизнес-лимиты
- **Business rules**: изменения влияют на всю бизнес-логику
- **Rate limiting**: MAX_REQUESTS_PER_MINUTE критичен для защиты от DDoS
- **Financial limits**: MIN_WITHDRAWAL_AMOUNT влияет на финансовые операции
- **Performance**: интервалы обновления влияют на производительность

**Тесты / рекомендации по покрытию**

- **Business logic тесты**: проверка соответствия бизнес-требованиям
- **Validation тесты**: использование лимитов в валидаторах
- **Performance тесты**: проверка влияния интервалов на производительность
- **Integration тесты**: использование в реальных бизнес-сценариях

**Оценка сложности (low/medium/high)**
**LOW** - простые числовые константы с хорошей документацией

**TODO / Рефакторинг**

- ✅ Отличная документация с JSDoc комментариями
- ✅ Семантические имена вместо магических чисел
- ✅ Следует Rule 20 (запрет избыточности)
- ✅ Логическая группировка по доменам
- 💡 Рекомендация: рассмотреть добавление валидации диапазонов значений
- 💡 Рекомендация: добавить unit тесты для проверки разумности значений
