### Путь: packages/constants/src/user.ts

**Краткое назначение (1 предложение)**
Пользовательские константы включающие маппинг ролей к приложениям, сообщения API и конфигурацию пользователей.

**Подробное описание (3–6 предложений)**
Файл содержит константы связанные с пользователями и их взаимодействием с системой. Определяет области приложений (admin-panel, web) и маппинг ролей пользователей к соответствующим приложениям. Включает сообщения для пользовательского API (ошибки и успешные операции) на русском языке. Содержит конфигурацию пользователей с лимитами заказов и параметрами верификации. Определяет статусы заказов, которые можно отменить, следуя принципу переиспользования из exchange.ts.

**Экспортируемые сущности / API (все на 100%)**

- `APP_SCOPE` - области приложений (ADMIN_PANEL: 'admin-panel', WEB_APP: 'web')
- `ROLE_TO_APP_MAPPING` - маппинг ролей к приложениям:
  - ADMIN → admin-panel
  - OPERATOR, SUPPORT, USER → web app
- `USER_MESSAGES` - сообщения об ошибках:
  - NOT_FOUND, ORDER_NOT_FOUND, NO_ORDER_ACCESS, INVALID_PASSWORD и др.
  - ACTIVE_ORDERS_EXIST(count) - функция для сообщения с количеством заказов
- `USER_CONFIG` - конфигурация пользователей:
  - DEFAULT_ORDERS_LIMIT: 20, MAX_ORDERS_LIMIT: 100
  - VERIFICATION_CODE_BASE: 36, VERIFICATION_CODE_LENGTH: 8
- `USER_SUCCESS_MESSAGES` - сообщения об успехе:
  - PASSWORD_CHANGED, PROFILE_UPDATED, ORDER_CANCELLED и др.
- `CANCELLABLE_ORDER_STATUSES` - статусы для отмены заказов ['pending', 'processing']
- `AppScope` - тип для областей приложений

**Входы (expected inputs) / Параметры (все на 100%)**

- Импортирует `USER_ROLES` из './business'
- `ACTIVE_ORDERS_EXIST(count: number)` - количество активных заказов

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает пользовательские константы и конфигурации
- Нет побочных эффектов - чистые константы
- `ACTIVE_ORDERS_EXIST` возвращает строку с интерполированным количеством
- Обеспечивает маппинг ролей к приложениям

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: business.ts (USER_ROLES)
- **Потенциальные потребители**:
  - Middleware авторизации
  - API роутеры пользователей
  - Компоненты отображения сообщений
  - Валидаторы доступа к приложениям
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Области приложений
APP_SCOPE: {
  ADMIN_PANEL: 'admin-panel',
  WEB_APP: 'web'
}

// Маппинг ролей
ROLE_TO_APP_MAPPING: {
  admin: ['admin-panel'],           // Только админ-панель
  operator: ['web'],               // Только веб-приложение
  support: ['web'],                // Только веб-приложение
  user: ['web']                    // Только веб-приложение
}

// Сообщения об ошибках
USER_MESSAGES: {
  NOT_FOUND: 'Пользователь не найден',
  ORDER_NOT_FOUND: 'Заявка не найдена',
  NO_ORDER_ACCESS: 'Нет доступа к этой заявке',
  ACTIVE_ORDERS_EXIST: (count) => `Нельзя удалить аккаунт с активными заявками (${count})`
}

// Конфигурация
USER_CONFIG: {
  DEFAULT_ORDERS_LIMIT: 20,
  MAX_ORDERS_LIMIT: 100,
  VERIFICATION_CODE_BASE: 36,
  VERIFICATION_CODE_LENGTH: 8
}
```

**Риски и безопасность**

- **Высокий риск**: содержит критически важную логику авторизации
- **Access control**: маппинг ролей определяет доступ к приложениям
- **Security messages**: сообщения не должны раскрывать чувствительную информацию
- **Business logic**: лимиты заказов влияют на бизнес-процессы
- **User experience**: сообщения влияют на понимание пользователем системы

**Тесты / рекомендации по покрытию**

- **Authorization тесты**: проверка корректности маппинга ролей
- **Message consistency тесты**: соответствие сообщений бизнес-логике
- **Access control тесты**: правильность ограничения доступа
- **Localization тесты**: корректность русских сообщений
- **Integration тесты**: использование в API роутерах

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит критически важную логику авторизации и пользовательские сообщения

**TODO / Рефакторинг**

- ✅ Правильное переиспользование USER_ROLES (следует Rule 20)
- ✅ Хорошая структуризация по типам сообщений
- ✅ Функциональные сообщения с параметрами
- ✅ Логическое разделение конфигурации и сообщений
- ⚠️ Сообщения на русском - рассмотреть миграцию на i18n
- 💡 Рекомендация: добавить валидацию корректности маппинга ролей
- 💡 Рекомендация: рассмотреть вынос сообщений в отдельный файл
- 💡 Рекомендация: добавить типизацию для функциональных сообщений
