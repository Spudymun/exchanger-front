### Путь: packages/constants/src/api.ts

**Краткое назначение (1 предложение)**
Централизованные константы для API endpoints, HTTP статусов, методов и типов контента.

**Подробное описание (3–6 предложений)**
Файл содержит все API-related константы проекта, обеспечивая единый источник истины для HTTP коммуникации. Включает endpoints для пользователей, аутентификации, обменных операций и торговли. Все HTTP статус коды организованы по категориям (успех, клиентские ошибки, серверные ошибки). Методы HTTP и типы контента определены для типобезопасного использования. Все константы экспортируются с соответствующими TypeScript типами для строгой типизации.

**Экспортируемые сущности / API (все на 100%)**

- `API_ENDPOINTS` - объект с endpoint'ами (USERS, AUTH_LOGIN, EXCHANGES, ORDERS и др.)
- `HTTP_STATUS` - объект с HTTP статус кодами (OK: 200, BAD_REQUEST: 400, и др.)
- `API_METHODS` - объект с HTTP методами (GET, POST, PUT, PATCH, DELETE)
- `CONTENT_TYPES` - объект с типами контента (JSON, FORM_DATA, URL_ENCODED)
- `ApiEndpoint` - тип для endpoint'ов
- `HttpStatus` - тип для HTTP статусов
- `ApiMethod` - тип для HTTP методов
- `ContentType` - тип для типов контента

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только константы
- Импортируется как ES модуль: `import { API_ENDPOINTS, HTTP_STATUS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные константы для использования в HTTP запросах
- Нет побочных эффектов - чистые константы
- Не изменяет состояние и не делает запросы
- Обеспечивает type safety через `as const` assertions

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортируется в**: docs/CONSTANTS_EXAMPLES.ts (подтверждено grep поиском)
- **Потенциальные потребители**: tRPC роутеры, API клиенты, middleware
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Основные структуры
API_ENDPOINTS: {
  USERS: '/api/users',
  AUTH_LOGIN: '/api/auth/login',
  EXCHANGES: '/api/exchanges',
  // ... остальные endpoints
}

HTTP_STATUS: {
  OK: 200,
  BAD_REQUEST: 400,
  INTERNAL_SERVER_ERROR: 500,
  // ... остальные статусы
}

// Типы
type ApiEndpoint = string (literal types)
type HttpStatus = number (literal types)
```

**Риски и безопасность**

- **Низкий риск**: только константы, нет секретов
- **Endpoint security**: endpoints не содержат чувствительной информации
- **Type safety**: строгая типизация предотвращает ошибки
- **No secrets**: не содержит API ключей или токенов

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка корректности значений констант
- **Type тесты**: проверка соответствия типов
- **Integration тесты**: использование в реальных HTTP запросах
- **Покрытие**: проверка всех экспортируемых констант

**Оценка сложности (low/medium/high)**
**LOW** - простые константы без логики

**TODO / Рефакторинг**

- ✅ Хорошо структурировано по доменам
- ✅ Правильная типизация с `as const`
- ✅ Следует принципам DRY
- 💡 Рекомендация: добавить JSDoc комментарии для лучшей документации
