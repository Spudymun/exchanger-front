### Путь: packages/constants/src/exchange.ts

**Краткое назначение (1 предложение)**
Специфичные константы для ExchangeGO включающие лимиты сумм, валидацию, таймауты и настройки обменных операций.

**Подробное описание (3–6 предложений)**
Файл содержит константы специфичные для криптовалютной биржи ExchangeGO. Определяет лимиты сумм обмена в USD эквиваленте (мин $10, макс $5000). Включает валидационные ограничения для ID заказов, криптоадресов и номеров карт. Содержит настройки таймаутов, повторных попыток и задержек для обменных операций. Импортирует и переиспользует константы из других файлов для предотвращения дублирования. Включает настройки пагинации для заказов.

**Экспортируемые сущности / API (все на 100%)**

- `AMOUNT_LIMITS` - лимиты сумм (MIN_USD: 10, MAX_USD: 5000)
- `EXCHANGE_VALIDATION_LIMITS` - ограничения валидации (ORDER_ID_LENGTH, CRYPTO_ADDRESS_MAX_LENGTH, CARD_NUMBER_LENGTH)
- `EXCHANGE_VALIDATION_PATTERNS` - regex паттерны (CARD_NUMBER)
- `EXCHANGE_VALIDATION_MESSAGES` - сообщения валидации (fallback на английском)
- `EXCHANGE_STATUS_*` - статусы обмена (PROCESSING, COMPLETED, FAILED)
- `EXCHANGE_REQUEST_TIMEOUT_MS` - таймаут запросов
- `EXCHANGE_RETRY_ATTEMPTS` - количество повторных попыток (3)
- `EXCHANGE_RETRY_DELAY_MS` - задержка между попытками (1000ms)
- `MIN_EXCHANGE_AMOUNT` / `MAX_EXCHANGE_AMOUNT` - лимиты сумм обмена
- `EXCHANGE_AMOUNT_PRECISION` - точность сумм
- `API_DELAY_MS` / `ORDER_CREATION_DELAY_MS` - задержки API
- `DEFAULT_ORDER_LIMIT` / `MAX_ORDER_LIMIT` - лимиты пагинации заказов

**Входы (expected inputs) / Параметры (все на 100%)**

- Импортирует константы из других файлов:
  - `DECIMAL_PRECISION` из './decimal-precision'
  - `REQUEST_TIMEOUT_CONSTANTS` из './time-constants'
  - `VALIDATION_BOUNDS` из './validation-bounds'

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает типизированные константы для обменных операций
- Нет побочных эффектов - чистые константы
- Переиспользует существующие константы (следует Rule 20)
- Предоставляет fallback сообщения на английском языке

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Импортирует из**: decimal-precision.ts, time-constants.ts, validation-bounds.ts
- **Потенциальные потребители**: обменные компоненты, валидаторы, API роутеры
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Лимиты сумм
AMOUNT_LIMITS: {
  MIN_USD: 10,
  MAX_USD: 5000
}

// Валидационные ограничения
EXCHANGE_VALIDATION_LIMITS: {
  ORDER_ID_LENGTH: 36,
  CRYPTO_ADDRESS_MAX_LENGTH: 100,
  CARD_NUMBER_LENGTH: 16
}

// Regex паттерны
EXCHANGE_VALIDATION_PATTERNS: {
  CARD_NUMBER: /^\d{16}$/
}

// Настройки повторных попыток
EXCHANGE_RETRY_ATTEMPTS: 3
EXCHANGE_RETRY_DELAY_MS: 1000
```

**Риски и безопасность**

- **Средний риск**: содержит бизнес-лимиты и валидацию
- **Financial limits**: критически важные лимиты сумм обмена
- **Validation security**: regex для валидации номеров карт
- **Timeout security**: защита от зависших запросов
- **No secrets**: не содержит API ключей или токенов

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка корректности лимитов и паттернов
- **Validation тесты**: проверка regex паттернов
- **Business logic тесты**: проверка соответствия бизнес-требованиям
- **Integration тесты**: использование в обменных операциях

**Оценка сложности (low/medium/high)**
**MEDIUM** - содержит бизнес-логику и зависимости от других констант

**TODO / Рефакторинг**

- ✅ Правильное переиспользование констант (Rule 20)
- ✅ Хорошо структурировано по назначению
- ✅ Комментарии объясняют источники значений
- ⚠️ Fallback сообщения на английском - рассмотреть полный переход на i18n
- 💡 Рекомендация: добавить JSDoc для бизнес-лимитов
- 💡 Рекомендация: рассмотреть вынос regex в отдельный файл patterns.ts
