### Путь: packages/constants/src/rate-limits.ts

**Краткое назначение (1 предложение)**
Конфигурация rate limiting для защиты критических операций ExchangeGO от злоупотреблений.

**Подробное описание (3–6 предложений)**
Файл содержит конфигурацию ограничений частоты запросов для критически важных операций обменника. Определяет лимиты для создания заказов, регистрации, входа в систему и сброса паролей с указанием количества попыток, продолжительности окна и времени блокировки. Обеспечивает защиту от DDoS атак, брутфорс атак и злоупотреблений системой. Настроен с учетом пользовательского опыта - разумные лимиты, не мешающие нормальному использованию. Критически важен для безопасности и стабильности платформы.

**Экспортируемые сущности / API (все на 100%)**

- `RATE_LIMITS` - конфигурация ограничений:
  - `CREATE_ORDER` - создание заказов (3 попытки за час, блокировка на час)
  - `REGISTER` - регистрация (5 попыток за 24 часа, блокировка на 24 часа)
  - `LOGIN` - вход в систему (10 попыток за 15 минут, блокировка на 15 минут)
  - `RESET_PASSWORD` - сброс пароля (3 попытки за час, блокировка на час)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - файл содержит только конфигурацию
- Импортируется как ES модуль: `import { RATE_LIMITS } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает конфигурацию rate limiting
- Нет побочных эффектов - чистые константы
- Используется middleware для ограничения запросов
- Влияет на безопасность и производительность системы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Потенциальные потребители**:
  - Rate limiting middleware
  - API роутеры аутентификации
  - Обработчики создания заказов
  - Системы мониторинга безопасности
- **Не импортирует**: никаких других модулей (zero dependencies)
- **Экспортируется через**: packages/constants/src/index.ts

**Домен данных / типы (все на 100%)**

```typescript
// Конфигурация rate limits
RATE_LIMITS: {
  CREATE_ORDER: {
    points: 3,           // 3 заказа
    duration: 3600,      // за 1 час (3600 сек)
    blockDuration: 3600  // блокировка на 1 час
  },
  REGISTER: {
    points: 5,           // 5 регистраций
    duration: 86400,     // за 24 часа
    blockDuration: 86400 // блокировка на 24 часа
  },
  LOGIN: {
    points: 10,          // 10 попыток входа
    duration: 900,       // за 15 минут
    blockDuration: 900   // блокировка на 15 минут
  },
  RESET_PASSWORD: {
    points: 3,           // 3 сброса пароля
    duration: 3600,      // за 1 час
    blockDuration: 3600  // блокировка на 1 час
  }
}
```

**Риски и безопасность**

- **Критический риск**: прямо влияет на безопасность системы
- **DDoS protection**: защита от атак на доступность сервиса
- **Brute force protection**: защита от подбора паролей
- **Business continuity**: предотвращение злоупотреблений заказами
- **User experience**: баланс между безопасностью и удобством

**Тесты / рекомендации по покрытию**

- **Security тесты**: проверка эффективности защиты от атак
- **Performance тесты**: влияние на производительность системы
- **User experience тесты**: не блокируют ли лимиты нормальных пользователей
- **Integration тесты**: корректная работа с middleware
- **Load тесты**: поведение под высокой нагрузкой

**Оценка сложности (low/medium/high)**
**MEDIUM** - критически важная конфигурация безопасности

**TODO / Рефакторинг**

- ✅ Разумные лимиты для разных типов операций
- ✅ Учет специфики каждой операции
- ✅ Баланс между безопасностью и UX
- 💡 Рекомендация: добавить мониторинг срабатывания лимитов
- 💡 Рекомендация: рассмотреть динамические лимиты на основе поведения
- 💡 Рекомендация: добавить whitelist для доверенных IP
- 💡 Рекомендация: логирование попыток превышения лимитов
