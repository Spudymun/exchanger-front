### Путь: packages/constants/src/index.ts

**Краткое назначение (1 предложение)**
Центральная точка экспорта всех констант пакета, обеспечивающая единый интерфейс для импорта.

**Подробное описание (3–6 предложений)**
Файл служит единой точкой входа для всех констант проекта, реэкспортируя содержимое всех модулей пакета. Организован по логическим группам: базовые константы (api, auth, business, ui, validation, user), специфичные для ExchangeGO (exchange, currencies, banks), технические (linter-limits), семантические константы для устранения магических чисел. Включает контактную информацию и социальные ссылки. Следует принципу barrel exports для удобного импорта из других пакетов.

**Экспортируемые сущности / API (все на 100%)**

- **Базовые модули**: api, auth, business, ui, validation, user
- **ExchangeGO специфичные**: exchange, exchange-currencies, fiat-currencies, banks, rate-limits, order-statuses
- **Технические**: linter-limits
- **Семантические константы**: decimal-precision, validation-bounds, percentage-calculations, time-constants, currency-formats, business-limits
- **Контактная информация**: contacts

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - только реэкспорты
- Импортируется как: `import { API_ENDPOINTS, USER_ROLES, THEME_MODES } from '@repo/constants'`

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет все константы через единый интерфейс
- Нет побочных эффектов - только реэкспорты
- Обеспечивает tree-shaking для неиспользуемых констант
- Упрощает импорты в других пакетах

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Реэкспортирует из**: всех файлов в packages/constants/src/
- **Импортируется в**: все пакеты проекта (ui, hooks, utils, exchange-core, apps)
- **Основная точка доступа** для внешних пакетов к константам

**Домен данных / типы (все на 100%)**

```typescript
// Все экспорты доступны через единый интерфейс:
import {
  // API и HTTP
  API_ENDPOINTS,
  HTTP_STATUS,
  // Бизнес-логика
  USER_ROLES,
  ORDER_TYPES,
  TRANSACTION_TYPES,
  // UI и темизация
  THEME_MODES,
  BUTTON_VARIANTS,
  UI_NUMERIC_CONSTANTS,
  // Валидация
  VALIDATION_LIMITS,
  AUTH_CONSTANTS,
  // ExchangeGO специфичные
  AMOUNT_LIMITS,
  EXCHANGE_VALIDATION_LIMITS,
} from '@repo/constants';
```

**Риски и безопасность**

- **Низкий риск**: только реэкспорты без логики
- **Dependency management**: централизованный доступ к константам
- **Version control**: изменения в одном месте влияют на весь проект
- **Tree-shaking**: неиспользуемые константы исключаются из бандла

**Тесты / рекомендации по покрытию**

- **Export тесты**: проверка доступности всех экспортов
- **Integration тесты**: проверка импортов в других пакетах
- **Bundle тесты**: проверка tree-shaking
- **Type тесты**: проверка корректности типов экспортов

**Оценка сложности (low/medium/high)**
**LOW** - простые реэкспорты без логики

**TODO / Рефакторинг**

- ✅ Хорошо организовано по логическим группам
- ✅ Комментарии объясняют назначение групп
- ✅ Следует принципу barrel exports
- ✅ Поддерживает tree-shaking
- 💡 Рекомендация: рассмотреть группировку экспортов по namespace'ам для больших проектов
