# Анализ файла: packages/eslint-config/next.js

## 📋 Назначение

ESLint конфигурация для Next.js приложений в ExchangeGO монорепо. Объединяет базовые правила с React и Next.js специфичными настройками, включая оптимизации производительности и контроль стилизации.

## 📝 Описание

Комплексная конфигурация для Next.js приложений, включающая:

- **Базовая конфигурация** - наследование от base.js
- **React правила** - корректная работа с React компонентами
- **Next.js оптимизации** - Core Web Vitals и рекомендуемые правила
- **React Hooks** - правила для корректного использования хуков
- **Style control** - запрет inline стилей в пользу CSS классов
- **Service Worker support** - поддержка PWA функциональности

Особый акцент на производительность и соответствие современным стандартам веб-разработки.

## 🔌 API и интерфейсы

### Основной экспорт:

```javascript
export const nextJsConfig = [
  ...baseConfig, // Базовые правила enterprise уровня
  js.configs.recommended, // Рекомендуемые JS правила
  eslintConfigPrettier, // Prettier интеграция
  ...tseslint.configs.recommended, // TypeScript правила

  // React конфигурация
  {
    ...pluginReact.configs.flat.recommended,
    languageOptions: {
      globals: { ...globals.serviceworker }, // Service Worker поддержка
    },
  },

  // Next.js правила
  {
    plugins: { '@next/next': pluginNext },
    rules: {
      ...pluginNext.configs.recommended.rules, // Базовые Next.js правила
      ...pluginNext.configs['core-web-vitals'].rules, // Core Web Vitals оптимизации
    },
  },

  // React Hooks + кастомные правила
  {
    plugins: { 'react-hooks': pluginReactHooks },
    settings: { react: { version: 'detect' } },
    rules: {
      ...pluginReactHooks.configs.recommended.rules,
      'react/react-in-jsx-scope': 'off', // Новый JSX transform
      'react/forbid-dom-props': [
        'warn',
        {
          // Запрет inline стилей
          forbid: [{ propName: 'style', message: '...' }],
        },
      ],
      'no-magic-numbers': [
        'warn',
        {
          // Контроль магических чисел
          detectObjects: true, // Включая style объекты
        },
      ],
    },
  },
];
```

### Ключевые плагины:

- **@next/eslint-plugin-next** - Next.js специфичные правила
- **eslint-plugin-react** - React правила и best practices
- **eslint-plugin-react-hooks** - Rules of Hooks соблюдение
- **globals** - Service Worker API поддержка

## 📥 Входящие зависимости

```javascript
import js from '@eslint/js';
import pluginNext from '@next/eslint-plugin-next';
import eslintConfigPrettier from 'eslint-config-prettier';
import pluginReact from 'eslint-plugin-react';
import pluginReactHooks from 'eslint-plugin-react-hooks';
import globals from 'globals';
import tseslint from 'typescript-eslint';
import { config as baseConfig } from './base.js';
```

### Критические зависимости:

- **base.js** - базовая enterprise конфигурация
- **@next/eslint-plugin-next** - Core Web Vitals правила
- **eslint-plugin-react** - React ecosystem

## 📤 Исходящие зависимости

- **Next.js приложения** - web app, admin-panel, docs
- **React компоненты** - валидация всех React файлов
- **Build pipeline** - оптимизации производительности
- **Developer workflow** - real-time валидация в IDE

## 🔗 Взаимосвязи с другими компонентами

### Архитектурные связи:

- **base.js** - наследование enterprise правил
- **react.js** - возможное дублирование React правил
- **Tailwind CSS** - интеграция с утилитарными классами
- **Design system** - проверка использования дизайн-токенов

### Применение в проектах:

- **apps/web** - основное криптообменное приложение
- **apps/admin-panel** - административный интерфейс
- **apps/docs** - документационное приложение
- **Storybook** - документация компонентов

## 📊 Типы данных

### Структура конфигурации:

```javascript
interface NextJsConfig extends Array<EslintConfigRule> {
  // Наследует от базовой конфигурации
  // Добавляет React и Next.js специфичные правила
}

interface ReactRules {
  'react/react-in-jsx-scope': 'off';
  'react/forbid-dom-props': [string, ForbidDomPropsConfig];
}

interface ForbidDomPropsConfig {
  forbid: Array<{
    propName: string;
    message: string;
  }>;
}

interface NextJsRules {
  // Из @next/eslint-plugin-next
  '@next/next/no-img-element': string;
  '@next/next/no-html-link-for-pages': string;
  // Core Web Vitals правила
  '@next/next/no-sync-scripts': string;
  '@next/next/no-css-tags': string;
}
```

## ⚠️ Потенциальные проблемы и риски

### Проблемы производительности:

- **Plugin overhead**: Множество плагинов увеличивают время линтинга
- **Duplicate rules**: Возможное дублирование с base.js и react.js
- **Core Web Vitals strictness**: Очень строгие правила могут блокировать разработку

### Проблемы конфигурации:

- **Version conflicts**: Конфликты версий между React и Next.js плагинами
- **Rule conflicts**: Потенциальные конфликты между различными конфигурациями
- **Globals compatibility**: Service Worker globals могут конфликтовать с Node.js

### Проблемы разработки:

- **Style restrictions**: Запрет inline стилей может быть слишком строгим
- **Magic numbers**: Обнаружение в style объектах может давать ложные срабатывания
- **JSX transform**: Зависимость от версии React для 'react-in-jsx-scope'

### Проблемы команды:

- **Learning curve**: Сложность для разработчиков, не знакомых с Next.js
- **Rule fatigue**: Большое количество правил может демотивировать
- **Debugging complexity**: Сложность отладки из-за множества слоев конфигурации

## ✅ Тестирование

- **Rule validation tests**: Отсутствуют
- **Next.js compatibility tests**: Отсутствуют
- **Performance tests**: Отсутствуют

### Рекомендации по тестированию:

- Тестирование на реальных Next.js компонентах
- Валидация Core Web Vitals правил
- Performance тесты влияния на время сборки
- Integration тесты с различными версиями Next.js

## 🔧 Техническая сложность

**Уровень: Средний-Высокий**

### Метрики сложности:

- **Размер**: 75 строк с комплексной структурой
- **Зависимости**: 7+ внешних плагинов
- **Наследование**: Сложная цепочка наследования конфигураций
- **Специализация**: Высокая (Next.js + React + PWA)

### Анализ архитектуры:

- Правильное наследование от базовой конфигурации
- Логическое разделение по типам правил
- Современные Next.js best practices
- Баланс между строгостью и практичностью

## 📝 TODO и области для улучшения

### Критические улучшения:

1. **Deduplication**: Устранение дублирования правил с base.js и react.js
2. **Performance optimization**: Оптимизация времени линтинга
3. **Version compatibility**: Проверка совместимости версий плагинов
4. **Rule documentation**: Документация каждого кастомного правила

### Рекомендуемые улучшения:

1. **Flexible style control**: Более гибкий контроль inline стилей
2. **PWA optimization**: Дополнительные правила для Progressive Web Apps
3. **Accessibility rules**: Интеграция с eslint-plugin-jsx-a11y
4. **Image optimization**: Правила для Next.js Image component
5. **Bundle analysis**: Правила для оптимизации размера bundle

### Долгосрочные задачи:

1. **Auto-migration**: Автоматическая миграция на новые версии Next.js
2. **Custom rules**: Разработка custom правил для crypto-exchange специфики
3. **Performance monitoring**: Интеграция с Core Web Vitals мониторингом
4. **Team customization**: Настраиваемые правила для разных команд
5. **AI-powered optimization**: Автоматические рекомендации по оптимизации
6. **Multi-framework support**: Поддержка других React frameworks
7. **Real-time validation**: Live валидация производительности во время разработки
