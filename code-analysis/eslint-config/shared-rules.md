# Анализ файла: packages/eslint-config/shared-rules.js

## 📋 Назначение

Централизованное хранилище переиспользуемых ESLint правил для устранения дублирования и обеспечения консистентности в монорепо ExchangeGO. Реализует DRY принцип для ESLint конфигураций с мемоизацией для оптимизации производительности.

## 📝 Описание

Центральная библиотека ESLint правил, включающая:

- **Centralized rule management** - единое место определения общих правил
- **Context-specific rule sets** - наборы правил для разных контекстов (тесты, демо, конфиги)
- **Performance optimization** - мемоизированные правила для снижения накладных расходов
- **Technical debt elimination** - устранение дублирования правил согласно ai-agent-rules.yml
- **Graduated strictness** - разные уровни строгости для разных типов кода
- **Composable rule sets** - композиция правил для создания специализированных конфигураций

Служит фундаментом для всех других ESLint конфигураций в проекте.

## 🔌 API и интерфейсы

### Базовые наборы правил:

```javascript
// Основные TypeScript правила
export const baseTsRules = {
  '@typescript-eslint/no-unused-vars': 'error',
  'prefer-const': 'error',
  'no-var': 'error',
};

// Производительность и безопасность
export const performanceRules = {
  'no-eval': 'error',
  'no-implied-eval': 'error',
  'no-new-func': 'error',
  'no-script-url': 'error',
  'no-loop-func': 'error',
  'no-caller': 'error',
};

// Качество кода
export const qualityRules = {
  'no-debugger': 'error',
  'no-alert': 'error',
  'prefer-arrow-callback': 'error',
  'no-param-reassign': 'error',
};
```

### Контекстные наборы правил:

```javascript
// Relaxed правила для конфигурационных файлов
export const configRelaxedRules = {
  'no-console': 'off', // Конфиги могут логировать
  'no-magic-numbers': 'off', // Допустимы числовые константы
  'unicorn/prefer-module': 'off', // CommonJS в конфигах
  '@typescript-eslint/no-require-imports': 'off', // require() в Node.js конфигах
  'no-undef': 'off', // Node.js globals
};

// Relaxed правила для тестов
export const testRelaxedRules = {
  'no-magic-numbers': 'off', // Тестовые данные могут содержать числа
  'sonarjs/no-duplicate-string': 'off', // Повторения в тестах допустимы
  'no-restricted-syntax': 'off', // localhost в тестах
  '@typescript-eslint/no-unused-vars': 'off', // Тестовые переменные
  'turbo/no-undeclared-env-vars': 'off', // CI/PLAYWRIGHT переменные
};

// Relaxed правила для демо/примеров
export const demoRelaxedRules = {
  'no-console': 'off', // Демо код может логировать
  'no-alert': 'off', // Демо взаимодействия
  '@typescript-eslint/no-unused-vars': 'off', // Демо переменные
  'no-magic-numbers': 'off', // Демо константы
};
```

### Градуированные правила:

```javascript
// Console правила с разными уровнями строгости
export const consoleRules = {
  strict: { 'no-console': 'error' }, // Продакшн код
  warn: { 'no-console': 'warn' }, // UI компоненты
  off: { 'no-console': 'off' }, // Демо/тесты
};

// Функциональное программирование (для utils)
export const functionalRules = {
  ...baseTsRules,
  ...performanceRules,
  ...qualityRules,
  'max-statements': ['error', 10], // Ограничение сложности функций
  'max-nested-callbacks': ['error', 2], // Ограничение вложенности
};
```

### Мемоизированные правила:

```javascript
// Оптимизированные версии для производительности
export const createOptimizedBaseTsRules = memoize(() => ({ ... }));
export const createOptimizedPerformanceRules = memoize(() => ({ ... }));
export const createOptimizedQualityRules = memoize(() => ({ ... }));
export const createFunctionalRules = memoize(() => ({ ... }));
```

## 📥 Входящие зависимости

```javascript
import { memoize } from './lazy-loading.js';
```

### Единственная зависимость:

- **lazy-loading.js** - функция memoize для кэширования правил

## 📤 Исходящие зависимости

- **react.js** - использует baseTsRules, consoleRules
- **api.js** - использует performanceRules, qualityRules
- **base.js** - использует базовые наборы правил
- **testing.js** - использует testRelaxedRules
- **ui-library.js** - использует functionalRules
- **utils.js** - использует functionalRules
- **configs.js** - использует configRelaxedRules

## 🔗 Взаимосвязи с другими компонентами

### Архитектурные связи:

- **lazy-loading.js** - получает мемоизацию для оптимизации
- **Все ESLint конфигурации** - используют правила из этого файла
- **ai-agent-rules.yml** - реализует требования по устранению технического долга

### Паттерн использования:

```javascript
// В других конфигурациях
import { baseTsRules, consoleRules, testRelaxedRules } from './shared-rules.js';

const config = {
  rules: {
    ...baseTsRules, // Базовые TypeScript правила
    ...consoleRules.warn, // Контекстные console правила
    // + специфичные правила конфигурации
  },
};
```

## 📊 Типы данных

### Структура наборов правил:

```typescript
interface RuleSet {
  [ruleName: string]: 'error' | 'warn' | 'off' | [string, ...any[]];
}

interface SharedRules {
  baseTsRules: RuleSet;
  performanceRules: RuleSet;
  qualityRules: RuleSet;
  configRelaxedRules: RuleSet;
  testRelaxedRules: RuleSet;
  demoRelaxedRules: RuleSet;
  functionalRules: RuleSet;
}

interface ConsoleRules {
  strict: { 'no-console': 'error' };
  warn: { 'no-console': 'warn' };
  off: { 'no-console': 'off' };
}

interface MemoizedRuleSets {
  createOptimizedBaseTsRules: () => RuleSet;
  createOptimizedPerformanceRules: () => RuleSet;
  createOptimizedQualityRules: () => RuleSet;
  createFunctionalRules: () => RuleSet;
}
```

### Категории правил по назначению:

```typescript
interface RuleCategories {
  security: string[]; // 'no-eval', 'no-implied-eval', 'no-script-url'
  performance: string[]; // 'no-loop-func', 'prefer-const', 'no-var'
  quality: string[]; // 'no-debugger', 'prefer-arrow-callback'
  typescript: string[]; // '@typescript-eslint/no-unused-vars'
  complexity: string[]; // 'max-statements', 'max-nested-callbacks'
}
```

## ⚠️ Потенциальные проблемы и риски

### Проблемы архитектуры:

- **Single point of failure**: Изменения в shared-rules влияют на все конфигурации
- **Rule conflict potential**: Правила могут конфликтовать при композиции
- **Circular dependency risk**: Потенциальные циклические зависимости при расширении

### Проблемы производительности:

- **Memoization overhead**: Накладные расходы на мемоизацию для простых правил
- **Memory consumption**: Кэширование всех правил может потреблять память
- **Import resolution**: Множественные импорты одних правил в разных конфигурациях

### Проблемы сопровождения:

- **Rule versioning**: Сложность версионирования изменений правил
- **Context mismatches**: Неправильное применение контекстных правил
- **Documentation drift**: Рассинхронизация документации с реальными правилами

### Проблемы расширяемости:

- **Naming conflicts**: Конфликты имен при добавлении новых наборов
- **Composition complexity**: Усложнение композиции при росте количества наборов
- **Context explosion**: Рост количества контекстных вариантов правил

## ✅ Тестирование

- **Unit-тесты**: Отсутствуют
- **Rule composition tests**: Отсутствуют
- **Performance tests**: Отсутствуют

### Рекомендации по тестированию:

- Тесты корректности композиции правил
- Тесты мемоизации производительности
- Валидация правил на конфликты
- Тесты применения контекстных правил
- Интеграционные тесты с конфигурациями

## 🔧 Техническая сложность

**Уровень: Средний**

### Метрики сложности:

- **Размер**: 106 строк с высокой плотностью правил
- **Количество наборов**: 10+ специализированных наборов правил
- **Композиционная сложность**: Средняя (объединение наборов)
- **Архитектурная важность**: Критическая (все конфигурации зависят от этого файла)

### Анализ архитектуры:

- Чистое разделение ответственности по контекстам
- Эффективное использование мемоизации
- Хорошая организация правил по категориям
- Соответствие DRY принципу

## 📝 TODO и области для улучшения

### Критические улучшения:

1. **Rule validation system**: Система валидации правил на конфликты
2. **Automated testing**: Автоматические тесты всех наборов правил
3. **Documentation generation**: Автогенерация документации по правилам
4. **Performance benchmarking**: Измерение влияния мемоизации на производительность

### Рекомендуемые улучшения:

1. **Rule categorization enhancement**: Улучшенная категоризация правил по типам
2. **Context-aware composition**: Умная композиция правил в зависимости от контекста
3. **Rule deprecation system**: Система deprecation устаревших правил
4. **Configuration validation**: Валидация корректности применения правил
5. **Performance optimization**: Дальнейшая оптимизация мемоизации

### Долгосрочные задачи:

1. **AI-powered rule suggestion**: ИИ рекомендации оптимальных наборов правил
2. **Dynamic rule adaptation**: Динамическая адаптация правил под проект
3. **Rule effectiveness analytics**: Аналитика эффективности правил
4. **Automated rule updates**: Автоматическое обновление правил с новыми версиями плагинов
5. **Team-specific rule customization**: Кастомизация правил на уровне команд
6. **Rule impact analysis**: Анализ влияния изменений правил на codebase
7. **Cross-project rule sharing**: Система sharing правил между проектами
8. **Machine learning rule optimization**: ML оптимизация наборов правил для максимальной эффективности
