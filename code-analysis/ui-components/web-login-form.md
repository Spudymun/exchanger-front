### Путь: apps/web/src/components/forms/LoginForm.tsx

**Краткое назначение (1 предложение)**
Форма входа в систему с email, паролем и капчей, использующая переиспользуемые компоненты полей из дизайн-системы.

**Подробное описание (3–6 предложений)**
Компонент реализует форму аутентификации пользователя с тремя полями: email, пароль и математическая капча для защиты от ботов. Использует кастомный хук useLoginForm для инкапсуляции логики формы и интеграции с useFormWithNextIntl для локализованной валидации. Интегрируется с useAuthMutationAdapter для выполнения запроса входа через tRPC API с обработкой успешных операций и ошибок. Построен на переиспользуемых компонентах полей из @repo/ui (AuthEmailField, AuthPasswordField, AuthCaptchaField) для устранения дублирования кода. Включает кнопку переключения на регистрацию и полную поддержку loading состояний во всех элементах формы.

**Экспортируемые сущности / API (все на 100%)**

- `export function LoginForm({ onSuccess, onSwitchToRegister })` — основная форма входа
- `useLoginForm(onSuccess?)` — кастомный хук логики формы (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- `onSuccess?: () => void` — callback при успешном входе в систему
- `onSwitchToRegister?: () => void` — callback для переключения на форму регистрации

**Выходы / Побочные эффекты (все на 100%)**

- Выполняет login.mutateAsync с данными формы при отправке
- Вызывает onSuccess через createAuthFormSubmitHandler при успешном входе
- Обрабатывает ошибки через createAuthFormErrorHandler
- Управляет loading состоянием всех полей и кнопок
- Отображает локализованные сообщения валидации и интерфейса

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: AuthForms компоненте для режима входа
- **Импортирует**: AUTH_FIELD_IDS из @repo/constants для идентификаторов полей
- **Использует типы**: LoginFormData, LoginFormProps из @repo/exchange-core
- **Использует**: useFormWithNextIntl из @repo/hooks для локализованной формы
- **Импортирует UI**: Auth\* компоненты из @repo/ui для переиспользуемых полей
- **Использует валидацию**: loginSchema из @repo/utils

**Домен данных / типы (все на 100%)**

```typescript
interface LoginFormData {
  email: string;
  password: string;
  captcha: string;
}

interface LoginFormProps {
  onSuccess?: () => void;
  onSwitchToRegister?: () => void;
}

// Используемые константы
const AUTH_FIELD_IDS = {
  LOGIN: {
    EMAIL: 'login-email',
    PASSWORD: 'login-password',
  },
};

// Локализация namespaces
type TranslationNamespaces =
  | 'AdvancedExchangeForm' // для валидации
  | 'Layout.forms.login' // для UI текстов
  | 'Layout.captcha'; // для капчи
```

**Риски и безопасность**

- **Authentication security**: зависит от правильной реализации login мутации
- **Captcha protection**: использует математическую капчу для защиты от ботов
- **Input validation**: критически зависит от loginSchema валидации
- **Error handling**: обрабатывает ошибки через централизованный handler
- **Loading states**: предотвращает множественные отправки через isPending

**Тесты / рекомендации по покрытию**

- **Form submission тесты**: корректность отправки данных через login мутацию
- **Validation тесты**: правильность loginSchema валидации для всех полей
- **Captcha тесты**: функциональность математической капчи
- **Loading state тесты**: корректность disabled состояний при isPending
- **Callback тесты**: правильность вызова onSuccess и onSwitchToRegister

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная форма с аутентификацией и капчей

**TODO / Рефакторинг**

- ✅ Использование переиспользуемых Auth\* компонентов из дизайн-системы
- ✅ Кастомный хук useLoginForm для инкапсуляции логики
- ✅ Правильная интеграция с useAuthMutationAdapter
- ✅ Централизованные константы AUTH_FIELD_IDS
- ✅ Множественные namespace локализации для разных частей
- ✅ Обработка ошибок через createAuthFormErrorHandler
- 💡 Рекомендация: добавить forgot password функциональность
- 💡 Рекомендация: рассмотреть добавление remember me опции
- 💡 Рекомендация: добавить rate limiting для защиты от брутфорса
