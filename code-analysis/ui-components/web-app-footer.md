### Путь: apps/web/src/components/app-footer.tsx

**Краткое назначение (1 предложение)**
Компонент подвала приложения ExchangeGO с локализацией, социальными ссылками и структурированной информацией о компании.

**Подробное описание (3–6 предложений)**
Компонент реализует полнофункциональный подвал сайта используя Footer компонент из дизайн-системы @repo/ui с четырехколоночной структурой. Интегрирует локализацию через next-intl для отображения переводов из namespace 'Layout' и динамически генерирует ссылки на основе переводов. Использует константы из @repo/constants для социальных ссылок и информации о компании, обеспечивая централизованное управление данными. Включает секции: информация о компании, обменные операции, поддержка и контакты с соответствующими ссылками и описаниями. Содержит социальные иконки для Telegram и Twitter с inline SVG для оптимальной производительности и правовые ссылки в отдельной секции.

**Экспортируемые сущности / API (все на 100%)**

- `export function AppFooter({ className })` — основной компонент подвала приложения
- `getSocialLinks()` — функция генерации социальных ссылок (внутренняя)
- `getLegalLinks(t)` — функция генерации правовых ссылок (внутренняя)

**Входы (expected inputs) / Параметры (все на 100%)**

- `className?: string` — опциональные CSS классы для кастомизации стилей
- Использует переводы из namespace 'Layout' через useTranslations
- Импортирует константы SOCIAL_LINKS и COMPANY_INFO из @repo/constants

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит структурированный подвал с четырьмя основными секциями
- Отображает локализованный контент на основе текущей локали
- Генерирует социальные и правовые ссылки динамически
- Применяет темную тему через Footer компонент из дизайн-системы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: AppLayout как часть общей структуры страницы
- **Импортирует**: `Footer` из @repo/ui, константы из @repo/constants
- **Использует переводы**: из messages/[locale].json в namespace 'Layout'
- **Экспортируется через**: apps/web/src/components/index.ts

**Домен данных / типы (все на 100%)**

```typescript
interface AppFooterProps {
  className?: string;
}

interface SocialLink {
  name: string;
  href: string;
  icon: JSX.Element;
}

interface LegalLink {
  name: string;
  href: string;
}

type FooterSections = 'company' | 'exchange' | 'support' | 'contacts';
```

**Риски и безопасность**

- **Низкий риск**: статический UI компонент с внешними ссылками
- **External links**: социальные ссылки ведут на внешние ресурсы
- **Translation dependency**: зависит от наличия переводов в Layout namespace
- **SVG security**: inline SVG безопасны, но нужно следить за содержимым

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов для всех секций
- **Links тесты**: валидность всех внутренних и внешних ссылок
- **Social icons тесты**: правильность отображения SVG иконок
- **Responsive тесты**: адаптивность четырехколоночной структуры

**Оценка сложности (low/medium/high)**
**MEDIUM** - интеграция локализации, констант и сложная структура

**TODO / Рефакторинг**

- ✅ Правильное использование дизайн-системы Footer компонента
- ✅ Централизованное управление константами через @repo/constants
- ✅ Полная локализация всех текстов и ссылок
- ✅ Структурированная организация контента по секциям
- 💡 Рекомендация: вынести SVG иконки в отдельные компоненты для переиспользования
- 💡 Рекомендация: добавить rel="noopener noreferrer" для внешних ссылок
