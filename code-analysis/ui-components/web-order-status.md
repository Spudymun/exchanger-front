### Путь: apps/web/src/components/OrderStatus.tsx

**Краткое назначение (1 предложение)**
Компонент отображения статуса заказа обмена с автообновлением, детальной информацией и визуальными индикаторами прогресса.

**Подробное описание (3–6 предложений)**
Компонент реализует полнофункциональное отображение статуса заказа с автоматическим обновлением через useOrderStatus хук и настраиваемым интервалом из UI_REFRESH_INTERVALS. Использует конфигурацию статусов из ORDER_STATUS_CONFIG для отображения соответствующих цветов, иконок и описаний для каждого состояния заказа. Включает два режима отображения: компактный заголовок с иконкой и статусом, и детальный вид с полной информацией о заказе включая суммы, адреса и временные метки. Интегрируется с дизайн-системой через statusStyles, textStyles и cardStyles для консистентного внешнего вида. Обрабатывает состояния загрузки, ошибок и отсутствующих данных с соответствующими UI индикаторами.

**Экспортируемые сущности / API (все на 100%)**

- `export function OrderStatus({ orderId, showDetails })` — основной компонент статуса заказа
- `OrderStatusHeader({ orderData, statusConfig })` — заголовок с иконкой и статусом (внутренний)
- `OrderStatusDetails({ orderData, statusConfig })` — детальная информация заказа (внутренний)
- `getIconColorClass(color)` — утилита для получения CSS класса цвета иконки (внутренняя)

**Входы (expected inputs) / Параметры (все на 100%)**

- `orderId: string` — идентификатор заказа для загрузки данных
- `showDetails?: boolean` — флаг отображения детальной информации (по умолчанию true)

**Выходы / Побочные эффекты (все на 100%)**

- Загружает данные заказа через useOrderStatus с автообновлением
- Отображает статус с соответствующей иконкой и цветовой схемой
- Показывает детальную информацию: ID, суммы, адреса, временные метки
- Обрабатывает loading, error и empty состояния с соответствующими UI
- Использует анимацию для иконки PROCESSING статуса

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: страницах отслеживания заказов, модальных окнах статуса
- **Импортирует**: ORDER_STATUS_CONFIG, ORDER_STATUSES, UI_REFRESH_INTERVALS из @repo/constants
- **Использует типы**: Order из @repo/exchange-core для типизации данных
- **Использует**: useOrderStatus из локальных хуков для загрузки данных
- **Импортирует UI**: statusStyles, textStyles, cardStyles, combineStyles из @repo/ui
- **Использует иконки**: CheckCircle, Clock, Loader2, XCircle из lucide-react

**Домен данных / типы (все на 100%)**

```typescript
interface OrderStatusProps {
  orderId: string;
  showDetails?: boolean;
}

interface StatusConfig {
  label: string;
  color: 'success' | 'warning' | 'info' | 'destructive';
  icon: string;
  description: string;
}

const STATUS_ICONS = {
  PENDING: Clock,
  PAID: CheckCircle,
  PROCESSING: Loader2,
  COMPLETED: CheckCircle,
  CANCELLED: XCircle,
} as const;

// Данные заказа из Order типа
interface Order {
  id: string;
  status: string;
  cryptoAmount: number;
  currency: string;
  uahAmount: number;
  depositAddress: string;
  createdAt: string;
  updatedAt: string;
  txHash?: string;
}
```

**Риски и безопасность**

- **Data loading**: критически зависит от useOrderStatus для получения актуальных данных
- **Type safety**: использует type assertion для Order типа, может быть небезопасно
- **Auto-refresh**: постоянные запросы могут создавать нагрузку на сервер
- **Sensitive data**: отображает адреса и хеши транзакций, требует осторожности
- **Error handling**: обрабатывает ошибки загрузки, но может не покрывать все случаи

**Тесты / рекомендации по покрытию**

- **Status display тесты**: корректность отображения всех статусов заказа
- **Auto-refresh тесты**: правильность работы автообновления данных
- **Loading states тесты**: корректность UI для loading, error, empty состояний
- **Details toggle тесты**: правильность работы showDetails параметра
- **Icon animation тесты**: корректность анимации для PROCESSING статуса

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложный компонент с автообновлением и множественными состояниями

**TODO / Рефакторинг**

- ✅ Использование констант из @repo/constants для конфигурации
- ✅ Интеграция с дизайн-системой через style utilities
- ✅ Автообновление через настраиваемый интервал
- ✅ Обработка всех состояний: loading, error, empty, success
- ✅ Модульная структура с отдельными компонентами для заголовка и деталей
- ✅ Правильная типизация через Order интерфейс
- 🔴 Критично: убрать type assertion и добавить proper type guards
- 💡 Рекомендация: добавить локализацию вместо хардкод русских текстов
- 💡 Рекомендация: рассмотреть добавление copy-to-clipboard для адресов и хешей
- 💡 Рекомендация: добавить прогресс-бар для визуализации этапов заказа
