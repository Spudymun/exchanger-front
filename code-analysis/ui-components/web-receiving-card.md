### Путь: apps/web/src/components/exchange-form/ReceivingCard.tsx

**Краткое назначение (1 предложение)**
Карточка получения средств в форме обмена с выбором фиатной валюты, банка и отображением рассчитанной суммы получения.

**Подробное описание (3–6 предложений)**
Компонент реализует правую часть формы обмена, отвечающую за настройку получения средств пользователем. Включает селектор фиатных валют из констант FIAT_CURRENCIES с автоматическим сбросом выбранного банка при смене валюты. Содержит селектор банков, получающий список доступных банков через getBanksForCurrency для выбранной валюты. Отображает рассчитанную сумму получения в readonly поле с форматированием до 2 знаков после запятой. Включает информационную секцию с резервами и временем обработки, а также использует зеленую цветовую схему для визуального выделения как карточки получения.

**Экспортируемые сущности / API (все на 100%)**

- `export function ReceivingCard({ form, banks, calculatedAmount, t })` — основная карточка получения
- `export function FiatCurrencySelector({ form, t })` — селектор фиатной валюты
- `export function BankSelector({ form, banks, t })` — селектор банка
- `ReceivingInfo({ form, t })` — информационная секция (внутренний компонент)

**Входы (expected inputs) / Параметры (все на 100%)**

- `form: ReturnType<typeof useForm<HeroExchangeFormData>>` — объект формы с типизацией
- `banks: ReturnType<typeof getBanksForCurrency>` — массив доступных банков для валюты
- `calculatedAmount: number` — рассчитанная сумма получения для отображения
- `t: (key: string) => string` — функция локализации для переводов

**Выходы / Побочные эффекты (все на 100%)**

- Обновляет form.values.toCurrency при выборе валюты
- Сбрасывает form.values.selectedBankId при смене валюты
- Обновляет form.values.selectedBankId при выборе банка
- Отображает readonly поле с форматированной суммой получения
- Показывает информацию о резервах и времени обработки

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroExchangeForm как правая карточка формы обмена
- **Импортирует**: FIAT_CURRENCIES, getBanksForCurrency из @repo/constants
- **Использует**: useForm hook из @repo/hooks для управления формой
- **Импортирует UI**: Card, Select, Input, FormField компоненты из @repo/ui
- **Зависит от переводов**: ключи receiving.\* в локализации

**Домен данных / типы (все на 100%)**

```typescript
interface ReceivingCardProps {
  form: ReturnType<typeof useForm<HeroExchangeFormData>>;
  banks: ReturnType<typeof getBanksForCurrency>;
  calculatedAmount: number;
  t: (key: string) => string;
}

interface HeroExchangeFormData {
  toCurrency: string;
  selectedBankId: string;
  toAmount?: number;
}

type FiatCurrency = (typeof FIAT_CURRENCIES)[number];
type BankOption = { id: string; name: string; currency: string };
```

**Риски и безопасность**

- **Form validation**: зависит от валидации в родительской форме
- **Bank data consistency**: критически зависит от getBanksForCurrency
- **Amount precision**: использует toFixed(2) без проверки на NaN
- **Translation dependency**: требует наличия всех ключей receiving.\*
- **State synchronization**: автоматический сброс банка при смене валюты

**Тесты / рекомендации по покрытию**

- **Form integration тесты**: корректность обновления значений формы
- **Currency selection тесты**: правильность сброса банка при смене валюты
- **Bank filtering тесты**: соответствие банков выбранной валюте
- **Amount display тесты**: корректность форматирования calculatedAmount
- **Localization тесты**: отображение всех переводов

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная форма с множественными селекторами и логикой

**TODO / Рефакторинг**

- ✅ Правильная типизация через HeroExchangeFormData
- ✅ Модульная структура с отдельными компонентами селекторов
- ✅ Автоматический сброс зависимых полей при изменении валюты
- ✅ Использование констант из @repo/constants
- ✅ Зеленая цветовая схема для карточки получения
- 💡 Рекомендация: добавить валидацию calculatedAmount на NaN/Infinity
- 💡 Рекомендация: добавить loading состояние для селектора банков
- 💡 Рекомендация: рассмотреть мемоизацию банков для производительности
