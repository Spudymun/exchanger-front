### Путь: apps/web/src/components/index.ts

**Краткое назначение (1 предложение)**
Barrel export файл для основных layout компонентов приложения, обеспечивающий централизованный импорт AppHeader, AppFooter и AppLayout.

**Подробное описание (3–6 предложений)**
Файл служит центральной точкой экспорта для основных структурных компонентов приложения, следуя паттерну barrel exports. Экспортирует только ключевые layout компоненты: AppHeader для шапки, AppFooter для подвала и AppLayout для общей структуры страницы. Не включает все компоненты директории, что указывает на селективный подход к публичному API компонентов. Позволяет импортировать основные layout компоненты из единой точки входа для удобства использования в других частях приложения. Является частью архитектуры компонентов, где layout компоненты выделены как основные строительные блоки.

**Экспортируемые сущности / API (все на 100%)**

- `export { AppHeader }` — компонент шапки приложения
- `export { AppFooter }` — компонент подвала приложения
- `export { AppLayout }` — компонент общей структуры страницы

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - только реэкспорты
- Импортируется как: `import { AppHeader, AppFooter, AppLayout } from './components'`

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет централизованный доступ к layout компонентам
- Нет побочных эффектов - только реэкспорты
- Упрощает импорты основных структурных компонентов
- Обеспечивает чистый публичный API для layout системы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Реэкспортирует из**: ./app-header, ./app-footer, ./app-layout
- **Импортируется в**: другие компоненты и страницы приложения
- **Не экспортирует**: специализированные компоненты (forms, sections, etc.)
- **Фокус на**: layout и структурных компонентах

**Домен данных / типы (все на 100%)**

```typescript
// Экспортируемые компоненты:
interface LayoutComponents {
  AppHeader: React.ComponentType<AppHeaderProps>;
  AppFooter: React.ComponentType<AppFooterProps>;
  AppLayout: React.ComponentType<AppLayoutProps>;
}

type LayoutComponentNames = 'AppHeader' | 'AppFooter' | 'AppLayout';
```

**Риски и безопасность**

- **Низкий риск**: простые реэкспорты без логики
- **API surface**: ограниченный набор экспортов снижает complexity
- **Dependency management**: централизованный контроль над публичным API
- **Maintenance**: изменения в структуре требуют обновления экспортов

**Тесты / рекомендации по покрытию**

- **Export тесты**: проверка доступности всех экспортируемых компонентов
- **Integration тесты**: корректность импортов в других частях приложения
- **API тесты**: соответствие экспортов реальным компонентам
- **Bundle тесты**: влияние на размер bundle при импорте

**Оценка сложности (low/medium/high)**
**LOW** - простые реэкспорты основных компонентов

**TODO / Рефакторинг**

- ✅ Селективный подход к экспортам (только layout компоненты)
- ✅ Чистый публичный API для структурных компонентов
- ✅ Следует паттерну barrel exports
- 💡 Рекомендация: рассмотреть добавление JSDoc комментариев для документации
- 💡 Рекомендация: добавить type-only exports если используются только типы
