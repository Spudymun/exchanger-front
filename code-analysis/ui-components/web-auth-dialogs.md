### Путь: apps/web/src/components/auth-dialogs.tsx

**Краткое назначение (1 предложение)**
Компонент модальных диалогов аутентификации для входа и регистрации с локализацией и интеграцией форм.

**Подробное описание (3–6 предложений)**
Компонент управляет отображением модальных окон для процессов входа и регистрации пользователей, используя Dialog компонент из дизайн-системы. Интегрирует локализацию через next-intl для отображения переводов заголовков и элементов управления из namespace 'Layout.auth'. Использует единый AuthForms компонент с разными режимами (login/register) для обеспечения консистентности пользовательского опыта. Обеспечивает правильное управление состоянием через callback функции для открытия/закрытия диалогов и обработки успешной аутентификации. Включает accessibility атрибуты как closeButtonAriaLabel для поддержки screen readers.

**Экспортируемые сущности / API (все на 100%)**

- `export function AuthDialogs({ isLoginOpen, isRegisterOpen, onLoginClose, onRegisterClose, onAuthSuccess })` — компонент модальных диалогов аутентификации

**Входы (expected inputs) / Параметры (все на 100%)**

- `isLoginOpen: boolean` — состояние открытия диалога входа
- `isRegisterOpen: boolean` — состояние открытия диалога регистрации
- `onLoginClose: () => void` — callback для закрытия диалога входа
- `onRegisterClose: () => void` — callback для закрытия диалога регистрации
- `onAuthSuccess?: () => void` — опциональный callback при успешной аутентификации

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит два модальных диалога для входа и регистрации
- Отображает локализованные заголовки и элементы управления
- Вызывает callback функции при изменении состояния диалогов
- Передает onAuthSuccess в AuthForms для обработки успешной аутентификации

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: AppHeader для управления диалогами аутентификации
- **Импортирует**: Dialog компоненты из @repo/ui, AuthForms из ./forms/AuthForms
- **Использует переводы**: из messages/[locale].json в namespace 'Layout.auth'
- **Управляется**: через состояние в useAuthDialogs хуке в AppHeader

**Домен данных / типы (все на 100%)**

```typescript
interface AuthDialogsProps {
  isLoginOpen: boolean;
  isRegisterOpen: boolean;
  onLoginClose: () => void;
  onRegisterClose: () => void;
  onAuthSuccess?: () => void;
}

type AuthMode = 'login' | 'register';
type DialogState = 'open' | 'closed';

interface AuthDialogConfig {
  mode: AuthMode;
  title: string;
  isOpen: boolean;
  onClose: () => void;
}
```

**Риски и безопасность**

- **Низкий риск**: UI компонент без прямой обработки аутентификации
- **State management**: зависит от правильного управления состоянием в родительском компоненте
- **Form security**: безопасность зависит от реализации AuthForms компонента
- **Accessibility**: правильное использование ARIA атрибутов для screen readers

**Тесты / рекомендации по покрытию**

- **Dialog тесты**: корректность открытия/закрытия модальных окон
- **Localization тесты**: правильность отображения переводов заголовков
- **Integration тесты**: взаимодействие с AuthForms компонентом
- **Accessibility тесты**: поддержка keyboard navigation и screen readers

**Оценка сложности (low/medium/high)**
**LOW** - простая обертка для модальных диалогов с формами

**TODO / Рефакторинг**

- ✅ Правильное использование Dialog компонентов из дизайн-системы
- ✅ Локализация заголовков и accessibility атрибутов
- ✅ Единый AuthForms компонент для обоих режимов
- ✅ Правильное управление состоянием через callback'и
- 💡 Рекомендация: добавить анимации открытия/закрытия диалогов
- 💡 Рекомендация: рассмотреть добавление escape key handling
