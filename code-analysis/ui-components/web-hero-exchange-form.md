### Путь: apps/web/src/components/HeroExchangeForm.tsx

**Краткое назначение (1 предложение)**
Главная форма обмена криптовалют для hero секции с адаптивным контейнером, валидацией и интеграцией подкомпонентов.

**Подробное описание (3–6 предложений)**
Компонент реализует основную форму обмена для hero секции лендинга с поддержкой адаптивной ширины через AdaptiveContainer и полной интеграцией с дизайн-системой. Использует кастомный хук useHeroExchangeForm для управления состоянием формы, валидации и бизнес-логики обмена. Структурирован с разделением на подкомпоненты ExchangeFormCards и ExchangeFormAction для лучшей организации и тестируемости кода. Включает SendingCard и ReceivingCard с ExchangeArrow между ними, ExchangeBenefits для отображения преимуществ и кнопку отправки с анимациями. Поддерживает два режима контейнера: обычный и адаптивный с настраиваемыми параметрами ширины и роста.

**Экспортируемые сущности / API (все на 100%)**

- `export function HeroExchangeForm(props)` — основной компонент формы обмена
- `export interface HeroExchangeFormData` — интерфейс данных формы
- `ExchangeFormCards({ form, calculatedAmount, banks, constants, t })` — подкомпонент карточек (внутренний)
- `ExchangeFormAction({ isValid, t })` — подкомпонент действий (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- `onExchange?: (data: HeroExchangeFormData) => void` — callback при отправке формы
- `className?: string` — дополнительные CSS классы
- `useAdaptiveContainer?: boolean` — использование адаптивного контейнера
- `adaptiveMode?: 'clamp' | 'responsive'` — режим адаптации ширины
- `minWidth?, maxWidth?, preferredWidth?, growthFactor?` — параметры адаптивности

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полную форму обмена с карточками отправки и получения
- Выполняет валидацию данных формы в реальном времени
- Вызывает onExchange callback при успешной отправке формы
- Применяет адаптивную ширину в зависимости от настроек контейнера

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroSection как основная форма обмена
- **Импортирует**: ExchangeForm из @repo/ui, подкомпоненты из ./exchange-form/
- **Использует**: useHeroExchangeForm хук для управления состоянием
- **Интегрируется**: с FloatingExchangeButton через data-testid

**Домен данных / типы (все на 100%)**

```typescript
interface HeroExchangeFormData extends Record<string, unknown> {
  fromAmount: string;
  fromCurrency: string;
  tokenStandard?: string;
  toCurrency: string;
  selectedBankId: string;
}

interface AdaptiveWidthProps {
  adaptationMode: 'clamp' | 'responsive';
  minWidth: number;
  maxWidth: number;
  preferredWidth: number;
  growthFactor: number;
}
```

**Риски и безопасность**

- **Средний риск**: обработка пользовательских данных и валидация
- **Form validation**: зависит от корректной работы useHeroExchangeForm хука
- **Data integrity**: важна правильная типизация HeroExchangeFormData
- **UI consistency**: адаптивный контейнер может влиять на layout

**Тесты / рекомендации по покрытию**

- **Form validation тесты**: корректность валидации всех полей формы
- **Adaptive container тесты**: правильность работы адаптивной ширины
- **Integration тесты**: взаимодействие с подкомпонентами и хуками
- **Responsive тесты**: адаптивность на разных размерах экрана

**Оценка сложности (low/medium/high)**
**HIGH** - сложная интеграция формы, валидации, адаптивности и подкомпонентов

**TODO / Рефакторинг**

- ✅ Хорошее разделение на подкомпоненты для тестируемости
- ✅ Поддержка адаптивного контейнера с настраиваемыми параметрами
- ✅ Правильная интеграция с дизайн-системой ExchangeForm
- ✅ Типобезопасность через TypeScript интерфейсы
- 💡 Рекомендация: добавить loading состояния для async операций
- ✅ Реализовано: добавлен ExchangeErrorBoundary для обработки ошибок формы
