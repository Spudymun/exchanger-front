### Путь: apps/web/src/components/forms/AuthForms.tsx

**Краткое назначение (1 предложение)**
Централизованный компонент аутентификации с переключением между формами входа и регистрации в едином layout.

**Подробное описание (3–6 предложений)**
Компонент реализует высокоуровневую абстракцию для управления процессом аутентификации, объединяя LoginForm и RegisterForm в единый интерфейс. Использует AuthFormLayout из дизайн-системы для обеспечения консистентного внешнего вида и функциональности переключения между режимами. Включает оптимизацию производительности через React.memo для предотвращения лишних ре-рендеров и useCallback для стабилизации функций. Поддерживает настройку режима по умолчанию и callback для обработки успешной аутентификации. Разделен на два компонента: основной AuthForms для управления состоянием и AuthFormsContent для условного рендеринга форм.

**Экспортируемые сущности / API (все на 100%)**

- `export const AuthForms` — основной компонент аутентификации с мемоизацией
- `AuthFormsContent` — внутренний компонент для условного рендеринга форм

**Входы (expected inputs) / Параметры (все на 100%)**

- `onAuthSuccess?: () => void` — опциональный callback при успешной аутентификации
- `defaultMode?: 'login' | 'register'` — начальный режим формы (по умолчанию 'login')

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит AuthFormLayout с переключателем режимов
- Условно отображает LoginForm или RegisterForm в зависимости от режима
- Вызывает onAuthSuccess при успешной аутентификации в любой форме
- Управляет локальным состоянием режима через useState

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: auth-dialogs компоненте для модальных окон аутентификации
- **Импортирует**: AuthFormLayout из @repo/ui для layout и переключения
- **Использует**: LoginForm и RegisterForm из локальной директории
- **Использует локализацию**: через useTranslations('Layout.auth')
- **Интегрируется**: с общей системой аутентификации приложения

**Домен данных / типы (все на 100%)**

```typescript
interface AuthFormsProps {
  onAuthSuccess?: () => void;
  defaultMode?: 'login' | 'register';
}

interface AuthFormsContentProps {
  mode: 'login' | 'register';
  onAuthSuccess?: () => void;
}

type AuthMode = 'login' | 'register';
type AuthCallback = () => void;
```

**Риски и безопасность**

- **Низкий риск**: компонент-контейнер без прямой обработки данных
- **State management**: простое локальное состояние без сложной логики
- **Callback handling**: зависит от корректности onAuthSuccess от родителя
- **Form validation**: делегирует валидацию дочерним формам
- **Memory leaks**: React.memo и useCallback предотвращают утечки памяти

**Тесты / рекомендации по покрытию**

- **Mode switching тесты**: корректность переключения между login и register
- **Callback тесты**: правильность передачи onAuthSuccess в дочерние формы
- **Default props тесты**: корректность defaultMode по умолчанию
- **Memoization тесты**: эффективность React.memo оптимизации
- **Integration тесты**: взаимодействие с AuthFormLayout

**Оценка сложности (low/medium/high)**
**LOW** - простой компонент-контейнер с базовым состоянием

**TODO / Рефакторинг**

- ✅ Правильная мемоизация с React.memo для производительности
- ✅ Использование useCallback для стабилизации функций
- ✅ Четкое разделение на контейнер и презентационный компонент
- ✅ Правильная типизация с TypeScript интерфейсами
- ✅ Использование AuthFormLayout из дизайн-системы
- ✅ Подробная JSDoc документация с описанием архитектуры
- 💡 Рекомендация: рассмотреть добавление loading состояния
- ✅ Реализовано: добавлен BaseErrorBoundary для обработки ошибок форм
