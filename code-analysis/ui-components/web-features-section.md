### Путь: apps/web/src/components/FeaturesSection.tsx

**Краткое назначение (1 предложение)**
Секция лендинга с преимуществами ExchangeGO, отображающая карточки функций с иконками и локализованными описаниями.

**Подробное описание (3–6 предложений)**
Компонент реализует секцию преимуществ для лендинга криптообменника с адаптивной сеткой карточек функций. Использует Card компоненты из дизайн-системы @repo/ui с кастомными стилями для hover эффектов и теней, поддерживающими темную тему. Интегрирует локализацию через next-intl для отображения переводов из namespace 'HomePage.features' включая заголовки и описания всех функций. Включает шесть основных преимуществ: скорость, выгодные курсы, безопасность, доступность 24/7, поддержка и надежность с соответствующими иконками из Lucide React. Структурирован с разделением на FeatureCard подкомпонент и функцию createFeatures для генерации данных на основе переводов.

**Экспортируемые сущности / API (все на 100%)**

- `export function FeaturesSection()` — основная секция преимуществ
- `FeatureCard({ icon, title, description })` — компонент карточки функции (внутренний)
- `createFeatures(t)` — функция создания массива функций (внутренняя)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает переводы через useTranslations
- Использует переводы из namespace 'HomePage.features' для всех текстов
- Импортирует иконки из Lucide React для визуального представления функций

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит секцию с заголовком, подзаголовком и сеткой из 6 карточек функций
- Отображает локализованный контент на основе текущей локали
- Применяет адаптивную сетку: 1 колонка на мобильных, 2 на планшетах, 3 на десктопах
- Использует hover эффекты и анимации для улучшения пользовательского опыта

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: apps/web/app/[locale]/page.tsx как часть главной страницы
- **Импортирует**: Card компоненты из @repo/ui, иконки из lucide-react
- **Использует переводы**: из messages/[locale].json в namespace 'HomePage.features'
- **Часть**: лендинговой структуры между HeroSection и HowItWorksSection

**Домен данных / типы (все на 100%)**

```typescript
interface FeatureCardProps {
  icon: React.ComponentType<React.SVGProps<SVGSVGElement>>;
  title: string;
  description: string;
}

interface Feature {
  icon: LucideIcon;
  title: string;
  description: string;
}

type FeatureType = 'speed' | 'rates' | 'security' | 'availability' | 'support' | 'reliability';
```

**Риски и безопасность**

- **Низкий риск**: статический UI компонент без sensitive операций
- **Translation dependency**: зависит от наличия переводов в HomePage.features namespace
- **Performance**: использует статические иконки без динамической загрузки
- **Accessibility**: Card компоненты должны поддерживать keyboard navigation

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов для всех функций
- **Responsive тесты**: адаптивность сетки на разных размерах экрана
- **Card interaction тесты**: hover эффекты и анимации
- **Accessibility тесты**: поддержка screen readers и keyboard navigation

**Оценка сложности (low/medium/high)**
**MEDIUM** - интеграция локализации, адаптивный дизайн и кастомные стили

**TODO / Рефакторинг**

- ✅ Правильное использование Card компонентов из дизайн-системы
- ✅ Полная локализация всех текстов через next-intl
- ✅ Адаптивная сетка с правильными breakpoints
- ✅ Хорошее разделение на подкомпоненты и утилитарные функции
- 💡 Рекомендация: добавить анимации появления карточек при скролле
- 💡 Рекомендация: рассмотреть добавление ссылок на детальные страницы функций
