### Путь: apps/web/src/components/HeroSection.tsx

**Краткое назначение (1 предложение)**
Hero секция главной страницы с заголовком, описанием и основной формой обмена криптовалют.

**Подробное описание (3–6 предложений)**
Компонент реализует главную hero секцию лендинга с адаптивным заголовком, описанием и интегрированной формой обмена. Использует локализацию через next-intl для отображения переводов заголовка и описания из namespace 'HomePage'. Включает обработчик handleHeroExchange для обработки данных формы обмена с логированием и планируемой навигацией на страницу обмена. Структурирован с mobile-first подходом и адаптивными размерами текста от 3xl на мобильных до 6xl на больших экранах. Содержит id="exchange-section" для навигации через FloatingExchangeButton и якорные ссылки в навигации.

**Экспортируемые сущности / API (все на 100%)**

- `export function HeroSection()` — основная hero секция

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает переводы через useTranslations
- Использует переводы из namespace 'HomePage' для title и description
- Получает данные формы через handleHeroExchange callback

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит hero секцию с заголовком, описанием и формой обмена
- Отображает локализованный контент на основе текущей локали
- Логирует данные формы обмена в консоль при отправке
- Планируется навигация на страницу обмена с данными формы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: apps/web/app/[locale]/page.tsx как первая секция лендинга
- **Импортирует**: HeroExchangeForm и его типы из ./HeroExchangeForm
- **Использует переводы**: из messages/[locale].json в namespace 'HomePage'
- **Связана с**: FloatingExchangeButton через id="exchange-section"

**Домен данных / типы (все на 100%)**

```typescript
interface HeroSectionContent {
  title: string;
  description: string;
}

type ResponsiveTextSize = '3xl' | '4xl' | '5xl' | '6xl';
type SpacingVariant = 'space-y-6' | 'space-y-8' | 'space-y-12';

interface HeroExchangeHandler {
  (data: HeroExchangeFormData): void;
}
```

**Риски и безопасность**

- **Низкий риск**: UI компонент с простой обработкой данных
- **Console logging**: логирование данных формы может содержать sensitive информацию
- **Navigation planning**: будущая навигация должна быть безопасной
- **Form data handling**: важна правильная валидация данных от HeroExchangeForm

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов заголовка и описания
- **Form integration тесты**: правильность обработки данных от HeroExchangeForm
- **Responsive тесты**: адаптивность размеров текста и отступов
- **Navigation тесты**: работа якорной ссылки exchange-section

**Оценка сложности (low/medium/high)**
**LOW** - простая композиция заголовка, описания и формы

**TODO / Рефакторинг**

- ✅ Правильная интеграция с HeroExchangeForm
- ✅ Адаптивный дизайн с mobile-first подходом
- ✅ Локализация заголовка и описания
- ✅ Якорная навигация через id="exchange-section"
- ⚠️ Рекомендация: убрать console.log из production кода
- 💡 Рекомендация: реализовать навигацию на страницу обмена с данными формы
- 💡 Рекомендация: добавить error handling для обработки данных формы
