### Путь: apps/web/src/components/exchange-form/ExchangeArrow.tsx

**Краткое назначение (1 предложение)**
Декоративный компонент стрелки для визуального разделения карточек отправки и получения в форме обмена.

**Подробное описание (3–6 предложений)**
Компонент реализует простую визуальную стрелку направления обмена между карточками отправки и получения средств. Использует круглую форму с градиентным фоном и границей в цветах primary темы для создания привлекательного визуального элемента. Включает hover эффект с изменением прозрачности фона для интерактивности. Стрелка отображается как Unicode символ "→" с крупным шрифтом и центрированием. Компонент полностью статичен и служит только для улучшения пользовательского опыта через визуальное указание направления операции обмена.

**Экспортируемые сущности / API (все на 100%)**

- `export function ExchangeArrow({ className })` — компонент декоративной стрелки обмена

**Входы (expected inputs) / Параметры (все на 100%)**

- `className?: string` — дополнительные CSS классы для кастомизации позиционирования

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит круглую стрелку с hover эффектом
- Применяет primary цвета темы для консистентности дизайна
- Использует flex-shrink-0 для предотвращения сжатия в flex контейнере
- Отображает Unicode стрелку "→" как визуальный индикатор направления

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroExchangeForm между SendingCard и ReceivingCard
- **Не импортирует**: никаких внешних компонентов кроме React
- **Часть**: визуального дизайна формы обмена для улучшения UX
- **Позиционирование**: между карточками в горизонтальном layout формы

**Домен данных / типы (все на 100%)**

```typescript
interface ExchangeArrowProps {
  className?: string; // Опциональные CSS классы
}

// Визуальные константы
const ARROW_SIZE = '12'; // w-12 h-12 (48px)
const ARROW_SYMBOL = '→'; // Unicode стрелка вправо
const HOVER_OPACITY = '20'; // bg-primary/20 при hover
const DEFAULT_OPACITY = '10'; // bg-primary/10 по умолчанию
```

**Риски и безопасность**

- **Минимальный риск**: чисто декоративный статичный компонент
- **Accessibility**: отсутствует aria-label, но это декоративный элемент
- **Theme dependency**: зависит от primary цветов в CSS переменных
- **Unicode support**: использует Unicode символ, может отображаться по-разному в разных шрифтах

**Тесты / рекомендации по покрытию**

- **Rendering тесты**: корректность отображения стрелки и стилей
- **Props тесты**: правильная обработка className prop
- **Hover тесты**: корректность hover эффекта
- **Accessibility тесты**: проверка декоративной роли элемента

**Оценка сложности (low/medium/high)**
**LOW** - простейший декоративный компонент без логики

**TODO / Рефакторинг**

- ✅ Минималистичный дизайн без избыточности
- ✅ Правильное использование Tailwind классов
- ✅ Hover эффект для интерактивности
- ✅ Flex-shrink-0 для корректного позиционирования
- 💡 Рекомендация: добавить aria-hidden="true" для accessibility
- 💡 Рекомендация: рассмотреть использование иконки из Lucide React для консистентности
- 💡 Рекомендация: добавить анимацию поворота при смене направления обмена
