### Путь: apps/web/src/components/ExchangeRates.tsx

**Краткое назначение (1 предложение)**
Компонент отображения актуальных курсов обмена криптовалют с загрузкой данных через API и адаптивной сеткой карточек.

**Подробное описание (3–6 предложений)**
Компонент реализует отображение курсов обмена криптовалют с использованием кастомного хука useExchangeRates для получения данных через API. Использует стили из дизайн-системы @repo/ui для консистентного внешнего вида карточек, текста, загрузки и сетки. Включает обработку различных состояний: загрузка с spinner, ошибка с сообщением, отсутствие данных и успешное отображение курсов. Отображает информацию о каждой валюте включая название, курс в гривнах, комиссию и время последнего обновления. Структурирован с разделением на подкомпоненты RateCard и LastUpdateInfo для лучшей организации кода и переиспользования.

**Экспортируемые сущности / API (все на 100%)**

- `export function ExchangeRates()` — основной компонент отображения курсов
- `RateCard({ rate })` — компонент карточки отдельного курса (внутренний)
- `LastUpdateInfo({ timestamp })` — компонент информации о последнем обновлении (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает данные через useExchangeRates хук
- Использует CURRENCY_NAMES из @repo/constants для отображения названий валют
- Получает ExchangeRate типы из @repo/exchange-core

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит сетку карточек с курсами валют или состояния загрузки/ошибки
- Отображает курсы в гривнах с локализованным форматированием чисел
- Показывает комиссию для каждой валюты и время последнего обновления
- Использует иконки Lucide React для визуального улучшения

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Может использоваться**: в различных частях приложения для отображения курсов
- **Импортирует**: константы из @repo/constants, типы из @repo/exchange-core, стили из @repo/ui
- **Использует**: useExchangeRates хук из ../hooks/useExchangeMutation
- **Зависит от**: API endpoint для получения актуальных курсов

**Домен данных / типы (все на 100%)**

```typescript
interface RatesResponse {
  rates: ExchangeRate[];
  timestamp: Date;
}

interface RateCardProps {
  rate: ExchangeRate;
}

interface LastUpdateInfoProps {
  timestamp?: Date;
}

type LoadingState = 'loading' | 'success' | 'error' | 'empty';
```

**Риски и безопасность**

- **Низкий риск**: компонент отображения без sensitive операций
- **API dependency**: зависит от доступности и корректности API курсов
- **Type safety**: использует type assertion для типизации API ответа
- **Error handling**: правильная обработка ошибок загрузки данных

**Тесты / рекомендации по покрытию**

- **Loading states тесты**: корректность отображения всех состояний загрузки
- **Data display тесты**: правильность форматирования и отображения курсов
- **Error handling тесты**: обработка различных типов ошибок API
- **Responsive тесты**: адаптивность сетки карточек на разных устройствах

**Оценка сложности (low/medium/high)**
**MEDIUM** - интеграция с API, обработка состояний и использование дизайн-системы

**TODO / Рефакторинг**

- ✅ Правильное использование стилей из дизайн-системы
- ✅ Хорошее разделение на подкомпоненты
- ✅ Обработка всех состояний загрузки и ошибок
- ✅ Локализованное форматирование чисел
- 💡 Рекомендация: добавить автообновление курсов через polling или WebSocket
- 💡 Рекомендация: улучшить типизацию API ответа без type assertion
