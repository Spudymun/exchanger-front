### Путь: apps/web/src/components/app-layout.tsx

**Краткое назначение (1 предложение)**
Основной layout компонент приложения, объединяющий провайдеры, шапку, контент и подвал в единую структуру страницы.

**Подробное описание (3–6 предложений)**
Компонент определяет базовую структуру всех страниц приложения с интеграцией необходимых провайдеров и layout элементов. Оборачивает приложение в ThemeProvider для управления темами и TRPCProvider для API коммуникации, обеспечивая доступность этих сервисов во всех дочерних компонентах. Реализует классическую структуру веб-приложения: header, main content area, footer с использованием flexbox для правильного позиционирования. Включает overflow-x-hidden для предотвращения горизонтального скролла на мобильных устройствах и role="main" для accessibility. Предоставляет гибкость через опциональный className prop для кастомизации стилей на уровне страниц.

**Экспортируемые сущности / API (все на 100%)**

- `export function AppLayout({ children, className })` — основной layout компонент приложения

**Входы (expected inputs) / Параметры (все на 100%)**

- `children: React.ReactNode` — содержимое страниц для рендеринга в main области
- `className?: string` — опциональные CSS классы для кастомизации layout

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полную структуру страницы с провайдерами, header, main, footer
- Предоставляет ThemeProvider контекст для управления темами
- Предоставляет TRPCProvider контекст для API коммуникации
- Применяет базовые стили для min-height, flexbox и overflow управления

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: apps/web/app/[locale]/layout.tsx для оборачивания всех страниц
- **Импортирует**: ThemeProvider из @repo/providers, TRPCProvider из lib
- **Рендерит**: AppHeader и AppFooter как структурные элементы
- **Экспортируется через**: apps/web/src/components/index.ts

**Домен данных / типы (все на 100%)**

```typescript
interface AppLayoutProps {
  children: React.ReactNode;
  className?: string;
}

interface LayoutStructure {
  providers: [ThemeProvider, TRPCProvider];
  header: AppHeaderComponent;
  main: MainContentArea;
  footer: AppFooterComponent;
}

type LayoutVariant = 'default' | 'fullscreen' | 'minimal';
```

**Риски и безопасность**

- **Низкий риск**: структурный компонент без sensitive операций
- **Provider dependency**: зависит от корректной работы ThemeProvider и TRPCProvider
- **Layout stability**: изменения могут повлиять на все страницы приложения
- **Accessibility**: правильное использование semantic HTML и ARIA roles

**Тесты / рекомендации по покрытию**

- **Provider тесты**: корректность предоставления контекстов Theme и TRPC
- **Layout тесты**: правильность структуры header/main/footer
- **Responsive тесты**: адаптивность и overflow поведение
- **Accessibility тесты**: поддержка screen readers и semantic HTML

**Оценка сложности (low/medium/high)**
**LOW** - простая композиция провайдеров и структурных компонентов

**TODO / Рефакторинг**

- ✅ Правильная интеграция провайдеров в нужном порядке
- ✅ Семантическая HTML структура с role="main"
- ✅ Flexbox layout для правильного позиционирования footer
- ✅ Overflow управление для мобильных устройств
- ✅ Реализовано: добавлен LayoutErrorBoundary для обработки ошибок layout
- 💡 Рекомендация: рассмотреть добавление skip navigation link для accessibility
