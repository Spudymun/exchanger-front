### Путь: apps/web/src/components/forms/ExchangeForm.tsx

**Краткое назначение (1 предложение)**
Форма создания заявки на обмен криптовалют с валидацией, расчетом курса и интеграцией с API через мутации.

**Подробное описание (3–6 предложений)**
Компонент реализует полнофункциональную форму обмена с тремя основными полями: выбор криптовалюты, ввод суммы и email пользователя. Использует Zod схему для валидации с проверкой корректности email и положительности суммы криптовалюты. Интегрируется с useExchangeMutation для создания заказов через API с обработкой успешных операций и ошибок через систему уведомлений. Включает автоматический расчет суммы в гривнах через временный курс обмена и передачу всех необходимых данных в API. Структурирован модульно с отдельными компонентами для каждого поля формы и кнопки отправки с loading состоянием.

**Экспортируемые сущности / API (все на 100%)**

- `export const ExchangeForm` — основная форма обмена криптовалют
- `ExchangeCurrencyField` — поле выбора валюты (внутренний)
- `ExchangeAmountField` — поле ввода суммы (внутренний)
- `ExchangeEmailField` — поле ввода email (внутренний)
- `ExchangeSubmitButton` — кнопка отправки с loading (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет внешних параметров - самодостаточная форма
- Использует CRYPTOCURRENCIES из @repo/constants для списка валют
- Зависит от useExchangeMutation и useNotifications хуков

**Выходы / Побочные эффекты (все на 100%)**

- Создает заказ через exchangeMutation.createOrder.mutateAsync
- Показывает уведомления через notifications.orderCreated при успехе
- Обрабатывает ошибки через notifications.handleExchangeError
- Вычисляет uahAmount на основе mockExchangeRate (1,000,000)
- Управляет loading состоянием кнопки отправки

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: страницах обмена или модальных окнах создания заявок
- **Импортирует**: CRYPTOCURRENCIES из @repo/constants
- **Использует**: useForm, useNotifications из @repo/hooks
- **Использует**: useExchangeMutation из локальных хуков
- **Импортирует UI**: FormField, Input, Button, Select из @repo/ui

**Домен данных / типы (все на 100%)**

```typescript
interface ExchangeFormData extends Record<string, unknown> {
  currency: string;
  cryptoAmount: string;
  email: string;
}

const exchangeFormSchema = z.object({
  currency: z.enum(['BTC', 'ETH', 'USDT', 'LTC'] as const),
  cryptoAmount: z
    .string()
    .min(1)
    .refine(val => Number(val) > 0),
  email: z.string().email(),
});

interface CreateOrderData {
  currency: 'BTC' | 'ETH' | 'USDT' | 'LTC';
  cryptoAmount: number;
  email: string;
  uahAmount: number;
}

const mockExchangeRate = 1000000; // Временная заглушка
```

**Риски и безопасность**

- **Mock exchange rate**: критический риск использования фиксированного курса 1,000,000
- **Input validation**: зависит от Zod схемы для предотвращения некорректных данных
- **API integration**: критически зависит от useExchangeMutation для создания заказов
- **Email validation**: использует стандартную email валидацию Zod
- **Number conversion**: риск ошибок при конвертации строки в число

**Тесты / рекомендации по покрытию**

- **Form validation тесты**: корректность Zod схемы для всех полей
- **Submission тесты**: правильность создания заказа с вычисленным uahAmount
- **Error handling тесты**: обработка ошибок API через notifications
- **Loading state тесты**: корректность disabled состояния кнопки
- **Currency selection тесты**: правильность работы селектора валют

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная форма с API интеграцией и расчетами

**TODO / Рефакторинг**

- ✅ Модульная структура с отдельными компонентами полей
- ✅ Правильная Zod валидация с типизацией
- ✅ Интеграция с системой уведомлений
- ✅ Loading состояние для UX
- ✅ Использование констант из @repo/constants
- 🔴 Критично: заменить mockExchangeRate на реальный API курса
- 🔴 Критично: добавить локализацию вместо хардкод текстов
- 💡 Рекомендация: добавить минимальные/максимальные суммы валидации
- 💡 Рекомендация: рассмотреть добавление preview суммы в гривнах
