### Путь: apps/web/src/components/exchange-form/ExchangeBenefits.tsx

**Краткое назначение (1 предложение)**
Компонент отображения ключевых преимуществ обмена под формой с локализованными текстами для повышения доверия пользователей.

**Подробное описание (3–6 предложений)**
Компонент реализует секцию преимуществ, отображаемую под формой обмена для повышения конверсии и доверия пользователей. Использует переданную функцию локализации для отображения переводов из namespace 'benefits' с заголовком и тремя ключевыми преимуществами. Включает адаптивный дизайн с flex-wrap для корректного отображения на мобильных устройствах и различными отступами для разных размеров экрана. Структурирован с центрированным заголовком и горизонтальным списком преимуществ в primary цвете для привлечения внимания. Использует приглушенные цвета для заголовка и яркие primary цвета для самих преимуществ, создавая визуальную иерархию.

**Экспортируемые сущности / API (все на 100%)**

- `export function ExchangeBenefits({ t })` — компонент преимуществ обмена с локализацией

**Входы (expected inputs) / Параметры (все на 100%)**

- `t: (key: string) => string` — функция локализации для получения переводов
- Ожидает переводы для ключей: 'benefits.fast', 'benefits.items.exchange5min', 'benefits.items.noRegistration', 'benefits.items.bestRate'

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит центрированный заголовок с описанием скорости
- Отображает три преимущества в горизонтальном адаптивном layout
- Применяет primary цвет для выделения ключевых преимуществ
- Использует responsive отступы (gap-4 на мобильных, gap-6 на больших экранах)

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroExchangeForm как дополнительная информация под формой
- **Получает локализацию**: через prop t от родительского компонента
- **Зависит от переводов**: в messages/[locale].json в namespace benefits
- **Часть**: пользовательского опыта формы обмена для повышения конверсии

**Домен данных / типы (все на 100%)**

```typescript
interface ExchangeBenefitsProps {
  t: (key: string) => string; // Функция локализации
}

// Ожидаемые ключи переводов
type BenefitTranslationKeys =
  | 'benefits.fast'
  | 'benefits.items.exchange5min'
  | 'benefits.items.noRegistration'
  | 'benefits.items.bestRate';

// Структура преимуществ
const BENEFITS_COUNT = 3;
const BENEFITS_LAYOUT = 'horizontal-responsive';
```

**Риски и безопасность**

- **Низкий риск**: статический информационный компонент
- **Translation dependency**: критически зависит от наличия переводов для всех ключей
- **Prop validation**: отсутствует проверка функции t на корректность
- **Layout breaking**: длинные переводы могут нарушить layout на мобильных

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения всех переводов
- **Responsive тесты**: правильность адаптивного layout на разных экранах
- **Props тесты**: обработка некорректной функции локализации
- **Visual тесты**: соответствие дизайну и цветовой схеме

**Оценка сложности (low/medium/high)**
**LOW** - простой презентационный компонент с локализацией

**TODO / Рефакторинг**

- ✅ Правильная локализация через prop функцию
- ✅ Адаптивный дизайн с responsive отступами
- ✅ Использование design tokens (primary, muted-foreground)
- ✅ Client-side компонент для интерактивности
- 💡 Рекомендация: добавить PropTypes или TypeScript валидацию для t
- 💡 Рекомендация: рассмотреть добавление иконок к преимуществам
- 💡 Рекомендация: добавить fallback для отсутствующих переводов
