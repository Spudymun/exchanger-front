### Путь: apps/web/src/components/HowItWorksSection.tsx

**Краткое назначение (1 предложение)**
Секция "Как это работает" с пошаговым объяснением процесса обмена криптовалют через карточки с иконками и нумерацией.

**Подробное описание (3–6 предложений)**
Компонент реализует информационную секцию, объясняющую процесс обмена криптовалют в три простых шага с визуальным представлением. Использует Card компоненты из дизайн-системы с кастомными стилями для hover эффектов, теней и поддержки темной темы. Интегрирует локализацию через next-intl для отображения переводов заголовков и описаний шагов из namespace 'HomePage.howItWorks'. Включает три основных шага: выбор валют и суммы, отправка криптовалюты, получение средств на карту с соответствующими иконками из Lucide React. Структурирован с разделением на StepCard подкомпонент для лучшей организации и переиспользования логики отображения шагов.

**Экспортируемые сущности / API (все на 100%)**

- `export function HowItWorksSection()` — основная секция объяснения процесса
- `StepCard({ stepNumber, icon, title, description })` — компонент карточки шага (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает переводы через useTranslations
- Использует переводы из namespace 'HomePage.howItWorks' для заголовка и шагов
- Импортирует иконки ArrowLeftRight, Send, CreditCard из Lucide React

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит секцию с заголовком и сеткой из 3 карточек шагов
- Отображает локализованный контент для всех шагов процесса
- Применяет адаптивную сетку: 1 колонка на мобильных, 3 на десктопах
- Использует hover эффекты и анимации для улучшения пользовательского опыта

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: apps/web/app/[locale]/page.tsx как часть главной страницы
- **Импортирует**: Card компоненты из @repo/ui, иконки из lucide-react
- **Использует переводы**: из messages/[locale].json в namespace 'HomePage.howItWorks'
- **Часть**: лендинговой структуры после FeaturesSection

**Домен данных / типы (все на 100%)**

```typescript
interface StepCardProps {
  stepNumber: number;
  icon: React.ComponentType<React.SVGProps<SVGSVGElement>>;
  title: string;
  description: string;
}

interface ProcessStep {
  stepNumber: 1 | 2 | 3;
  icon: LucideIcon;
  title: string;
  description: string;
}

type ExchangeProcess = [ProcessStep, ProcessStep, ProcessStep];
```

**Риски и безопасность**

- **Низкий риск**: статический информационный компонент
- **Translation dependency**: зависит от наличия переводов в howItWorks namespace
- **Icon loading**: использует статические иконки без динамической загрузки
- **Accessibility**: Card компоненты должны поддерживать screen readers

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов для всех шагов
- **Responsive тесты**: адаптивность сетки на разных размерах экрана
- **Card interaction тесты**: hover эффекты и анимации
- **Content тесты**: правильность отображения номеров шагов и иконок

**Оценка сложности (low/medium/high)**
**LOW** - простая информационная секция с карточками

**TODO / Рефакторинг**

- ✅ Правильное использование Card компонентов из дизайн-системы
- ✅ Полная локализация всех текстов через next-intl
- ✅ Хорошее разделение на StepCard подкомпонент
- ✅ Адаптивная сетка с правильными breakpoints
- 💡 Рекомендация: добавить анимации появления карточек при скролле
- 💡 Рекомендация: рассмотреть добавление интерактивных элементов для лучшего engagement
