### Путь: apps/web/src/components/app-header.tsx

**Краткое назначение (1 предложение)**
Компонент шапки приложения ExchangeGO с адаптивным дизайном, аутентификацией, локализацией и навигацией.

**Подробное описание (3–6 предложений)**
Компонент реализует полнофункциональную шапку сайта с адаптивным дизайном для мобильных и десктопных устройств, используя Header компонент из дизайн-системы. Интегрирует систему аутентификации через tRPC с модальными диалогами входа и регистрации, управляемыми через кастомный хук useAuthDialogs. Включает локализацию через next-intl с переключателем языков и навигацию с подсветкой активных ссылок на основе текущего маршрута. Содержит логотип с разными версиями для мобильных и десктопных устройств, переключатель темы и пользовательское меню. Реализует умную навигацию с плавным скроллом к секции обмена на главной странице и обычной навигацией на других страницах.

**Экспортируемые сущности / API (все на 100%)**

- `export function AppHeader({ className })` — основной компонент шапки приложения
- `useAuthDialogs()` — кастомный хук управления диалогами аутентификации (внутренний)
- `AppHeaderMobile()` — мобильная версия шапки (внутренний)
- `AppHeaderDesktop()` — десктопная версия шапки (внутренний)
- `AppHeaderLogoMobile()` — мобильный логотип (внутренний)
- `AppHeaderLogoDesktop()` — десктопный логотип (внутренний)
- `AppHeaderNavigationLinks()` — навигационные ссылки (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- `className?: string` — опциональные CSS классы для кастомизации
- Использует переводы из namespace 'Layout' через useTranslations
- Получает данные сессии через trpc.auth.getSession.useQuery()
- Использует pathname и locale из Next.js navigation hooks

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит адаптивную шапку с мобильной и десктопной версиями
- Управляет состоянием модальных диалогов аутентификации
- Выполняет навигацию и смену локали через router
- Осуществляет плавный скролл к секциям на главной странице

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: AppLayout как часть общей структуры страницы
- **Импортирует**: Header из @repo/ui, trpc клиент, AuthDialogs, ThemeToggleI18n
- **Использует**: Link из i18n navigation для локализованной маршрутизации
- **Экспортируется через**: apps/web/src/components/index.ts

**Домен данных / типы (все на 100%)**

```typescript
interface AppHeaderProps {
  className?: string;
}

interface SessionData {
  user: { id: string; email: string; isVerified: boolean } | null;
}

interface AuthDialogsState {
  isLoginDialogOpen: boolean;
  isRegisterDialogOpen: boolean;
}

type NavigationLinkState = 'active' | 'inactive';
type DeviceVariant = 'mobile' | 'desktop';
```

**Риски и безопасность**

- **Средний риск**: интеграция с системой аутентификации
- **Session management**: зависит от корректной работы tRPC auth
- **Navigation security**: использует локализованные ссылки для безопасности
- **State management**: правильная мемоизация callback'ов предотвращает циклы

**Тесты / рекомендации по покрытию**

- **Authentication тесты**: корректность работы диалогов входа/регистрации
- **Navigation тесты**: правильность подсветки активных ссылок и навигации
- **Responsive тесты**: адаптивность мобильной и десктопной версий
- **Localization тесты**: работа переключателя языков и локализованных ссылок

**Оценка сложности (low/medium/high)**
**HIGH** - сложная интеграция аутентификации, навигации, локализации и адаптивности

**TODO / Рефакторинг**

- ✅ Правильная мемоизация callback'ов для предотвращения циклов
- ✅ Адаптивный дизайн с отдельными компонентами для мобильных и десктопных
- ✅ Интеграция с дизайн-системой через Header компонент
- ✅ Умная навигация с плавным скроллом на главной странице
- 💡 Рекомендация: вынести логику навигации в отдельный хук
- 💡 Рекомендация: добавить loading состояния для tRPC запросов
