### Путь: apps/web/src/components/theme-toggle-i18n.tsx

**Краткое назначение (1 предложение)**
Интернационализированная обертка для компонента переключения темы с локализованными подписями через next-intl.

**Подробное описание (3–6 предложений)**
Компонент реализует тонкую обертку над базовым ThemeToggle из дизайн-системы, добавляя поддержку локализации через next-intl. Использует переводы из namespace 'theme' для получения локализованных названий тем: светлая, темная и системная. Предоставляет объект labels с переводами для всех опций темы, включая fallback для toggle label. Следует паттерну композиции, делегируя всю основную функциональность базовому компоненту и добавляя только слой локализации. Является примером правильного разделения ответственности между UI логикой и интернационализацией.

**Экспортируемые сущности / API (все на 100%)**

- `export function ThemeToggleI18n()` — интернационализированный переключатель темы

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает переводы через useTranslations
- Зависит от наличия переводов в namespace 'theme' с ключами: light, dark, system

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит ThemeToggle с локализованными подписями
- Передает объект labels с переводами в базовый компонент
- Использует 'light' как fallback для toggle label
- Делегирует всю функциональность переключения темы базовому компоненту

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: app-header или других компонентах интерфейса
- **Импортирует**: ThemeToggle из @repo/ui для базовой функциональности
- **Использует**: useTranslations('theme') для получения переводов
- **Зависит от**: messages/[locale].json с переводами в theme namespace
- **Паттерн**: композиция для добавления локализации к базовому компоненту

**Домен данных / типы (все на 100%)**

```typescript
// Интерфейс для labels объекта
interface ThemeLabels {
  light: string;
  dark: string;
  system: string;
  toggle: string;
}

// Ожидаемые ключи переводов
type ThemeTranslationKeys = 'light' | 'dark' | 'system';

// Namespace локализации
const THEME_NAMESPACE = 'theme';
```

**Риски и безопасность**

- **Минимальный риск**: простая обертка без собственной логики
- **Translation dependency**: критически зависит от наличия переводов theme.\*
- **Fallback handling**: использует 'light' как fallback для toggle, может быть неочевидно
- **Component coupling**: тесно связан с интерфейсом ThemeToggle компонента
- **Missing translations**: может отображать ключи вместо переводов при их отсутствии

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность получения всех переводов theme namespace
- **Props passing тесты**: правильность передачи labels в ThemeToggle
- **Fallback тесты**: корректность fallback для toggle label
- **Integration тесты**: совместимость с базовым ThemeToggle компонентом

**Оценка сложности (low/medium/high)**
**LOW** - простая обертка для добавления локализации

**TODO / Рефакторинг**

- ✅ Правильное использование композиции для добавления локализации
- ✅ Четкое разделение ответственности с базовым компонентом
- ✅ Использование next-intl для интернационализации
- ✅ Подробная JSDoc документация назначения
- 💡 Рекомендация: улучшить fallback для toggle label (использовать отдельный ключ)
- 💡 Рекомендация: добавить error boundary для обработки отсутствующих переводов
- 💡 Рекомендация: рассмотреть добавление PropTypes или TypeScript интерфейса для labels
- 💡 Рекомендация: добавить мемоизацию labels объекта для производительности
