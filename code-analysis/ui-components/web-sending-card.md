### Путь: apps/web/src/components/exchange-form/SendingCard.tsx

**Краткое назначение (1 предложение)**
Карточка отправки средств в форме обмена с выбором криптовалюты, токен-стандарта, вводом суммы и валидацией.

**Подробное описание (3–6 предложений)**
Компонент реализует левую часть формы обмена, отвечающую за настройку отправляемых криптовалютных средств. Включает селектор криптовалют (BTC, ETH, USDT, LTC) с автоматической установкой токен-стандарта по умолчанию при смене валюты. Содержит условный селектор токен-стандартов, который отображается только для мульти-сетевых токенов через isMultiNetworkToken проверку. Реализует продвинутый ввод суммы с валидацией через validateCryptoAmount, форматированием через useNumericInput и real-time проверкой ошибок. Включает информационную секцию с минимальной суммой и текущим курсом обмена, а также использует синюю цветовую схему для визуального выделения как карточки отправки.

**Экспортируемые сущности / API (все на 100%)**

- `export function SendingCard({ form, t, exchangeRate, minAmount })` — основная карточка отправки
- `export function TokenStandardSelector({ form, t })` — селектор токен-стандарта
- `export function CurrencySelector({ form, t })` — селектор криптовалюты
- `AmountInput({ form, t })` — поле ввода суммы с валидацией (внутренний)
- `SendingInfo({ form, t, exchangeRate, minAmount })` — информационная секция (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- `form: ReturnType<typeof useForm<HeroExchangeFormData>>` — объект формы с типизацией
- `t: (key: string) => string` — функция локализации для переводов
- `exchangeRate: number` — текущий курс обмена для отображения
- `minAmount: number` — минимальная сумма для валидации и информации

**Выходы / Побочные эффекты (все на 100%)**

- Обновляет form.values.fromCurrency при выборе валюты
- Автоматически устанавливает form.values.tokenStandard при смене валюты
- Обновляет form.values.fromAmount с валидацией и форматированием
- Показывает real-time ошибки валидации через useState
- Отображает информацию о минимальной сумме и курсе обмена

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroExchangeForm как левая карточка формы обмена
- **Импортирует**: getDefaultTokenStandard, isMultiNetworkToken, getTokenStandards из @repo/constants
- **Использует**: validateCryptoAmount из @repo/exchange-core для валидации
- **Использует**: useForm из @repo/hooks, useNumericInput из @repo/utils
- **Импортирует UI**: Card, Select, Input, FormField компоненты из @repo/ui

**Домен данных / типы (все на 100%)**

```typescript
interface SendingCardProps {
  form: ReturnType<typeof useForm<HeroExchangeFormData>>;
  t: (key: string) => string;
  exchangeRate: number;
  minAmount: number;
}

interface HeroExchangeFormData {
  fromCurrency: string;
  tokenStandard: string;
  fromAmount: string;
  toCurrency: string;
}

type CryptoCurrency = 'BTC' | 'ETH' | 'USDT' | 'LTC';
type TokenStandard = string; // ERC20, TRC20, etc.
```

**Риски и безопасность**

- **Amount validation**: критически важная валидация через validateCryptoAmount
- **Input formatting**: зависит от useNumericInput для корректного форматирования
- **State synchronization**: сложная логика синхронизации валюты и токен-стандарта
- **Real-time validation**: может создавать performance issues при частом вводе
- **Currency constants**: хардкод списка валют ['BTC', 'ETH', 'USDT', 'LTC']

**Тесты / рекомендации по покрытию**

- **Validation тесты**: корректность validateCryptoAmount для всех валют
- **Input formatting тесты**: правильность useNumericInput обработки
- **Currency selection тесты**: автоматическая установка токен-стандарта
- **Token standard тесты**: условное отображение для мульти-сетевых токенов
- **Real-time validation тесты**: корректность обновления ошибок

**Оценка сложности (low/medium/high)**
**HIGH** - сложная логика валидации, форматирования и условного отображения

**TODO / Рефакторинг**

- ✅ Продвинутая валидация через validateCryptoAmount
- ✅ Условное отображение токен-стандартов для мульти-сетевых токенов
- ✅ Real-time валидация с useState для ошибок
- ✅ Автоматическая синхронизация валюты и токен-стандарта
- ✅ Синяя цветовая схема для карточки отправки
- 💡 Рекомендация: вынести список валют в константы
- 💡 Рекомендация: добавить debounce для real-time валидации
- 💡 Рекомендация: рассмотреть мемоизацию validateAmount функции
