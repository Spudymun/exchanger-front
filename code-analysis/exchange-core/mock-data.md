# Анализ файла: packages/exchange-core/src/data/mock-data.ts

## 📋 Назначение

Централизованное хранилище mock данных для тестирования и разработки crypto-exchange системы ExchangeGO. Обеспечивает консистентные тестовые данные согласно ai-agent-rules.yml (правило 20) с устранением дублирования констант.

## 📝 Описание

Систематизированная коллекция mock данных, включающая:

- **Centralized constants** - централизованные константы согласно DRY принципам
- **Authentication mock data** - тестовые данные для аутентификации
- **User information** - mock email адреса и пользовательские данные
- **Timestamp consistency** - консистентные временные метки
- **Transaction data** - crypto и payment mock данные
- **Dynamic ID generation** - генерация уникальных ID через сервисы
- **Type safety** - строгая типизация всех mock данных

Служит единым источником истины для тестовых данных в development окружении.

## 🔌 API и интерфейсы

### Authentication Mock Data:

```typescript
export const MOCK_AUTH_DATA = {
  EXAMPLE_HASH: '$2b$10$example_hash', // Стандартный bcrypt хеш для тестов
  ADMIN_HASH: '$2b$10$example_hash_admin', // Админский хеш пароля
} as const;
```

### User Mock Data:

```typescript
export const MOCK_USER_EMAILS = {
  TEST_USER: 'test@example.com', // Email тестового пользователя
  ADMIN_USER: 'admin@exchangego.com', // Email администратора
} as const;
```

### Timestamp Mock Data:

```typescript
export const MOCK_TIMESTAMPS = {
  BASE_CREATED_AT: '2025-06-29T10:00:00.000Z', // Базовое время создания
  LAST_LOGIN_AT: '2025-06-29T10:00:00.000Z', // Время последнего входа
  ORDER_UPDATED_AT: '2025-06-29T12:00:00.000Z', // Время обновления ордера
  ORDER_PROCESSED_AT: '2025-06-29T12:00:00.000Z', // Время обработки ордера
  MOCK_ORDER_1_TIMESTAMP: 1703847600000, // Unix timestamp для ID генерации
  MOCK_ORDER_2_TIMESTAMP: 1703847660000, // Unix timestamp для ID генерации
} as const;
```

### Transaction Mock Data:

```typescript
export const MOCK_TRANSACTION_DATA = {
  BTC_ADDRESS: '1A1zP1eP5QGefi2DMPTfTL5SLmv7DivfNa', // Genesis Bitcoin адрес
  CARD_NUMBER: '1234567890123456', // Mock номер карты
  TX_HASH: 'example_tx_hash_123', // Mock хеш транзакции
} as const;
```

### Dynamic Order IDs:

```typescript
export const MOCK_ORDER_IDS = {
  ORDER_1: generateTestOrderId(MOCK_TIMESTAMPS.MOCK_ORDER_1_TIMESTAMP, 'abc123'),
  ORDER_2: generateTestOrderId(MOCK_TIMESTAMPS.MOCK_ORDER_2_TIMESTAMP, 'def456'),
} as const;

// Использует IdGenerationService для генерации детерминированных ID
```

### Type Definitions:

```typescript
export type MockAuthData = typeof MOCK_AUTH_DATA;
export type MockUserEmails = typeof MOCK_USER_EMAILS;
export type MockTimestamps = typeof MOCK_TIMESTAMPS;
export type MockTransactionData = typeof MOCK_TRANSACTION_DATA;
export type MockOrderIds = typeof MOCK_ORDER_IDS;
```

## 📥 Входящие зависимости

```typescript
import { generateTestOrderId } from '../services/id-generation';
```

### Единственная зависимость:

- **../services/id-generation** - сервис для генерации детерминированных тестовых ID

## 📤 Исходящие зависимости

- **./manager.ts** - использует все mock константы для инициализации данных
- **Test suites** - тесты используют mock данные для setup
- **Development tools** - инструменты разработки используют тестовые данные
- **Storybook** - компоненты используют mock данные для демонстрации
- **Mock factories** - фабрики используют базовые константы

## 🔗 Взаимосвязи с другими компонентами

### Архитектурные связи:

- **ai-agent-rules.yml** - соответствует правилу 20 (централизация констант)
- **id-generation service** - использует для dynamic ID creation
- **manager.ts** - primary consumer mock данных
- **exchange-core types** - соответствует типам системы

### Data consistency цепочка:

```
mock-data.ts → manager.ts → data/index.ts → exchange-core/index.ts → apps
```

### DRY principle implementation:

```
┌─────────────────────────────────────┐
│         Single Source of Truth      │
│           (mock-data.ts)            │ ← Единый источник
├─────────────────────────────────────┤
│      Multiple Consumers             │
│ (manager, tests, stories, factories) │
└─────────────────────────────────────┘
```

## 📊 Типы данных

### Mock Data Categories:

```typescript
interface MockDataStructure {
  authentication: {
    standardHash: string; // Стандартный bcrypt хеш
    adminHash: string; // Админский хеш
  };

  users: {
    testEmail: string; // Тестовый email
    adminEmail: string; // Админский email
  };

  temporal: {
    baseTimestamp: string; // ISO 8601 timestamps
    specificTimestamps: string[]; // Специфичные временные метки
    unixTimestamps: number[]; // Unix timestamps для ID генерации
  };

  financial: {
    cryptoAddress: string; // Bitcoin адрес
    paymentData: string; // Номер карты
    transactionHash: string; // Blockchain хеш
  };

  identifiers: {
    dynamicOrderIds: string[]; // Генерируемые ID ордеров
  };
}
```

### Constants organization:

```typescript
interface ConstantsOrganization {
  naming: 'SCREAMING_SNAKE_CASE'; // Стандарт именования констант
  grouping: 'by_domain'; // Группировка по доменам
  immutability: 'as_const'; // TypeScript immutability
  centralization: 'single_file'; // Централизация в одном файле
}
```

### Usage patterns:

```typescript
interface MockDataUsage {
  development: boolean; // Использование в development
  testing: boolean; // Использование в тестах
  storybook: boolean; // Использование в stories
  production: false; // НЕ используется в production
}
```

## ⚠️ Потенциальные проблемы и риски

### Проблемы безопасности:

- **Production leakage**: Риск попадания mock данных в production
- **Weak passwords**: Использование простых хешей паролей
- **Predictable IDs**: Детерминированные ID могут быть предсказуемыми
- **Real data exposure**: Использование реальных форматов данных

### Проблемы поддержки:

- **Data staleness**: Mock данные могут устаревать относительно production схемы
- **Inconsistency risk**: Несоответствие между mock и real данными
- **Update complexity**: Сложность обновления всех зависимых компонентов
- **Documentation gap**: Недостаток документации назначения mock данных

### Проблемы тестирования:

- **Limited coverage**: Ограниченное покрытие edge cases
- **False positives**: Тесты могут проходить на mock данных, но падать на real
- **Data variety lack**: Недостаток разнообразия тестовых сценариев
- **Boundary testing**: Отсутствие граничных значений для тестирования

### Проблемы производительности:

- **Import overhead**: Накладные расходы на импорт всех констант
- **Memory consumption**: Потребление памяти для хранения констант
- **Initialization cost**: Стоимость инициализации mock данных
- **Bundle size impact**: Влияние на размер production bundle

## ✅ Тестирование

- **Unit-тесты**: Отсутствуют
- **Consistency tests**: Отсутствуют
- **Type safety tests**: Отсутствуют

### Рекомендации по тестированию:

- Тесты соответствия mock данных production схеме
- Валидация типов всех mock констант
- Тесты уникальности генерируемых ID
- Проверка отсутствия mock данных в production build
- Performance тесты импорта констант

## 🔧 Техническая сложность

**Уровень: Низкий**

### Метрики сложности:

- **Размер**: 68 строк с простой структурой констант
- **Категорий данных**: 5 основных доменов
- **Зависимостей**: 1 (id-generation service)
- **Архитектурная роль**: Утилитарная (support layer)

### Анализ архитектуры:

- Простая и понятная организация констант
- Четкое разделение по доменам
- Эффективное использование TypeScript типов
- Соответствие DRY принципу

## 📝 TODO и области для улучшения

### Критические улучшения:

1. **Production safety**: Механизмы предотвращения попадания в production
2. **Data validation**: Валидация соответствия mock данных схеме
3. **Dynamic generation**: Расширение dynamic генерации данных
4. **Environment isolation**: Изоляция mock данных по окружениям

### Рекомендуемые улучшения:

1. **Data variety expansion**: Расширение разнообразия тестовых данных
2. **Edge cases coverage**: Добавление граничных случаев
3. **Localization support**: Поддержка локализации mock данных
4. **Performance optimization**: Оптимизация загрузки констант
5. **Documentation enhancement**: Улучшение документации назначения данных

### Долгосрочные задачи:

1. **AI-generated mock data**: ИИ генерация реалистичных тестовых данных
2. **Dynamic mock factories**: Динамические фабрики для различных сценариев
3. **Real data synchronization**: Синхронизация с production данными для актуальности
4. **Advanced data generation**: Продвинутая генерация на основе business rules
5. **Mock data analytics**: Аналитика использования mock данных
6. **Automated data refresh**: Автоматическое обновление mock данных
7. **Cross-platform compatibility**: Совместимость mock данных между платформами
8. **Privacy-compliant mocking**: Mock данные с соблюдением privacy требований
