### Путь: apps/web/src/components/forms/index.ts

**Краткое назначение (1 предложение)**
Централизованный файл экспорта всех форм приложения для удобного импорта и управления зависимостями.

**Подробное описание (3–6 предложений)**
Файл реализует паттерн barrel exports для всех компонентов форм в директории forms, обеспечивая единую точку входа для импорта. Экспортирует четыре основных формы: ExchangeForm для операций обмена, LoginForm и RegisterForm для аутентификации, и AuthForms как составной компонент аутентификации. Следует стандартной практике организации модулей в React приложениях, позволяя импортировать все формы из одного места. Включает JSDoc комментарии для документирования назначения файла и улучшения developer experience.

**Экспортируемые сущности / API (все на 100%)**

- `export { ExchangeForm }` — форма обмена криптовалют
- `export { LoginForm }` — форма входа в систему
- `export { RegisterForm }` — форма регистрации пользователя
- `export { AuthForms }` — составной компонент аутентификации

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых входных параметров - это файл экспорта
- Зависит от существования соответствующих файлов компонентов в той же директории

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет централизованный доступ к всем формам приложения
- Упрощает импорты в других частях приложения
- Создает единую точку управления экспортами форм

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: других компонентах приложения для импорта форм
- **Экспортирует**: ExchangeForm, LoginForm, RegisterForm, AuthForms из локальных файлов
- **Часть**: архитектуры модульной организации компонентов
- **Паттерн**: barrel exports для упрощения структуры импортов

**Домен данных / типы (все на 100%)**

```typescript
// Экспортируемые типы компонентов
type ExchangeFormComponent = React.ComponentType<ExchangeFormProps>;
type LoginFormComponent = React.ComponentType<LoginFormProps>;
type RegisterFormComponent = React.ComponentType<RegisterFormProps>;
type AuthFormsComponent = React.ComponentType<AuthFormsProps>;

// Структура экспортов
interface FormsModuleExports {
  ExchangeForm: ExchangeFormComponent;
  LoginForm: LoginFormComponent;
  RegisterForm: RegisterFormComponent;
  AuthForms: AuthFormsComponent;
}
```

**Риски и безопасность**

- **Минимальный риск**: простой файл экспорта без логики
- **Dependency chain**: создает цепочку зависимостей между формами
- **Bundle size**: может увеличить размер бандла при неиспользуемых экспортах
- **Circular dependencies**: потенциальный риск циклических зависимостей

**Тесты / рекомендации по покрытию**

- **Import тесты**: корректность всех экспортов
- **Module resolution тесты**: правильность путей к компонентам
- **Bundle analysis**: проверка влияния на размер бандла
- **Dependency тесты**: отсутствие циклических зависимостей

**Оценка сложности (low/medium/high)**
**LOW** - простой файл экспорта без бизнес-логики

**TODO / Рефакторинг**

- ✅ Правильная организация экспортов с JSDoc документацией
- ✅ Использование named exports для лучшего tree-shaking
- ✅ Логичная группировка форм по функциональности
- ✅ Соответствие паттерну barrel exports
- 💡 Рекомендация: рассмотреть добавление type-only экспортов для типов
- 💡 Рекомендация: добавить re-export типов пропсов для удобства
- 💡 Рекомендация: рассмотреть lazy loading для больших форм
