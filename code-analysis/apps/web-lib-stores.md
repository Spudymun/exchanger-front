### Путь: apps/web/lib/stores.ts

**Краткое назначение (1 предложение)**
Legacy файл-мост для обратной совместимости, реэкспортирующий stores из централизованного пакета @repo/hooks.

**Подробное описание (3–6 предложений)**
Файл служит переходным мостом для миграции от локальных stores к централизованной системе управления состоянием в монорепозитории. Содержит четкие комментарии с инструкциями по использованию новой архитектуры через @repo/hooks/src/client-hooks. Реэкспортирует useUIStore и useTradingStore для поддержания обратной совместимости существующего кода. Предотвращает дублирование логики состояния между приложениями. Является частью архитектурной миграции к централизованному управлению состоянием в монорепозитории.

**Экспортируемые сущности / API (все на 100%)**

- `export { useUIStore, useTradingStore }` — реэкспорт из @repo/hooks/src/client-hooks

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - только реэкспорты
- Импортируется как: `import { useUIStore, useTradingStore } from './lib/stores'`

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет доступ к централизованным Zustand stores
- Нет побочных эффектов - только реэкспорты
- Обеспечивает обратную совместимость для legacy импортов
- Поддерживает консистентность состояния в монорепозитории

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Реэкспортирует из**: @repo/hooks/src/client-hooks
- **Импортируется в**: legacy компоненты apps/web, которые еще не мигрированы
- **Заменяется на**: прямые импорты из @repo/hooks
- **Часть миграции**: от локальных stores к централизованной архитектуре

**Домен данных / типы (все на 100%)**

```typescript
// Реэкспортируемые типы из @repo/hooks:
type UIStore = ReturnType<typeof useUIStore>;
type TradingStore = ReturnType<typeof useTradingStore>;

// Legacy импорт (deprecated):
import { useUIStore } from './lib/stores';
// Новый рекомендуемый импорт:
import { useUIStore } from '@repo/hooks/src/client-hooks';
```

**Риски и безопасность**

- **Низкий риск**: простой реэкспорт без логики
- **Deprecation risk**: файл предназначен для удаления после миграции
- **Consistency**: обеспечивает единообразие состояния в монорепозитории
- **Migration safety**: позволяет постепенную миграцию без breaking changes

**Тесты / рекомендации по покрытию**

- **Migration тесты**: проверка корректности реэкспортов
- **Integration тесты**: совместимость с legacy кодом
- **Deprecation тесты**: отслеживание использования legacy импортов
- **Consistency тесты**: идентичность поведения с оригинальными stores

**Оценка сложности (low/medium/high)**
**LOW** - простой реэкспорт для обратной совместимости

**TODO / Рефакторинг**

- 🔄 **Legacy файл**: предназначен для удаления после полной миграции
- 📝 **Миграция**: обновить все импорты на @repo/hooks/src/client-hooks
- ✅ **Документация**: четкие инструкции по использованию новой архитектуры
- 🎯 **Цель**: удалить файл после завершения миграции компонентов
