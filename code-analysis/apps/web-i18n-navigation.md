### Путь: apps/web/src/i18n/navigation.ts

**Краткое назначение (1 предложение)**
Локализованные навигационные утилиты Next.js, созданные через next-intl для поддержки многоязычной маршрутизации.

**Подробное описание (3–6 предложений)**
Файл создает локализованные обертки над стандартными навигационными API Next.js через createNavigation из next-intl с учетом конфигурации маршрутизации. Использует routing конфигурацию для автоматического добавления локали к URL и обеспечения корректной навигации между языковыми версиями. Предоставляет пять основных навигационных утилит: Link для локализованных ссылок, redirect для перенаправлений с учетом локали, usePathname и useRouter для хуков навигации, getPathname для получения пути. Является центральной точкой для всех навигационных операций в приложении, обеспечивая консистентную обработку локализации во всех компонентах. Следует best practices next-intl с созданием lightweight wrappers без дополнительной логики.

**Экспортируемые сущности / API (все на 100%)**

- `export const { Link, redirect, usePathname, useRouter, getPathname }` — локализованные навигационные утилиты

**Входы (expected inputs) / Параметры (все на 100%)**

- Зависит от routing конфигурации из './routing'
- Использует createNavigation из next-intl для создания утилит

**Выходы / Побочные эффекты (все на 100%)**

- `Link` — компонент локализованных ссылок с автоматическим добавлением локали
- `redirect` — функция перенаправления с учетом текущей локали
- `usePathname` — хук получения текущего пути с локалью
- `useRouter` — хук маршрутизатора с поддержкой локализации
- `getPathname` — утилита получения пути для серверных компонентов

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: всех компонентах приложения для навигации
- **Импортирует**: createNavigation из next-intl для создания утилит
- **Зависит от**: routing конфигурации для настроек локализации
- **Часть**: системы интернационализации приложения
- **Заменяет**: стандартные Next.js навигационные API локализованными версиями

**Домен данных / типы (все на 100%)**

```typescript
// Типы из next-intl
type LocalizedLink = ComponentType<LinkProps & { locale?: string }>;
type LocalizedRedirect = (pathname: string, type?: RedirectType) => never;
type LocalizedUsePathname = () => string;
type LocalizedUseRouter = () => AppRouterInstance & {
  push: (href: string, options?: NavigateOptions) => void;
};
type LocalizedGetPathname = (options: { href: string; locale?: string }) => string;

// Структура экспорта
interface LocalizedNavigation {
  Link: LocalizedLink;
  redirect: LocalizedRedirect;
  usePathname: LocalizedUsePathname;
  useRouter: LocalizedUseRouter;
  getPathname: LocalizedGetPathname;
}
```

**Риски и безопасность**

- **Минимальный риск**: простые обертки над проверенными API
- **Routing dependency**: критически зависит от корректности routing конфигурации
- **Locale handling**: должен правильно обрабатывать все поддерживаемые локали
- **URL generation**: некорректная генерация URL может нарушить SEO
- **Type safety**: зависит от типизации next-intl для безопасности

**Тесты / рекомендации по покрытию**

- **Link generation тесты**: корректность генерации локализованных ссылок
- **Redirect тесты**: правильность перенаправлений с учетом локали
- **Pathname тесты**: корректность получения путей для всех локалей
- **Router тесты**: функциональность локализованного роутера
- **Integration тесты**: совместимость с routing конфигурацией

**Оценка сложности (low/medium/high)**
**LOW** - простые обертки над next-intl API

**TODO / Рефакторинг**

- ✅ Правильное использование createNavigation из next-intl
- ✅ Интеграция с routing конфигурацией
- ✅ Экспорт всех необходимых навигационных утилит
- ✅ Lightweight wrappers без дополнительной логики
- ✅ Четкие комментарии о назначении файла
- 💡 Рекомендация: добавить JSDoc для каждой экспортируемой утилиты
- 💡 Рекомендация: рассмотреть добавление типов для лучшей документации
- 💡 Рекомендация: добавить примеры использования в комментариях
- 💡 Рекомендация: рассмотреть добавление дополнительных утилит навигации
