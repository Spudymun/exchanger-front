### Путь: apps/web/app/error.tsx

**Краткое назначение (1 предложение)**
Next.js App Router error boundary компонент для обработки глобальных ошибок с пользовательским интерфейсом восстановления.

**Подробное описание (3–6 предложений)**
Компонент реализует Next.js App Router error boundary для перехвата и отображения ошибок на уровне приложения. Предоставляет пользовательский интерфейс с иконкой ошибки, описанием проблемы и кнопками для восстановления. Отображает сообщение об ошибке в безопасном формате с возможностью повторной попытки или возврата на главную страницу. Использует компоненты из дизайн-системы (@repo/ui) для консистентного стиля и поддерживает темную тему. Включает градиентный фон и responsive дизайн для улучшения пользовательского опыта при ошибках.

**Экспортируемые сущности / API (все на 100%)**

- `export default function GlobalError({ error, reset })` — основной error boundary компонент
- `function ErrorIcon()` — внутренний компонент иконки ошибки (не экспортируется)

**Входы (expected inputs) / Параметры (все на 100%)**

- `error: Error & { digest?: string }` — объект ошибки с опциональным digest
- `reset: () => void` — функция для повторной попытки рендеринга
- Автоматически вызывается Next.js при ошибках в App Router

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранный UI с информацией об ошибке
- Предоставляет кнопки "Try Again" (вызывает reset) и "Go Home" (redirect на /en)
- Отображает error.message в безопасном контейнере
- Использует window.location.href для навигации на главную

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js автоматически при ошибках в App Router
- **Импортирует**: `Button` из @repo/ui для консистентного UI
- **Используется**: как fallback для всех ошибок приложения
- **Навигация**: redirect на /en при клике "Go Home"

**Домен данных / типы (все на 100%)**

```typescript
interface ErrorProps {
  error: Error & { digest?: string };
  reset: () => void;
}

interface ErrorBoundaryState {
  hasError: boolean;
  error?: Error;
}

type ErrorRecoveryActions = 'retry' | 'home';
```

**Риски и безопасность**

- **Низкий риск**: стандартный error boundary без sensitive операций
- **Error exposure**: отображает error.message - может содержать sensitive данные
- **Navigation**: использует window.location.href вместо Next.js router
- **XSS prevention**: использует текстовое отображение ошибки без innerHTML

**Тесты / рекомендации по покрытию**

- **Error boundary тесты**: корректность перехвата различных типов ошибок
- **UI тесты**: отображение компонентов в error состоянии
- **Recovery тесты**: функциональность кнопок "Try Again" и "Go Home"
- **Accessibility тесты**: поддержка screen readers и keyboard navigation

**Оценка сложности (low/medium/high)**
**LOW** - стандартный error boundary с простым UI

**TODO / Рефакторинг**

- ✅ Правильная интеграция с Next.js App Router error boundary
- ✅ Использование дизайн-системы для консистентного UI
- ✅ Поддержка темной темы и responsive дизайн
- 💡 Рекомендация: добавить логирование ошибок в monitoring систему
- 💡 Рекомендация: использовать Next.js router вместо window.location.href
- ⚠️ Рекомендация: фильтровать sensitive данные из error.message
