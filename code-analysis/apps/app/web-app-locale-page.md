### Путь: apps/web/app/[locale]/page.tsx

**Краткое назначение (1 предложение)**
Главная страница приложения ExchangeGO с локализацией, содержащая основные секции лендинга и floating кнопку обмена.

**Подробное описание (3–6 предложений)**
Компонент реализует главную страницу криптообменника с полной поддержкой локализации через setRequestLocale для статического рендеринга. Структурирован как лендинг с основными секциями: HeroSection с формой обмена, FeaturesSection с преимуществами, HowItWorksSection с объяснением процесса. Включает FloatingExchangeButton для быстрого доступа к функции обмена с любого места на странице. Использует responsive дизайн с адаптивными отступами и максимальной шириной контейнера для оптимального отображения на всех устройствах. Обеспечивает overflow-x-hidden для предотвращения горизонтального скролла на мобильных устройствах.

**Экспортируемые сущности / API (все на 100%)**

- `export default async function HomePage({ params })` — главная страница приложения

**Входы (expected inputs) / Параметры (все на 100%)**

- `params: Promise<{ locale: string }>` — параметры маршрута с локалью
- Получает локаль для настройки статического рендеринга
- Компоненты получают локализацию через NextIntlClientProvider контекст

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полную структуру главной страницы с секциями
- Устанавливает locale для статического рендеринга через setRequestLocale
- Отображает HeroSection, FeaturesSection, HowItWorksSection последовательно
- Добавляет FloatingExchangeButton поверх основного контента

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: как главная страница для всех локалей (/, /ru, /en)
- **Импортирует**: компоненты из `../../src/components/`
- **Рендерит**: HeroSection, FeaturesSection, HowItWorksSection, FloatingExchangeButton
- **Интегрируется**: с locale layout для получения переводов

**Домен данных / типы (все на 100%)**

```typescript
interface HomePageProps {
  params: Promise<{ locale: string }>;
}

type PageSection = 'hero' | 'features' | 'howItWorks';
type ResponsiveSpacing = 'space-y-8' | 'sm:space-y-12' | 'lg:space-y-16';

interface LandingPageStructure {
  hero: HeroSectionComponent;
  features: FeaturesSectionComponent;
  howItWorks: HowItWorksSectionComponent;
  floatingButton: FloatingExchangeButtonComponent;
}
```

**Риски и безопасность**

- **Низкий риск**: статическая страница без sensitive операций
- **Performance**: статический рендеринг обеспечивает быструю загрузку
- **SEO**: правильная структура для поисковой индексации
- **Mobile UX**: overflow-x-hidden предотвращает проблемы с мобильным скроллом

**Тесты / рекомендации по покрытию**

- **Render тесты**: корректность отображения всех секций
- **Responsive тесты**: адаптивность на разных размерах экрана
- **Localization тесты**: правильность работы с разными локалями
- **Integration тесты**: взаимодействие с FloatingExchangeButton

**Оценка сложности (low/medium/high)**
**LOW** - простая композиция компонентов без сложной логики

**TODO / Рефакторинг**

- ✅ Правильная структура лендинга с логическими секциями
- ✅ Поддержка локализации через setRequestLocale
- ✅ Responsive дизайн с адаптивными отступами
- ✅ Floating кнопка для улучшения UX
- 💡 Рекомендация: добавить lazy loading для секций ниже fold
- 💡 Рекомендация: рассмотреть добавление анимаций при скролле
