### Путь: apps/web/app/loading.tsx

**Краткое назначение (1 предложение)**
Next.js App Router loading UI компонент для отображения состояния загрузки на глобальном уровне приложения.

**Подробное описание (3–6 предложений)**
Компонент предоставляет глобальный loading UI для Next.js App Router, отображаемый во время навигации между страницами или загрузки данных. Использует простой spinner с анимацией вращения и текстовое сообщение "Loading..." для информирования пользователя о процессе загрузки. Реализован с помощью Tailwind CSS классов для центрирования контента и создания анимированного индикатора. Поддерживает темную тему через условные классы для текста. Обеспечивает консистентный пользовательский опыт во время асинхронных операций в приложении.

**Экспортируемые сущности / API (все на 100%)**

- `export default function GlobalLoading()` — компонент глобального loading состояния

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет входных параметров - статический UI компонент
- Автоматически отображается Next.js во время загрузки страниц

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранный loading UI с центрированным контентом
- Отображает анимированный spinner с синей границей
- Показывает текст "Loading..." с поддержкой темной темы
- Занимает минимальную высоту экрана для полного покрытия

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js автоматически во время загрузки страниц
- **Используется**: как fallback UI для всех loading состояний в App Router
- **Не импортирует**: внешние зависимости - только Tailwind CSS
- **Отображается**: между навигацией и рендерингом новой страницы

**Домен данных / типы (все на 100%)**

```typescript
interface LoadingComponent {
  (): JSX.Element;
}

type LoadingState = 'idle' | 'loading' | 'loaded';
type SpinnerVariant = 'small' | 'medium' | 'large';
```

**Риски и безопасность**

- **Низкий риск**: статический UI компонент без логики
- **Performance**: простая анимация не влияет на производительность
- **Accessibility**: отсутствует aria-label для screen readers
- **UX**: может показываться слишком часто при быстрой навигации

**Тесты / рекомендации по покрытию**

- **Render тесты**: корректность отображения компонента
- **Animation тесты**: работа CSS анимации spinner
- **Theme тесты**: поддержка светлой и темной темы
- **Accessibility тесты**: поддержка screen readers

**Оценка сложности (low/medium/high)**
**LOW** - простой статический UI компонент

**TODO / Рефакторинг**

- ✅ Простая и эффективная реализация loading UI
- ✅ Поддержка темной темы
- ✅ Responsive дизайн с центрированием
- 💡 Рекомендация: добавить aria-label="Loading" для accessibility
- 💡 Рекомендация: рассмотреть использование компонента Spinner из @repo/ui
- 💡 Рекомендация: добавить задержку показа для предотвращения мерцания
