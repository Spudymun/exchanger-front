### Путь: apps/web/app/[locale]/layout.tsx

**Краткое назначение (1 предложение)**
Локализованный layout компонент Next.js App Router с валидацией локали, загрузкой переводов и интеграцией next-intl.

**Подробное описание (3–6 предложений)**
Компонент обеспечивает локализованную обертку для всех страниц в рамках определенной локали с полной интеграцией next-intl. Выполняет валидацию локали против поддерживаемых языков из routing конфигурации и возвращает 404 для неподдерживаемых локалей. Динамически загружает соответствующие переводы из messages файлов и предоставляет их через NextIntlClientProvider для клиентских компонентов. Включает расширенные SEO метаданные с Open Graph и Twitter Card поддержкой для лучшей социальной интеграции. Использует setRequestLocale для оптимизации статического рендеринга и generateStaticParams для pre-generation всех поддерживаемых локалей.

**Экспортируемые сущности / API (все на 100%)**

- `export const metadata: Metadata` — расширенные SEO метаданные с Open Graph
- `export const viewport` — настройки viewport для мобильных устройств
- `export function generateStaticParams()` — генерация статических параметров для всех локалей
- `export default async function LocaleLayout({ children, params })` — основной локализованный layout

**Входы (expected inputs) / Параметры (все на 100%)**

- `children: React.ReactNode` — содержимое страниц для рендеринга
- `params: Promise<{ locale: string }>` — параметры маршрута с локалью
- Динамически импортирует `messages/${locale}.json` файлы переводов
- Использует `routing.locales` для валидации поддерживаемых языков

**Выходы / Побочные эффекты (все на 100%)**

- Оборачивает children в NextIntlClientProvider с загруженными переводами
- Рендерит AppLayout компонент как основную структуру приложения
- Вызывает notFound() для неподдерживаемых локалей
- Устанавливает locale для статического рендеринга через setRequestLocale

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: Next.js для всех страниц в [locale] сегменте
- **Импортирует**: `AppLayout` из `../../src/components/app-layout`, `routing` из i18n
- **Загружает**: переводы из `../../messages/${locale}.json`
- **Оборачивает**: все локализованные страницы в NextIntlClientProvider

**Домен данных / типы (все на 100%)**

```typescript
interface LocaleLayoutProps {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}

type SupportedLocale = 'en' | 'ru';
type MessagesType = Record<string, any>;

interface SEOMetadata extends Metadata {
  openGraph: OpenGraphType;
  twitter: TwitterMetadata;
  robots: RobotsMetadata;
}
```

**Риски и безопасность**

- **Средний риск**: динамическая загрузка переводов может привести к ошибкам
- **Locale validation**: правильная валидация предотвращает некорректные локали
- **Static generation**: generateStaticParams обеспечивает pre-generation
- **SEO exposure**: метаданные публично доступны (это нормально для SEO)

**Тесты / рекомендации по покрытию**

- **Locale validation тесты**: проверка обработки поддерживаемых и неподдерживаемых локалей
- **Messages loading тесты**: корректность загрузки переводов для всех локалей
- **Static generation тесты**: правильность generateStaticParams
- **SEO тесты**: валидность Open Graph и Twitter метаданных

**Оценка сложности (low/medium/high)**
**MEDIUM** - интеграция локализации, валидация и динамическая загрузка переводов

**TODO / Рефакторинг**

- ✅ Правильная интеграция с next-intl и Next.js App Router
- ✅ Валидация локали с fallback на notFound()
- ✅ Расширенные SEO метаданные для социальных сетей
- ✅ Статическая генерация для всех поддерживаемых локалей
- 💡 Рекомендация: добавить error boundary для обработки ошибок загрузки переводов
- 💡 Рекомендация: рассмотреть кэширование загруженных переводов
