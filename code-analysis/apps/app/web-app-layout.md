### Путь: apps/web/app/layout.tsx

**Краткое назначение (1 предложение)**
Root layout компонент Next.js App Router с настройкой метаданных, интернационализации и темизации для всего приложения.

**Подробное описание (3–6 предложений)**
Компонент определяет корневую HTML структуру приложения с поддержкой интернационализации через middleware заголовки. Настраивает SEO метаданные для криптообменника ExchangeGO включая title, description и keywords для поисковой оптимизации. Интегрирует систему темизации через ThemeScript из @repo/providers для поддержки светлой и темной тем без flickering. Включает viewport настройки для мобильной оптимизации и theme-color метатеги для браузерного UI. Использует suppressHydrationWarning для предотвращения ошибок гидратации при работе с темами.

**Экспортируемые сущности / API (все на 100%)**

- `export const metadata: Metadata` — SEO метаданные приложения
- `export const viewport` — настройки viewport для мобильных устройств
- `export default async function RootLayout({ children })` — корневой layout компонент

**Входы (expected inputs) / Параметры (все на 100%)**

- `children: React.ReactNode` — содержимое страниц для рендеринга
- `x-locale` заголовок из middleware для определения языка
- Использует `headers()` из Next.js для получения заголовков запроса

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит корневую HTML структуру с html и body тегами
- Устанавливает lang атрибут на основе локали из middleware
- Включает ThemeScript для инициализации темы до гидратации
- Применяет antialiased класс для улучшения рендеринга шрифтов

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: Next.js как корневой layout для всех страниц
- **Получает данные**: от middleware через x-locale заголовок
- **Импортирует**: `ThemeScript` из @repo/providers, `globals.css` стили
- **Оборачивает**: все страницы приложения в [locale] директории

**Домен данных / типы (все на 100%)**

```typescript
interface RootLayoutProps {
  children: React.ReactNode;
}

type SupportedLocale = 'en' | 'ru';
type ThemeMode = 'light' | 'dark' | 'system';

interface AppMetadata extends Metadata {
  title: string;
  description: string;
  keywords: string;
}
```

**Риски и безопасность**

- **Низкий риск**: стандартный Next.js layout без sensitive операций
- **SEO exposure**: метаданные публично доступны (это нормально)
- **Hydration**: suppressHydrationWarning может скрыть реальные проблемы
- **Theme security**: ThemeScript выполняется до гидратации (проверить содержимое)

**Тесты / рекомендации по покрытию**

- **Metadata тесты**: корректность SEO метаданных
- **Locale тесты**: правильность установки lang атрибута
- **Theme тесты**: корректность инициализации темы
- **Accessibility тесты**: валидность HTML структуры

**Оценка сложности (low/medium/high)**
**LOW** - стандартный Next.js root layout с базовыми настройками

**TODO / Рефакторинг**

- ✅ Правильная интеграция с Next.js App Router
- ✅ Поддержка интернационализации через middleware
- ✅ Корректная настройка темизации без flickering
- ✅ SEO оптимизация с релевантными метаданными
- 💡 Рекомендация: добавить structured data для лучшего SEO
- 💡 Рекомендация: рассмотреть добавление favicon и app icons
