### Путь: apps/web/app/[locale]/exchange/page.tsx

**Краткое назначение (1 предложение)**
Страница обмена криптовалют с локализованным заголовком и основной формой ExchangeForm для проведения операций обмена.

**Подробное описание (3–6 предложений)**
Компонент реализует dedicated страницу для обмена криптовалют с фокусом на основной функциональности приложения. Использует client-side рендеринг для интерактивности формы обмена и получения переводов через useTranslations из namespace 'ExchangePage'. Включает локализованные заголовок и описание страницы для лучшего пользовательского опыта на разных языках. Применяет градиентный фон и responsive дизайн для визуальной консистентности с остальным приложением. Центрирует ExchangeForm компонент с соответствующими отступами и максимальной шириной для оптимального отображения.

**Экспортируемые сущности / API (все на 100%)**

- `export default function ExchangePage()` — страница обмена криптовалют

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает локаль из NextIntlClientProvider контекста
- Использует переводы из namespace 'ExchangePage' через useTranslations
- ExchangeForm получает необходимые данные через props или контекст

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранную страницу обмена с заголовком и формой
- Отображает локализованные title и description из переводов
- Включает ExchangeForm для проведения операций обмена
- Применяет градиентный фон с поддержкой темной темы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Доступна по маршруту**: /{locale}/exchange для всех поддерживаемых локалей
- **Импортирует**: `ExchangeForm` из `../../../src/components/forms/ExchangeForm`
- **Использует переводы**: из messages/[locale].json в namespace 'ExchangePage'
- **Интегрируется**: с locale layout для получения локализации

**Домен данных / типы (все на 100%)**

```typescript
interface ExchangePageComponent {
  (): JSX.Element;
}

type ExchangePageTranslationKeys = 'title' | 'description';

interface ExchangePageTranslations {
  title: string;
  description: string;
}

interface ExchangePageLayout {
  header: LocalizedHeader;
  form: ExchangeFormComponent;
}
```

**Риски и безопасность**

- **Низкий риск**: UI страница без sensitive операций
- **Client-side rendering**: необходимо для интерактивности формы
- **Translation dependency**: зависит от наличия переводов в ExchangePage namespace
- **Form security**: безопасность зависит от реализации ExchangeForm компонента

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов заголовка и описания
- **Integration тесты**: взаимодействие с ExchangeForm компонентом
- **Responsive тесты**: адаптивность на разных размерах экрана
- **Theme тесты**: поддержка светлой и темной темы

**Оценка сложности (low/medium/high)**
**LOW** - простая страница с заголовком и формой

**TODO / Рефакторинг**

- ✅ Правильная структура dedicated страницы обмена
- ✅ Локализация заголовка и описания
- ✅ Responsive дизайн с градиентным фоном
- ✅ Центрирование и правильные отступы для формы
- 💡 Рекомендация: добавить breadcrumbs для навигации
- 💡 Рекомендация: рассмотреть добавление SEO метаданных для страницы
