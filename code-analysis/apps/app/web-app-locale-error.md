### Путь: apps/web/app/[locale]/error.tsx

**Краткое назначение (1 предложение)**
Локализованный Next.js App Router error boundary компонент с поддержкой интернационализации через next-intl.

**Подробное описание (3–6 предложений)**
Компонент реализует локализованный error boundary для обработки ошибок в рамках определенной локали с переводами через next-intl. Разделен на логические подкомпоненты ErrorIcon, ErrorContent и ErrorActions для лучшей организации и тестируемости кода. Использует хук useTranslations для получения переводов из namespace 'Error' и отображения локализованных сообщений пользователю. Предоставляет тот же функционал что и глобальный error.tsx, но с поддержкой множественных языков. Обеспечивает консистентный пользовательский опыт обработки ошибок с учетом языковых предпочтений пользователя.

**Экспортируемые сущности / API (все на 100%)**

- `export default function Error({ error, reset })` — основной локализованный error boundary
- `function ErrorIcon()` — компонент иконки ошибки (внутренний)
- `function ErrorContent({ error, t })` — компонент контента ошибки (внутренний)
- `function ErrorActions({ reset, t })` — компонент действий восстановления (внутренний)

**Входы (expected inputs) / Параметры (все на 100%)**

- `error: Error & { digest?: string }` — объект ошибки с опциональным digest
- `reset: () => void` — функция для повторной попытки рендеринга
- `t: (key: string) => string` — функция перевода из useTranslations
- Автоматически получает локаль из URL параметра [locale]

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранный локализованный UI с информацией об ошибке
- Отображает переведенные тексты: title, description, tryAgain, goHome
- Предоставляет кнопки с локализованными подписями
- Использует window.location.href для навигации на главную

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js при ошибках в [locale] сегменте
- **Импортирует**: `Button` из @repo/ui, `useTranslations` из next-intl
- **Использует переводы**: из messages/[locale].json в namespace 'Error'
- **Навигация**: redirect на корень приложения при клике "Go Home"

**Домен данных / типы (все на 100%)**

```typescript
interface ErrorProps {
  error: Error & { digest?: string };
  reset: () => void;
}

interface ErrorContentProps {
  error: Error;
  t: (key: string) => string;
}

interface ErrorActionsProps {
  reset: () => void;
  t: (key: string) => string;
}

type ErrorTranslationKeys = 'title' | 'description' | 'tryAgain' | 'goHome';
```

**Риски и безопасность**

- **Низкий риск**: стандартный error boundary с локализацией
- **Error exposure**: отображает error.message без фильтрации
- **Translation dependency**: зависит от наличия переводов в messages файлах
- **Navigation**: использует window.location.href вместо Next.js router

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность переводов для всех поддерживаемых языков
- **Error boundary тесты**: перехват ошибок в локализованном контексте
- **Component тесты**: отдельное тестирование ErrorContent и ErrorActions
- **Integration тесты**: работа с next-intl и переводами

**Оценка сложности (low/medium/high)**
**MEDIUM** - добавлена сложность локализации и разделения на компоненты

**TODO / Рефакторинг**

- ✅ Правильная интеграция с next-intl для локализации
- ✅ Хорошее разделение на логические компоненты
- ✅ Использование дизайн-системы для консистентного UI
- 💡 Рекомендация: добавить fallback для отсутствующих переводов
- 💡 Рекомендация: использовать Next.js router для навигации
- ⚠️ Рекомендация: фильтровать sensitive данные из error.message
