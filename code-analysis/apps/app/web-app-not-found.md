### Путь: apps/web/app/not-found.tsx

**Краткое назначение (1 предложение)**
Next.js App Router глобальный not-found компонент, перенаправляющий на локализованную страницу 404 ошибки.

**Подробное описание (3–6 предложений)**
Компонент обрабатывает глобальные 404 ошибки в Next.js App Router и перенаправляет пользователей на соответствующую локализованную страницу not-found. Получает информацию о локали из middleware заголовка x-locale для определения правильного языка перенаправления. Использует Next.js redirect функцию для server-side перенаправления на `/{locale}/not-found-page` маршрут. Обеспечивает консистентную обработку 404 ошибок с поддержкой интернационализации. Является частью архитектуры локализации, где каждая локаль имеет свою собственную страницу ошибки.

**Экспортируемые сущности / API (все на 100%)**

- `export default async function GlobalNotFound()` — глобальный not-found handler

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - вызывается Next.js автоматически
- Использует `x-locale` заголовок из middleware для определения языка
- Получает заголовки через `headers()` из Next.js

**Выходы / Побочные эффекты (все на 100%)**

- Выполняет server-side redirect на `/{locale}/not-found-page`
- Не рендерит UI - только перенаправление
- Использует статус код 307 (Temporary Redirect) по умолчанию
- Сохраняет локаль пользователя при обработке ошибки

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js при 404 ошибках на глобальном уровне
- **Получает данные**: от middleware через x-locale заголовок
- **Перенаправляет на**: `apps/web/app/[locale]/not-found-page/page.tsx`
- **Использует**: Next.js `redirect` и `headers` функции

**Домен данных / типы (все на 100%)**

```typescript
interface NotFoundHandler {
  (): Promise<never>; // redirect никогда не возвращает значение
}

type SupportedLocale = 'en' | 'ru';
type NotFoundRedirectPath = `/${SupportedLocale}/not-found-page`;
```

**Риски и безопасность**

- **Низкий риск**: простое перенаправление без sensitive операций
- **Redirect loops**: потенциальный риск если not-found-page не существует
- **Locale validation**: нет валидации полученной локали
- **SEO**: правильное использование redirect для поисковых систем

**Тесты / рекомендации по покрытию**

- **Redirect тесты**: корректность перенаправления для разных локалей
- **Locale тесты**: обработка отсутствующего или некорректного x-locale заголовка
- **Integration тесты**: работа с middleware и локализованными страницами
- **SEO тесты**: правильность HTTP статус кодов

**Оценка сложности (low/medium/high)**
**LOW** - простое перенаправление с получением заголовка

**TODO / Рефакторинг**

- ✅ Правильная интеграция с Next.js App Router not-found системой
- ✅ Поддержка интернационализации через middleware
- ✅ Использование server-side redirect для SEO
- 💡 Рекомендация: добавить валидацию локали с fallback на 'en'
- 💡 Рекомендация: добавить логирование 404 ошибок для мониторинга
- 💡 Рекомендация: рассмотреть использование permanent redirect если уместно
