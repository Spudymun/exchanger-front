### Путь: apps/web/app/[locale]/not-found-page/page.tsx

**Краткое назначение (1 предложение)**
Локализованная страница 404 ошибки с переводами через next-intl и навигацией на главную страницу.

**Подробное описание (3–6 предложений)**
Компонент реализует dedicated страницу 404 ошибки с полной поддержкой локализации через server-side переводы. Использует getTranslations для получения переводов на сервере из namespace 'NotFound' для оптимальной производительности. Включает setRequestLocale для правильной настройки статического рендеринга в рамках определенной локали. Предоставляет локализованную навигацию через Link компонент из i18n navigation для корректной работы с маршрутизацией. Использует минималистичный дизайн с крупным номером ошибки 404, локализованными заголовком и описанием, и кнопкой возврата на главную.

**Экспортируемые сущности / API (все на 100%)**

- `export default async function NotFoundPage({ params })` — локализованная страница 404

**Входы (expected inputs) / Параметры (все на 100%)**

- `params: Promise<{ locale: string }>` — параметры маршрута с локалью
- Получает переводы через getTranslations из namespace 'NotFound'
- Использует locale для настройки статического рендеринга

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранную страницу 404 с локализованным контентом
- Отображает статический номер "404" и переведенные title, description, goHome
- Предоставляет Link для навигации на главную страницу с сохранением локали
- Устанавливает locale для статического рендеринга через setRequestLocale

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: как target для redirect из глобального not-found.tsx
- **Импортирует**: `Link` из `../../../src/i18n/navigation` для локализованной навигации
- **Получает переводы**: из messages/[locale].json в namespace 'NotFound'
- **Навигация**: на главную страницу с сохранением текущей локали

**Домен данных / типы (все на 100%)**

```typescript
interface NotFoundPageProps {
  params: Promise<{ locale: string }>;
}

type NotFoundTranslationKeys = 'title' | 'description' | 'goHome';

interface NotFoundTranslations {
  title: string;
  description: string;
  goHome: string;
}

interface NotFoundPageStructure {
  errorCode: '404';
  localizedContent: NotFoundTranslations;
  navigation: LocalizedLink;
}
```

**Риски и безопасность**

- **Низкий риск**: статическая страница ошибки без sensitive операций
- **SEO**: правильная обработка 404 для поисковых систем
- **Navigation**: использует локализованный Link для корректной маршрутизации
- **Translation dependency**: зависит от наличия переводов в NotFound namespace

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность переводов для всех поддерживаемых языков
- **Navigation тесты**: правильность работы Link с локализацией
- **Static generation тесты**: корректность setRequestLocale
- **Accessibility тесты**: поддержка screen readers для error страницы

**Оценка сложности (low/medium/high)**
**LOW** - простая статическая страница с локализацией

**TODO / Рефакторинг**

- ✅ Правильная интеграция с next-intl server-side переводами
- ✅ Использование локализованной навигации через i18n Link
- ✅ Минималистичный и понятный дизайн 404 страницы
- ✅ Статическая генерация через setRequestLocale
- 💡 Рекомендация: добавить поиск или популярные страницы для лучшего UX
- 💡 Рекомендация: рассмотреть добавление breadcrumbs или истории навигации
