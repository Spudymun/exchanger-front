### Путь: apps/web/app/[locale]/loading.tsx

**Краткое назначение (1 предложение)**
Локализованный loading UI компонент для Next.js App Router с переводами через next-intl и компонентами дизайн-системы.

**Подробное описание (3–6 предложений)**
Компонент предоставляет локализованный loading UI для страниц в рамках определенной локали с поддержкой переводов через next-intl. Использует Spinner компонент из дизайн-системы @repo/ui вместо самодельного spinner для консистентности с остальным приложением. Отображает переведенные тексты loading и loadingDescription из namespace 'common' для лучшего пользовательского опыта. Включает градиентный фон и поддержку темной темы для визуальной консистентности с остальным приложением. Обеспечивает более профессиональный и локализованный loading опыт по сравнению с глобальным loading компонентом.

**Экспортируемые сущности / API (все на 100%)**

- `export default function Loading()` — локализованный loading компонент

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - получает локаль из контекста NextIntlClientProvider
- Использует переводы из namespace 'common' через useTranslations
- Автоматически отображается Next.js во время загрузки локализованных страниц

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит полноэкранный локализованный loading UI
- Отображает Spinner компонент размера 'xl' из дизайн-системы
- Показывает переведенные тексты loading и loadingDescription
- Применяет градиентный фон с поддержкой темной темы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js во время загрузки страниц в [locale] сегменте
- **Импортирует**: `Spinner` из @repo/ui, `useTranslations` из next-intl
- **Использует переводы**: из messages/[locale].json в namespace 'common'
- **Получает контекст**: от NextIntlClientProvider в locale layout

**Домен данных / типы (все на 100%)**

```typescript
interface LoadingComponent {
  (): JSX.Element;
}

type SpinnerSize = 'sm' | 'md' | 'lg' | 'xl';
type CommonTranslationKeys = 'loading' | 'loadingDescription';

interface LoadingTranslations {
  loading: string;
  loadingDescription: string;
}
```

**Риски и безопасность**

- **Низкий риск**: статический UI компонент с переводами
- **Translation dependency**: зависит от наличия переводов в common namespace
- **Performance**: использует оптимизированный Spinner из дизайн-системы
- **Accessibility**: Spinner компонент должен включать proper ARIA attributes

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность отображения переводов для всех локалей
- **Component тесты**: правильность использования Spinner из @repo/ui
- **Theme тесты**: поддержка светлой и темной темы
- **Accessibility тесты**: поддержка screen readers через Spinner

**Оценка сложности (low/medium/high)**
**LOW** - простой UI компонент с локализацией

**TODO / Рефакторинг**

- ✅ Использование компонентов дизайн-системы для консистентности
- ✅ Правильная интеграция с next-intl для локализации
- ✅ Поддержка темной темы и градиентного фона
- ✅ Более профессиональный вид по сравнению с глобальным loading
- 💡 Рекомендация: добавить fallback для отсутствующих переводов
- 💡 Рекомендация: рассмотреть добавление прогресс-бара для длительных операций
