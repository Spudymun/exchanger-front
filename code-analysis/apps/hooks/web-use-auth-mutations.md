### Путь: apps/web/src/hooks/useAuthMutations.ts

**Краткое назначение (1 предложение)**
Хук для основных мутаций аутентификации (login, register, logout) с интегрированными уведомлениями и инвалидацией кеша.

**Подробное описание (3–6 предложений)**
Хук инкапсулирует три основные мутации аутентификации через tRPC API с полной интеграцией системы уведомлений и управления кешем. Каждая мутация включает обработчики успешных операций с показом локализованных уведомлений через notifications.success и инвалидацией сессии через utils.auth.getSession.invalidate. Обрабатывает ошибки через централизованный notifications.handleApiError с локализованными сообщениями об ошибках. Использует переводы из namespace 'Layout.auth.messages' для всех пользовательских сообщений, обеспечивая полную локализацию. Следует паттерну tRPC мутаций с правильной типизацией и возвращает объект с тремя мутациями для использования в компонентах.

**Экспортируемые сущности / API (все на 100%)**

- `export function useAuthMutations()` — хук основных мутаций аутентификации

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - использует внутренние зависимости
- Зависит от trpc client, useNotifications, useTranslations

**Выходы / Побочные эффекты (все на 100%)**

- `login` — tRPC мутация входа с обработкой успеха/ошибок
- `register` — tRPC мутация регистрации с уведомлениями
- `logout` — tRPC мутация выхода с инвалидацией сессии
- Показывает уведомления при успешных операциях и ошибках
- Инвалидирует кеш сессии при login и logout

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: useAuthMutationAdapter для композиции с password мутациями
- **Импортирует**: useNotifications из @repo/hooks для системы уведомлений
- **Использует**: trpc client из локального trpc-provider
- **Использует локализацию**: через useTranslations('Layout.auth.messages')
- **Интегрируется**: с tRPC utils для инвалидации кеша

**Домен данных / типы (все на 100%)**

```typescript
// Возвращаемый тип хука
interface UseAuthMutationsReturn {
  login: ReturnType<typeof trpc.auth.login.useMutation>;
  register: ReturnType<typeof trpc.auth.register.useMutation>;
  logout: ReturnType<typeof trpc.auth.logout.useMutation>;
}

// Типы мутаций (из tRPC)
type LoginMutation = UseMutationResult<LoginResponse, TRPCError, LoginRequest>;
type RegisterMutation = UseMutationResult<RegisterResponse, TRPCError, RegisterRequest>;
type LogoutMutation = UseMutationResult<void, TRPCError, void>;

// Локализация ключи
type AuthMessageKeys =
  | 'loginSuccess'
  | 'loginSuccessDescription'
  | 'loginError'
  | 'registerSuccess'
  | 'registerSuccessDescription'
  | 'registerError'
  | 'logoutSuccess'
  | 'logoutSuccessDescription'
  | 'logoutError';
```

**Риски и безопасность**

- **Authentication security**: критически важные операции аутентификации
- **Error handling**: зависит от notifications.handleApiError для обработки ошибок
- **Cache invalidation**: правильная инвалидация сессии критична для безопасности
- **Translation dependency**: требует наличия всех ключей в Layout.auth.messages
- **tRPC integration**: критически зависит от правильной настройки tRPC client

**Тесты / рекомендации по покрытию**

- **Mutation success тесты**: корректность обработки успешных операций
- **Error handling тесты**: правильность обработки ошибок API
- **Cache invalidation тесты**: корректность инвалидации сессии
- **Notification тесты**: правильность показа уведомлений
- **Localization тесты**: корректность всех переводов

**Оценка сложности (low/medium/high)**
**MEDIUM** - интеграция с tRPC, уведомлениями и кешем

**TODO / Рефакторинг**

- ✅ Правильная интеграция с tRPC мутациями
- ✅ Централизованная обработка ошибок через notifications.handleApiError
- ✅ Локализация всех пользовательских сообщений
- ✅ Правильная инвалидация кеша сессии при login/logout
- ✅ Четкая типизация возвращаемых мутаций
- ✅ JSDoc документация назначения хука
- 💡 Рекомендация: добавить retry логику для критичных операций
- 💡 Рекомендация: рассмотреть добавление loading индикаторов
- 💡 Рекомендация: добавить метрики для мониторинга аутентификации
