### Путь: apps/web/src/components/exchange-form/useHeroExchangeForm.ts

**Краткое назначение (1 предложение)**
Кастомный хук для управления состоянием и логикой формы обмена криптовалют с валидацией, расчетами и интеграцией банков.

**Подробное описание (3–6 предложений)**
Хук инкапсулирует всю бизнес-логику формы обмена, включая управление состоянием через useFormWithNextIntl с локализованной валидацией. Реализует автоматический расчет суммы получения через useMemo с фиксированным курсом EXCHANGE_RATE и динамическое получение списка банков через getBanksForCurrency. Использует Zod схему heroExchangeSchema для валидации с cryptoAmountStringSchema для проверки криптовалютных сумм и enum валидацией для валют. Включает комплексную проверку валидности формы с учетом минимальных сумм, валидации полей и обязательного выбора банка. Предоставляет начальные значения с USDT как валютой по умолчанию и автоматической установкой токен-стандарта через getDefaultTokenStandard.

**Экспортируемые сущности / API (все на 100%)**

- `export function useHeroExchangeForm(t, onExchange?)` — основной хук формы обмена

**Входы (expected inputs) / Параметры (все на 100%)**

- `t: (key: string) => string` — функция локализации для валидационных сообщений
- `onExchange?: (data: HeroExchangeFormData) => void` — опциональный callback при отправке формы

**Выходы / Побочные эффекты (все на 100%)**

- `form` — объект формы с values, errors, isValid, setValue, handleSubmit
- `calculatedAmount: number` — рассчитанная сумма получения на основе курса
- `banks: BankOption[]` — массив доступных банков для выбранной валюты
- `isValid: boolean` — комплексная валидность формы с бизнес-правилами
- `constants` — объект с EXCHANGE_RATE и MIN_AMOUNTS для использования в UI

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: HeroExchangeForm компоненте для управления состоянием
- **Импортирует**: CRYPTOCURRENCIES, FIAT_CURRENCIES, getBanksForCurrency из @repo/constants
- **Использует**: useFormWithNextIntl из @repo/hooks для локализованной формы
- **Использует**: cryptoAmountStringSchema из @repo/utils для валидации сумм
- **Интегрируется**: с SendingCard и ReceivingCard через form объект

**Домен данных / типы (все на 100%)**

```typescript
interface HeroExchangeFormData {
  fromAmount: string;
  fromCurrency: (typeof CRYPTOCURRENCIES)[number];
  tokenStandard?: string;
  toCurrency: (typeof FIAT_CURRENCIES)[number];
  selectedBankId: string;
}

const heroExchangeSchema = z.object({
  fromAmount: cryptoAmountStringSchema,
  fromCurrency: z.enum(CRYPTOCURRENCIES),
  tokenStandard: z.string().optional(),
  toCurrency: z.enum(FIAT_CURRENCIES),
  selectedBankId: z.string().min(1),
});

const EXCHANGE_RATE = 40.5; // Фиксированный курс
const MIN_AMOUNTS = { from: 10, to: 100 }; // Минимальные суммы
```

**Риски и безопасность**

- **Fixed exchange rate**: использует хардкод курса 40.5 вместо реального API
- **Validation schema**: критически зависит от актуальности CRYPTOCURRENCIES и FIAT_CURRENCIES
- **Bank filtering**: ограничен только ['UAH', 'USD', 'EUR'] валютами
- **Calculation precision**: может накапливать ошибки округления при расчетах
- **Form state**: сложная логика валидности может приводить к race conditions

**Тесты / рекомендации по покрытию**

- **Form validation тесты**: корректность Zod схемы для всех полей
- **Calculation тесты**: точность расчета calculatedAmount
- **Bank filtering тесты**: правильность getBanksForCurrency для разных валют
- **Initial values тесты**: корректность начальных значений и токен-стандарта
- **Complex validation тесты**: проверка isValid логики с минимальными суммами

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная бизнес-логика с валидацией и расчетами

**TODO / Рефакторинг**

- ✅ Использование useFormWithNextIntl для локализованной валидации
- ✅ Мемоизация calculatedAmount и banks для производительности
- ✅ Чистая Zod схема без хардкода сообщений
- ✅ Комплексная валидация с бизнес-правилами
- ✅ Интеграция с константами из @repo/constants
- 🔴 Критично: заменить фиксированный EXCHANGE_RATE на реальный API
- 💡 Рекомендация: добавить поддержку всех фиатных валют в bank filtering
- 💡 Рекомендация: добавить debounce для calculatedAmount при частых изменениях
- 💡 Рекомендация: рассмотреть вынос MIN_AMOUNTS в константы
