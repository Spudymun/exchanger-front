### Путь: apps/web/src/hooks/useExchangeMutation.ts

**Краткое назначение (1 предложение)**
Хук для мутаций обмена криптовалют с управлением кешем, получением курсов и отслеживанием статуса заказов.

**Подробное описание (3–6 предложений)**
Хук предоставляет комплексную функциональность для операций обмена, включая создание заказов через createOrder мутацию и проверку статуса через getOrderStatus. Интегрируется с React Query для управления кешем с автоматической инвалидацией курсов валют и истории заказов при создании новых заказов. Включает дополнительные хуки useExchangeRates для получения актуальных курсов с автообновлением каждые 30 секунд и useOrderStatus для отслеживания конкретного заказа. Использует tRPC для всех API вызовов с правильной типизацией через CreateOrderRequest и соответствующие Response интерфейсы. Предоставляет детальные loading состояния для различных операций и централизованную обработку ошибок через опциональные callbacks.

**Экспортируемые сущности / API (все на 100%)**

- `export function useExchangeMutation(options?)` — основной хук мутаций обмена
- `export function useExchangeRates()` — хук получения курсов валют с автообновлением
- `export function useOrderStatus(orderId, options?)` — хук отслеживания статуса заказа
- `export type UseExchangeMutationReturn` — тип возвращаемого значения основного хука

**Входы (expected inputs) / Параметры (все на 100%)**

- `useExchangeMutation`: `options?: UseExchangeMutationOptions` с onSuccess и onError callbacks
- `useOrderStatus`: `orderId: string` и `options?: { enabled?, refetchInterval? }`
- `useExchangeRates`: нет параметров - использует фиксированные настройки обновления

**Выходы / Побочные эффекты (все на 100%)**

- `createOrder` — мутация создания заказа с инвалидацией кеша
- `getOrderStatus` — мутация получения статуса с обновлением кеша
- `isLoading`, `isCreatingOrder`, `isCheckingStatus` — детальные loading состояния
- Автоматическая инвалидация кеша курсов и истории заказов
- Обновление кеша конкретного заказа при проверке статуса

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: ExchangeForm, OrderStatus и других компонентах обмена
- **Импортирует типы**: CreateOrderRequest из @repo/exchange-core
- **Использует**: trpc client и utils для API вызовов
- **Интегрируется**: с React Query для управления кешем
- **Зависит от**: правильной настройки tRPC роутеров exchange.\*

**Домен данных / типы (все на 100%)**

```typescript
interface CreateOrderResponse {
  orderId: string;
  depositAddress: string;
  cryptoAmount: number;
  uahAmount: number;
  currency: string;
  status: string;
  createdAt: Date;
}

interface OrderStatusResponse {
  id: string;
  status: string;
  cryptoAmount: number;
  uahAmount: number;
  currency: string;
  depositAddress: string;
  createdAt: Date;
  updatedAt: Date;
  processedAt?: Date;
  txHash?: string;
}

interface UseExchangeMutationOptions {
  onSuccess?: (order: CreateOrderResponse | OrderStatusResponse) => void;
  onError?: (error: TRPCError) => void;
}

// Настройки обновления
const EXCHANGE_RATES_REFRESH_INTERVAL = 30000; // 30 секунд
const EXCHANGE_RATES_STALE_TIME = 30000; // 30 секунд
```

**Риски и безопасность**

- **Financial operations**: критически важные операции с деньгами требуют особой осторожности
- **Cache management**: неправильная инвалидация кеша может привести к устаревшим данным
- **Rate updates**: частые обновления курсов могут создавать нагрузку на сервер
- **Error handling**: критически важна правильная обработка ошибок для финансовых операций
- **Type safety**: использует type assertions для TRPCError, может быть небезопасно

**Тесты / рекомендации по покрытию**

- **Order creation тесты**: корректность создания заказов и обработки ответов
- **Status tracking тесты**: правильность отслеживания статуса заказов
- **Cache invalidation тесты**: корректность инвалидации и обновления кеша
- **Rate updates тесты**: правильность автообновления курсов валют
- **Error handling тесты**: обработка различных типов ошибок API

**Оценка сложности (low/medium/high)**
**HIGH** - сложная интеграция с финансовыми операциями и кешем

**TODO / Рефакторинг**

- ✅ Комплексная функциональность обмена в одном хуке
- ✅ Правильная интеграция с React Query для кеша
- ✅ Автообновление курсов валют каждые 30 секунд
- ✅ Детальные loading состояния для разных операций
- ✅ Типизация через интерфейсы CreateOrderRequest/Response
- ✅ Централизованные хуки для курсов и статуса заказов
- 💡 Рекомендация: добавить retry логику для критичных операций
- 💡 Рекомендация: рассмотреть добавление optimistic updates
- 💡 Рекомендация: добавить метрики для мониторинга финансовых операций
- 💡 Рекомендация: улучшить error handling с более специфичными типами
