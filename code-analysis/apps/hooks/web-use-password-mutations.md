### Путь: apps/web/src/hooks/usePasswordMutations.ts

**Краткое назначение (1 предложение)**
Хук для мутаций управления паролями и верификации email с интегрированными уведомлениями и обработкой ошибок.

**Подробное описание (3–6 предложений)**
Хук предоставляет три специализированные мутации для управления паролями и верификации: requestPasswordReset для запроса сброса пароля, resetPassword для установки нового пароля и verifyEmail для подтверждения email адреса. Каждая мутация интегрирована с системой уведомлений через notifications.success для успешных операций и notifications.handleApiError для централизованной обработки ошибок. Использует локализованные сообщения из namespace 'Layout.auth.messages' для всех пользовательских уведомлений, обеспечивая консистентность с другими компонентами аутентификации. Следует паттерну tRPC мутаций с правильной типизацией и возвращает объект с тремя мутациями для композиции в useAuthMutationAdapter. Дополняет useAuthMutations, предоставляя полный набор операций для управления учетными записями пользователей.

**Экспортируемые сущности / API (все на 100%)**

- `export function usePasswordMutations()` — хук мутаций управления паролями и верификации

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - использует внутренние зависимости
- Зависит от trpc client, useNotifications, useTranslations

**Выходы / Побочные эффекты (все на 100%)**

- `requestPasswordReset` — tRPC мутация запроса сброса пароля
- `resetPassword` — tRPC мутация установки нового пароля
- `verifyEmail` — tRPC мутация верификации email адреса
- Показывает уведомления при успешных операциях
- Обрабатывает ошибки через централизованный handleApiError

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: useAuthMutationAdapter для композиции с основными auth мутациями
- **Импортирует**: useNotifications из @repo/hooks для системы уведомлений
- **Использует**: trpc client из локального trpc-provider
- **Использует локализацию**: через useTranslations('Layout.auth.messages')
- **Дополняет**: useAuthMutations для полного набора операций аутентификации

**Домен данных / типы (все на 100%)**

```typescript
// Возвращаемый тип хука
interface UsePasswordMutationsReturn {
  requestPasswordReset: ReturnType<typeof trpc.auth.requestPasswordReset.useMutation>;
  resetPassword: ReturnType<typeof trpc.auth.resetPassword.useMutation>;
  verifyEmail: ReturnType<typeof trpc.auth.verifyEmail.useMutation>;
}

// Типы мутаций (из tRPC)
type PasswordResetRequestMutation = UseMutationResult<void, TRPCError, PasswordResetRequest>;
type ResetPasswordMutation = UseMutationResult<void, TRPCError, ResetPasswordRequest>;
type VerifyEmailMutation = UseMutationResult<void, TRPCError, EmailVerificationRequest>;

// Локализация ключи
type PasswordMessageKeys =
  | 'passwordResetSent'
  | 'passwordResetSentDescription'
  | 'passwordChanged'
  | 'passwordChangedDescription'
  | 'emailVerified'
  | 'emailVerifiedDescription';
```

**Риски и безопасность**

- **Password security**: критически важные операции с паролями требуют особой безопасности
- **Email verification**: верификация email критична для безопасности аккаунта
- **Error handling**: зависит от notifications.handleApiError для обработки ошибок
- **Translation dependency**: требует наличия всех ключей в Layout.auth.messages
- **tRPC integration**: критически зависит от правильной настройки auth роутеров

**Тесты / рекомендации по покрытию**

- **Password reset тесты**: корректность процесса сброса пароля
- **Email verification тесты**: правильность верификации email
- **Error handling тесты**: обработка различных типов ошибок
- **Notification тесты**: правильность показа уведомлений
- **Localization тесты**: корректность всех переводов

**Оценка сложности (low/medium/high)**
**MEDIUM** - критичные операции безопасности с интеграциями

**TODO / Рефакторинг**

- ✅ Правильная интеграция с tRPC мутациями
- ✅ Централизованная обработка ошибок через notifications.handleApiError
- ✅ Локализация всех пользовательских сообщений
- ✅ Четкая типизация возвращаемых мутаций
- ✅ JSDoc документация назначения хука
- ✅ Логичная группировка операций управления паролями
- 💡 Рекомендация: добавить rate limiting для операций сброса пароля
- 💡 Рекомендация: рассмотреть добавление прогресс индикаторов
- 💡 Рекомендация: добавить логирование для аудита безопасности
- 💡 Рекомендация: рассмотреть добавление двухфакторной аутентификации
