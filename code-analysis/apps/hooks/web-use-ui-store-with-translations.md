### Путь: apps/web/src/hooks/useUIStoreWithTranslations.ts

**Краткое назначение (1 предложение)**
Локализованный wrapper для useUIStore, добавляющий уведомления при смене темы и расширенную функциональность переключения.

**Подробное описание (3–6 предложений)**
Хук следует архитектурному паттерну wrapper'а, расширяя базовый useUIStore локализованными уведомлениями без изменения основной логики управления UI состоянием. Переопределяет метод setTheme для автоматического показа уведомления при смене темы через интеграцию с useNotificationsWithTranslations. Добавляет новый метод toggleThemeWithNotification для циклического переключения между темами (light → dark → system → light) с уведомлениями. Использует типизацию ThemeMode из @repo/constants для обеспечения типобезопасности при работе с темами. Предоставляет доступ к локализованным уведомлениям через свойство notifications для дополнительной функциональности в компонентах.

**Экспортируемые сущности / API (все на 100%)**

- `export function useUIStoreWithTranslations()` — основной локализованный wrapper UI store
- `export type UseUIStoreWithTranslationsReturn` — тип возвращаемого значения

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - композирует базовый useUIStore
- Зависит от useNotificationsWithTranslations для уведомлений

**Выходы / Побочные эффекты (все на 100%)**

- Все методы и свойства базового useUIStore
- `setTheme(theme)` — переопределенный метод с уведомлениями
- `toggleThemeWithNotification()` — новый метод циклического переключения тем
- `notifications` — доступ к локализованным уведомлениям
- Показывает уведомления при каждой смене темы

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: компонентах UI для управления темой с уведомлениями
- **Композирует**: useUIStore из @repo/hooks для базовой функциональности
- **Использует**: useNotificationsWithTranslations для локализованных уведомлений
- **Импортирует типы**: ThemeMode из @repo/constants для типизации
- **Паттерн**: wrapper для добавления уведомлений без изменения базовой логики

**Домен данных / типы (все на 100%)**

```typescript
// Тип темы из констант
type ThemeMode = 'light' | 'dark' | 'system';

// Расширенный интерфейс UI store
interface UseUIStoreWithTranslationsReturn extends ReturnType<typeof useUIStore> {
  setTheme: (theme: ThemeMode) => void; // Переопределенный с уведомлениями
  toggleThemeWithNotification: () => void; // Новый метод
  notifications: ReturnType<typeof useNotificationsWithTranslations>;
}

// Логика переключения тем
const THEME_CYCLE: Record<ThemeMode, ThemeMode> = {
  light: 'dark',
  dark: 'system',
  system: 'light',
};
```

**Риски и безопасность**

- **Минимальный риск**: простой wrapper без изменения базовой логики
- **Method override**: переопределение setTheme может нарушить ожидания компонентов
- **Notification dependency**: зависит от правильной работы уведомлений
- **Theme persistence**: базовый store должен правильно сохранять тему
- **Cycle logic**: логика переключения тем должна быть консистентной

**Тесты / рекомендации по покрытию**

- **Theme switching тесты**: корректность setTheme с уведомлениями
- **Toggle cycle тесты**: правильность циклического переключения тем
- **Notification тесты**: показ уведомлений при каждой смене темы
- **Integration тесты**: совместимость с базовым useUIStore
- **Type safety тесты**: корректность типизации ThemeMode

**Оценка сложности (low/medium/high)**
**LOW** - простой wrapper с добавлением уведомлений

**TODO / Рефакторинг**

- ✅ Архитектурный паттерн wrapper'а без изменения базовой логики
- ✅ Переопределение setTheme с сохранением совместимости
- ✅ Добавление нового метода toggleThemeWithNotification
- ✅ Правильная типизация через ThemeMode из констант
- ✅ Интеграция с локализованными уведомлениями
- ✅ JSDoc документация архитектурных решений
- 💡 Рекомендация: рассмотреть мемоизацию для методов
- 💡 Рекомендация: добавить возможность отключения уведомлений
- 💡 Рекомендация: рассмотреть добавление анимаций при смене темы
- 💡 Рекомендация: добавить персистентность предпочтений пользователя
