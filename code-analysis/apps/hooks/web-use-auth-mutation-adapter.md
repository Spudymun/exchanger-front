### Путь: apps/web/src/hooks/useAuthMutationAdapter.ts

**Краткое назначение (1 предложение)**
Адаптер-композиция для объединения мутаций аутентификации и управления паролями в единый интерфейс.

**Подробное описание (3–6 предложений)**
Хук реализует паттерн композиции для объединения функциональности useAuthMutations и usePasswordMutations в единый интерфейс для форм аутентификации. Предоставляет все необходимые мутации для полного цикла аутентификации: login, register, logout из базовых мутаций и requestPasswordReset, resetPassword, verifyEmail из мутаций паролей. Следует принципу правильной архитектуры с устранением избыточной абстракции и прямой интеграцией с системой уведомлений. Использует TypeScript intersection types для объединения возвращаемых типов обоих хуков, обеспечивая полную типобезопасность. Представляет собой результат рефакторинга с упрощением архитектуры и удалением лишних слоев абстракции.

**Экспортируемые сущности / API (все на 100%)**

- `export function useAuthMutationAdapter()` — композитный адаптер мутаций аутентификации

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - композирует существующие хуки
- Зависит от useAuthMutations и usePasswordMutations

**Выходы / Побочные эффекты (все на 100%)**

- `login` — мутация входа в систему
- `register` — мутация регистрации пользователя
- `logout` — мутация выхода из системы
- `requestPasswordReset` — мутация запроса сброса пароля
- `resetPassword` — мутация сброса пароля
- `verifyEmail` — мутация верификации email

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: LoginForm, RegisterForm и других компонентах аутентификации
- **Композирует**: useAuthMutations для базовых операций аутентификации
- **Композирует**: usePasswordMutations для операций с паролями
- **Часть**: архитектуры аутентификации с упрощенным интерфейсом
- **Заменяет**: избыточную абстракцию useAuthMutation после рефакторинга

**Домен данных / типы (все на 100%)**

```typescript
// Композитный тип возвращаемого значения
type AuthMutationAdapterReturn = ReturnType<typeof useAuthMutations> &
  ReturnType<typeof usePasswordMutations>;

// Структура возвращаемого объекта
interface AuthMutationAdapterReturn {
  // Из useAuthMutations
  login: UseMutationResult<LoginResponse, Error, LoginRequest>;
  register: UseMutationResult<RegisterResponse, Error, RegisterRequest>;
  logout: UseMutationResult<void, Error, void>;

  // Из usePasswordMutations
  requestPasswordReset: UseMutationResult<void, Error, PasswordResetRequest>;
  resetPassword: UseMutationResult<void, Error, ResetPasswordRequest>;
  verifyEmail: UseMutationResult<void, Error, EmailVerificationRequest>;
}
```

**Риски и безопасность**

- **Низкий риск**: простая композиция без дополнительной логики
- **Dependency coupling**: тесно связан с useAuthMutations и usePasswordMutations
- **Type safety**: использует intersection types для безопасного объединения
- **Error handling**: делегирует обработку ошибок композируемым хукам
- **State management**: не добавляет собственного состояния, только композирует

**Тесты / рекомендации по покрытию**

- **Composition тесты**: корректность объединения всех мутаций
- **Type checking тесты**: правильность intersection типов
- **Integration тесты**: совместимость с формами аутентификации
- **Mutation availability тесты**: доступность всех ожидаемых мутаций
- **Error propagation тесты**: корректность передачи ошибок от композируемых хуков

**Оценка сложности (low/medium/high)**
**LOW** - простая композиция существующих хуков

**TODO / Рефакторинг**

- ✅ Правильная архитектура с композицией вместо избыточной абстракции
- ✅ Использование intersection types для типобезопасности
- ✅ Устранение лишних слоев абстракции
- ✅ Прямая интеграция с системой уведомлений через композируемые хуки
- ✅ Четкая JSDoc документация архитектурных принципов
- 💡 Рекомендация: рассмотреть мемоизацию возвращаемого объекта
- 💡 Рекомендация: добавить JSDoc для каждой мутации в возвращаемом объекте
- 💡 Рекомендация: рассмотреть добавление loading состояния для всех мутаций
