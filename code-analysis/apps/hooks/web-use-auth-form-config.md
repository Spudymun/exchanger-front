### Путь: apps/web/src/hooks/useAuthFormConfig.ts

**Краткое назначение (1 предложение)**
Утилитарные функции для централизованной обработки успешных отправок и ошибок форм аутентификации.

**Подробное описание (3–6 предложений)**
Файл содержит две утилитарные функции-фабрики для создания обработчиков событий форм аутентификации после рефакторинга и удаления неиспользуемого кода. Функция createAuthFormSubmitHandler создает обработчик успешной отправки формы, который вызывает переданный callback при успешной операции. Функция createAuthFormErrorHandler создает обработчик ошибок, который делегирует обработку ошибок централизованному хуку useAuthMutationAdapter. Файл следует принципам CODE_STYLE_GUIDE.md с удалением console.error и неиспользуемой функции useAuthFormConfig. Представляет собой результат рефакторинга с переходом на автоматическое управление локалью через next-intl routing.

**Экспортируемые сущности / API (все на 100%)**

- `export function createAuthFormSubmitHandler(onSuccess?)` — фабрика обработчика успешной отправки
- `export function createAuthFormErrorHandler()` — фабрика обработчика ошибок

**Входы (expected inputs) / Параметры (все на 100%)**

- `createAuthFormSubmitHandler`: `onSuccess?: () => void` — опциональный callback при успехе
- `createAuthFormErrorHandler`: нет параметров — возвращает универсальный обработчик ошибок

**Выходы / Побочные эффекты (все на 100%)**

- `createAuthFormSubmitHandler` возвращает функцию, которая вызывает onSuccess?.()
- `createAuthFormErrorHandler` возвращает функцию, которая принимает \_error: unknown
- Обработка ошибок делегируется useAuthMutationAdapter хуку
- Не производит side effects кроме вызова переданных callbacks

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: LoginForm и RegisterForm для обработки событий форм
- **Интегрируется**: с useAuthMutationAdapter для централизованной обработки ошибок
- **Часть**: архитектуры обработки форм аутентификации
- **Заменяет**: удаленную функцию useAuthFormConfig после рефакторинга

**Домен данных / типы (все на 100%)**

```typescript
// Типы функций-фабрик
type AuthFormSubmitHandler = () => void;
type AuthFormErrorHandler = (error: unknown) => void;

// Фабрики обработчиков
type CreateAuthFormSubmitHandler = (onSuccess?: () => void) => AuthFormSubmitHandler;
type CreateAuthFormErrorHandler = () => AuthFormErrorHandler;

// Callback типы
type SuccessCallback = () => void;
type ErrorType = unknown;
```

**Риски и безопасность**

- **Минимальный риск**: простые утилитарные функции без сложной логики
- **Error handling delegation**: полностью зависит от useAuthMutationAdapter для обработки ошибок
- **Callback safety**: использует optional chaining для безопасного вызова onSuccess
- **Type safety**: принимает unknown для ошибок, что безопасно но не информативно
- **No side effects**: функции чистые, не производят неожиданных побочных эффектов

**Тесты / рекомендации по покрытию**

- **Factory function тесты**: корректность создания обработчиков
- **Callback invocation тесты**: правильность вызова onSuccess в success handler
- **Error delegation тесты**: корректность делегирования ошибок
- **Optional callback тесты**: безопасность при отсутствии onSuccess
- **Integration тесты**: совместимость с формами аутентификации

**Оценка сложности (low/medium/high)**
**LOW** - простые утилитарные функции-фабрики

**TODO / Рефакторинг**

- ✅ Удаление неиспользуемого кода (useAuthFormConfig)
- ✅ Соблюдение CODE_STYLE_GUIDE.md (удален console.error)
- ✅ Переход на автоматическое управление локалью через next-intl
- ✅ Централизованная обработка ошибок через useAuthMutationAdapter
- ✅ Использование optional chaining для безопасности
- ✅ Четкая JSDoc документация назначения функций
- 💡 Рекомендация: рассмотреть добавление более специфичных типов для ошибок
- 💡 Рекомендация: добавить JSDoc для параметров функций
- 💡 Рекомендация: рассмотреть объединение в один объект конфигурации
