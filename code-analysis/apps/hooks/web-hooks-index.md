### Путь: apps/web/src/hooks/index.ts

**Краткое назначение (1 предложение)**
Централизованный файл экспорта локализованных хуков web приложения с архитектурными принципами wrapper паттерна.

**Подробное описание (3–6 предложений)**
Файл реализует паттерн barrel exports для локализованных хуков, следуя четкому архитектурному принципу wrapper'ов над базовой функциональностью. Экспортирует три основных локализованных хука: useNotificationsWithTranslations для уведомлений, useExchangeStoreWithTranslations для состояния обмена, и useUIStoreWithTranslations для UI состояния. Включает экспорт соответствующих TypeScript типов для обеспечения типобезопасности при использовании хуков. Содержит подробную JSDoc документацию с описанием архитектурных принципов: использование существующей системы next-intl + Zod, отсутствие дублирования логики, интеграция с централизованными системами.

**Экспортируемые сущности / API (все на 100%)**

- `export { useNotificationsWithTranslations }` — локализованные уведомления
- `export { useExchangeStoreWithTranslations }` — локализованное состояние обмена
- `export { useUIStoreWithTranslations }` — локализованное UI состояние
- `export type { UseExchangeStoreWithTranslationsReturn }` — тип возвращаемого значения exchange store
- `export type { UseUIStoreWithTranslationsReturn }` — тип возвращаемого значения UI store

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых входных параметров - это файл экспорта
- Зависит от существования соответствующих файлов хуков в той же директории
- Требует наличия TypeScript типов в экспортируемых модулях

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет централизованный доступ к локализованным хукам
- Упрощает импорты в компонентах приложения
- Обеспечивает типобезопасность через экспорт типов
- Создает единую точку управления локализованными хуками

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: компонентах web приложения для импорта локализованных хуков
- **Экспортирует**: локализованные wrapper хуки из локальных файлов
- **Часть**: архитектуры локализации web приложения
- **Паттерн**: barrel exports с архитектурными принципами wrapper'ов

**Домен данных / типы (все на 100%)**

```typescript
// Архитектурные принципы
interface WrapperHookPrinciples {
  pattern: 'wrapper'; // Паттерн wrapper'ов
  localization: 'next-intl + Zod'; // Система локализации
  duplication: false; // Отсутствие дублирования логики
  integration: 'centralized'; // Интеграция с централизованными системами
}

// Экспортируемые типы
type UseExchangeStoreWithTranslationsReturn = ReturnType<typeof useExchangeStoreWithTranslations>;
type UseUIStoreWithTranslationsReturn = ReturnType<typeof useUIStoreWithTranslations>;

// Структура модуля
interface LocalizedHooksModule {
  notifications: typeof useNotificationsWithTranslations;
  exchangeStore: typeof useExchangeStoreWithTranslations;
  uiStore: typeof useUIStoreWithTranslations;
}
```

**Риски и безопасность**

- **Минимальный риск**: простой файл экспорта без логики
- **Dependency chain**: создает цепочку зависимостей между локализованными хуками
- **Type safety**: критически зависит от корректности экспортируемых типов
- **Architecture compliance**: требует соблюдения wrapper паттерна в хуках
- **Circular dependencies**: потенциальный риск циклических зависимостей

**Тесты / рекомендации по покрытию**

- **Import тесты**: корректность всех экспортов хуков и типов
- **Type checking тесты**: правильность TypeScript типов
- **Architecture тесты**: соблюдение wrapper паттерна в экспортируемых хуках
- **Dependency тесты**: отсутствие циклических зависимостей

**Оценка сложности (low/medium/high)**
**LOW** - простой файл экспорта с архитектурной документацией

**TODO / Рефакторинг**

- ✅ Четкие архитектурные принципы в JSDoc документации
- ✅ Правильная организация экспортов с типами
- ✅ Использование named exports для лучшего tree-shaking
- ✅ Логичная группировка локализованных хуков
- ✅ Экспорт TypeScript типов для типобезопасности
- 💡 Рекомендация: рассмотреть добавление JSDoc для каждого экспорта
- 💡 Рекомендация: добавить версионирование для breaking changes в типах
- 💡 Рекомендация: рассмотреть lazy loading для больших хуков
