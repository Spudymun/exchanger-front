### Путь: apps/web/src/hooks/useExchangeStoreWithTranslations.ts

**Краткое назначение (1 предложение)**
Локализованный wrapper для useExchangeStore, добавляющий переводы, валидацию с Zod и интеграцию с системой уведомлений.

**Подробное описание (3–6 предложений)**
Хук реализует архитектурный паттерн wrapper'а, расширяя базовый useExchangeStore локализованной валидацией и уведомлениями без дублирования основной логики. Использует createNextIntlZodErrorMap для локализации ошибок валидации и интегрируется с централизованными Zod схемами из @repo/utils. Включает комплексную валидацию формы обмена через createLocalizedExchangeFormSchema с проверкой всех полей: валюты, суммы, email, номера карты и согласия. Предоставляет локализованные методы для получения информации о шагах процесса, статусах заказов и специализированные уведомления для операций обмена. Структурирован модульно с вынесением сложной логики валидации в отдельные функции для улучшения читаемости и тестируемости.

**Экспортируемые сущности / API (все на 100%)**

- `export function useExchangeStoreWithTranslations()` — основной локализованный wrapper
- `export type UseExchangeStoreWithTranslationsReturn` — тип возвращаемого значения
- `createLocalizedExchangeFormSchema(validationT)` — фабрика локализованной схемы (внутренняя)
- `processValidationErrors(errors, notifications)` — обработчик ошибок валидации (внутренняя)
- `getFieldSchema(fieldName)` — получение схемы для поля (внутренняя)
- `getStepKey(steps, step)` — безопасное получение ключа шага (внутренняя)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - композирует базовый useExchangeStore
- Зависит от useNotificationsWithTranslations для уведомлений
- Использует переводы из 'AdvancedExchangeForm.validation' и 'exchange' namespaces

**Выходы / Побочные эффекты (все на 100%)**

- Все методы и свойства базового useExchangeStore
- `validateForm()` — валидация всей формы с локализованными ошибками
- `validateField(fieldName, value)` — валидация отдельного поля
- `getLocalizedStepInfo(step)` — локализованная информация о шаге
- `getLocalizedOrderStatus(status)` — локализованный статус заказа
- Специализированные методы уведомлений для операций обмена

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: компонентах обмена для локализованного управления состоянием
- **Композирует**: useExchangeStore из @repo/hooks для базовой функциональности
- **Использует**: useNotificationsWithTranslations для локализованных уведомлений
- **Импортирует схемы**: emailSchema, cryptoAmountStringSchema, currencySchema из @repo/utils
- **Использует**: createNextIntlZodErrorMap для локализации ошибок Zod

**Домен данных / типы (все на 100%)**

```typescript
// Схема локализованной формы обмена
const exchangeFormSchema = z.object({
  fromCurrency: currencySchema,
  fromAmount: cryptoAmountStringSchema,
  userEmail: emailSchema,
  cardNumber: z.string().min(1),
  agreementAccepted: z.boolean().refine(val => val === true),
});

// Типы валидации
interface FieldValidationResult {
  isValid: boolean;
  error: string | null;
}

interface LocalizedStepInfo {
  title: string;
  description: string;
}

// Ключи шагов процесса
type StepKey = 'form' | 'review' | 'payment' | 'completed';

// Поддерживаемые поля валидации
type ValidatableField = 'fromCurrency' | 'fromAmount' | 'userEmail' | 'cardNumber';
```

**Риски и безопасность**

- **Validation complexity**: сложная логика валидации может содержать ошибки
- **Translation dependency**: критически зависит от наличия всех переводов
- **Schema synchronization**: схемы должны быть синхронизированы с формой
- **Error handling**: обработка ошибок валидации может быть неполной
- **Type safety**: использует z.unknown() как fallback, может быть небезопасно

**Тесты / рекомендации по покрытию**

- **Validation тесты**: корректность валидации всех полей формы
- **Localization тесты**: правильность всех переводов и error mapping
- **Step navigation тесты**: корректность getLocalizedStepInfo для всех шагов
- **Error processing тесты**: правильность processValidationErrors
- **Integration тесты**: совместимость с базовым useExchangeStore

**Оценка сложности (low/medium/high)**
**HIGH** - сложная интеграция валидации, локализации и состояния

**TODO / Рефакторинг**

- ✅ Архитектурный паттерн wrapper'а без дублирования логики
- ✅ Интеграция с централизованными Zod схемами
- ✅ Локализация ошибок через createNextIntlZodErrorMap
- ✅ Модульная структура с вынесением сложной логики
- ✅ Безопасное получение ключей шагов через findIndex
- ✅ Комплексная валидация формы с обработкой множественных ошибок
- 💡 Рекомендация: добавить мемоизацию для схем валидации
- 💡 Рекомендация: улучшить type safety для getFieldSchema fallback
- 💡 Рекомендация: рассмотреть разделение на более мелкие хуки
- 💡 Рекомендация: добавить unit тесты для всех helper функций
