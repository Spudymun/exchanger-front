### Путь: apps/web/src/hooks/useNotificationsWithTranslations.ts

**Краткое назначение (1 предложение)**
Локализованный wrapper для useNotifications, расширяющий базовую функциональность специализированными уведомлениями для API, обмена и валидации.

**Подробное описание (3–6 предложений)**
Хук следует архитектурному паттерну wrapper'а, добавляя локализованные уведомления к базовому useNotifications без изменения его логики. Организован модульно через helper функции createApiNotifications, createExchangeNotifications и createValidationNotifications для группировки связанной функциональности. Предоставляет специализированные методы для различных типов уведомлений: API операции (успех/ошибка/загрузка), операции обмена (создание/завершение заказов, ошибки расчета), валидация форм (одиночные/множественные ошибки). Использует переводы из namespace 'notifications' с поддержкой параметризованных сообщений для динамического контента. Включает константы для часто используемых заголовков уведомлений для оптимизации производительности и консистентности.

**Экспортируемые сущности / API (все на 100%)**

- `export function useNotificationsWithTranslations()` — основной локализованный wrapper уведомлений
- `createApiNotifications(baseNotifications, t, titles)` — фабрика API уведомлений (внутренняя)
- `createExchangeNotifications(baseNotifications, t, titles)` — фабрика уведомлений обмена (внутренняя)
- `createValidationNotifications(baseNotifications, t, title)` — фабрика уведомлений валидации (внутренняя)

**Входы (expected inputs) / Параметры (все на 100%)**

- Нет прямых параметров - композирует базовый useNotifications
- Использует переводы из namespace 'notifications' с различными подключами
- Зависит от наличия переводов для api._, exchange._, validation._, theme._

**Выходы / Побочные эффекты (все на 100%)**

- Все методы базового useNotifications (success, error, info, warning)
- API уведомления: showApiSuccess, showApiError, showApiLoading, showUnknownError
- Exchange уведомления: showExchangeSuccess, showExchangeError, showOrderCreated, showOrderCompleted, showCalculationError
- Validation уведомления: showValidationError, showMultipleValidationErrors, showFormError
- Theme уведомления: showThemeChanged с локализованными названиями тем

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: useExchangeStoreWithTranslations и других локализованных хуках
- **Композирует**: useNotifications из @repo/hooks для базовой функциональности
- **Использует**: useTranslations('notifications') для получения переводов
- **Интегрируется**: с формами, API мутациями и компонентами обмена
- **Паттерн**: wrapper для добавления локализации без изменения базовой логики

**Домен данных / типы (все на 100%)**

```typescript
// Типы уведомлений
interface ApiNotifications {
  showApiSuccess: (message?: string) => void;
  showApiError: (message?: string) => void;
  showApiLoading: (message?: string) => void;
  showUnknownError: () => void;
}

interface ExchangeNotifications {
  showExchangeSuccess: () => void;
  showExchangeError: (message?: string) => void;
  showOrderCreated: (orderId: string) => void;
  showOrderCompleted: (orderId: string) => void;
  showCalculationError: () => void;
}

interface ValidationNotifications {
  showValidationError: (message?: string) => void;
  showMultipleValidationErrors: (count: number) => void;
  showFormError: () => void;
}

interface ThemeNotifications {
  showThemeChanged: (themeName: 'light' | 'dark' | 'system') => void;
}

// Константы заголовков
type NotificationTitles = {
  API_SUCCESS_TITLE: string;
  API_ERROR_TITLE: string;
  VALIDATION_ERROR_TITLE: string;
  EXCHANGE_ERROR_TITLE: string;
};
```

**Риски и безопасность**

- **Низкий риск**: wrapper без изменения базовой логики уведомлений
- **Translation dependency**: критически зависит от наличия всех переводов notifications.\*
- **Parameter validation**: не валидирует параметры типа orderId или count
- **Fallback handling**: использует fallback сообщения при отсутствии параметров
- **Memory usage**: создает множественные helper функции, может влиять на память

**Тесты / рекомендации по покрытию**

- **Localization тесты**: корректность всех переводов и параметризованных сообщений
- **Notification types тесты**: правильность всех типов уведомлений (success, error, info)
- **Parameter handling тесты**: корректность обработки orderId, count и других параметров
- **Fallback тесты**: правильность fallback сообщений при отсутствии параметров
- **Integration тесты**: совместимость с базовым useNotifications

**Оценка сложности (low/medium/high)**
**MEDIUM** - модульная организация с множественными helper функциями

**TODO / Рефакторинг**

- ✅ Архитектурный паттерн wrapper'а без изменения базовой логики
- ✅ Модульная организация через helper функции по типам уведомлений
- ✅ Константы для часто используемых заголовков
- ✅ Поддержка параметризованных сообщений для динамического контента
- ✅ Специализированные методы для всех типов операций приложения
- ✅ Fallback сообщения при отсутствии параметров
- 💡 Рекомендация: добавить мемоизацию для helper функций
- 💡 Рекомендация: добавить валидацию параметров (orderId, count)
- 💡 Рекомендация: рассмотреть lazy loading для больших наборов переводов
- 💡 Рекомендация: добавить типизацию для ключей переводов
