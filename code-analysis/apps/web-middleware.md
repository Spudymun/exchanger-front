### Путь: apps/web/middleware.ts

**Краткое назначение (1 предложение)**
Next.js middleware для интернационализации с next-intl, обрабатывающий локализацию запросов и добавляющий locale заголовки.

**Подробное описание (3–6 предложений)**
Middleware интегрирует next-intl для автоматической обработки локализации в Next.js приложении. Использует конфигурацию routing из i18n модуля для определения поддерживаемых локалей и маршрутизации. Извлекает локаль из pathname URL (ru/en) и добавляет x-locale заголовок для использования в root layout. Настроен matcher для исключения API routes, статических файлов и служебных путей Next.js. Обеспечивает корректную работу интернационализации на уровне сервера перед рендерингом компонентов.

**Экспортируемые сущности / API (все на 100%)**

- `export default function middleware(request: NextRequest)` — основная middleware функция
- `export const config` — конфигурация matcher для Next.js

**Входы (expected inputs) / Параметры (все на 100%)**

- `request: NextRequest` — объект запроса Next.js с URL, заголовками и методом
- Использует `routing` конфигурацию из `./src/i18n/routing`
- Обрабатывает pathname для извлечения локали

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает `NextResponse` с обработанной локализацией
- Добавляет заголовок `x-locale` со значением 'ru' или 'en'
- Выполняет redirect/rewrite согласно next-intl логике
- Влияет на все запросы, соответствующие matcher паттерну

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Вызывается**: Next.js автоматически для каждого запроса
- **Импортирует**: `createMiddleware` из next-intl, `routing` из `./src/i18n/routing`
- **Используется**: перед рендерингом всех страниц и компонентов
- **Влияет на**: apps/web/app/[locale]/layout.tsx (через x-locale заголовок)

**Домен данных / типы (все на 100%)**

```typescript
interface MiddlewareConfig {
  matcher: string; // Regex паттерн для обработки путей
}

type LocaleType = 'ru' | 'en'; // Поддерживаемые локали
type MiddlewareFunction = (request: NextRequest) => NextResponse | Response;
```

**Риски и безопасность**

- **Низкий риск**: стандартная next-intl интеграция
- **Performance**: выполняется на каждом запросе, важна оптимизация
- **Locale detection**: простая логика может не покрывать все edge cases
- **Header injection**: безопасно, только внутренние заголовки

**Тесты / рекомендации по покрытию**

- **Unit тесты**: проверка извлечения локали из разных pathname
- **Integration тесты**: работа с next-intl middleware
- **E2E тесты**: корректность redirect для разных локалей
- **Header тесты**: проверка установки x-locale заголовка

**Оценка сложности (low/medium/high)**
**LOW** - простая интеграция с next-intl без сложной логики

**TODO / Рефакторинг**

- ✅ Правильная интеграция с next-intl
- ✅ Корректный matcher для исключения служебных путей
- 💡 Рекомендация: добавить более надежное определение локали с fallback
- 💡 Рекомендация: рассмотреть кэширование для производительности
