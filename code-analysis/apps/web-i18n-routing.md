### Путь: apps/web/src/i18n/routing.ts

**Краткое назначение (1 предложение)**
Конфигурация маршрутизации next-intl с определением поддерживаемых локалей и локали по умолчанию.

**Подробное описание (3–6 предложений)**
Файл создает конфигурацию маршрутизации для next-intl через defineRouting с использованием централизованных констант локалей из @repo/constants. Определяет список поддерживаемых локалей через SUPPORTED_LOCALES и устанавливает английский язык как локаль по умолчанию для fallback случаев. Служит центральной точкой конфигурации для всей системы интернационализации приложения, используемой в navigation.ts и request.ts. Обеспечивает консистентность локалей во всем приложении через использование централизованных констант вместо хардкода. Является основой для автоматической генерации локализованных маршрутов и правильной обработки URL с локалями.

**Экспортируемые сущности / API (все на 100%)**

- `export const routing` — конфигурация маршрутизации next-intl

**Входы (expected inputs) / Параметры (все на 100%)**

- `SUPPORTED_LOCALES` — массив поддерживаемых локалей из @repo/constants
- Конфигурация defineRouting с locales и defaultLocale

**Выходы / Побочные эффекты (все на 100%)**

- Объект конфигурации маршрутизации для next-intl
- Определение поддерживаемых локалей для всего приложения
- Установка fallback локали для некорректных запросов
- Основа для генерации локализованных URL

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: navigation.ts для создания локализованных навигационных утилит
- **Используется в**: request.ts для валидации локалей и fallback логики
- **Импортирует**: SUPPORTED_LOCALES из @repo/constants для централизованного управления
- **Использует**: defineRouting из next-intl/routing для создания конфигурации
- **Часть**: основы системы интернационализации приложения

**Домен данных / типы (все на 100%)**

```typescript
// Конфигурация маршрутизации
interface RoutingConfig {
  locales: readonly string[];
  defaultLocale: string;
}

// Поддерживаемые локали (из констант)
type SupportedLocale = (typeof SUPPORTED_LOCALES)[number];

// Локаль по умолчанию
const DEFAULT_LOCALE = 'en';

// Структура routing объекта
const routing: RoutingConfig = {
  locales: SUPPORTED_LOCALES,
  defaultLocale: DEFAULT_LOCALE,
};
```

**Риски и безопасность**

- **Минимальный риск**: простая конфигурация без логики
- **Constants dependency**: критически зависит от SUPPORTED_LOCALES из констант
- **Default locale**: должна быть включена в список поддерживаемых локалей
- **Routing consistency**: изменения должны быть синхронизированы с файлами переводов
- **URL generation**: влияет на генерацию всех локализованных URL

**Тесты / рекомендации по покрытию**

- **Configuration тесты**: корректность конфигурации routing объекта
- **Locale validation тесты**: defaultLocale должна быть в списке locales
- **Constants integration тесты**: синхронизация с SUPPORTED_LOCALES
- **URL generation тесты**: правильность генерации локализованных маршрутов
- **Fallback тесты**: корректность работы defaultLocale

**Оценка сложности (low/medium/high)**
**LOW** - простая конфигурация с использованием констант

**TODO / Рефакторинг**

- ✅ Использование централизованных констант из @repo/constants
- ✅ Правильное использование defineRouting из next-intl
- ✅ Установка разумной defaultLocale (английский)
- ✅ Минималистичная конфигурация без избыточности
- ✅ Четкие комментарии о назначении параметров
- 💡 Рекомендация: добавить валидацию что defaultLocale включена в locales
- 💡 Рекомендация: рассмотреть добавление JSDoc документации
- 💡 Рекомендация: добавить типизацию для лучшей документации
- 💡 Рекомендация: рассмотреть добавление дополнительных опций routing
