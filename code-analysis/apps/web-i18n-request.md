### Путь: apps/web/src/i18n/request.ts

**Краткое назначение (1 предложение)**
Конфигурация next-intl для серверных запросов с определением локали и динамической загрузкой переводов.

**Подробное описание (3–6 предложений)**
Файл экспортирует конфигурацию next-intl через getRequestConfig для обработки серверных запросов с поддержкой интернационализации. Реализует логику определения локали из запроса с валидацией через hasLocale и fallback на defaultLocale из routing конфигурации при некорректной локали. Использует динамический импорт для загрузки соответствующих файлов переводов из директории messages на основе определенной локали. Обеспечивает безопасную обработку локали с проверкой на поддерживаемые значения и предотвращением ошибок при неизвестных локалях. Является критической частью серверной интернационализации, обрабатывая каждый запрос для правильной настройки локали и переводов.

**Экспортируемые сущности / API (все на 100%)**

- `export default getRequestConfig(async ({ requestLocale }) => {...})` — конфигурация обработки запросов

**Входы (expected inputs) / Параметры (все на 100%)**

- `requestLocale` — промис с локалью из URL сегмента [locale]
- Зависит от routing конфигурации для списка поддерживаемых локалей
- Требует наличия файлов переводов в директории messages/

**Выходы / Побочные эффекты (все на 100%)**

- `locale` — определенная и валидированная локаль для запроса
- `messages` — объект переводов, загруженный динамически для локали
- Fallback на defaultLocale при некорректной requestLocale
- Динамическая загрузка JSON файлов переводов

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: Next.js App Router для каждого серверного запроса
- **Импортирует**: hasLocale, getRequestConfig из next-intl/server
- **Зависит от**: routing конфигурации для локалей и defaultLocale
- **Загружает**: файлы переводов из messages/[locale].json
- **Часть**: серверной конфигурации интернационализации

**Домен данных / типы (все на 100%)**

```typescript
// Параметры конфигурации
interface RequestConfigParams {
  requestLocale: Promise<string | undefined>;
}

// Возвращаемая конфигурация
interface RequestConfig {
  locale: string;
  messages: Record<string, any>;
}

// Структура файлов переводов
type MessagesStructure = {
  [key: string]: string | MessagesStructure;
};

// Поддерживаемые локали (из routing)
type SupportedLocale = 'en' | 'ru'; // Пример
```

**Риски и безопасность**

- **Locale validation**: критически важна валидация локали для предотвращения атак
- **Dynamic imports**: могут вызывать ошибки при отсутствии файлов переводов
- **Fallback handling**: должен корректно обрабатывать некорректные локали
- **Performance**: динамическая загрузка может влиять на производительность
- **File system access**: зависит от наличия файлов переводов в файловой системе

**Тесты / рекомендации по покрытию**

- **Locale validation тесты**: корректность hasLocale проверки
- **Fallback тесты**: правильность fallback на defaultLocale
- **Dynamic import тесты**: обработка отсутствующих файлов переводов
- **Request handling тесты**: корректность обработки различных requestLocale
- **Integration тесты**: совместимость с routing конфигурацией

**Оценка сложности (low/medium/high)**
**MEDIUM** - критическая серверная логика с динамическими импортами

**TODO / Рефакторинг**

- ✅ Правильное использование getRequestConfig из next-intl
- ✅ Валидация локали через hasLocale
- ✅ Fallback на defaultLocale при некорректной локали
- ✅ Динамическая загрузка переводов на основе локали
- ✅ Интеграция с routing конфигурацией
- 💡 Рекомендация: добавить error handling для динамических импортов
- 💡 Рекомендация: рассмотреть кеширование загруженных переводов
- 💡 Рекомендация: добавить логирование для отладки локали
- 💡 Рекомендация: рассмотреть добавление метрик для мониторинга
