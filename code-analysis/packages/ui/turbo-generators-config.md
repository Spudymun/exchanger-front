### Путь: packages/ui/turbo/generators/config.ts

**Краткое назначение (1 предложение)**
Конфигурация Turborepo генератора для автоматического создания новых React компонентов в UI библиотеке.

**Подробное описание (3–6 предложений)**
Файл настраивает Turborepo генератор кода для упрощения создания новых React компонентов в UI пакете через интерактивный CLI интерфейс. Определяет генератор 'react-component' который запрашивает имя компонента у пользователя и автоматически создает файл компонента из шаблона templates/component.hbs. Включает два действия: создание нового .tsx файла с kebab-case именованием и автоматическое добавление экспорта в package.json для правильной структуры модуля. Использует Plop.js под капотом для обработки шаблонов и файловых операций, обеспечивая консистентность создаваемых компонентов. Является частью инфраструктуры разработки для ускорения создания новых UI компонентов с правильной структурой и экспортами.

**Экспортируемые сущности / API (все на 100%)**

- `export default function generator(plop)` — функция конфигурации Turborepo генератора

**Входы (expected inputs) / Параметры (все на 100%)**

- `plop: PlopTypes.NodePlopAPI` — API объект Plop.js для настройки генераторов
- Интерактивный ввод: имя компонента через CLI prompt

**Выходы / Побочные эффекты (все на 100%)**

- Создает новый .tsx файл в src/ директории с kebab-case именем
- Добавляет экспорт в package.json в секцию "exports"
- Использует шаблон templates/component.hbs для генерации кода
- Обеспечивает консистентную структуру новых компонентов

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется**: через Turborepo CLI команды для генерации кода
- **Импортирует**: PlopTypes из @turbo/gen для типизации
- **Зависит от**: templates/component.hbs шаблона для генерации
- **Модифицирует**: package.json для добавления экспортов
- **Часть**: инфраструктуры разработки UI пакета

**Домен данных / типы (все на 100%)**

```typescript
// Типы из Turborepo
interface PlopTypes.NodePlopAPI {
  setGenerator: (name: string, config: GeneratorConfig) => void;
}

// Конфигурация генератора
interface GeneratorConfig {
  description: string;
  prompts: PromptConfig[];
  actions: ActionConfig[];
}

// Типы промптов и действий
interface PromptConfig {
  type: 'input' | 'list' | 'confirm';
  name: string;
  message: string;
}

interface ActionConfig {
  type: 'add' | 'append' | 'modify';
  path: string;
  templateFile?: string;
  pattern?: RegExp;
  template?: string;
}

// Генерируемая структура
const GENERATED_STRUCTURE = {
  componentFile: 'src/{{kebabCase name}}.tsx',
  exportEntry: '"./{{kebabCase name}}": "./src/{{kebabCase name}}.tsx"',
  template: 'templates/component.hbs'
};
```

**Риски и безопасность**

- **File system operations**: создает и модифицирует файлы, может перезаписать существующие
- **Package.json modification**: изменения могут нарушить структуру пакета
- **Template dependency**: критически зависит от наличия templates/component.hbs
- **Naming conflicts**: не проверяет существование компонентов с таким именем
- **Pattern matching**: regex для package.json может быть хрупким

**Тесты / рекомендации по покрытию**

- **Generator execution тесты**: корректность создания компонентов
- **Package.json modification тесты**: правильность добавления экспортов
- **Template rendering тесты**: корректность обработки шаблонов
- **Naming convention тесты**: правильность kebab-case преобразования
- **File conflict тесты**: обработка существующих файлов

**Оценка сложности (low/medium/high)**
**LOW** - простая конфигурация генератора с базовыми действиями

**TODO / Рефакторинг**

- ✅ Правильная настройка Turborepo генератора
- ✅ Интерактивный промпт для имени компонента
- ✅ Автоматическое создание файла из шаблона
- ✅ Автоматическое добавление экспорта в package.json
- ✅ Использование kebab-case для консистентности
- ✅ Типизация через PlopTypes для безопасности
- 💡 Рекомендация: добавить проверку существования компонента
- 💡 Рекомендация: добавить валидацию имени компонента
- 💡 Рекомендация: рассмотреть добавление опций для типа компонента
- 💡 Рекомендация: добавить автоматическое создание тестов и stories
