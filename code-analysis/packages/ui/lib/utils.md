### Путь: packages/ui/src/lib/utils.ts

**Краткое назначение (1 предложение)**
Утилитарная функция для безопасного объединения CSS классов с разрешением конфликтов Tailwind.

**Подробное описание (3–6 предложений)**
Модуль экспортирует функцию cn (className) которая объединяет clsx для условного объединения классов и twMerge для разрешения конфликтов Tailwind CSS. Функция принимает любое количество ClassValue аргументов (строки, объекты, массивы, условные выражения) и возвращает оптимизированную строку классов. clsx обрабатывает условную логику и различные типы входных данных, а twMerge удаляет конфликтующие Tailwind классы, оставляя только последние в порядке приоритета. Это стандартная утилита в современных React проектах с Tailwind CSS для предотвращения конфликтов стилей и обеспечения предсказуемого поведения. Широко используется во всех UI компонентах для объединения базовых стилей с пользовательскими className пропсами.

**Экспортируемые сущности / API (все на 100%)**

- `export function cn(...inputs: ClassValue[])` — функция объединения CSS классов

**Входы (expected inputs) / Параметры (все на 100%)**

- `...inputs: ClassValue[]` — любое количество значений классов различных типов:
  - `string` — строки с классами
  - `object` — объекты с условными классами `{ 'class': condition }`
  - `array` — массивы классов
  - `undefined | null | false` — игнорируются
  - Условные выражения и комбинации вышеперечисленного

**Выходы / Побочные эффекты (все на 100%)**

- Возвращает строку с объединенными и оптимизированными CSS классами
- Разрешает конфликты Tailwind CSS (например, `p-4 p-6` → `p-6`)
- Удаляет дублирующиеся классы
- Обрабатывает условную логику из clsx
- Возвращает пустую строку если все входы falsy

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: всех UI компонентах для объединения className
- **Импортирует**: clsx для условного объединения классов
- **Импортирует**: twMerge из tailwind-merge для разрешения конфликтов
- **Стандартная утилита**: в современных React + Tailwind проектах
- **Часть**: базовой инфраструктуры UI пакета

**Домен данных / типы (все на 100%)**

```typescript
// Типы из clsx
type ClassValue = ClassArray | ClassDictionary | string | number | null | boolean | undefined;

interface ClassDictionary {
  [id: string]: any;
}

type ClassArray = ClassValue[];

// Примеры использования
const examples = {
  strings: cn('p-4', 'bg-white', 'rounded'),
  conditional: cn('p-4', { 'bg-red-500': isError, 'bg-green-500': isSuccess }),
  arrays: cn(['p-4', 'bg-white'], ['rounded', 'shadow']),
  mixed: cn('base-class', { active: isActive }, undefined, 'final-class'),
  conflicts: cn('p-4 p-6 m-2 m-4'), // → 'p-6 m-4'
};
```

**Риски и безопасность**

- **Минимальный риск**: простая утилитарная функция без побочных эффектов
- **Performance**: может быть медленной при очень большом количестве классов
- **Tailwind dependency**: критически зависит от правильной настройки Tailwind
- **Bundle size**: добавляет зависимости clsx и tailwind-merge
- **Type safety**: полагается на TypeScript для валидации входных данных

**Тесты / рекомендации по покрытию**

- **Basic merging тесты**: корректность объединения простых строк
- **Conditional logic тесты**: правильность обработки условных объектов
- **Conflict resolution тесты**: корректность разрешения Tailwind конфликтов
- **Edge cases тесты**: обработка null, undefined, empty values
- **Performance тесты**: производительность при большом количестве классов

**Оценка сложности (low/medium/high)**
**LOW** - простая композиция двух утилитарных библиотек

**TODO / Рефакторинг**

- ✅ Правильная композиция clsx и twMerge
- ✅ Типизация через ClassValue из clsx
- ✅ Краткое и понятное имя функции (cn)
- ✅ Стандартная реализация для React + Tailwind проектов
- ✅ Поддержка всех типов входных данных clsx
- 💡 Рекомендация: добавить JSDoc с примерами использования
- 💡 Рекомендация: рассмотреть мемоизацию для часто используемых комбинаций
- 💡 Рекомендация: добавить development mode валидацию классов
- 💡 Рекомендация: рассмотреть создание специализированных версий для performance
