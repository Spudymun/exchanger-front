### Путь: packages/ui/src/lib/useMathCaptchaLocal.ts

**Краткое назначение (1 предложение)**
Локальная копия математической CAPTCHA логики для UI пакета с генерацией вопросов и валидацией ответов.

**Подробное описание (3–6 предложений)**
Хук реализует полную логику математической CAPTCHA с генерацией случайных арифметических задач (сложение, вычитание, умножение) и валидацией пользовательских ответов. Создан как временное решение для избежания зависимости от @repo/hooks в UI пакете согласно CODE_STYLE_GUIDE.md с планом рефакторинга на передачу данных через props. Включает конфигурируемые параметры сложности через MathCaptchaConfig с предустановленными уровнями (easy, medium, hard) и поддержку различных математических операций. Управляет состоянием challenge, пользовательского ответа, валидации и ошибок с автоматической проверкой правильности ответа. Предоставляет методы для обновления challenge и обработки blur событий для UX улучшений.

**Экспортируемые сущности / API (все на 100%)**

- `export function useMathCaptchaLocal(config?)` — основной хук математической CAPTCHA
- `export interface MathChallenge` — интерфейс математического вызова
- `export interface MathCaptchaConfig` — интерфейс конфигурации CAPTCHA
- `export const CAPTCHA_CONFIGS_LOCAL` — предустановленные конфигурации сложности

**Входы (expected inputs) / Параметры (все на 100%)**

- `config?: MathCaptchaConfig` — конфигурация с minNumber, maxNumber, operations
- По умолчанию: minNumber=1, maxNumber=20, operations=['add', 'subtract', 'multiply']

**Выходы / Побочные эффекты (все на 100%)**

- `challenge: MathChallenge` — текущий математический вызов с question, answer, id
- `userAnswer: string` — введенный пользователем ответ
- `isValid: boolean` — корректность текущего ответа
- `isVerified: boolean` — подтвержденная корректность ответа
- `hasError: boolean` — наличие ошибки после blur
- `setUserAnswer: (answer: string) => void` — функция обновления ответа
- `onBlur: () => void` — обработчик blur события
- `refreshChallenge: () => void` — генерация нового вызова

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: math-captcha UI компоненте для CAPTCHA функциональности
- **Временное решение**: избегает зависимости от @repo/hooks в UI пакете
- **План рефакторинга**: замена на передачу данных через props
- **Часть**: изоляции UI пакета согласно CODE_STYLE_GUIDE.md
- **Предоставляет**: локальную реализацию для UI компонентов

**Домен данных / типы (все на 100%)**

```typescript
// Математический вызов
interface MathChallenge {
  question: string; // "5 + 3", "12 - 7", "4 × 6"
  answer: number; // Правильный ответ
  id: string; // Уникальный идентификатор
}

// Конфигурация CAPTCHA
interface MathCaptchaConfig {
  minNumber: number;
  maxNumber: number;
  operations: Array<'add' | 'subtract' | 'multiply'>;
}

// Предустановленные уровни сложности
const DIFFICULTY_LEVELS = {
  easy: { minNumber: 1, maxNumber: 10, operations: ['add', 'subtract'] },
  medium: { minNumber: 1, maxNumber: 20, operations: ['add', 'subtract', 'multiply'] },
  hard: { minNumber: 5, maxNumber: 50, operations: ['add', 'subtract', 'multiply'] },
};

// Константы генерации
const MULTIPLY_MAX_NUMBER = 10; // Ограничение для умножения
const ID_GENERATION = { BASE: 36, START: 2, LENGTH: 9 };
```

**Риски и безопасность**

- **Security**: клиентская валидация может быть обойдена, нужна серверная проверка
- **Predictability**: Math.random() может быть предсказуемым для атак
- **Accessibility**: математические символы могут быть недоступны для screen readers
- **Temporary solution**: планируется рефакторинг, может создать технический долг
- **State management**: сложное состояние может приводить к race conditions

**Тесты / рекомендации по покрытию**

- **Challenge generation тесты**: корректность генерации для всех операций
- **Validation тесты**: правильность проверки ответов
- **State management тесты**: корректность обновления состояний
- **Configuration тесты**: работа с различными конфигурациями сложности
- **Edge cases тесты**: обработка некорректных входных данных

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная логика генерации и валидации с состоянием

**TODO / Рефакторинг**

- ✅ Полная реализация математической CAPTCHA логики
- ✅ Конфигурируемые уровни сложности
- ✅ Поддержка трех математических операций
- ✅ Валидация с обработкой blur событий
- ✅ Уникальные идентификаторы для challenges
- ✅ Предустановленные конфигурации (easy, medium, hard)
- 🔴 Критично: рефакторинг на передачу данных через props (план)
- 💡 Рекомендация: добавить серверную валидацию для безопасности
- 💡 Рекомендация: улучшить accessibility для математических символов
- 💡 Рекомендация: использовать crypto.getRandomValues() для лучшей безопасности
- 💡 Рекомендация: добавить rate limiting для предотвращения брутфорса
