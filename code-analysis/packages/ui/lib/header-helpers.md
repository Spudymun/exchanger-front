### Путь: packages/ui/src/lib/header-helpers.tsx

**Краткое назначение (1 предложение)**
Утилитарные функции для рендеринга пользовательского меню и обогащения дочерних компонентов контекстом заголовка.

**Подробное описание (3–6 предложений)**
Модуль предоставляет набор helper функций для работы с заголовком приложения, включая рендеринг аутентифицированных и неаутентифицированных пользователей с responsive дизайном. Функции renderAuthenticatedUser и renderUnauthenticatedUser создают соответствующие UI элементы с кнопками и подписями, адаптированными для мобильных и desktop экранов. Включает getUserMenuContent для выбора контента меню на основе состояния аутентификации и enhanceChildWithContext для автоматического обогащения дочерних компонентов свойствами из HeaderContext. Использует модульный подход с отдельными функциями для добавления каждого свойства контекста (locale, authentication, callbacks). Поддерживает локализацию через опциональные labels и предоставляет fallback значения для всех текстовых элементов.

**Экспортируемые сущности / API (все на 100%)**

- `export function renderAuthenticatedUser(userName, onSignOut, labels?)` — рендер меню аутентифицированного пользователя
- `export function renderUnauthenticatedUser(onSignIn, labels?)` — рендер кнопки входа
- `export function getUserMenuContent(children, options)` — выбор контента меню на основе состояния
- `export function enhanceChildWithContext(child, context)` — обогащение дочерних компонентов контекстом

**Входы (expected inputs) / Параметры (все на 100%)**

- `renderAuthenticatedUser`: `userName?: string`, `onSignOut?: () => void`, `labels?: { signOut?, signOutShort? }`
- `renderUnauthenticatedUser`: `onSignIn?: () => void`, `labels?: { signIn?, signInShort? }`
- `getUserMenuContent`: `children?: React.ReactNode`, `options: UserMenuOptions`
- `enhanceChildWithContext`: `child: React.ReactNode`, `context?: HeaderContextValue`

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают JSX элементы для пользовательского меню
- Применяют responsive классы (hidden sm:inline, sm:hidden)
- Используют Button компонент с вариантами outline/default и размером compact
- enhanceChildWithContext клонирует React элементы с дополнительными props
- Предоставляют fallback значения для labels

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: header компонентах для рендеринга пользовательского меню
- **Импортирует**: Button из components/ui/button для UI элементов
- **Использует типы**: HeaderContextValue из header-types для типизации
- **Интегрируется**: с React Context API для передачи свойств
- **Часть**: header системы UI пакета

**Домен данных / типы (все на 100%)**

```typescript
// Опции пользовательского меню
interface UserMenuOptions {
  isAuth: boolean;
  userName?: string;
  onSignOut?: () => void;
  onSignIn?: () => void;
  labels?: {
    signIn?: string;
    signInShort?: string;
    signOut?: string;
    signOutShort?: string;
  };
}

// Labels для локализации
interface AuthLabels {
  signIn?: string;
  signInShort?: string;
  signOut?: string;
  signOutShort?: string;
}

// Контекст заголовка (из header-types)
interface HeaderContextValue {
  currentLocale?: string;
  onLocaleChange?: (locale: string) => void;
  isAuthenticated?: boolean;
  onSignIn?: () => void;
  onSignOut?: () => void;
}
```

**Риски и безопасность**

- **React cloning**: enhanceChildWithContext может нарушить React оптимизации
- **Type safety**: использует Record<string, unknown> что может быть небезопасно
- **Context dependency**: критически зависит от правильности HeaderContextValue
- **Callback safety**: не валидирует переданные callback функции
- **Performance**: клонирование элементов может влиять на производительность

**Тесты / рекомендации по покрытию**

- **Rendering тесты**: корректность рендеринга для auth/unauth состояний
- **Responsive тесты**: правильность отображения на мобильных и desktop
- **Context enhancement тесты**: корректность обогащения дочерних компонентов
- **Fallback тесты**: правильность fallback значений для labels
- **Callback тесты**: корректность вызова onSignIn/onSignOut

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная логика обогащения компонентов и условного рендеринга

**TODO / Рефакторинг**

- ✅ Модульный подход с отдельными функциями для каждого свойства
- ✅ Responsive дизайн с адаптивными labels
- ✅ Поддержка локализации через опциональные labels
- ✅ Fallback значения для всех текстовых элементов
- ✅ Типизация через HeaderContextValue интерфейс
- ✅ Условное обогащение только при отсутствии child props
- 💡 Рекомендация: улучшить type safety для enhanceChildWithContext
- 💡 Рекомендация: добавить мемоизацию для производительности
- 💡 Рекомендация: рассмотреть использование более специфичных типов вместо Record
- 💡 Рекомендация: добавить валидацию callback функций
