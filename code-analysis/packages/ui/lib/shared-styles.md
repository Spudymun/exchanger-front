### Путь: packages/ui/src/lib/shared-styles.ts

**Краткое назначение (1 предложение)**
Централизованная библиотека переиспользуемых CSS стилей для устранения дублирования в business-компонентах.

**Подробное описание (3–6 предложений)**
Модуль предоставляет набор константных объектов со стилями для различных категорий компонентов: карточки, текст, статусы, loading состояния, grid layouts, layout компоненты, заголовки страниц, кнопки и таблицы. Создан для устранения нарушений Rule 20 (избыточность кода) путем централизации часто используемых CSS классов в business-компонентах. Использует TypeScript const assertions для type safety и включает utility функцию combineStyles для безопасного объединения стилей. Базируется на существующих design-tokens и предоставляет типы для type-safe использования стилей в компонентах. Структурирован по функциональным группам с логичной иерархией (размеры, варианты, состояния) для удобства использования и поддержки.

**Экспортируемые сущности / API (все на 100%)**

- `export const cardStyles` — стили для карточек (base, hover, focus)
- `export const textStyles` — стили для текста (heading, body, accent)
- `export const statusStyles` — стили для статусов (success, warning, info, error, neutral)
- `export const loadingStyles` — стили для loading состояний
- `export const gridStyles` — стили для grid layouts (responsive, cards, form)
- `export const layoutStyles` — стили для layout компонентов
- `export const pageStyles` — стили для заголовков страниц
- `export const buttonStyles` — дополнительные стили для кнопок
- `export const tableStyles` — стили для data-table компонентов
- `export function combineStyles(...styles)` — utility для объединения стилей
- Экспорт типов для type-safe использования

**Входы (expected inputs) / Параметры (все на 100%)**

- `combineStyles`: `...styles: Array<string | undefined | false>` — массив стилей для объединения
- Константные объекты не принимают параметры

**Выходы / Побочные эффекты (все на 100%)**

- Возвращают строки с CSS классами Tailwind
- `combineStyles` возвращает объединенную строку стилей
- Предоставляют type safety через TypeScript типы
- Обеспечивают консистентность стилей в приложении

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: business-компонентах для устранения дублирования стилей
- **Базируется на**: существующих design-tokens проекта
- **Интегрируется с**: Tailwind CSS классами
- **Часть**: дизайн-системы UI пакета для централизации стилей
- **Решает**: Rule 20 нарушения (избыточность кода)

**Домен данных / типы (все на 100%)**

```typescript
// Типы для type-safe использования
type CardStyleKeys = keyof typeof cardStyles; // 'base' | 'hover' | 'focus'
type TextStyleCategories = keyof typeof textStyles; // 'heading' | 'body' | 'accent'
type StatusStyleKeys = keyof typeof statusStyles; // 'success' | 'warning' | 'info' | 'error' | 'neutral'
type LayoutStyleKeys = keyof typeof layoutStyles;
type PageStyleKeys = keyof typeof pageStyles;
type TableStyleKeys = keyof typeof tableStyles;

// Структура стилей
interface StyleCategory {
  [key: string]: string | { [key: string]: string };
}

// Utility функция
type CombineStylesInput = Array<string | undefined | false>;
```

**Риски и безопасность**

- **Минимальный риск**: статические константы без побочных эффектов
- **CSS conflicts**: возможны конфликты при неправильном объединении стилей
- **Maintenance**: изменения могут повлиять на множество компонентов
- **Tailwind dependency**: критически зависит от Tailwind CSS классов
- **Type safety**: const assertions обеспечивают безопасность типов

**Тесты / рекомендации по покрытию**

- **Style consistency тесты**: корректность всех CSS классов
- **Type safety тесты**: правильность TypeScript типов
- **CombineStyles тесты**: корректность объединения стилей
- **Integration тесты**: совместимость с компонентами
- **Tailwind compatibility тесты**: валидность Tailwind классов

**Оценка сложности (low/medium/high)**
**LOW** - статические константы с простой utility функцией

**TODO / Рефакторинг**

- ✅ Централизация стилей для устранения дублирования (Rule 20)
- ✅ Логичная группировка по функциональным категориям
- ✅ TypeScript const assertions для type safety
- ✅ Utility функция combineStyles для безопасного объединения
- ✅ Экспорт типов для type-safe использования
- ✅ Базирование на существующих design-tokens
- ✅ Подробная JSDoc документация назначения
- 💡 Рекомендация: добавить валидацию Tailwind классов
- 💡 Рекомендация: рассмотреть интеграцию с CSS-in-JS решениями
- 💡 Рекомендация: добавить темизацию для dark mode
- 💡 Рекомендация: создать автоматические тесты для всех стилей
