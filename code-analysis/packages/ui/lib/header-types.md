### Путь: packages/ui/src/lib/header-types.ts

**Краткое назначение (1 предложение)**
Определение TypeScript интерфейсов для контекста заголовка и пропсов compound компонентов.

**Подробное описание (3–6 предложений)**
Файл содержит два основных интерфейса для системы заголовка: HeaderContextValue для типизации контекста и HeaderCompoundProps для базовых пропсов compound компонентов. HeaderContextValue включает все необходимые свойства для управления состоянием заголовка: меню (isMenuOpen, onToggleMenu), локализация (currentLocale, onLocaleChange), аутентификация (isAuthenticated, userName, onSignIn, onSignOut) и локализованные подписи. Интерфейс labels содержит тексты для кнопок аутентификации с поддержкой полных и сокращенных версий для responsive дизайна. HeaderCompoundProps предоставляет базовую структуру для compound компонентов с поддержкой children и className. Все свойства опциональны, что обеспечивает гибкость использования в различных сценариях.

**Экспортируемые сущности / API (все на 100%)**

- `export interface HeaderContextValue` — интерфейс контекста заголовка
- `export interface HeaderCompoundProps` — базовые пропсы compound компонентов

**Входы (expected inputs) / Параметры (все на 100%)**

- Это файл типов, не принимает параметры
- Определяет структуру данных для контекста и пропсов

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет типизацию для TypeScript компилятора
- Обеспечивает type safety для header компонентов
- Документирует ожидаемую структуру данных
- Поддерживает IntelliSense в IDE

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: header-helpers.tsx для типизации функций
- **Используется в**: header compound компонентах для пропсов
- **Используется в**: React Context для типизации значения
- **Часть**: типизации header системы UI пакета
- **Импортируется**: в компонентах заголовка для type safety

**Домен данных / типы (все на 100%)**

```typescript
// Контекст заголовка
interface HeaderContextValue {
  // Состояние меню
  isMenuOpen?: boolean;
  onToggleMenu?: () => void;

  // Локализация
  currentLocale?: string;
  onLocaleChange?: (locale: string) => void;

  // Аутентификация
  isAuthenticated?: boolean;
  userName?: string;
  onSignIn?: () => void;
  onSignOut?: () => void;

  // Локализованные подписи
  labels?: {
    signIn?: string;
    signOut?: string;
    signInShort?: string;
    signOutShort?: string;
  };
}

// Базовые пропсы compound компонентов
interface HeaderCompoundProps {
  children?: React.ReactNode;
  className?: string;
}

// Группы функциональности
type MenuState = Pick<HeaderContextValue, 'isMenuOpen' | 'onToggleMenu'>;
type LocaleState = Pick<HeaderContextValue, 'currentLocale' | 'onLocaleChange'>;
type AuthState = Pick<
  HeaderContextValue,
  'isAuthenticated' | 'userName' | 'onSignIn' | 'onSignOut'
>;
type LabelState = Pick<HeaderContextValue, 'labels'>;
```

**Риски и безопасность**

- **Минимальный риск**: файл только с определениями типов
- **Type safety**: обеспечивает безопасность типов для header системы
- **Interface evolution**: изменения могут нарушить совместимость
- **Optional properties**: все свойства опциональны, может потребовать runtime проверок
- **Callback types**: не валидирует корректность callback функций

**Тесты / рекомендации по покрытию**

- **Type checking тесты**: корректность TypeScript типизации
- **Interface compatibility тесты**: совместимость с использующими компонентами
- **Optional properties тесты**: правильность обработки undefined значений
- **Callback signature тесты**: корректность типов callback функций

**Оценка сложности (low/medium/high)**
**LOW** - простые определения интерфейсов без логики

**TODO / Рефакторинг**

- ✅ Полная типизация контекста заголовка
- ✅ Логичная группировка свойств по функциональности
- ✅ Поддержка локализации через labels интерфейс
- ✅ Опциональные свойства для гибкости использования
- ✅ Базовые пропсы для compound компонентов
- ✅ Responsive поддержка через short/full labels
- 💡 Рекомендация: добавить JSDoc комментарии для лучшей документации
- 💡 Рекомендация: рассмотреть добавление generic типов для расширяемости
- 💡 Рекомендация: добавить utility types для частичного использования
- 💡 Рекомендация: рассмотреть добавление validation схем для runtime проверок
