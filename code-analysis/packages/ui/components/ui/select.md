### Путь: packages/ui/src/components/ui/select.tsx

**Краткое назначение (1 предложение)**
Компонент выпадающего списка на основе Radix UI Select с кастомными стилями и поддержкой размеров.

**Подробное описание (3–6 предложений)**
Компонент реализует полнофункциональный выпадающий список через композицию Radix UI Select примитивов с кастомными стилями и дополнительной функциональностью. Включает все необходимые части: Select (корневой), SelectTrigger с поддержкой размеров (sm/default), SelectContent с анимациями и позиционированием, SelectItem с индикатором выбора. Использует data-slot атрибуты для идентификации компонентов и поддерживает accessibility через ARIA атрибуты и keyboard navigation. Интегрирован с дизайн-системой через CSS переменные (border-input, bg-popover, text-muted-foreground) и включает scroll кнопки для длинных списков. Предоставляет дополнительные компоненты: SelectGroup для группировки, SelectLabel для заголовков, SelectSeparator для разделителей.

**Экспортируемые сущности / API (все на 100%)**

- `export { Select }` — корневой компонент выпадающего списка
- `export { SelectTrigger }` — кнопка-триггер с поддержкой размеров
- `export { SelectContent }` — контейнер содержимого с анимациями
- `export { SelectItem }` — элемент списка с индикатором выбора
- `export { SelectValue }` — отображение выбранного значения
- `export { SelectGroup, SelectLabel, SelectSeparator }` — вспомогательные компоненты
- `export { SelectScrollUpButton, SelectScrollDownButton }` — кнопки прокрутки

**Входы (expected inputs) / Параметры (все на 100%)**

- `SelectTrigger`: `size?: 'sm' | 'default'` — размер триггера
- `SelectContent`: `position?: 'popper' | 'item-aligned'` — позиционирование контента
- Все компоненты принимают стандартные React props и className для кастомизации
- Поддерживает все Radix UI Select props (value, onValueChange, disabled, etc.)

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит доступный выпадающий список с keyboard navigation
- Применяет анимации открытия/закрытия через CSS transitions
- Использует Portal для рендеринга контента вне DOM иерархии
- Поддерживает scroll для длинных списков с кнопками прокрутки
- Отображает индикатор выбранного элемента (CheckIcon)

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: формах обмена, аутентификации и других компонентах с выбором
- **Импортирует**: Radix UI Select примитивы для базовой функциональности
- **Использует иконки**: CheckIcon, ChevronDownIcon, ChevronUpIcon из lucide-react
- **Использует утилиты**: cn из lib/utils для объединения CSS классов
- **Часть**: дизайн-системы UI пакета для переиспользования

**Домен данных / типы (все на 100%)**

```typescript
// Размеры триггера
type TriggerSize = 'sm' | 'default';

// Позиционирование контента
type ContentPosition = 'popper' | 'item-aligned';

// Пропсы компонентов (наследуют от Radix UI)
interface SelectTriggerProps extends React.ComponentProps<typeof SelectPrimitive.Trigger> {
  size?: TriggerSize;
}

interface SelectContentProps extends React.ComponentProps<typeof SelectPrimitive.Content> {
  position?: ContentPosition;
}

// Data-slot атрибуты для идентификации
type DataSlot =
  | 'select'
  | 'select-trigger'
  | 'select-content'
  | 'select-item'
  | 'select-value'
  | 'select-group'
  | 'select-label'
  | 'select-separator';
```

**Риски и безопасность**

- **Accessibility**: критически зависит от правильной реализации Radix UI
- **Performance**: Portal рендеринг может влиять на производительность
- **Styling conflicts**: сложные CSS селекторы могут конфликтовать с глобальными стилями
- **Keyboard navigation**: должен правильно обрабатывать все keyboard events
- **Screen readers**: зависит от правильных ARIA атрибутов от Radix UI

**Тесты / рекомендации по покрытию**

- **Accessibility тесты**: keyboard navigation, screen reader support
- **Size variants тесты**: корректность отображения sm и default размеров
- **Animation тесты**: правильность анимаций открытия/закрытия
- **Scroll тесты**: функциональность scroll кнопок для длинных списков
- **Selection тесты**: корректность выбора и отображения индикатора

**Оценка сложности (low/medium/high)**
**MEDIUM** - сложная композиция Radix UI примитивов с кастомными стилями

**TODO / Рефакторинг**

- ✅ Полная композиция всех необходимых Select компонентов
- ✅ Поддержка размеров триггера (sm/default)
- ✅ Интеграция с дизайн-системой через CSS переменные
- ✅ Data-slot атрибуты для идентификации компонентов
- ✅ Анимации и transitions для улучшения UX
- ✅ Scroll кнопки для длинных списков
- ✅ Accessibility через Radix UI примитивы
- 💡 Рекомендация: добавить поддержку loading состояния
- 💡 Рекомендация: рассмотреть добавление search функциональности
- 💡 Рекомендация: добавить поддержку multiple selection
