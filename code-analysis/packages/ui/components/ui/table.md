### Путь: packages/ui/src/components/ui/table.tsx

**Краткое назначение (1 предложение)**
Набор компонентов для создания стилизованных таблиц с поддержкой responsive дизайна и интеграцией с дизайн-системой.

**Подробное описание (3–6 предложений)**
Компонент предоставляет полный набор элементов таблицы: Table (контейнер с overflow), TableHeader/TableBody/TableFooter для структуры, TableRow для строк с hover эффектами, TableHead/TableCell для ячеек с правильным выравниванием. Использует data-slot атрибуты для идентификации компонентов и интегрируется с дизайн-системой через CSS переменные (bg-muted, text-foreground, border-b). Включает responsive поддержку через overflow-x-auto в контейнере и специальную обработку checkbox элементов с правильным позиционированием. Поддерживает состояния строк через data-state атрибуты и применяет hover эффекты для улучшения UX. TableFooter имеет отличительный стиль с фоном и жирным шрифтом для итоговых данных.

**Экспортируемые сущности / API (все на 100%)**

- `export { Table }` — основной контейнер таблицы с responsive wrapper
- `export { TableHeader }` — заголовок таблицы (thead)
- `export { TableBody }` — тело таблицы (tbody)
- `export { TableFooter }` — подвал таблицы (tfoot) с особым стилем
- `export { TableRow }` — строка таблицы с hover эффектами
- `export { TableHead }` — ячейка заголовка (th)
- `export { TableCell }` — обычная ячейка (td)
- `export { TableCaption }` — подпись таблицы

**Входы (expected inputs) / Параметры (все на 100%)**

- Все компоненты принимают стандартные HTML атрибуты соответствующих элементов
- `className` для кастомизации стилей во всех компонентах
- `TableRow` поддерживает `data-state="selected"` для выделения выбранных строк
- Поддержка checkbox элементов с автоматическим позиционированием

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит семантически правильную HTML таблицу
- Применяет responsive overflow для горизонтальной прокрутки
- Добавляет hover эффекты на строки для интерактивности
- Использует data-slot атрибуты для идентификации компонентов
- Поддерживает выделение выбранных строк через data-state

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: DataTable компонентах, admin панелях, списках данных
- **Использует**: cn из lib/utils для объединения CSS классов
- **Интегрируется**: с дизайн-системой через CSS переменные
- **Поддерживает**: checkbox компоненты с правильным позиционированием
- **Часть**: UI пакета для отображения табличных данных

**Домен данных / типы (все на 100%)**

```typescript
// Все компоненты используют стандартные HTML типы
type TableProps = React.ComponentProps<'table'>;
type TableHeaderProps = React.ComponentProps<'thead'>;
type TableBodyProps = React.ComponentProps<'tbody'>;
type TableFooterProps = React.ComponentProps<'tfoot'>;
type TableRowProps = React.ComponentProps<'tr'>;
type TableHeadProps = React.ComponentProps<'th'>;
type TableCellProps = React.ComponentProps<'td'>;
type TableCaptionProps = React.ComponentProps<'caption'>;

// Data-slot атрибуты для идентификации
type TableDataSlot =
  | 'table-container'
  | 'table'
  | 'table-header'
  | 'table-body'
  | 'table-footer'
  | 'table-row'
  | 'table-head'
  | 'table-cell'
  | 'table-caption';

// Состояния строк
type RowState = 'selected' | undefined;
```

**Риски и безопасность**

- **Accessibility**: должен поддерживать screen readers и keyboard navigation
- **Performance**: большие таблицы могут влиять на производительность рендеринга
- **Responsive**: overflow может скрывать важный контент на мобильных устройствах
- **Styling conflicts**: сложные CSS селекторы могут конфликтовать с глобальными стилями
- **Checkbox positioning**: специальная логика позиционирования может ломаться при изменениях

**Тесты / рекомендации по покрытию**

- **Accessibility тесты**: правильность семантики HTML таблицы
- **Responsive тесты**: корректность overflow и прокрутки на разных экранах
- **Hover effects тесты**: правильность интерактивных эффектов
- **Checkbox integration тесты**: корректность позиционирования checkbox элементов
- **State management тесты**: правильность обработки data-state="selected"

**Оценка сложности (low/medium/high)**
**LOW** - простые wrapper компоненты над HTML элементами

**TODO / Рефакторинг**

- ✅ Полный набор компонентов таблицы
- ✅ Responsive дизайн с overflow-x-auto
- ✅ Интеграция с дизайн-системой через CSS переменные
- ✅ Data-slot атрибуты для идентификации
- ✅ Hover эффекты и состояния строк
- ✅ Специальная обработка checkbox элементов
- ✅ Семантически правильная HTML структура
- 💡 Рекомендация: добавить поддержку сортировки в заголовках
- 💡 Рекомендация: рассмотреть добавление sticky headers
- 💡 Рекомендация: добавить поддержку loading состояния
- 💡 Рекомендация: улучшить accessibility с ARIA атрибутами
