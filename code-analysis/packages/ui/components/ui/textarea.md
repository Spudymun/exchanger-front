### Путь: packages/ui/src/components/ui/textarea.tsx

**Краткое назначение (1 предложение)**
Стилизованный компонент textarea с поддержкой валидации, focus состояний и интеграцией с дизайн-системой.

**Подробное описание (3–6 предложений)**
Компонент реализует textarea элемент с комплексной стилизацией включая border, focus ring, validation состояния и responsive типографику. Использует field-sizing-content для автоматического изменения размера в зависимости от содержимого и устанавливает минимальную высоту в 16 единиц (4rem). Интегрирован с дизайн-системой через CSS переменные для цветов (border-input, text-muted-foreground, ring-destructive) и поддерживает темную тему. Включает validation состояния через aria-invalid атрибут с соответствующими визуальными индикаторами (красная граница и ring). Поддерживает disabled состояние с пониженной прозрачностью и отключенным курсором, а также responsive типографику (text-base на мобильных, text-sm на desktop).

**Экспортируемые сущности / API (все на 100%)**

- `export const Textarea` — основной компонент textarea с ref forwarding
- `export interface TextareaProps` — интерфейс пропсов компонента

**Входы (expected inputs) / Параметры (все на 100%)**

- Все стандартные HTML textarea атрибуты (value, onChange, placeholder, disabled, etc.)
- `className?: string` — дополнительные CSS классы для кастомизации
- `ref` — React ref для доступа к DOM элементу

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит стилизованный textarea элемент
- Применяет focus ring при фокусе с transition эффектами
- Показывает validation состояния через aria-invalid
- Автоматически изменяет размер с field-sizing-content
- Поддерживает disabled состояние с визуальными индикаторами

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: формах, комментариях, описаниях, многострочных полях ввода
- **Использует**: cn из lib/utils для объединения CSS классов
- **Интегрируется**: с дизайн-системой через CSS переменные
- **Поддерживает**: form validation через aria-invalid атрибут
- **Часть**: UI пакета для ввода многострочного текста

**Домен данных / типы (все на 100%)**

```typescript
// Интерфейс пропсов
interface TextareaProps extends React.ComponentProps<'textarea'> {
  className?: string;
}

// Стандартные HTML textarea атрибуты
type StandardTextareaProps = {
  value?: string;
  defaultValue?: string;
  onChange?: (event: React.ChangeEvent<HTMLTextAreaElement>) => void;
  placeholder?: string;
  disabled?: boolean;
  readOnly?: boolean;
  required?: boolean;
  rows?: number;
  cols?: number;
  maxLength?: number;
  minLength?: number;
  'aria-invalid'?: boolean;
};

// Data-slot для идентификации
const DATA_SLOT = 'textarea';
```

**Риски и безопасность**

- **Input validation**: должен использоваться с proper validation на форме
- **XSS protection**: не выполняет sanitization введенного текста
- **Accessibility**: зависит от правильного использования labels и aria атрибутов
- **Performance**: field-sizing-content может влиять на производительность при большом тексте
- **Browser support**: field-sizing-content может не поддерживаться в старых браузерах

**Тесты / рекомендации по покрытию**

- **Input handling тесты**: корректность onChange и value обработки
- **Validation тесты**: правильность отображения aria-invalid состояний
- **Focus тесты**: корректность focus ring и transition эффектов
- **Disabled state тесты**: правильность disabled состояния
- **Responsive тесты**: корректность типографики на разных экранах

**Оценка сложности (low/medium/high)**
**LOW** - простой wrapper над HTML textarea с стилизацией

**TODO / Рефакторинг**

- ✅ Ref forwarding для доступа к DOM элементу
- ✅ Интеграция с дизайн-системой через CSS переменные
- ✅ Поддержка validation состояний через aria-invalid
- ✅ Responsive типографика (base на мобильных, sm на desktop)
- ✅ Field-sizing-content для автоматического изменения размера
- ✅ Disabled состояние с визуальными индикаторами
- ✅ Data-slot атрибут для идентификации
- 💡 Рекомендация: добавить поддержку размеров (sm, default, lg)
- 💡 Рекомендация: рассмотреть добавление character counter
- 💡 Рекомендация: добавить поддержку resize variants (none, vertical, horizontal)
- 💡 Рекомендация: улучшить browser compatibility для field-sizing-content
