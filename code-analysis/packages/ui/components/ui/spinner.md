### Путь: packages/ui/src/components/ui/spinner.tsx

**Краткое назначение (1 предложение)**
Набор компонентов спиннеров для индикации загрузки с различными размерами, вариантами и режимами отображения.

**Подробное описание (3–6 предложений)**
Компонент предоставляет три типа спиннеров: базовый Spinner с настраиваемыми размерами и цветовыми вариантами, SpinnerOverlay для полноэкранной загрузки с backdrop, и InlineSpinner для использования в кнопках и мелких компонентах. Использует class-variance-authority для типобезопасного управления вариантами стилей с поддержкой размеров (sm, default, lg, xl) и цветовых схем (default, secondary, muted, accent, destructive, success, warning, info). Включает accessibility поддержку через role="status", aria-label и sr-only текст для screen readers. Реализует условный рендеринг через параметр show и поддерживает кастомизацию через className и стандартные HTML атрибуты. SpinnerOverlay добавляет backdrop с настраиваемой прозрачностью и возможностью отображения дополнительного контента под спиннером.

**Экспортируемые сущности / API (все на 100%)**

- `export { Spinner }` — базовый спиннер с размерами и вариантами
- `export { SpinnerOverlay }` — полноэкранный спиннер с backdrop
- `export { InlineSpinner }` — компактный спиннер для inline использования
- `export { spinnerVariants, overlayVariants, inlineSpinnerVariants }` — CVA варианты для кастомизации

**Входы (expected inputs) / Параметры (все на 100%)**

- `Spinner`: `size?: 'sm' | 'default' | 'lg' | 'xl'`, `variant?: 'default' | 'secondary' | 'muted' | 'accent' | 'destructive' | 'success' | 'warning' | 'info'`, `show?: boolean`, `srText?: string`
- `SpinnerOverlay`: все параметры Spinner + `overlayOpacity?: 'light' | 'medium' | 'heavy'`, `content?: React.ReactNode`
- `InlineSpinner`: `size?: 'xs' | 'sm' | 'base'` + остальные параметры Spinner кроме размеров

**Выходы / Побочные эффекты (все на 100%)**

- Рендерит анимированный спиннер с CSS animation (animate-spin)
- Применяет accessibility атрибуты (role="status", aria-label)
- SpinnerOverlay создает fixed overlay с backdrop-blur эффектом
- Условный рендеринг: возвращает null при show=false
- Поддерживает кастомизацию через className и HTML атрибуты

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: кнопках, формах, страницах загрузки, API запросах
- **Импортирует**: cva, VariantProps из class-variance-authority для вариантов
- **Использует**: cn из lib/utils для объединения CSS классов
- **Использует**: React.forwardRef для ref forwarding
- **Часть**: дизайн-системы UI пакета для индикации состояний загрузки

**Домен данных / типы (все на 100%)**

```typescript
// Размеры базового спиннера
type SpinnerSize = 'sm' | 'default' | 'lg' | 'xl';

// Цветовые варианты
type SpinnerVariant =
  | 'default'
  | 'secondary'
  | 'muted'
  | 'accent'
  | 'destructive'
  | 'success'
  | 'warning'
  | 'info';

// Прозрачность overlay
type OverlayOpacity = 'light' | 'medium' | 'heavy';

// Размеры inline спиннера
type InlineSpinnerSize = 'xs' | 'sm' | 'base';

// Интерфейсы компонентов
interface SpinnerProps
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof spinnerVariants> {
  show?: boolean;
  srText?: string;
}

interface SpinnerOverlayProps extends Omit<SpinnerProps, 'content'> {
  overlayOpacity?: OverlayOpacity;
  content?: React.ReactNode;
}

interface InlineSpinnerProps extends Omit<SpinnerProps, 'size'> {
  size?: InlineSpinnerSize;
}
```

**Риски и безопасность**

- **Accessibility**: критически важны правильные ARIA атрибуты и sr-only текст
- **Performance**: анимации могут влиять на производительность на слабых устройствах
- **Overlay z-index**: SpinnerOverlay использует z-50, может конфликтовать с другими overlay
- **Animation preferences**: не учитывает prefers-reduced-motion для пользователей с ограничениями
- **Memory leaks**: длительные анимации могут вызывать утечки памяти

**Тесты / рекомендации по покрытию**

- **Accessibility тесты**: правильность ARIA атрибутов и screen reader support
- **Variant тесты**: корректность всех размеров и цветовых вариантов
- **Conditional rendering тесты**: правильность show/hide логики
- **Overlay тесты**: функциональность backdrop и z-index
- **Animation тесты**: корректность CSS анимаций

**Оценка сложности (low/medium/high)**
**MEDIUM** - множественные варианты с CVA и accessibility требованиями

**TODO / Рефакторинг**

- ✅ Три типа спиннеров для разных use cases
- ✅ CVA для типобезопасного управления вариантами
- ✅ Accessibility поддержка через ARIA атрибуты
- ✅ Условный рендеринг через show параметр
- ✅ Ref forwarding для всех компонентов
- ✅ Кастомизация через className и HTML атрибуты
- ✅ SpinnerOverlay с backdrop и дополнительным контентом
- 💡 Рекомендация: добавить поддержку prefers-reduced-motion
- 💡 Рекомендация: рассмотреть добавление progress индикатора
- 💡 Рекомендация: добавить timeout для автоматического скрытия
- 💡 Рекомендация: оптимизировать анимации для производительности
