### Путь: packages/ui/src/types/auth-fields.ts

**Краткое назначение (1 предложение)**
Централизованные TypeScript типы для полей аутентификации, устраняющие дублирование интерфейсов в auth компонентах.

**Подробное описание (3–6 предложений)**
Модуль предоставляет набор базовых и специализированных интерфейсов для типизации полей форм аутентификации согласно Rule 20 (запрет избыточности кода). Включает базовые интерфейсы для различных типов полей: EmailFormFields, PasswordFormFields, ConfirmPasswordFormFields, CaptchaFormFields, каждый из которых расширяет Record<string, unknown> для гибкости. Определяет BaseAuthFieldProps как общий интерфейс для всех auth полей с обязательными свойствами: form (UseFormReturn), isLoading, локализация (t), и fieldId для идентификации. Создает специализированные типы для каждого поля через generic параметры, обеспечивая type safety при использовании в auth компонентах. Устраняет избыточность через удаление captchaVerified поля и централизацию всех auth типов в одном месте.

**Экспортируемые сущности / API (все на 100%)**

- `export interface EmailFormFields` — интерфейс для полей с email
- `export interface PasswordFormFields` — интерфейс для полей с паролем
- `export interface ConfirmPasswordFormFields` — интерфейс для подтверждения пароля
- `export interface CaptchaFormFields` — интерфейс для полей с капчей
- `export interface BaseAuthFieldProps<T>` — базовый интерфейс для всех auth полей
- `export type AuthEmailFieldProps<T>` — тип для email поля
- `export type AuthPasswordFieldProps<T>` — тип для password поля
- `export type AuthConfirmPasswordFieldProps<T>` — тип для confirm password поля
- `export type AuthCaptchaFieldProps<T>` — тип для captcha поля

**Входы (expected inputs) / Параметры (все на 100%)**

- Это файл типов, не принимает параметры
- Generic параметры T для расширения базовых интерфейсов
- UseFormReturn из @repo/hooks для типизации формы

**Выходы / Побочные эффекты (все на 100%)**

- Предоставляет типизацию для TypeScript компилятора
- Обеспечивает type safety для auth компонентов
- Устраняет дублирование интерфейсов
- Поддерживает IntelliSense в IDE

**Взаимосвязи (кто вызывает / что вызывает) (все на 100%)**

- **Используется в**: auth компонентах (AuthEmailField, AuthPasswordField, etc.)
- **Импортирует**: UseFormReturn из @repo/hooks для типизации формы
- **Часть**: централизованной системы типов UI пакета
- **Решает**: Rule 20 нарушения (избыточность интерфейсов)
- **Обеспечивает**: type safety для всех auth полей

**Домен данных / типы (все на 100%)**

```typescript
// Базовые интерфейсы полей
interface EmailFormFields extends Record<string, unknown> {
  email: string;
}

interface PasswordFormFields extends Record<string, unknown> {
  password: string;
}

interface ConfirmPasswordFormFields extends Record<string, unknown> {
  confirmPassword: string;
}

interface CaptchaFormFields extends Record<string, unknown> {
  captcha: string;
}

// Базовый интерфейс для всех auth полей
interface BaseAuthFieldProps<T extends Record<string, unknown>> {
  form: UseFormReturn<T>;
  isLoading: boolean;
  t: (key: string) => string;
  fieldId: string;
}

// Специализированные типы с generic параметрами
type AuthEmailFieldProps<T extends EmailFormFields = EmailFormFields> = BaseAuthFieldProps<T>;
type AuthPasswordFieldProps<T extends PasswordFormFields = PasswordFormFields> =
  BaseAuthFieldProps<T>;
type AuthConfirmPasswordFieldProps<
  T extends ConfirmPasswordFormFields = ConfirmPasswordFormFields,
> = BaseAuthFieldProps<T>;
type AuthCaptchaFieldProps<T extends CaptchaFormFields = CaptchaFormFields> = BaseAuthFieldProps<T>;
```

**Риски и безопасность**

- **Минимальный риск**: файл только с определениями типов
- **Type safety**: обеспечивает безопасность типов для auth системы
- **Generic complexity**: сложные generic типы могут быть трудны для понимания
- **Dependency coupling**: зависит от UseFormReturn из @repo/hooks
- **Interface evolution**: изменения могут нарушить совместимость

**Тесты / рекомендации по покрытию**

- **Type checking тесты**: корректность TypeScript типизации
- **Generic constraints тесты**: правильность ограничений generic параметров
- **Interface compatibility тесты**: совместимость с auth компонентами
- **UseFormReturn integration тесты**: корректность интеграции с hooks

**Оценка сложности (low/medium/high)**
**LOW** - определения типов с простыми generic параметрами

**TODO / Рефакторинг**

- ✅ Централизация всех auth типов в одном месте (Rule 20)
- ✅ Устранение избыточности (удалено captchaVerified поле)
- ✅ Базовый интерфейс BaseAuthFieldProps для всех полей
- ✅ Generic параметры для гибкости и расширяемости
- ✅ Правильная типизация через UseFormReturn
- ✅ Расширение Record<string, unknown> для гибкости
- ✅ JSDoc документация с объяснением Rule 20
- 💡 Рекомендация: добавить utility types для комбинирования интерфейсов
- 💡 Рекомендация: рассмотреть добавление validation типов
- 💡 Рекомендация: добавить примеры использования в комментариях
- 💡 Рекомендация: рассмотреть создание branded types для большей безопасности
