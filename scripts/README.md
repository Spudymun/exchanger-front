# 🧪 Load Testing Scripts

Набор скриптов для нагрузочного тестирования системы создания заявок.

## 🚀 Основной скрипт

### `load-test-concurrent.mjs` ⭐ РЕКОМЕНДУЕТСЯ

```bash
node scripts/load-test-concurrent.mjs
```

- ✅ **100% рабочий** - протестировано и работает стабильно
- ✅ **USDT только** - без конфликтов валидации
- ✅ **10 concurrent заявок** - реальное нагрузочное тестирование
- ✅ **Автоматическая подготовка БД** - db:reset + db:seeds
- ✅ **Валидные карты Луна** - реалистичные украинские банки
- ✅ **Проверка результатов в БД** - подтверждение сохранения

**Результат:** 10/10 заявок успешно созданы, 5.38 секунд

## 🔧 Утилиты

### Проверка готовности системы

```bash
# API Health Check
curl http://localhost:3000/api/health | jq

# Bull Board Dashboard (визуальный мониторинг)
npm run dev:bull-board
# Открыть: http://localhost:3010

# Docker контейнеры
docker stats exchanger-postgres exchanger-redis

# Database GUI
npm run db:studio
# Открыть: http://localhost:5555
```

### `validate-cards.mjs`

```bash
node scripts/validate-cards.mjs
```

Генератор валидных карт с алгоритмом Луна для украинских банков.

## 📚 Документация

См. `docs/load-testing/` для подробной документации:

- `ORDER_SYSTEM_LOAD_TESTING_GUIDE.md` - полное руководство
- `QUICK_START.md` - быстрый старт

## ⚠️ Конфигурация

**Rate Limits:** В `packages/constants/src/rate-limits.ts` временно увеличены лимиты для тестирования (`CREATE_ORDER: 100 points/hour`). В продакшене рекомендуется вернуть к разумным значениям.

## 🎯 Быстрый запуск

```bash
# 1. Проверка системы
curl http://localhost:3000/api/health | jq
npm run dev:bull-board  # Визуальный мониторинг (опционально)

# 2. Запуск тестирования
node scripts/load-test-concurrent.mjs

# 3. Анализ результатов в консоли + проверка БД
```

Готово! 🎉
