# 🤖 Telegram Bot Testing Tools

Профессиональные инструменты для тестирования Telegram Bot API через webhook симуляцию.

## 📦 Компоненты

### 1. **test-telegram-bot.ps1** (Главный скрипт)

PowerShell скрипт для симуляции клиентских и операторских взаимодействий с Telegram ботом.

**Возможности:**

- ✅ Интерактивный режим с меню
- ✅ Предопределенные тестовые сценарии
- ✅ Произвольные команды и сообщения
- ✅ Rate limiting тестирование
- ✅ Детальное логирование
- ✅ Проверка доступности бота

### 2. **TelegramBotTester.psm1** (PowerShell модуль)

Переиспользуемый модуль с helper функциями для создания Telegram updates.

**Функции:**

- `New-TelegramTextUpdate` - создание текстовых сообщений
- `New-TelegramCallbackUpdate` - создание callback queries
- `Send-TelegramWebhook` - отправка webhook запросов
- `Test-TelegramBotAvailability` - проверка доступности бота
- Готовые функции для типовых сценариев

### 3. **test-scenarios.json** (Тестовые сценарии)

JSON файл с готовыми тестовыми сценариями и ожидаемыми результатами.

---

## 🚀 Быстрый старт

### Запуск бота

```powershell
# Terminal 1: Запустить telegram-bot
cd e:\project\kiro\exchanger-front\apps\telegram-bot
pnpm dev
```

### Интерактивный режим (рекомендуется)

```powershell
# Terminal 2: Запустить тесты
cd e:\project\kiro\exchanger-front
.\scripts\test-telegram-bot.ps1 -Interactive
```

**Меню:**

1. Client /start - клиент открывает бота
2. Client message - клиент пишет в поддержку
3. Operator /start - оператор открывает бота
4. Rate limit test - проверка лимитов
5. Custom command - произвольная команда
6. Custom message - произвольное сообщение

---

## 📖 Примеры использования

### Пример 1: Симуляция клиента

```powershell
# Клиент отправляет /start
.\scripts\test-telegram-bot.ps1 -Scenario client-start

# Клиент отправляет сообщение в поддержку
.\scripts\test-telegram-bot.ps1 `
    -Scenario client-message `
    -Message "Не пришла криптовалюта на кошелек"
```

### Пример 2: Симуляция оператора

```powershell
# Оператор открывает бота (используется ID из .env)
.\scripts\test-telegram-bot.ps1 -Scenario operator-start

# Произвольный оператор
.\scripts\test-telegram-bot.ps1 `
    -Scenario operator-start `
    -UserId 621882329 `
    -Username "my_operator"
```

### Пример 3: Rate limiting тест

```powershell
# Отправка 6 сообщений подряд (лимит: 5/мин)
.\scripts\test-telegram-bot.ps1 -Scenario rate-limit
```

### Пример 4: Произвольная команда

```powershell
# Произвольная команда
.\scripts\test-telegram-bot.ps1 `
    -Scenario custom `
    -Command "/help" `
    -UserId 123456789 `
    -Username "test_user"

# Произвольное сообщение
.\scripts\test-telegram-bot.ps1 `
    -Scenario custom `
    -Message "Тестовое сообщение" `
    -UserId 123456789
```

### Пример 5: Детальный вывод

```powershell
# С детальными логами HTTP запросов/ответов
.\scripts\test-telegram-bot.ps1 `
    -Scenario client-message `
    -Message "Тест с логами" `
    -Verbose
```

---

## 🔧 Использование модуля напрямую

Для продвинутых сценариев можно использовать PowerShell модуль напрямую:

```powershell
# Импорт модуля
Import-Module .\scripts\telegram-bot\TelegramBotTester.psm1

# Проверка доступности бота
$status = Test-TelegramBotAvailability -BotUrl "http://localhost:3003"
if ($status.Available) {
    Write-Host "✅ Bot available"
}

# Создание и отправка client update
$update = New-ClientMessageUpdate -Message "Помогите с заявкой" -UserId 555555555
$result = $update | Send-TelegramWebhook -BotUrl "http://localhost:3003" -ShowResponse

# Создание operator update
$operatorUpdate = New-OperatorLoginUpdate -OperatorId 621882329 -Username "operator_1"
$operatorUpdate | Send-TelegramWebhook -ShowResponse

# Batch отправка (несколько updates)
$updates = @(
    (New-ClientMessageUpdate -Message "Сообщение 1" -UserId 111111111),
    (New-ClientMessageUpdate -Message "Сообщение 2" -UserId 222222222),
    (New-ClientMessageUpdate -Message "Сообщение 3" -UserId 333333333)
)

$batchResult = Send-TelegramWebhookBatch -Updates $updates -DelayMilliseconds 500
Write-Host "Успешно: $($batchResult.Success) из $($batchResult.Total)"
```

---

## 📊 Тестовые сценарии из JSON

Файл `test-scenarios.json` содержит готовые сценарии:

1. **client-basic-flow** - полный flow клиента с rate limit
2. **operator-basic-flow** - полный flow оператора
3. **client-attempts-operator-commands** - попытки клиента использовать операторские команды
4. **support-message-variations** - различные типы обращений в поддержку
5. **rate-limit-recovery** - восстановление после rate limit
6. **operator-takeorder-flow** - взятие заявки оператором
7. **concurrent-clients** - одновременные обращения нескольких клиентов

---

## ✅ Ожидаемые результаты

### Client flow

**Команда `/start`:**

```
👋 Добро пожаловать в службу поддержки ExchangeGO!

Опишите вашу проблему или задайте вопрос.
Наши операторы ответят вам в ближайшее время.
```

**Текстовое сообщение:**

```
✅ Ваше сообщение получено!

Оператор свяжется с вами в ближайшее время.
Среднее время ответа: 1-3 часа
```

**Операторы получают:**

```
💬 **Новое обращение клиента в поддержку**

👤 Пользователь: @test_user
📱 Telegram ID: `555555555`

💬 Сообщение:
Не пришла криптовалюта

ℹ️ Ответьте клиенту в личных сообщениях Telegram
```

### Operator flow

**Команда `/start`:**

```
Добро пожаловать в ExchangeGO Bot! 👋

Я помогаю операторам управлять заявками.

Доступные команды:
/start - Начать работу с ботом
/help - Показать справку
/login - Войти как оператор
/takeorder - Взять заявку в работу
/orders - Показать активные заявки
```

**Команда `/login`:**

```
✅ Вы вошли как оператор!

Теперь доступны операторские команды:
• /takeorder - взять заявку в работу
• /orders - показать активные заявки
```

### Rate limiting

После 5 сообщений в минуту:

```
⚠️ Слишком много сообщений

Пожалуйста, подождите минуту перед отправкой следующего сообщения.
Лимит: 5 сообщений в минуту
```

---

## 🔍 Проверка результатов

### 1. Логи telegram-bot (Terminal 1)

Смотреть вывод:

```log
CLIENT_MESSAGE_RECEIVED { userId: 555555555, username: '@test_user', messageLength: 25 }
OPERATOR_NOTIFIED_CLIENT_MESSAGE { operatorId: '621882329', clientUserId: 555555555 }
CLIENT_MESSAGE_FORWARDED { userId: 555555555, operatorsNotified: 2, totalOperators: 2 }
```

### 2. Telegram операторов

Откройте Telegram с аккаунтами операторов (ID: 621882329, 8068430102) и проверьте уведомления.

### 3. HTTP ответы

С флагом `-Verbose` или в интерактивном режиме вы увидите полные HTTP запросы и ответы.

---

## 🐛 Troubleshooting

### Проблема: "Бот недоступен"

**Решение:**

```powershell
# Проверить что бот запущен
cd e:\project\kiro\exchanger-front\apps\telegram-bot
pnpm dev

# Проверить порт
netstat -ano | findstr :3003
```

### Проблема: "Rate limit не работает"

**Проверка:**

```powershell
# Запустить rate limit тест
.\scripts\test-telegram-bot.ps1 -Scenario rate-limit

# Смотреть логи бота - должно быть:
# CLIENT_RATE_LIMIT_EXCEEDED { userId: X, messageCount: 5, windowMs: 60000 }
```

### Проблема: "Операторы не получают уведомления"

**Проверка:**

1. Убедитесь что operator ID в `.env`:

   ```
   AUTHORIZED_TELEGRAM_OPERATORS=621882329,8068430102
   ```

2. Проверьте логи:

   ```log
   OPERATOR_NOTIFIED_CLIENT_MESSAGE { operatorId: '621882329', clientUserId: X }
   ```

3. Проверьте `TELEGRAM_BOT_TOKEN` в `.env`

### Проблема: "Модуль не найден"

**Решение:**

```powershell
# Убедитесь что вы в корне проекта
cd e:\project\kiro\exchanger-front

# Проверьте путь к модулю
Test-Path .\scripts\telegram-bot\TelegramBotTester.psm1

# Используйте абсолютный путь при импорте
Import-Module e:\project\kiro\exchanger-front\scripts\telegram-bot\TelegramBotTester.psm1
```

---

## 📈 Расширенное использование

### Автоматизация тестирования

```powershell
# Скрипт для CI/CD тестирования
$scenarios = @("client-start", "client-message", "operator-start")

foreach ($scenario in $scenarios) {
    Write-Host "Testing: $scenario"
    .\scripts\test-telegram-bot.ps1 -Scenario $scenario
    Start-Sleep -Seconds 2
}
```

### Нагрузочное тестирование

```powershell
Import-Module .\scripts\telegram-bot\TelegramBotTester.psm1

# Создать 10 клиентских сообщений
$updates = 1..10 | ForEach-Object {
    New-ClientMessageUpdate `
        -Message "Нагрузочное сообщение #$_" `
        -UserId (Get-Random -Minimum 100000000 -Maximum 999999999)
}

# Отправить batch с задержкой 200ms
$result = Send-TelegramWebhookBatch -Updates $updates -DelayMilliseconds 200
Write-Host "Результат: $($result.Success) успешных из $($result.Total)"
```

### Интеграция с мониторингом

```powershell
# Мониторинг доступности бота
while ($true) {
    $status = Test-TelegramBotAvailability

    if (-not $status.Available) {
        Write-Host "❌ Bot unavailable: $($status.Error)" -ForegroundColor Red
        # Send alert
    }
    else {
        Write-Host "✅ Bot available" -ForegroundColor Green
    }

    Start-Sleep -Seconds 30
}
```

---

## 📝 Структура файлов

```
scripts/
├── test-telegram-bot.ps1          # Главный скрипт
└── telegram-bot/
    ├── TelegramBotTester.psm1     # PowerShell модуль
    ├── test-scenarios.json         # Тестовые сценарии
    └── README.md                   # Эта документация
```

---

## 🔗 Связанные документы

- `docs/tasks/SUPPORT/TESTING_GUIDE.md` - Общий гайд по тестированию
- `docs/tasks/SUPPORT/CLIENT_SUPPORT_TELEGRAM_SENIOR_PLAN.md` - План реализации
- `apps/telegram-bot/.env` - Конфигурация бота

---

## 💡 Best Practices

1. **Всегда запускайте бота перед тестами**

   ```powershell
   cd apps/telegram-bot && pnpm dev
   ```

2. **Используйте интерактивный режим для исследования**

   ```powershell
   .\scripts\test-telegram-bot.ps1 -Interactive
   ```

3. **Проверяйте логи бота параллельно с тестами**

4. **Для operator тестов используйте реальные ID из `.env`**

5. **Rate limit тесты требуют ожидания между запусками (60 сек)**

6. **В production используйте реальные Telegram аккаунты для финального тестирования**

---

## 🎯 Roadmap

- [ ] GUI интерфейс для тестирования
- [ ] Автоматические тесты на основе JSON сценариев
- [ ] Интеграция с Playwright для E2E тестов
- [ ] Метрики и отчеты по результатам тестов
- [ ] Docker контейнер с тестовым окружением

---

**Версия:** 1.0.0  
**Дата:** October 9, 2025  
**Автор:** ExchangeGO Development Team
