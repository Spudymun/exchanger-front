# Глубокий анализ проблем пакета packages/utils

**Дата анализа:** 13 августа 2025  
**Аналитик:** AI Agent (следуя Rule 8 - запрет на предположения)  
**Цель:** Выявление всех архитектурных проблем для создания плана исправления

## Методология анализа

Анализ проводится по принципам:

- Rule 8: 100% уверенность в фактах, запрет на предположения
- Rule 20: Поиск всех видов избыточности кода
- Rule 2: Архитектурный анализ по логическим группам
- Rule 5: Контекстное понимание всей архитектуры

## ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА #1: Дублирование базовых схем валидации

**Местоположение:**

- `packages/utils/src/validation/schemas-basic.ts` (89 строк)
- `packages/utils/src/validation/schemas/basic.ts` (56 строк)

**Детали проблемы:**

**Анализ дублирования:**

1. **Полное функциональное дублирование:**
   - `emailSchema` - 2 разные реализации с разными подходами к сообщениям
   - `passwordSchema` - 2 КАРДИНАЛЬНО разные реализации regex
   - `legacyPasswordSchema` - разные минимальные длины (6 vs 8)
   - `usernameSchema` - разные regex паттерны (`[a-zA-Z0-9_]+` vs `[a-zA-Z0-9_-]+`)
   - `idSchema` - идентичные реализации

2. **Архитектурные противоречия:**
   - `schemas-basic.ts`: Использует собственные константы + хардкод сообщений
   - `schemas/basic.ts`: Использует `@repo/constants` + i18n подход
   - Два разных подхода к одной задаче в одном пакете

3. **Импорты и экспорты:**
   - `schemas-basic.ts` активно используется в 6 файлах
   - `schemas/basic.ts` НЕ используется нигде в коде
   - Файл `schemas/basic.ts` является **мертвым кодом**

**Вердикт:** Критическое нарушение Rule 20 - полное дублирование с мертвым кодом

---

### 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА #2: Функциональное дублирование форматирования

**Местоположение:** `packages/utils/src/formatting.ts`

**Детали проблемы:**
**Анализ дублирования:**

1. **Идентичная реализация:**

   ```typescript
   export function formatUahAmount(amount: number): string {
     return amount.toFixed(PERCENTAGE_CALCULATIONS.UAH_ROUNDING_PRECISION);
   }

   export function formatCommissionAmount(amount: number): string {
     return amount.toFixed(PERCENTAGE_CALCULATIONS.UAH_ROUNDING_PRECISION); // ИДЕНТИЧНО!
   }
   ```

2. **Комментарий признает дублирование:** "Format commission amount (same as UAH)"

3. **Использование в проекте:** Функции НЕ используются нигде в коде - **мертвый код**

4. **Архитектурная проблема:** Создание двух функций для одной операции нарушает принцип DRY

**Вердикт:** Нарушение Rule 20 - функциональное дублирование + мертвый код

---

### 🚨 ПРОБЛЕМА #3: Legacy система паролей

**Местоположение:** Множественные файлы
**Детали проблемы:**

1. **Противоречивые реализации legacy паролей:**
   - `schemas-basic.ts`: `min(6)` - использует `LEGACY_PASSWORD_MIN_LENGTH = 6`
   - `schemas/basic.ts`: `min(8)` - использует `VALIDATION_LIMITS.PASSWORD_MIN_LENGTH = 8`
   - **Разные минимальные требования для одной и той же схемы!**

2. **Дублирование констант:**
   - `schemas-basic.ts` определяет собственные константы
   - `@repo/constants` содержит централизованные константы
   - **Нарушение единого источника истины**

3. **Использование в проекте:**
   - `loginSchema` - использует legacy для входа
   - `changePasswordEnhancedSchema` - использует legacy для текущего пароля
   - `single-field.ts` - комментарий о НЕ использовании legacy

4. **Архитектурная несогласованность:**
   - Два разных подхода к legacy паролям в одном пакете
   - Неопределенность в требованиях к существующим паролям

**Вердикт:** Критическая архитектурная проблема - противоречивые требования

---

### 🚨 ПРОБЛЕМА #4: Legacy фабрики ошибок

**Местоположение:** `packages/utils/src/trpc-errors.ts`
**Детали проблемы:**

1. **Явно помеченные legacy функции:**
   - `createUserError()` - 4 типа ошибок пользователей
   - `createOrderError()` - 4 типа ошибок заказов
   - `createSupportError()` - 1 тип ошибок поддержки
   - `createSecurityError()` - 1 тип ошибок безопасности

2. **Использование в проекте:** Функции НЕ используются нигде - **мертвый код**

3. **Архитектурные проблемы:**
   - Дублирование логики с современными фабриками
   - Хардкод сообщений на английском языке
   - Нет интеграции с i18n системой проекта
   - String-based типизация вместо enum/union types

4. **Техническая проблема:**
   - Legacy API принимает `string` типы вместо типизированных
   - Fallback на generic `createBadRequestError` для неизвестных типов
   - Нет валидации входных параметров

**Вердикт:** Legacy код без использования - кандидат на удаление

---

### 🚨 ПРОБЛЕМА #5: Незавершенный рефакторинг server-i18n-errors

**Местоположение:** Множественные файлы
**Детали проблемы:**

1. **Комментарии о незавершенном рефакторинге:**
   - `trpc-errors.ts`: "ИСПРАВЛЕНО: Убрана зависимость от server-i18n-errors"
   - `order-status.ts`: "server-i18n-errors.ts removed - use getTranslations from next-intl/server"
   - `input-validation.ts`: "server-i18n-errors removed - use direct fallback messages"
   - `index.ts`: "server-i18n-errors.ts удален - используйте getTranslations из next-intl/server"

2. **Архитектурная проблема:**
   - Комментарии указывают на незавершенный переход к next-intl
   - Разные подходы в разных файлах (fallback messages vs getTranslations)
   - Нет единой стратегии замещения

3. **Техническая проблема:**
   - Комментарии остались в коде после рефакторинга
   - Указывают на техническую задолженность
   - Создают путаницу для разработчиков

**Вердикт:** Незавершенный рефакторинг - техническая задолженность

---

### 🚨 ПРОБЛЕМА #6: Дублирование regex паттернов

**Местоположение:** Множественные файлы
**Детали проблемы:**

1. **Дублирование паттернов username:**
   - `schemas-basic.ts`: `/^[a-zA-Z0-9_]+$/` (только подчеркивание)
   - `schemas/basic.ts`: `/^[a-zA-Z0-9_-]+$/` (подчеркивание + дефис)
   - **Разные требования к одному полю!**

2. **Дублирование паттернов сумм:**
   - `validation-schemas.ts`: `/^\d+\.?\d{0,2}$/` (UAH - 2 знака)
   - `schemas-crypto.ts`: `/^\d+\.?\d{0,8}$/` (Crypto - 8 знаков)
   - **Похожие паттерны с разной точностью**

3. **Дублирование паттернов паролей:**
   - `schemas-basic.ts`: 4 отдельных regex для разных требований
   - `schemas/basic.ts`: 1 сложный regex для всех требований
   - **Два подхода к одной валидации**

4. **Централизованные vs встроенные паттерны:**
   - `schemas-basic.ts` определяет `PATTERNS` объект
   - Другие файлы используют inline regex
   - **Нет единого подхода**

**Вердикт:** Архитектурная несогласованность в подходах к regex

---

### 🚨 ПРОБЛЕМА #7: Дублирование проверок на пустые значения

**Местоположение:** Множественные файлы
**Детали проблемы:**

1. **Дублирование паттерна `!value || value.trim() === ''`:**
   - `validation-schemas.ts:240` - проверка CAPTCHA
   - `single-field.ts:91` - проверка confirmPassword
   - **Идентичная логика в разных местах**

2. **Вариации проверок на пустоту:**
   - `field-validation.ts:79`: `String(fieldValue).trim() === ''`
   - `input-validation.ts:95`: `if (!value) return '';`
   - `env.ts:33`: `return !value;`
   - **Разные подходы к одной задаче**

3. **Проверки массивов:**
   - `order-utils.ts:208`: `if (items.length === 0) return true;`
   - **Отдельный паттерн для коллекций**

4. **Архитектурная проблема:**
   - Нет централизованной утилиты для проверки пустых значений
   - Каждый файл реализует свою логику
   - Разные подходы к типизации (string vs unknown)

**Вердикт:** Нарушение DRY - дублирование базовой логики валидации

---

### 🚨 ПРОБЛЕМА #8: Мертвый код и неиспользуемые функции

**Местоположение:** Множественные файлы
**Детали проблемы:**

1. **Полностью неиспользуемые модули:**
   - `formatting.ts` - все 4 функции не используются
   - `calculations.ts` - все 4 функции не используются
   - `env.ts` - все функции не используются
   - `scroll-utils.ts` - все функции не используются
   - `store-factory.ts` - все функции не используются

2. **Legacy функции (уже выявлены):**
   - `trpc-errors.ts` - 4 legacy функции не используются
   - `schemas/basic.ts` - весь файл не используется

3. **Архитектурная проблема:**
   - **~50% кода пакета является мертвым**
   - Функции созданы "на будущее" но не используются
   - Увеличивают сложность без пользы

4. **Техническая проблема:**
   - Мертвый код усложняет поддержку
   - Создает ложное впечатление о функциональности
   - Увеличивает размер bundle (если используется)

**Вердикт:** Критическая проблема - большой объем мертвого кода

---

### 🚨 ПРОБЛЕМА #9: Архитектурная несогласованность в подходах к валидации

**Местоположение:** Система валидации
**Детали проблемы:**

1. **Три разных подхода к сообщениям валидации:**
   - **Хардкод сообщений**: `schemas-basic.ts` и `schemas-crypto.ts` используют `{ message: 'English text' }`
   - **i18n ключи**: `handlers.ts` использует `t(VALIDATION_KEYS.*)`
   - **Без сообщений**: `schemas/basic.ts` полагается на createNextIntlZodErrorMap

2. **Архитектурная несогласованность:**
   - Одни схемы имеют встроенные сообщения, другие нет
   - Смешение английских хардкод сообщений с i18n системой
   - Нет единого подхода к локализации

3. **Проблемы с fallback:**
   - `handlers.ts` содержит английские fallback сообщения
   - Дублирование логики fallback в разных местах
   - Нет централизованной системы fallback

4. **Техническая проблема:**
   - Сложность поддержки разных подходов
   - Риск несогласованности сообщений
   - Трудность добавления новых языков

**Вердикт:** Критическая архитектурная несогласованность

---

### 🚨 ПРОБЛЕМА #10: Дублирование констант валидации

**Местоположение:** `schemas-basic.ts` vs `@repo/constants`
**Детали проблемы:**

1. **Полное дублирование констант:**
   - `schemas-basic.ts`: `PASSWORD_MIN_LENGTH = 8` vs `@repo/constants`: `PASSWORD_MIN_LENGTH: 8`
   - `schemas-basic.ts`: `PASSWORD_MAX_LENGTH = 128` vs `@repo/constants`: `PASSWORD_MAX_LENGTH: 128`
   - `schemas-basic.ts`: `USERNAME_MIN_LENGTH = 3` vs `@repo/constants`: `USERNAME_MIN_LENGTH: 3`
   - `schemas-basic.ts`: `USERNAME_MAX_LENGTH = 30` vs `@repo/constants`: `USERNAME_MAX_LENGTH: 30`

2. **Противоречие в EMAIL_MAX_LENGTH:**
   - `schemas-basic.ts`: `EMAIL_MAX_LENGTH = 254` (RFC 5321 limit)
   - `@repo/constants`: `EMAIL_MAX_LENGTH: 255`
   - **Разные значения для одного ограничения!**

3. **Дополнительные константы в utils:**
   - `LEGACY_PASSWORD_MIN_LENGTH = 6` - отсутствует в constants
   - `SEARCH_QUERY_MAX_LENGTH = 100` - отсутствует в constants
   - `PATTERNS` объект - дублирует функциональность Zod схем

4. **Архитектурная проблема:**
   - Нарушение принципа единого источника истины
   - Риск расхождения значений при изменениях
   - Два места для поддержки одних констант

**Вердикт:** Критическое нарушение Rule 17 - игнорирование централизованных систем

---

## СВОДКА ПРОБЛЕМ

### Критические проблемы (требуют немедленного исправления):

1. **Дублирование базовых схем валидации** - полное дублирование с мертвым кодом
2. **Противоречивые legacy пароли** - разные требования в одном пакете
3. **Дублирование констант** - нарушение единого источника истины
4. **Мертвый код** - ~50% функций не используются
5. **Архитектурная несогласованность валидации** - 3 разных подхода

### Серьезные проблемы:

6. **Функциональное дублирование форматирования** - идентичные функции
7. **Дублирование regex паттернов** - разные подходы к одним задачам
8. **Legacy фабрики ошибок** - неиспользуемый код
9. **Незавершенный рефакторинг** - комментарии о удаленных зависимостях
10. **Дублирование проверок на пустые значения** - повторяющаяся логика

### Общая оценка:

- **Нарушения Rule 20 (запрет избыточности): 8 из 10 проблем**
- **Нарушения Rule 11 (недопустимость техдолга): 6 из 10 проблем**
- **Нарушения Rule 17 (централизованные системы): 3 из 10 проблем**

**ВЕРДИКТ: Пакет требует кардинального рефакторинга для устранения критических архитектурных проблем**
